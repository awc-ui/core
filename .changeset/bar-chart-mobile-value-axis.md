---
"@awc-ui/core": patch
---

Prevent horizontal bar-chart value-axis labels from overlapping on narrow screens. Tick spacing now accounts for the rendered font and formatted values, keeps matching gridlines and tick marks, reserves room for endpoint labels, and restores additional ticks when the chart widens.
