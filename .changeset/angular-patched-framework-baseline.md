---
"@awc-ui/angular": major
---

Require Angular 20.3.31 or newer and build the wrapper with the patched Angular 20 compiler. Angular 17–19 no longer receive fixes for the compiler, sanitization, and server-rendering vulnerabilities affecting those versions. Upgrade Angular before updating the wrapper; the standalone components and form-accessor APIs are unchanged.
