/* eslint-disable */
/* tslint:disable */
/**
 * This is an autogenerated file created by the Stencil compiler.
 * It contains typing information for all components that exist in this project.
 */
import { HTMLStencilElement, JSXBase } from "@stencil/core/internal";
import { MdAccordionDragDetail } from "./components/md-accordion/md-accordion";
import { BarChartEngine, LineChartEngine, MdChartAnimation, MdChartAxis, MdChartAxisClickDetail, MdChartBarLayout, MdChartClickDetail, MdChartColorRole, MdChartCurve, MdChartDataPoint, MdChartHoverDetail, MdChartLegendPosition, MdChartMarkLine, MdChartSeries, MdChartStackMode, MdChartTitleAlign, MdChartTooltipRenderer, MdChartTooltipTrigger, MdChartXYSeries, PieChartEngine } from "./utils/charts";
import { MdAutocompleteOption } from "./components/md-autocomplete/md-autocomplete";
import { OptionRowSource, SelectOptionData, SelectOptionInit } from "./utils/select-options";
import { FilterMode } from "./utils/wasm-option-store";
import { MdAvatarShape, MdAvatarSize } from "./components/md-avatar/md-avatar";
import { MdBreadcrumbSelectDetail } from "./components/md-breadcrumb-item/md-breadcrumb-item";
import { MdBreadcrumbsExpandDetail } from "./components/md-breadcrumbs/md-breadcrumbs";
import { MdButtonChangeDetail, MdButtonClickDetail } from "./components/md-button/md-button";
import { MdButtonGroupChangeDetail } from "./components/md-button-group/md-button-group";
import { MdCardDragDetail } from "./components/md-card/md-card";
import { ColorFormat, ColorPickerVariant } from "./components/md-color-picker/md-color-picker";
import { MdDatePickerChangeDetail, MdDatePickerMenuOpenDetail, MdDatePickerMenuSelectDetail, MdDatePickerModeChangeDetail, MdDatePickerSelectedDetail, MdDatePickerViewChangeDetail } from "./components/md-date-picker/md-date-picker";
import { MdListInteractionMode, MdListSelectDetail, MdListSelectionMode, MdListStyle } from "./components/md-list/md-list";
import { MdListItemExpandDetail, MdListItemType } from "./components/md-list-item/md-list-item";
import { MenuElement, VirtualMenuProvider } from "./utils/types";
import { MdNavigationBarLabelBehavior } from "./components/md-navigation-bar/md-navigation-bar";
import { MdNavigationTabLabelBehavior } from "./components/md-navigation-tab/md-navigation-tab";
import { MdNumberFieldChangeDetail } from "./components/md-number-field/md-number-field";
import { OrgChartSelectionChangeDetail, OrgChartToggleDetail } from "./components/md-organization-chart/md-organization-chart";
import { MdOtpFieldCompleteDetail, MdOtpFieldInvalidDetail, MdOtpFieldValidityDetail } from "./components/md-otp-field/md-otp-field";
import { MdPieDatum } from "./components/md-pie-chart/md-pie-chart.types";
import { MdSearchLeadingIconClickDetail, MdSearchTrailingIconClickDetail } from "./components/md-search/md-search";
import { MdSliderDragDetail, MdSliderThumbDetail, MdSliderValueDetail } from "./components/md-slider/md-slider";
import { MdSparklineReferenceArea } from "./components/md-sparkline/md-sparkline";
import { MdStatusDotSize, MdStatusDotState } from "./components/md-status-dot/md-status-dot";
import { MdTableSelectionState, MdTableSortOrder, MdTableSortState } from "./components/md-table/md-table";
import { MdTimePickerChangeDetail, MdTimePickerFormat, MdTimePickerMode, MdTimePickerModeChangeDetail, MdTimePickerOrientation, MdTimePickerPeriodLayout } from "./components/md-time-picker/md-time-picker";
import { Placement } from "./components/md-tooltip/md-tooltip";
import { MdTransferListItem } from "./components/md-transfer-list/md-transfer-list";
export { MdAccordionDragDetail } from "./components/md-accordion/md-accordion";
export { BarChartEngine, LineChartEngine, MdChartAnimation, MdChartAxis, MdChartAxisClickDetail, MdChartBarLayout, MdChartClickDetail, MdChartColorRole, MdChartCurve, MdChartDataPoint, MdChartHoverDetail, MdChartLegendPosition, MdChartMarkLine, MdChartSeries, MdChartStackMode, MdChartTitleAlign, MdChartTooltipRenderer, MdChartTooltipTrigger, MdChartXYSeries, PieChartEngine } from "./utils/charts";
export { MdAutocompleteOption } from "./components/md-autocomplete/md-autocomplete";
export { OptionRowSource, SelectOptionData, SelectOptionInit } from "./utils/select-options";
export { FilterMode } from "./utils/wasm-option-store";
export { MdAvatarShape, MdAvatarSize } from "./components/md-avatar/md-avatar";
export { MdBreadcrumbSelectDetail } from "./components/md-breadcrumb-item/md-breadcrumb-item";
export { MdBreadcrumbsExpandDetail } from "./components/md-breadcrumbs/md-breadcrumbs";
export { MdButtonChangeDetail, MdButtonClickDetail } from "./components/md-button/md-button";
export { MdButtonGroupChangeDetail } from "./components/md-button-group/md-button-group";
export { MdCardDragDetail } from "./components/md-card/md-card";
export { ColorFormat, ColorPickerVariant } from "./components/md-color-picker/md-color-picker";
export { MdDatePickerChangeDetail, MdDatePickerMenuOpenDetail, MdDatePickerMenuSelectDetail, MdDatePickerModeChangeDetail, MdDatePickerSelectedDetail, MdDatePickerViewChangeDetail } from "./components/md-date-picker/md-date-picker";
export { MdListInteractionMode, MdListSelectDetail, MdListSelectionMode, MdListStyle } from "./components/md-list/md-list";
export { MdListItemExpandDetail, MdListItemType } from "./components/md-list-item/md-list-item";
export { MenuElement, VirtualMenuProvider } from "./utils/types";
export { MdNavigationBarLabelBehavior } from "./components/md-navigation-bar/md-navigation-bar";
export { MdNavigationTabLabelBehavior } from "./components/md-navigation-tab/md-navigation-tab";
export { MdNumberFieldChangeDetail } from "./components/md-number-field/md-number-field";
export { OrgChartSelectionChangeDetail, OrgChartToggleDetail } from "./components/md-organization-chart/md-organization-chart";
export { MdOtpFieldCompleteDetail, MdOtpFieldInvalidDetail, MdOtpFieldValidityDetail } from "./components/md-otp-field/md-otp-field";
export { MdPieDatum } from "./components/md-pie-chart/md-pie-chart.types";
export { MdSearchLeadingIconClickDetail, MdSearchTrailingIconClickDetail } from "./components/md-search/md-search";
export { MdSliderDragDetail, MdSliderThumbDetail, MdSliderValueDetail } from "./components/md-slider/md-slider";
export { MdSparklineReferenceArea } from "./components/md-sparkline/md-sparkline";
export { MdStatusDotSize, MdStatusDotState } from "./components/md-status-dot/md-status-dot";
export { MdTableSelectionState, MdTableSortOrder, MdTableSortState } from "./components/md-table/md-table";
export { MdTimePickerChangeDetail, MdTimePickerFormat, MdTimePickerMode, MdTimePickerModeChangeDetail, MdTimePickerOrientation, MdTimePickerPeriodLayout } from "./components/md-time-picker/md-time-picker";
export { Placement } from "./components/md-tooltip/md-tooltip";
export { MdTransferListItem } from "./components/md-transfer-list/md-transfer-list";
export namespace Components {
    interface MdAccordion {
        /**
          * When a floating accordion starts being dragged, bring it to the front by bumping `z-index`. Disable if you manage stacking yourself.
          * @default true
         */
        "bringToFront": boolean;
        /**
          * Comma-separated list of item indexes that should be expanded on first render. Items with `expanded` set in their own attribute also count.
          * @default ''
         */
        "defaultExpanded": string;
        /**
          * Density scale: `0` (default, 56 dp headers), `-1` (48 dp), `-2` (40 dp).
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * MD3 elevation level applied to the surface — `0` (default, flat) through `5` (highest). In the `filled` variant each item gets its own shadow; in the `outlined` variant the shadow is drawn on the shared chassis. Expanded items lift to the next elevation level for an expressive "lift off the page" affordance.
          * @default 0
         */
        "elevation": 0 | 1 | 2 | 3 | 4 | 5;
        /**
          * Whether only one item may be expanded at a time. When `true`, expanding an item collapses any other expanded item.
          * @default false
         */
        "exclusive": boolean;
        /**
          * Render the accordion as a free-floating panel that can be dragged anywhere on the screen via the top drag-bar. The panel uses `position: fixed` and translates via CSS transform, so it lifts out of normal document flow.
          * @default false
         */
        "floating": boolean;
        /**
          * ARIA heading level for each item's heading wrapper (`<h1>`–`<h6>`). Pick the value matching the page's information architecture — e.g. `2` inside a top-level page section, `4` deep inside a card. APG: "aria-level that is appropriate for the information architecture of the page". Default `3`.
          * @default 3
         */
        "headingLevel": 1 | 2 | 3 | 4 | 5 | 6;
        /**
          * Initial X translation (px) for floating panels — measured in PHYSICAL viewport coordinates from the top-left corner of the viewport, so a positive value always moves the panel to the right regardless of writing direction (LTR or RTL). Default: 24.
          * @default 24
         */
        "initialX": number;
        /**
          * Initial Y translation (px) for floating panels — measured in physical viewport coordinates from the top-left corner. Default: 24.
          * @default 24
         */
        "initialY": number;
        /**
          * Require at least one item to remain expanded. APG variant: the implementation does not permit the currently-open panel to be collapsed when doing so would leave zero panels open. The locked- open button advertises `aria-disabled="true"`. Pairs naturally with `exclusive` to enforce "exactly one panel is always open".
          * @default false
         */
        "keepOneExpanded": boolean;
        /**
          * Region-role policy for panels: - `auto` (default) — applies `role="region"` only when the item   count is at or below `region-threshold`, otherwise downgrades   to `role="group"` to avoid landmark proliferation (per APG   recommendation for accordions with more than ~6 panels). - `always` — every panel is a landmark region. - `never` — no panel carries a region role (still labelled via   `aria-labelledby` though).
          * @default 'auto'
         */
        "region": 'auto' | 'always' | 'never';
        /**
          * Threshold above which `region="auto"` downgrades panel roles from `region` to `group`. APG suggests ~6.
          * @default 6
         */
        "regionThreshold": number;
        /**
          * Allow drag-to-reorder of items via the leading drag handles. Items can also be reordered with `Alt + ArrowUp / ArrowDown` while the header is focused. Disabled items can't be moved.
          * @default false
         */
        "reorderable": boolean;
        /**
          * Motion preset applied when items expand and collapse. The prop is reflected so the item CSS can pick it up via `:host-context(md-accordion[transition="…"])`.  - `expressive` (default) — full MD3 Expressive choreography:    asymmetric easing (decelerate-on-open, accelerate-on-close),    spring-spatial chevron rotation, container shape & elevation    morph, leading-icon FILL-axis swap. Use for hero surfaces,    settings shells, anything that wants to feel premium. - `standard` — MD3 standard easing (symmetric `emphasized`) with    no spring overshoot. Container morph still happens. The    "neutral" preset — good default for dense product UI. - `fade` — content fades in/out alongside the height animation    using shorter `standard` easing. Chevron rotates without a    spring. Best for content-heavy panels (forms, long copy)    where the motion should be unobtrusive. - `collapse` — pure height collapse with simple `ease-in-out`    timing — no spring, no morph, no fade. The classic    "browser disclosure" look. - `none` — disables every transition (instant snap). Useful for    automated tests, screenshot harnesses, or environments where    motion is undesirable.
          * @default 'expressive'
         */
        "transition": 'expressive' | 'standard' | 'fade' | 'collapse' | 'none';
        /**
          * Visual variant. Both share the same expand/collapse mechanics. - `filled` (default) — each item is a tonal surface tile. - `outlined` — items share a single rounded chassis with an outline.
          * @default 'filled'
         */
        "variant": 'filled' | 'outlined';
    }
    interface MdAccordionItem {
        /**
          * Whether the item can be collapsed when it is expanded. Set to `false` to implement the APG variant where the accordion does not permit a panel to be collapsed — the open button then advertises `aria-disabled="true"`, and Enter / Space / click no longer toggle it. Typically managed by the parent's `keep-one-expanded` mode.
          * @default true
         */
        "collapsible": boolean;
        /**
          * Cap the panel body's block-size (e.g. `"220px"`, `"40vh"`). When set, the content region becomes a scroll container instead of growing to fit — and, crucially, it is made keyboard-focusable (`tabindex="0"`) so keyboard-only users can scroll it. A scrollable region whose content isn't itself focusable is otherwise unreachable without a pointer (WCAG 2.1.1 — axe `scrollable-region-focusable`). Its accessible name comes from the enclosing labelled panel region.  Prefer this over hand-rolling a slotted `overflow:auto` wrapper: an authored scroll `<div>` lives in light DOM the component can't make focusable, so it would trip the same axe rule. Leave empty (default) for a panel that grows to fit its content.
          * @default ''
         */
        "contentMaxHeight": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable interaction.
          * @default false
         */
        "disabled": boolean;
        /**
          * Whether the item is expanded. Two-way bindable.
          * @default false
         */
        "expanded": boolean;
        /**
          * Move keyboard focus to the item's header.  Uses `{ focusVisible: true }` so the focus ring is painted even when the call originates from a mouse-driven action (e.g. another button in the page calling `await item.focusHeader()`). Without it, browsers inherit the last input modality and skip the focus-visible ring on mouse-initiated programmatic focus, leaving the user wondering whether the call did anything. The option is supported in evergreen Chromium (137+), Firefox (134+) and Safari (17.4+); older runtimes ignore it harmlessly and fall back to the prior behaviour.
         */
        "focusHeader": () => Promise<void>;
        /**
          * ARIA heading level (`aria-level`) for the item's heading wrapper. Renders a native `<h1>`–`<h6>` so the implicit semantics match the page's information architecture. Per WAI-ARIA APG: "an element with role heading that has a value set for aria-level that is appropriate for the information architecture of the page".  Set per-item, or let the parent `<md-accordion heading-level>` propagate the same value to every item.
          * @default 3
         */
        "headingLevel": 1 | 2 | 3 | 4 | 5 | 6;
        /**
          * Header text (or use the `headline` slot).
          * @default ''
         */
        "headline": string;
        /**
          * Optional leading-icon (Material Symbols shorthand).
          * @default ''
         */
        "icon": string;
        /**
          * Role applied to the accordion panel. APG recommends `region` for structural clarity, but warns against using it when more than ~6 panels can be open at once (landmark proliferation). The parent `<md-accordion region>` orchestrates this automatically.  - `region` (default) — full landmark semantics. - `group` — semantically a group but not a landmark. - `none` — drop the role entirely (still labelled via aria-labelledby).
          * @default 'region'
         */
        "regionRole": | 'region'
    | 'group'
    | 'none';
        /**
          * Optional small caption below the headline.
          * @default ''
         */
        "supportingText": string;
        /**
          * Programmatically toggle the item.
         */
        "toggle": () => Promise<void>;
    }
    /**
     * `md-app-bar` — Material Design 3 App Bar.
     * Implements the current (M3 Expressive) app-bars specification:
     *   https://m3.material.io/components/app-bars/specs
     * Size variants: `small` (64px), `medium` (112/136px expanded), `large`
     * (120/152px expanded), and `search` (inline search field in the 64px row).
     * Flexible `medium` / `large` bars always render the expanded headline block.
     * Title alignment (`title-alignment`) is configuration, not a variant.
     * Selecting the search field should open an `md-search` view via `mdSearchActivate`.
     */
    interface MdAppBar {
        /**
          * Density scale: 0 (default 64px row), -1 (60px), -2 (56px), -3 (52px), -4 (48px). Tightens the action row, icon targets, search field, and expanded heights.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Inline or expanded headline text. Falls back to the `headline` slot when empty.
          * @default ''
         */
        "headline": string;
        /**
          * Material Symbols name for the leading navigation icon (slot takes priority).
          * @default ''
         */
        "leadingIcon": string;
        /**
          * Accessible label for the prop-based leading icon button. Required when `leading-icon` is set.
          * @default ''
         */
        "leadingIconLabel": string;
        /**
          * When true, applies the scrolled container surface colour per M3 app bar common-colors (`surface` → `surface-container`). Wire from your scroll listener; the component does not observe scroll position itself.
          * @default false
         */
        "scrolled": boolean;
        /**
          * Search variant only: accessible name for the inline search field. Per M3 labeling guidance, falls back to `search-placeholder` when unset.
          * @default ''
         */
        "searchAriaLabel": string;
        /**
          * Search variant only: disable the inline search field.
          * @default false
         */
        "searchDisabled": boolean;
        /**
          * Search variant only: hinted search text shown in the inline search field. Falls back to `placeholder` on the optional `search` slot when empty.
          * @default 'Search'
         */
        "searchPlaceholder": string;
        /**
          * Search variant only: current search field value (two-way bindable).
          * @default ''
         */
        "searchValue": string;
        /**
          * Optional subtitle. On flexible variants it sits below the expanded headline (and grows the container); on `small` it stacks under the inline title. Falls back to the `subtitle` slot when empty.
          * @default ''
         */
        "subtitle": string;
        /**
          * Title horizontal alignment — `start` (leading edge, default) or `center`. On search bars it centers the placeholder when empty and unfocused; once focused or when typing, the caret and text align to the inline-start edge.
          * @default 'start'
         */
        "titleAlignment": 'start' | 'center';
        /**
          * App bar size variant.  - `small` — 64px action row with inline title-large headline. - `medium` — flexible 112px (136px with subtitle) expanded headline block. - `large` — flexible 120px (152px with subtitle) expanded headline block. - `search` — 64px row with inline search field (M3 search app bar).
          * @default 'small'
         */
        "variant": 'small' | 'medium' | 'large' | 'search';
    }
    /**
     * md-area-chart — Material Design 3 area / streamgraph chart,
     * rendered by the in-house engine (Canvas2D + a DOM text
     * overlay). No ECharts.
     * Differences from `md-line-chart`:
     *   • the line is always filled with a gradient
     *   • stacking defaults to `'normal'` (areas stack additively)
     *   • supports the `silhouette` streamgraph baseline
     *   • markers are off by default
     */
    interface MdAreaChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation": MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Draw small perpendicular tick marks on the axes.
          * @default false
         */
        "axisTicks": boolean;
        /**
          * @default false
         */
        "connectNulls": boolean;
        /**
          * @default 'smooth'
         */
        "curve": 'linear' | 'smooth' | 'monotone' | 'step' | 'step-before' | 'step-middle';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Fill opacity for the gradient (0..1).
          * @default 0.55
         */
        "fillOpacity": number;
        "getInstance": () => Promise<LineChartEngine | null>;
        /**
          * Gridlines: horizontal (y ticks), vertical (x ticks), both, or none.
          * @default 'horizontal'
         */
        "grid": 'none' | 'horizontal' | 'vertical' | 'both';
        "heightProp"?: string;
        /**
          * Transpose the axes: the category / time axis runs down the side and values run across the bottom. Suits long category names (they read horizontally instead of rotated) and quantities naturally read as depth — altitude, ocean depth, a drill core. `stack` applies as usual, along the value axis.
          * @default false
         */
        "inverted": boolean;
        /**
          * @default ''
         */
        "label": string;
        /**
          * Message shown when `series` is empty. The `empty` slot overrides it.
          * @default 'No data to display'
         */
        "labelEmpty": string;
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the data with the arrow keys; this is what tells them so.
          * @default 'Chart data. Use the arrow keys to move between points, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot": string;
        /**
          * Template for the live announcement as keyboard focus moves. `%x%` is the axis value, `%values%` the series readings at it.
          * @default '%x%: %values%'
         */
        "labelPoint": string;
        /**
          * Accessible name for the zoom slider's end thumb.
          * @default 'Zoom range end'
         */
        "labelZoomEnd": string;
        /**
          * Accessible name for the zoom slider's start thumb.
          * @default 'Zoom range start'
         */
        "labelZoomStart": string;
        /**
          * @default 'top-end'
         */
        "legend": MdChartLegendPosition | 'none';
        /**
          * Line stroke width in px, for every series. Per-series `series[].lineWidth` wins.
         */
        "lineWidth"?: number;
        /**
          * Show the loading overlay instead of the plot.
          * @default false
         */
        "loading": boolean;
        /**
          * Text (and the spinner's accessible name) for the loading overlay.
          * @default 'Loading chart…'
         */
        "loadingLabel": string;
        /**
          * BCP-47 locale for the DEFAULT number / date formatting (axis ticks, tooltip values, screen-reader table). Empty follows the browser; an explicit `valueFormatter` always wins.
          * @default ''
         */
        "locale": string;
        /**
          * Marker RADIUS in px, for every series. Per-series `series[].markSize` wins.
         */
        "markSize"?: number;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation": boolean;
        /**
          * Re-read the MD3 tokens from the host's computed style and repaint.  The chart does this automatically when the tokens actually change, so reach for this only when a theme is applied in a way the watcher cannot observe — tokens injected into a stylesheet rather than onto an element, for instance. Cheaper and more direct than reassigning `series` to force a rebuild.
         */
        "refreshTheme": () => Promise<void>;
        /**
          * Replay the entry animation from the start (uses the current `animation`).
         */
        "replay": () => Promise<void>;
        /**
          * Drop the zoom window and show the full range.
         */
        "resetZoom": () => Promise<void>;
        "resize": () => Promise<void>;
        /**
          * @default []
         */
        "series": MdChartSeries[];
        /**
          * Label each series at its last point, so the name follows the end of the band — for racing / progression charts.
          * @default false
         */
        "seriesLabels": boolean;
        /**
          * Zoom to an absolute index range.
         */
        "setZoom": (startIndex: number, endIndex: number) => Promise<void>;
        /**
          * Print each point's value as a data label beside its marker.
          * @default false
         */
        "showLabels": boolean;
        /**
          * Draw the line on top of each area's fill. Default `true`. Set `false` for a fill-only area — useful when `fillOpacity` is high (a fully-opaque fill is the same colour as the line, so the line reads as part of it). A per-series `series[].stroke` still wins over this.
          * @default true
         */
        "showLine": boolean;
        /**
          * @default false
         */
        "showMarks": boolean;
        /**
          * Default stacking is `'normal'` (additive).
          * @default 'normal'
         */
        "stack": MdChartStackMode;
        /**
          * Sub-title, drawn under the title in the muted text colour.
         */
        "subtitle": string | undefined;
        /**
          * Replaces the generated `aria-label` outright. The default summary is assembled in English ("Traffic, Area chart, with 3 series, from Jan to Dec."); rather than translate it piecewise, hand over the whole sentence built in your own language.
          * @default ''
         */
        "summary": string;
        /**
          * Translatable chrome for the screen-reader data table. `%shown%` and `%total%` are substituted in `truncated`.
         */
        "tableLabels"?: { x?: string; index?: string; series?: string; truncated?: string };
        /**
          * Title alignment over the plot: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign": MdChartTitleAlign;
        "toDataURL": () => Promise<string>;
        /**
          * @default 'axis'
         */
        "tooltip": MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content. See `md-line-chart`'s `tooltipRenderer`.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        "valueFormatter"?: (value: number | null | undefined) => string;
        "xAxis": MdChartAxis | undefined;
        "yAxis": MdChartAxis | undefined;
        /**
          * @default 'none'
         */
        "zoom": 'none' | 'inside' | 'slider' | 'both';
    }
    /**
     * `md-autocomplete` — Material Design 3 combobox: a text field that filters
     * a dropdown listbox **as you type in the input** (no separate search row),
     * with single or multiple (chips) selection.
     * Built from the same primitives as `md-select` / `md-multi-select`:
     *   - the shared option model (`options` array of `{ value, label,
     *     supportingText?, icon?, iconColor?, disabled? }` or slotted
     *     `<md-select-option>` children),
     *   - `md-text-field` for the trigger (typing filters live),
     *   - `md-menu` as a WAI-ARIA listbox popup (options carry `aria-selected`),
     *   - `VirtualSelectController` for WASM-virtualized huge datasets
     *     (`virtualize`, `loadOptions()`, `filter-mode`) — the same engine as
     *     the selects, so tens of thousands of rows filter smoothly.
     * Keyboard: typing filters; ArrowDown moves focus into the listbox (the
     * focus-moves-into-popup combobox variant — option focus is announced
     * directly); Enter selects; Escape closes back to the input; Backspace on an
     * empty multi input removes the last chip.
     * ARIA: the real textbox — the `<input>` inside md-text-field's shadow root —
     * is promoted to `role="combobox"` with `aria-expanded` / `aria-autocomplete`
     * (handed over via `input-role`, since only that component renders the input),
     * and points at this component's listbox through ARIA element reflection. An
     * IDREF could not: it would have to resolve inside md-text-field's tree, where
     * the listbox is not. See `syncComboboxRefs`.
     * Extras kept from the surface this API mirrors: `free-solo` (commit arbitrary text),
     * `max-selected`, `clearable`, `disable-close-on-select`, `clear-on-blur`,
     * a custom `filterer` and `limit-results` (client-side path only).
     */
    interface MdAutocomplete {
        /**
          * Constraint-validation API, matching md-text-field and the native contract. Validity was already published to the form here, but with no public method a consumer could not ASK this control whether it was valid.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Where the selection chips live (`multiple` only): around the field (`'below'` default, `'top'`, `'left'`, `'right'`) or `'inline'` inside it, before the input, wrapping onto new rows as the selection grows (the field grows with them).
          * @default 'below'
         */
        "chipPosition": | 'below'
    | 'top'
    | 'left'
    | 'right'
    | 'inline';
        /**
          * Material Symbols glyph for the clear button.
          * @default 'close'
         */
        "clearIcon": string;
        /**
          * Clear a non-matching input on blur (strict single mode only).
          * @default false
         */
        "clearOnBlur": boolean;
        /**
          * Show a clear (×) button on the trailing edge.
          * @default true
         */
        "clearable": boolean;
        /**
          * Close the menu.
         */
        "closeMenu": () => Promise<void>;
        /**
          * Density forwarded to the text-field.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Keep the menu open after a selection in **single** mode.  `multiple` already stays open — picking several in a row is the point, and it dismisses on an outside click, on the trigger, or on Escape. This prop gives a single-select the same stickiness when a picker is being used to scan rather than to commit.
          * @default false
         */
        "disableCloseOnSelect": boolean;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled": boolean;
        /**
          * Caret glyph; rotates 180° when open (matches md-select). Slot `dropdown-icon` overrides it.
          * @default 'arrow_drop_down'
         */
        "dropdownIcon": string;
        /**
          * Error state — forwarded to the text-field.
          * @default false
         */
        "error": boolean;
        /**
          * Error text rendered in place of supporting text when `error` is true.
          * @default ''
         */
        "errorText": string;
        /**
          * Filter strategy for the virtualized (WASM) path.
          * @default 'substring'
         */
        "filterMode": FilterMode;
        /**
          * Custom client-side filter (ignored while virtualized — the WASM engine filters by `filter-mode` instead).
         */
        "filterer"?: (
    options: SelectOptionData[],
    state: { inputValue: string; selected: string[] },
  ) => SelectOptionData[];
        /**
          * Programmatically focus the input.
         */
        "focusInput": () => Promise<void>;
        /**
          * Allow committing arbitrary text (not just options) with Enter.
          * @default false
         */
        "freeSolo": boolean;
        /**
          * Resolve labels for a set of values (virtual-safe).
         */
        "getLabels": (values: string[]) => Promise<Record<string, string>>;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Live text of the input (separate from `value` so typing never commits).
          * @default ''
         */
        "inputValue": string;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label": string;
        /**
          * Max items shown in the dropdown (`0` = unlimited; client-side path only).
          * @default 0
         */
        "limitResults": number;
        /**
          * Load a large dataset into the WASM-backed virtualized list (rows are byte-packed off the JS heap). Accepts an array or a `{ count, getRow }` factory. Falls back to plain DOM when `virtualize="never"` or WASM is unavailable.
         */
        "loadOptions": (source: SelectOptionInit[] | OptionRowSource) => Promise<void>;
        /**
          * Busy state: progress bar in the menu while an async dataset loads.
          * @default false
         */
        "loading": boolean;
        /**
          * Text shown while `loading` is true.
          * @default 'Loading…'
         */
        "loadingText": string;
        /**
          * Match the dropdown width to the trigger.
          * @default true
         */
        "matchTriggerWidth": boolean;
        /**
          * Max height of the dropdown menu (px).
         */
        "maxHeight"?: number;
        /**
          * When `multiple`, the max number of selected items (`0` = no limit).
          * @default 0
         */
        "maxSelected": number;
        /**
          * Multi-select mode — the selection renders as removable chips.
          * @default false
         */
        "multiple": boolean;
        /**
          * Form name. Multi-mode submits as repeated `name=value` pairs.
          * @default ''
         */
        "name": string;
        /**
          * Empty-state text when the dataset has no options at all.
          * @default 'No options'
         */
        "noOptionsText": string;
        /**
          * Empty-state text when the typed filter matches nothing.
          * @default 'No results'
         */
        "noResultsText": string;
        /**
          * Open state.
          * @default false
         */
        "open": boolean;
        /**
          * Programmatic options (shared select model; `description` is accepted as a legacy alias for `supportingText`). Slotted `<md-select-option>` children take precedence when present.  Accepts either the array (property) or a **JSON string** (attribute), the same contract `md-organization-chart.nodes` uses — so a plain-HTML page can declare its dataset inline instead of reaching for a script:      <md-autocomplete options='[{"value":"a","label":"Apple"}]'></md-autocomplete>  Malformed JSON degrades to an empty list rather than throwing: a broken attribute must not take the whole field down.
          * @default []
         */
        "options": MdAutocompleteOption[] | string;
        /**
          * Placeholder for the input.
          * @default ''
         */
        "placeholder": string;
        /**
          * Menu placement.
          * @default 'bottom-start'
         */
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        "reportValidity": () => Promise<boolean>;
        /**
          * Required for native form parity.
          * @default false
         */
        "required": boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace": boolean;
        /**
          * Fixed virtualized row height (px). Auto-measured from the first row if unset.
         */
        "rowHeight"?: number;
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Open the menu.
         */
        "showMenu": () => Promise<void>;
        /**
          * Soft-disabled — focusable but non-interactive.
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Template for the polite screen-reader status while the popup is open (localisable). `{count}` is replaced with the visible option count.
          * @default '{count} suggestions available'
         */
        "statusTemplate": string;
        /**
          * Supporting / helper text below the field.
          * @default ''
         */
        "supportingText": string;
        /**
          * Single-mode value — option `value` or free-solo string. Multi-mode value — array of option `value`s.
          * @default ''
         */
        "value": string | string[];
        /**
          * Localized constraint-validation message shown when `required` is unmet. `errorText` still wins when set, so an app-supplied inline message and the native bubble stay in agreement.
          * @default 'Please make a selection'
         */
        "valueMissingLabel": string;
        /**
          * Visual variant of the inner text-field.
          * @default 'filled'
         */
        "variant": 'filled' | 'outlined';
        /**
          * Virtualization strategy (same engine as `md-select`):   - `'auto'` (default): virtualize above 200 rows.   - `'always'`: always virtualize the `options` / `loadOptions` dataset.   - `'never'`: plain DOM rendering.
          * @default 'auto'
         */
        "virtualize": 'auto' | 'always' | 'never';
    }
    /**
     * `md-avatar` — a presentational identity tile that resolves the first
     * available source from a clear fallback chain:
     *   1. `src` (image)  → `<img>` is rendered.
     *      On error/empty, automatically falls through to step 2.
     *   2. `initials`     → explicit initials string (verbatim, max 3 chars).
     *   3. `name`         → auto-derived initials. Two tokens → first letter
     *                       of each; one token → first letter only. RTL- and
     *                       diacritic-safe via `Intl.Segmenter` when available.
     *   4. default slot   → caller-provided fallback (icon, emoji, SVG).
     *   5. person icon    → material-symbols fallback (always available).
     */
    interface MdAvatar {
        /**
          * Alternative text for the image. When empty, the image is exposed to AT as decorative (`aria-hidden`); the host carries the accessible label instead (see `label`).
          * @default ''
         */
        "alt": string;
        /**
          * When `true` the avatar background is deterministically picked from a tonal palette using the initials/name as a hash seed. When `false`, the avatar uses the neutral on-surface tone.
          * @default true
         */
        "colorFromName": boolean;
        /**
          * `<img>` `crossorigin` attribute (forwarded only when set).
          * @default ''
         */
        "crossorigin": '' | 'anonymous' | 'use-credentials';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Explicit initials override. Bypasses the `name` parsing entirely. Truncated to the first 3 grapheme clusters at render time so a stray "WXYZ" can't blow out the layout.
          * @default ''
         */
        "initials": string;
        /**
          * Accessible label for the avatar. When empty:   - if `alt` is set, that is exposed instead   - if `name` is set, that is exposed instead   - otherwise the avatar is hidden from AT (`role="presentation"`)     so it doesn't add noise next to a labelled control.
          * @default ''
         */
        "label": string;
        /**
          * `<img>` `loading` attribute.
          * @default 'lazy'
         */
        "loading": 'eager' | 'lazy';
        /**
          * Full name. The component derives initials automatically:   - `"Ada Lovelace"` → `"AL"`   - `"Cher"`         → `"C"`   - `"  john   doe"` → `"JD"`  Honored only when `src` is not set (or fails to load) and `initials` is empty.
          * @default ''
         */
        "name": string;
        /**
          * Container shape. - `circle`  — fully rounded (`50%`)             [default] - `rounded` — `--md-sys-shape-corner-medium` (12px) - `square`  — sharp corners (`0`)
          * @default 'circle'
         */
        "shape": MdAvatarShape;
        /**
          * Avatar size. Accepts either a named preset or any CSS length.  Presets: - `small`   — 24×24 (typography: label-medium) - `medium`  — 40×40 (typography: title-medium)  [default] - `large`   — 56×56 (typography: title-large)  Custom values: - Unitless number → pixels (`size="72"` → 72px) - Any CSS length  → passes through (`size="4rem"`, `size="10vw"`,   `size="clamp(28px, 4vw, 72px)"`)  Custom values are wired through the existing `--md-avatar-size` CSS custom property, so per-instance overrides via `style` / class still win when set.
          * @default 'medium'
         */
        "size": MdAvatarSize;
        /**
          * Image URL. When set, the avatar renders as an `<img>` element.
          * @default ''
         */
        "src": string;
    }
    interface MdBadge {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Material Symbols icon name (large variant only, e.g. `"error"`)
          * @default ''
         */
        "icon": string;
        /**
          * Numeric threshold. When the parsed value exceeds this, the badge shows `{max}+` (e.g. `999+`). Ignored for non-numeric values.
          * @default 999
         */
        "max": number;
        /**
          * Text or number to display (large variant only, max 4 visible characters)
          * @default ''
         */
        "value": string;
        /**
          * Badge size — small renders a dot; large renders a label
          * @default 'large'
         */
        "variant": 'small' | 'large';
    }
    /**
     * md-bar-chart — Material Design 3 Expressive bar chart, rendered
     * by the in-house engine (Canvas2D + a DOM text overlay). No
     * ECharts.
     *   • vertical (default) and horizontal layouts
     *   • grouped / stacked / percentage modes
     *   • category + bar gap configuration
     *   • rounded outer corners (top for vertical, end for horizontal)
     *   • legend toggle, hover tooltip, click events
     *   • role="figure" + screen-reader-only data table
     */
    interface MdBarChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation": MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Draw small perpendicular tick marks on the axes.
          * @default false
         */
        "axisTicks": boolean;
        /**
          * Bar gap between sibling grouped bars (`"20%"` or `20`).
          * @default '20%'
         */
        "barGap": string | number;
        /**
          * Fixed bar WIDTH in px, centred in each slot. Default: auto — the bar fills its slot (the band minus the category/bar gaps). Clamped to the slot so it can't overrun a neighbour.
         */
        "barWidth"?: number;
        /**
          * Category gap as a percentage of the band width (`"30%"` or `30`).
          * @default '30%'
         */
        "categoryGap": string | number;
        /**
          * Draw the bars as CHEVRONS pointing away from the baseline instead of as rectangles. Along a stack they interlock, each segment's point sitting in the next one's notch — a force diagram, where the contributions are handed along in a direction rather than merely piled up.
          * @default false
         */
        "chevron": boolean;
        /**
          * Show a hand (`pointer`) cursor over the bars, signalling they're actionable — e.g. clickable to drill into. The chart can't know an interaction is wired, so set this when a `mdBarClick` handler does something (like `drill`). The cursor tracks the bar itself (hit-tested like a click), not the empty column above a short bar.
          * @default false
         */
        "clickable": boolean;
        /**
          * Bar corner radius in pixels.
          * @default 6
         */
        "cornerRadius": number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Animate the next data change as a drill through one column, so the levels of a hierarchy read as connected rather than as unrelated charts.  AWAIT it, then assign the new `series` / `xAxis`. Like every Stencil `@Method` this one resolves through a microtask, so a bare call lands AFTER an assignment on the next line — the swap renders un-animated and the drill arms itself for whatever change comes after.  ```ts await chart.drill(e.detail.dataIndex);   // descend into the clicked column chart.series = childrenOf(clicked);  await chart.drill(0, 'up');              // …and back out chart.series = parentLevel; ```  Descending, the incoming columns unfold out of the band the named column occupies right now. `'up'` pivots on the whole plot instead, which is the same motion run inside out — the level being returned to opens out of everything the level being left was filling. Falls back to a plain swap when the index doesn't resolve, or under `prefers-reduced-motion`.
         */
        "drill": (index: number, direction?: "down" | "up") => Promise<void>;
        "getInstance": () => Promise<BarChartEngine | null>;
        "heightProp"?: string;
        /**
          * @default ''
         */
        "label": string;
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the data with the arrow keys; this is what tells them so.
          * @default 'Chart data. Use the arrow keys to move between points, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot": string;
        /**
          * Template for the live announcement as keyboard focus moves. `%x%` is the axis value, `%values%` the series readings at it.
          * @default '%x%: %values%'
         */
        "labelPoint": string;
        /**
          * Accessible label for the zoom slider's end thumb.
          * @default 'Zoom range end'
         */
        "labelZoomEnd": string;
        /**
          * Accessible label for the zoom slider's start thumb.
          * @default 'Zoom range start'
         */
        "labelZoomStart": string;
        /**
          * Bar layout — `'horizontal'` swaps categories onto the y axis.
          * @default 'vertical'
         */
        "layout": MdChartBarLayout;
        /**
          * @default 'top-end'
         */
        "legend": MdChartLegendPosition | 'none';
        /**
          * Data is still on its way — the chart covers the plot with a loader instead of drawing an empty (or stale) axis, and marks itself `aria-busy`.  Set it while the fetch is in flight and clear it when `series` arrives; the entry animation replays on the way out, so the bars draw themselves in rather than appearing fully formed. Slot `loading` to replace the default indicator with your own skeleton.
          * @default false
         */
        "loading": boolean;
        /**
          * Accessible + visible text under the loader.
          * @default 'Loading chart…'
         */
        "loadingLabel": string;
        /**
          * BCP-47 locale for the DEFAULT number / date formatting (axis ticks, tooltip values, screen-reader table). Empty follows the browser; an explicit `valueFormatter` always wins.
          * @default ''
         */
        "locale": string;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation": boolean;
        /**
          * Wrap the bars around a circle: one concentric RING per category, with the value axis running as an ANGLE instead of a length. A radial (polar) bar chart.  Stacking, legend hiding and percentage normalisation all work unchanged — only the step from a value to geometry differs, and the value axis deliberately spans less than the full circle, since a ring that closes on itself has no readable start or end. The quarter it gives up is where the category names go; `polar-sweep` changes how much that is.
          * @default false
         */
        "polar": boolean;
        /**
          * Radius of the hole at the centre of a polar chart, 0..0.9. Default 0.22.
         */
        "polarHole"?: number;
        /**
          * Fraction of the circle the value axis spans on a polar chart. Default 0.75.
         */
        "polarSweep"?: number;
        /**
          * Re-read the MD3 tokens from the host's computed style and repaint.  The chart does this automatically when the tokens actually change, so reach for this only when a theme is applied in a way the watcher cannot observe — tokens injected into a stylesheet rather than onto an element, for instance. Cheaper and more direct than reassigning `series` to force a rebuild.
         */
        "refreshTheme": () => Promise<void>;
        /**
          * Replay the entry animation from the start (uses the current `animation`).
         */
        "replay": () => Promise<void>;
        /**
          * Drop the zoom window and show the full range.
         */
        "resetZoom": () => Promise<void>;
        "resize": () => Promise<void>;
        /**
          * @default []
         */
        "series": MdChartSeries[];
        /**
          * Zoom to an absolute category range.
         */
        "setZoom": (startIndex: number, endIndex: number) => Promise<void>;
        /**
          * Show numeric data labels on each bar.
          * @default false
         */
        "showLabels": boolean;
        /**
          * Print each stack's TOTAL past the end of the column, alongside the per-segment values `show-labels` prints inside it. Stacked charts only — an unstacked bar already IS its total.
          * @default false
         */
        "showTotals": boolean;
        /**
          * Stacking strategy: none (grouped) / normal / percentage.
          * @default 'none'
         */
        "stack": MdChartStackMode;
        /**
          * A quieter line under the title — a source, a unit, the caveat a title should not have to carry.
         */
        "subtitle": string | undefined;
        /**
          * Title alignment over the plot: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign": MdChartTitleAlign;
        "toDataURL": () => Promise<string>;
        /**
          * @default 'axis'
         */
        "tooltip": MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content. See `md-line-chart`'s `tooltipRenderer`.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        "valueFormatter"?: (value: number | null | undefined) => string;
        /**
          * Category axis configuration.
         */
        "xAxis": MdChartAxis | undefined;
        /**
          * Value axis configuration.
         */
        "yAxis": MdChartAxis | undefined;
        /**
          * A SECOND value axis, drawn in the opposite gutter and used by any series whose `yAxisIndex` is 1 — two quantities on one category axis that share no scale, like a count and a rate. Vertical charts.  It draws no gridlines of its own: two sets at two spacings turn the plot into a mesh nobody can read a value off.
         */
        "yAxis2": MdChartAxis | undefined;
        /**
          * Interactive zoom over the CATEGORY range — the same "zoom is a view" model as `md-line-chart`:   • `inside` — drag horizontally across the plot to zoom into that span of     bars; double-click the plot to reset.   • `slider` — a range slider under the plot; drag its thumbs to resize the     window, or the grip between them to pan it.   • `both` — both gestures.  `series` / `xAxis` are never mutated: the engine is simply fed the sliced window, and hover / click / keyboard indices are rebased so consumers still see ABSOLUTE data indices. Ignored on a `polar` chart (a ring has no span to zoom). For a *value*-range zoom, set `yAxis.min` / `yAxis.max`.
          * @default 'none'
         */
        "zoom": 'none' | 'inside' | 'slider' | 'both';
    }
    /**
     * MD3 Bottom sheet — secondary content anchored to the bottom of the screen.
     * Two variants per the M3 spec
     * (https://m3.material.io/components/bottom-sheets/overview). Both are
     * modal dialogs — they always render a scrim, trap focus, lock body
     * scroll, close on Escape, and support drag-to-dismiss.
     * - `standard` (default) — anchored to the bottom edge of the viewport.
     *   Full-width up to a responsive max, with rounded top corners only.
     * - `detached` — floats with a margin on every side and rounded corners
     *   on every side. Best for compact, dialog-like sheets on larger
     *   screens.
     * Width and height are entirely token-driven, so the only thing you
     * change to switch from anchored to floating chrome is the `variant`
     * attribute. The default media query reflows the sheet between
     * full-width on mobile and a centred 640px panel on desktop; consumers
     * can opt out via `--md-bottom-sheet-width` / `--md-bottom-sheet-max-width`.
     */
    interface MdBottomSheet {
        /**
          * Show divider between content and actions
          * @default false
         */
        "bottomDivider": boolean;
        "close": () => Promise<void>;
        /**
          * Show a close icon-button in the top-end corner of the header
          * @default false
         */
        "closeable": boolean;
        /**
          * Inline-axis alignment of the slotted content. Inherits to all default-slot children, so prose, lists, and forms all pick it up. `start` (default) reads left in LTR / right in RTL, `end` reads right in LTR / left in RTL, `center` is direction-agnostic. Slotted children that explicitly set their own `text-align` win.
          * @default 'start'
         */
        "contentAlign": 'start' | 'center' | 'end';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Headline text (or use the `headline` slot)
          * @default ''
         */
        "headline": string;
        /**
          * Inline-axis alignment of the headline text. `start` (default) reads left in LTR / right in RTL, `end` reads right in LTR / left in RTL, `center` is direction-agnostic. Use the matching `::part(headline)` selector for finer control (e.g. `text-align: justify`).
          * @default 'start'
         */
        "headlineAlign": 'start' | 'center' | 'end';
        /**
          * Whether the sheet is visible
          * @default false
         */
        "open": boolean;
        /**
          * Whether clicking the scrim closes the sheet
          * @default true
         */
        "scrimDismissible": boolean;
        /**
          * Retained for API compatibility, but now a no-op: the content area always plain-scrolls when it overflows (no scroll shadow / edge fades). Setting this has no visual effect.
          * @default true
         */
        "scrollShadow": boolean;
        /**
          * Custom aria-label for the container
          * @default ''
         */
        "sheetAriaLabel": string;
        "show": () => Promise<void>;
        /**
          * Show the drag handle indicator
          * @default true
         */
        "showDragHandle": boolean;
        /**
          * Show divider between header and content
          * @default false
         */
        "topDivider": boolean;
        /**
          * `standard` (default) anchors the sheet to the bottom edge. `detached` floats it with margin on every side. Both variants always render a scrim and trap focus.
          * @default 'standard'
         */
        "variant": 'standard' | 'detached';
    }
    interface MdBreadcrumbItem {
        /**
          * Marks this item as part of the collapsed middle section. Usually set by the parent `md-breadcrumbs` when `max-items` is exceeded. Declared as a public prop (instead of just a `data-*` attribute) so Stencil re-renders the item when the parent toggles its visibility.
          * @default false
         */
        "collapsed": boolean;
        /**
          * When `true`, mark this item as the current page. Usually the parent `md-breadcrumbs` auto-sets this on the last child, but explicit override is supported.
          * @default false
         */
        "current": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the item — renders without an anchor and is not focusable.
          * @default false
         */
        "disabled": boolean;
        /**
          * Destination URL. When empty the item renders as a plain span.
          * @default ''
         */
        "href": string;
        /**
          * Optional leading icon (Material Symbols shorthand).
          * @default ''
         */
        "icon": string;
        /**
          * Position in the trail (0-indexed). Set automatically by the parent `md-breadcrumbs`. Exposed as a reactive prop so children re-render the leading separator when their position changes.
          * @default 0
         */
        "itemIndex": number;
        /**
          * Total number of items in the trail. Set automatically by the parent.
          * @default 0
         */
        "itemTotal": number;
        /**
          * Anchor `rel` attribute.
          * @default ''
         */
        "rel": string;
        /**
          * Anchor `target` attribute (e.g. `_blank`).
          * @default ''
         */
        "target": string;
    }
    interface MdBreadcrumbs {
        /**
          * Programmatically re-collapse the trail to the configured before/after window. Has no effect when collapsing is disabled or when the trail is already collapsed. Emits `mdExpand` when the state changes.
         */
        "collapse": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Programmatically reveal the full breadcrumb trail. Has no effect when `maxItems` is `0` (collapsing disabled) or when the trail is already expanded. Emits `mdExpand` when the state changes.
         */
        "expand": () => Promise<void>;
        /**
          * Accessible name for the overflow ("…") toggle button. Surface this in your i18n layer so screen-reader users in non-English locales hear a localized announcement when collapsing is in effect.
          * @default 'Show full breadcrumb trail'
         */
        "expandLabel": string;
        /**
          * How many items to ALWAYS show at the end of the trail when collapsed. Default 2.
          * @default 2
         */
        "itemsAfterCollapse": number;
        /**
          * How many items to ALWAYS show at the start of the trail when collapsed. Default 1.
          * @default 1
         */
        "itemsBeforeCollapse": number;
        /**
          * Accessible name. Defaults to "Breadcrumb".
          * @default 'Breadcrumb'
         */
        "label": string;
        /**
          * When the number of crumbs exceeds `maxItems`, the middle ones collapse behind an "…" toggle. Set to `0` to disable collapsing.
          * @default 0
         */
        "maxItems": number;
        /**
          * Separator glyph placed between items. Use any single character or short string (e.g. `/`, `›`, `→`, `»`). For SVG / icon separators, override `::part(separator)` from the light DOM with a CSS `background-image` — see readme.
          * @default '/'
         */
        "separator": string;
    }
    interface MdButton {
        /**
          * @default false
         */
        "connectedLeft": boolean;
        /**
          * @default false
         */
        "connectedRight": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled": boolean;
        /**
          * Stretch the button to fill its inline-axis container.  Switches the host from `inline-flex` to `flex` and sets `inline-size: 100%` so the button consumes the full width of its parent — the standard pattern for primary CTAs in forms, modals, and bottom sheets. The size's `min-block-size` (touch target) is preserved.  For arbitrary fixed widths or heights, set the `--md-button-width`, `--md-button-height`, `--md-button-min-width`, `--md-button-min-height`, or `--md-button-max-width` CSS custom properties on the host instead.
          * @default false
         */
        "fullWidth": boolean;
        /**
          * @default null
         */
        "groupTabindex": number | null;
        /**
          * @default ''
         */
        "href": string;
        /**
          * @default ''
         */
        "icon": string;
        /**
          * @default false
         */
        "loading": boolean;
        /**
          * Mirror leading and trailing icons horizontally when the button is rendered in a right-to-left context. Set this on buttons that use **directional** Material Symbols (arrows, chevrons, send, reply…) — non-directional icons (add, search, favorite…) should leave it `false` so the glyph stays semantically correct.
          * @default false
         */
        "mirrorIcon": boolean;
        /**
          * @default true
         */
        "ripple": boolean;
        /**
          * Override the default `role="button"`. Used by composite widgets such as `md-date-picker` day cells (`role="gridcell"`). Leave empty for `button`.
          * @default ''
         */
        "roleOverride": string;
        /**
          * Current selected state (toggle mode)
          * @default false
         */
        "selected": boolean;
        /**
          * @default 'round'
         */
        "shape": 'round' | 'square';
        /**
          * Enable M3 Expressive shape morphing on press and toggle
          * @default true
         */
        "shapeMorph": boolean;
        /**
          * @default 'sm'
         */
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * @default false
         */
        "softDisabled": boolean;
        /**
          * When true, suppresses the default 180° trailing-icon rotation while `aria-expanded="true"`. Use when the consumer swaps the glyph (e.g. docked `md-date-picker` month/year triggers use `chevron_right`).
          * @default false
         */
        "suppressExpandIconFlip": boolean;
        /**
          * @default '_self'
         */
        "target": string;
        /**
          * Enable toggle behavior (selected/unselected)
          * @default false
         */
        "toggle": boolean;
        /**
          * @default ''
         */
        "trailingIcon": string;
        /**
          * Activation behavior, mirroring the native `<button type>`: - `button` (default): no implicit form action. - `submit`: submits the associated form (`requestSubmit`, so the form's   `submit` event fires and constraint validation runs). - `reset`: resets the associated form.
          * @default 'button'
         */
        "type": 'button' | 'submit' | 'reset';
        /**
          * @default ''
         */
        "value": string;
        /**
          * @default 'filled'
         */
        "variant": 'filled' | 'outlined' | 'text' | 'elevated' | 'tonal';
    }
    interface MdButtonGroup {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * When `true` the group stretches to 100% of its container's inline-size and its children share that space equally (`flex: 1 1 0`). Combine with the `--md-button-group-width` / `--md-button-group-max-width` CSS custom properties for fine-grained control over the final size.
          * @default false
         */
        "fullWidth": boolean;
        /**
          * When true, at least one button must remain selected
          * @default false
         */
        "required": boolean;
        /**
          * Selection mode
          * @default 'single-select'
         */
        "selectionMode": 'single-select' | 'multi-select';
        /**
          * Default shape applied to all child buttons
          * @default 'round'
         */
        "shape": 'round' | 'square';
        /**
          * Button size — controls gap (standard) and inner corner radii (connected)
          * @default 'sm'
         */
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Standard groups add padding between buttons; connected groups fuse them together
          * @default 'standard'
         */
        "variant": 'standard' | 'connected';
    }
    interface MdCard {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the card — only meaningful when interactive
          * @default false
         */
        "disabled": boolean;
        /**
          * Enables pointer-event-based drag on the card
          * @default false
         */
        "dragEnabled": boolean;
        /**
          * When `true` the card stretches to 100% of its container's block-size. The parent must have a definite block-size (e.g. a fixed height row in a CSS grid, or a flex item with `align-items: stretch`) for `100%` to resolve.
          * @default false
         */
        "fullHeight": boolean;
        /**
          * When `true` the card stretches to 100% of its container's inline-size. Pair with the `--md-card-max-width` custom property to cap the fluid width (useful inside dashboards and responsive grids).
          * @default false
         */
        "fullWidth": boolean;
        /**
          * Makes the card interactive (clickable, with ripple and state layers).  When the card's own content has no focusable controls the host exposes `role="button"` + `tabindex` and is keyboard-activatable. If the content *does* include its own focusable controls (buttons, links, form fields, interactive `md-*` components) the host does not claim `role="button"` — that would be a `nested-interactive` a11y violation — and instead stays a plain container that is still mouse-clickable (emits `mdClick`) while the inner controls remain the keyboard tab stops.
          * @default false
         */
        "interactive": boolean;
        /**
          * Whether the ripple effect is enabled when interactive
          * @default true
         */
        "ripple": boolean;
        /**
          * Soft-disabled: disabled visuals but remains focusable — only meaningful when interactive
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Visual style variant
          * @default 'elevated'
         */
        "variant": 'elevated' | 'filled' | 'outlined';
    }
    /**
     * `md-checkbox` — Material Design 3 checkbox (with tri-state / indeterminate).
     * Form-associated: inside a `<form>` it submits its `value` under `name` when
     * checked, and restores on form reset. It is a *labelable* element — wrap it in
     * a `<label>` (clicks on the label text toggle it AND give it its accessible
     * name natively), or pass `aria-label` / `aria-labelledby` on the host. A bare,
     * unlabeled checkbox has no accessible name (axe `aria-toggle-field-name`).
     */
    interface MdCheckbox {
        /**
          * Constraint-validation API, matching md-text-field and the native contract.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Whether the checkbox is checked
          * @default false
         */
        "checked": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the checkbox
          * @default false
         */
        "disabled": boolean;
        /**
          * Show the supporting line in the error colour.
          * @default false
         */
        "error": boolean;
        /**
          * Supporting / error message shown beneath the control.  A checkbox previously had nowhere to put a message, so a failed `required` could only surface as the browser's own validation balloon — visually unrelated to the field, and gone the moment you click away. This gives it the same inline supporting line md-text-field has.  The host is a fixed-size touch target, so the line is positioned OUT of that box rather than laid out inside it; see the CSS for why that cannot simply be a flex child.
          * @default ''
         */
        "errorText": string;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Whether the checkbox is in the indeterminate (mixed) state
          * @default false
         */
        "indeterminate": boolean;
        /**
          * Form name
          * @default ''
         */
        "name": string;
        /**
          * Like checkValidity(), but also shows the browser's validation message.
         */
        "reportValidity": () => Promise<boolean>;
        /**
          * Whether the checkbox is required in a form
          * @default false
         */
        "required": boolean;
        /**
          * App/server-side validation: a non-empty message marks the control invalid for its form until cleared with an empty string.
         */
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Soft-disabled: disabled visuals but remains focusable for discoverability
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Supporting text shown when there is no error.
          * @default ''
         */
        "supportingText": string;
        /**
          * Form value submitted when checked
          * @default 'on'
         */
        "value": string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please check this box if you want to proceed.'
         */
        "valueMissingLabel": string;
    }
    interface MdChip {
        /**
          * Surface style of the chip. Distinct from `variant`, which selects the M3 chip TYPE (assist / filter / input / suggestion); a chip has both.  - `outlined` (default) — transparent container with a 1px outline. - `filled` — tonal container, no outline. - `elevated` — tonal container with elevation and no outline.
          * @default 'outlined'
         */
        "appearance": 'outlined' | 'filled' | 'elevated';
        /**
          * Colour role applied to the chip's outline / label / container, depending on `appearance`. Omit for the neutral surface treatment.  Any role name the THEME defines is accepted, not just a fixed list — the chip resolves `<name>` to the `--md-sys-color-<name>` / `-<name>-container` / `-on-<name>-container` family at runtime. So `color="brand"` works as soon as a theme ships those tokens, and a name the theme does not define degrades to the neutral treatment rather than rendering broken.  `primary` | `secondary` | `tertiary` | `error` are the baseline M3 roles; `success` | `warning` | `info` ship in packages/tokens on the same tonal recipe. The union below is a hint for editor autocomplete only — the `string` arm is what makes custom roles work.
         */
        "color"?: | 'primary'
    | 'secondary'
    | 'tertiary'
    | 'error'
    | 'success'
    | 'warning'
    | 'info'
    | (string & {});
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the chip
          * @default false
         */
        "disabled": boolean;
        /**
          * Whether the chip is elevated (adds shadow instead of outline).
          * @deprecated Use `appearance="elevated"`. Kept as an alias so existing markup keeps working: when true it forces the elevated appearance and wins over `appearance`, since a consumer setting it is being explicit.
          * @default false
         */
        "elevated": boolean;
        /**
          * Material Symbols icon name for the leading icon
          * @default ''
         */
        "icon": string;
        /**
          * Label text — alternative to default slot
          * @default ''
         */
        "label": string;
        /**
          * When `true`, the chip renders a trailing remove ("✕") button and responds to Delete / Backspace from the chip itself. Works on every variant.  `variant="input"` is removable by default (per MD3 spec) — setting `removable="false"` explicitly opts out of that behavior.
         */
        "removable"?: boolean;
        /**
          * Whether clicking the chip body toggles `selected`. Defaults to true for `filter`/`input` variants. Set `selectable="false"` for a token that is only a display of a value (e.g. a multi-select's removable chips) — clicking the body then does nothing and the ✕ remove button is the only action, so the chip doesn't flash a misleading "active" state.
         */
        "selectable"?: boolean;
        /**
          * Whether the chip is selected (filter/input variants)
          * @default false
         */
        "selected": boolean;
        /**
          * Soft-disabled: disabled visuals but remains focusable
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Material Symbols icon name for the trailing icon (filter/input only)
          * @default ''
         */
        "trailingIcon": string;
        /**
          * Chip variant
          * @default 'assist'
         */
        "variant": 'assist' | 'filter' | 'input' | 'suggestion';
    }
    interface MdColorPicker {
        /**
          * Show the alpha slider + alpha hex field.
          * @default false
         */
        "alpha": boolean;
        /**
          * Accessible label for the whole picker. Defaults to "Color picker".
          * @default 'Color picker'
         */
        "ariaLabelProp": string;
        /**
          * Programmatically close the popover.
         */
        "close": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the entire control.
          * @default false
         */
        "disabled": boolean;
        /**
          * When true and `variant="popover"`, clicking outside the component closes the open panel. Defaults to `false` so existing popovers keep their current behaviour until opted in.
          * @default false
         */
        "dismissOnOutsideClick": boolean;
        /**
          * Output format for `value` and `mdChange` events.
          * @default 'hex'
         */
        "format": ColorFormat;
        /**
          * Whether the popover is open. Only meaningful when variant=`popover`.
          * @default false
         */
        "open": boolean;
        /**
          * Swatch presets. When non-empty, a small palette appears below the sliders for one-tap colour selection.  Takes either form, so it behaves like every other list-valued prop in the library rather than being the one exception:   - attribute — `presets="#000000,#FFFFFF,#FF5722"` (comma-separated)   - property  — `el.presets = ['#000000', '#FFFFFF', '#FF5722']`
          * @default ''
         */
        "presets": string | string[];
        /**
          * Programmatically open the popover.
         */
        "show": () => Promise<void>;
        /**
          * Show the hex input field below the sliders.
          * @default true
         */
        "showHex": boolean;
        /**
          * Show the RGB/HSL numeric inputs.
          * @default true
         */
        "showInputs": boolean;
        /**
          * Current value. Accepts any 3/4/6/8-digit hex, `rgb()`, `rgba()`, or the formats emitted by this component. Two-way bindable.
          * @default '#6750A4'
         */
        "value": string;
        /**
          * Layout variant. - `inline` (default) — picker UI is always visible. - `popover` — opens the picker in a floating panel anchored to itself,   behind a trigger YOU slot into `trigger`. There is no built-in trigger.
          * @default 'inline'
         */
        "variant": ColorPickerVariant;
    }
    /**
     * `md-date-picker` — a Material Design 3 date picker.
     * Variants:
     * - `modal-input` *(default)* — outlined text field + modal calendar dialog.
     * - `modal` — bare modal dialog surfaced via `show()` / `open`.
     * - `docked` — outlined text field + dropdown calendar anchored to the field.
     * The modal dialog can flip to a typed-entry view (date input) via the
     * header toggle, satisfying the MD3 "modal date input" pattern.
     */
    interface MdDatePicker {
        /**
          * Material Symbols name for the trailing calendar toggle on the field. Ignored when a `calendar-icon` slot is provided.
          * @default 'calendar_today'
         */
        "calendarIcon": string;
        /**
          * Label of the cancel button in the modal action row.
          * @default 'Cancel'
         */
        "cancelLabel": string;
        /**
          * Constraint-validation API, matching md-text-field and the native contract.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Tooltip suffix on the modal month/year nav button.
          * @default 'Choose month and year'
         */
        "chooseMonthAndYearLabel": string;
        /**
          * Accessible label for the docked month menu button.
          * @default 'Choose month'
         */
        "chooseMonthLabel": string;
        /**
          * Accessible label for the modal month/year toggle button.
          * @default 'Choose a different month and year'
         */
        "chooseMonthYearLabel": string;
        /**
          * Accessible label for the docked year menu button.
          * @default 'Choose year'
         */
        "chooseYearLabel": string;
        /**
          * Clear the current selection.
         */
        "clear": () => Promise<void>;
        /**
          * Accessible label for the clear button.
          * @default 'Clear date'
         */
        "clearLabel": string;
        /**
          * Show a clear ("×") button on the field trigger while a date is selected, which resets the value (same as calling `clear()`). Applies to the `modal-input` and `docked` variants.
          * @default false
         */
        "clearable": boolean;
        /**
          * Close the picker without committing.
         */
        "close": () => Promise<void>;
        /**
          * Trigger calendar icon label when the panel is open.
          * @default 'Close calendar'
         */
        "closeCalendarLabel": string;
        /**
          * Commit a day the moment it is clicked, instead of staging it behind the Cancel / OK row.  By default every variant STAGES a day click (`mdSelected`) and commits only on OK (`mdChange`). With this set the two collapse into one interaction: the click stages and commits together, the panel dismisses, and the built-in action row is not rendered at all — there is nothing left for it to confirm.  Suits a low-stakes, reversible field. Keep the default when the choice is expensive to get wrong and the user deserves an explicit confirm step.
          * @default false
         */
        "commitOnSelect": boolean;
        /**
          * Character between day, month, and year in the text field (display and parsing). Leave empty to use the locale default (e.g. `/` for `en-US`, `.` for `de-DE`). The `value` prop and `mdChange` / `mdSelected` events always use ISO `YYYY-MM-DD` regardless of this setting.
          * @example ```html <md-date-picker date-separator="/" value="2025-06-15"></md-date-picker> <!-- displays 06/15/2025 in en-US --> <md-date-picker date-separator="." locale="de-DE"></md-date-picker> <!-- displays and parses DD.MM.YYYY --> ```
          * @default ''
         */
        "dateSeparator": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — blocks all interaction and removes from tab order.
          * @default false
         */
        "disabled": boolean;
        /**
          * Large headline shown in typed-entry (input) mode.
          * @default 'Enter dates'
         */
        "enterDatesLabel": string;
        /**
          * Error state — paints the field with the error color and shows `error-text`.
          * @default false
         */
        "error": boolean;
        /**
          * Error message shown below the field when `error` is true.
          * @default ''
         */
        "errorText": string;
        /**
          * Visual variant of the trigger `md-text-field` (`modal-input` and `docked`). Use `filled` with `--md-date-picker-field-container-color` for branded fills.
          * @default 'outlined'
         */
        "fieldVariant": 'outlined' | 'filled';
        /**
          * First day of the week (0 = Sunday … 6 = Saturday). `-1` derives it from the locale.
          * @default -1
         */
        "firstDayOfWeek": number;
        /**
          * Move keyboard focus to the field's text input (if present).
         */
        "focusInput": () => Promise<void>;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Headline / supporting text shown at the top of the modal header.
          * @default 'Select date'
         */
        "headline": string;
        /**
          * Default error when a typed date cannot be parsed or is out of range.
          * @default 'Invalid date'
         */
        "invalidDateLabel": string;
        /**
          * Predicate that disables individual dates (return `true` to disable).
         */
        "isDateDisabled"?: (date: Date) => boolean;
        /**
          * Floating label / accessible name for the field.
          * @default 'Date'
         */
        "label": string;
        /**
          * BCP-47 locale for month/weekday names and date formatting.
          * @default ''
         */
        "locale": string;
        /**
          * Latest selectable date, inclusive, as ISO `YYYY-MM-DD`.
          * @default ''
         */
        "max": string;
        /**
          * Earliest selectable date, inclusive, as ISO `YYYY-MM-DD`.
          * @default ''
         */
        "min": string;
        /**
          * Form-association name attribute.
          * @default ''
         */
        "name": string;
        /**
          * Accessible label for the "next month" nav chevron.
          * @default 'Next month'
         */
        "nextMonthLabel": string;
        /**
          * Accessible label for the "next year" nav chevron.
          * @default 'Next year'
         */
        "nextYearLabel": string;
        /**
          * Label of the confirm button in the modal action row.
          * @default 'OK'
         */
        "okLabel": string;
        /**
          * Whether the modal / docked panel is open. Two-way bindable.
          * @default false
         */
        "open": boolean;
        /**
          * Trigger calendar icon label when the panel is closed.
          * @default 'Open calendar'
         */
        "openCalendarLabel": string;
        /**
          * Clicking outside a DOCKED panel dismisses it (like `scrim-dismissible` for the modal, which has a scrim to catch the click and a docked panel does not).  Set `false` to keep the panel open until Cancel / OK / Esc — useful when the picker sits inside another popup whose own outside-click handling would otherwise fight it, or when a stray click should not discard staged input.  Dismissing this way is a cancel: it emits `mdCancel` and discards staging, exactly as the modal scrim does.
          * @default true
         */
        "outsideClickDismissible": boolean;
        /**
          * Placeholder shown inside the text-field input.
          * @default ''
         */
        "placeholder": string;
        /**
          * Accessible label for the "previous month" nav chevron.
          * @default 'Previous month'
         */
        "previousMonthLabel": string;
        /**
          * Accessible label for the "previous year" nav chevron.
          * @default 'Previous year'
         */
        "previousYearLabel": string;
        "reportValidity": () => Promise<boolean>;
        /**
          * Marks the field as required (for form-validation contexts).
          * @default false
         */
        "required": boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace": boolean;
        /**
          * Clicking the modal scrim closes the picker when true.
          * @default true
         */
        "scrimDismissible": boolean;
        /**
          * Large headline when no date is staged in calendar mode.
          * @default 'Select date'
         */
        "selectDateLabel": string;
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Open the picker.
         */
        "show": () => Promise<void>;
        /**
          * Helper text shown below the field. Hidden while an error is displayed.
          * @default ''
         */
        "supportingText": string;
        /**
          * Mode-toggle label when in typed-entry mode (switch to calendar).
          * @default 'Switch to calendar input'
         */
        "toggleCalendarLabel": string;
        /**
          * Mode-toggle label when in calendar mode (switch to text).
          * @default 'Switch to text input'
         */
        "toggleTextLabel": string;
        /**
          * Selected date as ISO `YYYY-MM-DD`. Two-way bindable.
          * @default ''
         */
        "value": string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please choose a date.'
         */
        "valueMissingLabel": string;
        /**
          * Presentation variant.
          * @default 'modal-input'
         */
        "variant": 'modal' | 'modal-input' | 'docked';
        /**
          * Accessible label for the modal year-selection grid.
          * @default 'Year'
         */
        "yearGridLabel": string;
    }
    interface MdDialog {
        /**
          * Label for the default cancel action button (slot fallback). Leave empty to derive from `locale`.
          * @default ''
         */
        "cancelLabel": string;
        /**
          * Close the dialog
         */
        "close": () => Promise<void>;
        /**
          * Accessible label for the full-screen close button. Leave empty to derive from `locale`.
          * @default ''
         */
        "closeLabel": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Show divider between content and actions
          * @default false
         */
        "divider": boolean;
        /**
          * Full-screen variant
          * @default false
         */
        "fullscreen": boolean;
        /**
          * Show divider between header and content (full-screen)
          * @default false
         */
        "headerDivider": boolean;
        /**
          * Headline text (or use the headline slot)
          * @default ''
         */
        "headline": string;
        /**
          * Icon name (Material Symbols shorthand, or use the icon slot)
          * @default ''
         */
        "icon": string;
        /**
          * BCP-47 locale for built-in button labels (Close, Cancel, OK).
          * @default 'en-US'
         */
        "locale": string;
        /**
          * Label for the default confirm action button (slot fallback). Leave empty to derive from `locale`.
          * @default ''
         */
        "okLabel": string;
        /**
          * Whether the dialog is open
          * @default false
         */
        "open": boolean;
        /**
          * Whether clicking the scrim closes the dialog
          * @default true
         */
        "scrimDismissible": boolean;
        /**
          * Open the dialog
         */
        "show": () => Promise<void>;
    }
    interface MdDivider {
        /**
          * Middle-inset: 16px margin on both sides
          * @default false
         */
        "inset": boolean;
        /**
          * Inset end only: 0px leading margin, 16px trailing
          * @default false
         */
        "insetEnd": boolean;
        /**
          * Inset start only: 16px leading margin, 0px trailing
          * @default false
         */
        "insetStart": boolean;
        /**
          * Vertical orientation
          * @default false
         */
        "vertical": boolean;
    }
    interface MdFab {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled": boolean;
        /**
          * Whether the FAB shows its label (extended) or collapses to an icon-only circle. Leave unset for the default behaviour (extended whenever a `label` is provided). Set explicitly — e.g. by `<md-navigation-rail>` on collapse — to morph between extended and icon-only: the label width/opacity springs closed and the container shrinks to a 56px circle, while the label stays available as the accessible name.
         */
        "extended"?: boolean;
        /**
          * @default ''
         */
        "icon": string;
        /**
          * @default ''
         */
        "label": string;
        /**
          * @default false
         */
        "lowered": boolean;
        /**
          * @default true
         */
        "ripple": boolean;
        /**
          * @default 'standard'
         */
        "size": 'standard' | 'medium' | 'large';
        /**
          * @default false
         */
        "softDisabled": boolean;
        /**
          * @default 'primary-container'
         */
        "variant": 'surface' | 'primary-container' | 'secondary-container' | 'tertiary-container' | 'primary' | 'secondary' | 'tertiary';
    }
    interface MdFabMenu {
        /**
          * ID of the anchor FAB element to position relative to.
          * @default ''
         */
        "anchor": string;
        /**
          * Closes the menu programmatically.
         */
        "close": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Accessible name for the popup itself (the `role="menu"` container).  A prop rather than a hardcoded string, matching how every other user-facing string in the library is localised: components stay i18n-engine-agnostic and the consumer resolves the text from its own dictionary. The default is English, so a translated app must set it.
          * @default 'Actions'
         */
        "menuLabel": string;
        /**
          * Whether the menu is open.
          * @default false
         */
        "open": boolean;
        /**
          * Direction items fan out from the FAB. - 'up': items stack above the FAB (default, standard bottom-right placement) - 'down': items stack below the FAB - 'auto': detect based on available viewport space
          * @default 'up'
         */
        "placement": 'up' | 'down' | 'auto';
        /**
          * Skip open/close animation.
          * @default false
         */
        "quick": boolean;
        /**
          * Opens the menu programmatically.
         */
        "show": () => Promise<void>;
        /**
          * Color set for the menu. Controls the FAB's open-state color (vibrant) and item container color (softer).
          * @default 'primary'
         */
        "variant": 'primary' | 'secondary' | 'tertiary';
    }
    interface MdFabMenuItem {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Whether the item is disabled (removed from tab order).
          * @default false
         */
        "disabled": boolean;
        /**
          * Material Symbols icon name.
          * @default ''
         */
        "icon": string;
        /**
          * Item label text.
          * @default ''
         */
        "label": string;
        /**
          * When `true`, paints the keyboard-focus ring even if the item does not hold DOM focus (e.g. arrow-key navigation drove `focusItem()`). Set by `md-fab-menu` during programmatic roving — not on initial open.
          * @default false
         */
        "rovingFocusVisible": boolean;
        /**
          * Whether the item is soft-disabled (still focusable but not activatable).
          * @default false
         */
        "softDisabled": boolean;
    }
    interface MdIconButton {
        /**
          * M3 Expressive width (narrow, default, wide)
          * @default 'default'
         */
        "buttonWidth": 'narrow' | 'default' | 'wide';
        /**
          * @default false
         */
        "connectedLeft": boolean;
        /**
          * @default false
         */
        "connectedRight": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Prevents interaction and removes from tab order
          * @default false
         */
        "disabled": boolean;
        /**
          * @default null
         */
        "groupTabindex": number | null;
        /**
          * Turns the button into a link
          * @default ''
         */
        "href": string;
        /**
          * Material Symbols icon name
          * @default ''
         */
        "icon": string;
        /**
          * Whether to show the ripple effect
          * @default true
         */
        "ripple": boolean;
        /**
          * Current selected state (toggle mode)
          * @default false
         */
        "selected": boolean;
        /**
          * Icon shown when selected in toggle mode
          * @default ''
         */
        "selectedIcon": string;
        /**
          * Container shape
          * @default 'round'
         */
        "shape": 'round' | 'square';
        /**
          * Enable M3 Expressive shape morphing on press and toggle
          * @default true
         */
        "shapeMorph": boolean;
        /**
          * M3 Expressive size
          * @default 'sm'
         */
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Visually disabled but remains focusable for discoverability
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Link target attribute
          * @default '_self'
         */
        "target": string;
        /**
          * Enable toggle behavior (selected/unselected)
          * @default false
         */
        "toggle": boolean;
        /**
          * @default ''
         */
        "value": string;
        /**
          * Visual style variant
          * @default 'standard'
         */
        "variant": 'standard' | 'filled' | 'outlined' | 'tonal';
    }
    /**
     * md-line-chart — Material Design 3 line chart rendered by the
     * in-house chart engine (Canvas2D + a DOM text overlay). No
     * ECharts.
     * Pattern parity with MUI X Charts:
     *   • multi-series with per-series colour / label / curve
     *   • category, time, value, log scales
     *   • curve types: linear / smooth / monotone / step variants
     *   • stacking: normal / percentage
     *   • markers, area fills, gap bridging (`connectNulls`)
     *   • legend with toggle + custom position
     *   • hover crosshair + tooltip
     *   • emits `mdMarkerClick`, `mdLineClick`, `mdAreaClick`, `mdAxisClick`,
     *     `mdLegendClick`, `mdHover`
     * MD3 expressive layer on top:
     *   • palette resolves to `--md-sys-color-*` tokens
     *   • dark theme + brand re-themes via CSS variables only
     *   • a11y: role="figure" + screen-reader-only data table
     *   • fully responsive via ResizeObserver
     */
    interface MdLineChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation": MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Render the line area as a filled gradient below the line.
          * @default false
         */
        "area": boolean;
        /**
          * Draw small perpendicular tick marks on the axes.
          * @default false
         */
        "axisTicks": boolean;
        /**
          * Whether `null` values are bridged with a straight line.
          * @default false
         */
        "connectNulls": boolean;
        /**
          * Default curve interpolation. Overridable per-series.
          * @default 'smooth'
         */
        "curve": 'linear' | 'smooth' | 'monotone' | 'step' | 'step-before' | 'step-middle';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Return the underlying chart engine for advanced cases.
         */
        "getInstance": () => Promise<LineChartEngine | null>;
        /**
          * Gridlines: horizontal (y ticks), vertical (x ticks), both, or none.
          * @default 'horizontal'
         */
        "grid": 'none' | 'horizontal' | 'vertical' | 'both';
        /**
          * Force a specific height (CSS length).
         */
        "heightProp"?: string;
        /**
          * Transpose the axes — the x-axis data runs vertically and the values run horizontally (e.g. a temperature-by-altitude spline). Best with `curve="smooth"`.
          * @default false
         */
        "inverted": boolean;
        /**
          * Optional chart title rendered above the plot.
          * @default ''
         */
        "label": string;
        /**
          * Text shown when there is no data. The `empty` slot still overrides it — this is the prop form, for handing a string straight from a dictionary.
          * @default 'No data to display'
         */
        "labelEmpty": string;
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the data with the arrow keys; this is what tells them so.
          * @default 'Chart data. Use the arrow keys to move between points, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot": string;
        /**
          * Template for the live announcement made as keyboard focus moves. `%x%` is the axis value and `%values%` the series readings at it.
          * @default '%x%: %values%'
         */
        "labelPoint": string;
        /**
          * Accessible label for the zoom slider's end thumb.
          * @default 'Zoom range end'
         */
        "labelZoomEnd": string;
        /**
          * Accessible label for the zoom slider's start thumb.
          * @default 'Zoom range start'
         */
        "labelZoomStart": string;
        /**
          * Legend position. `'none'` hides the legend.
          * @default 'top-end'
         */
        "legend": MdChartLegendPosition | 'none';
        /**
          * Line stroke width in px, for every series. Default 2.5; a per-series `series[].lineWidth` overrides it.
         */
        "lineWidth"?: number;
        /**
          * Data is still on its way — the chart covers the plot with a loader instead of drawing an empty (or stale) axis, and marks itself `aria-busy`.  Set it while the fetch is in flight and clear it when `series` arrives; the entry animation replays on the way out, so the data draws itself in rather than appearing fully formed. Slot `loading` to replace the default indicator with your own skeleton.
          * @default false
         */
        "loading": boolean;
        /**
          * Accessible + visible text under the loader.
          * @default 'Loading chart…'
         */
        "loadingLabel": string;
        /**
          * BCP-47 locale for the DEFAULT number / date formatting — axis ticks, tooltip values and the screen-reader table. Empty follows the browser. An explicit `valueFormatter` / `xAxis.valueFormatter` always wins, so a consumer that formats its own values is unaffected.
          * @default ''
         */
        "locale": string;
        /**
          * Vertical reference lines across the plot (e.g. a "current time" divider).
         */
        "markLines": MdChartMarkLine[] | undefined;
        /**
          * Marker RADIUS in px, for every series. Overrides the per-symbol default; a per-series `series[].markSize` wins.
         */
        "markSize"?: number;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation": boolean;
        /**
          * Re-read the MD3 tokens from the host's computed style and repaint.  The chart does this automatically when the tokens actually change, so reach for this only when a theme is applied in a way the watcher cannot observe — tokens injected into a stylesheet rather than onto an element, for instance. Cheaper and more direct than reassigning `series` to force a rebuild.
         */
        "refreshTheme": () => Promise<void>;
        /**
          * Replay the entry animation from the start (uses the current `animation`).
         */
        "replay": () => Promise<void>;
        /**
          * Drop the zoom window and show the full range.
         */
        "resetZoom": () => Promise<void>;
        "resize": () => Promise<void>;
        /**
          * Series array. Each entry is one line. Empty → empty state.  `data` is either a bare list of y values, positioned by index against `xAxis.data`, or a list of points carrying their own x — `{ x: '2021-11-13', y: 0.12 }` / `['2021-11-13', 0.12]`. Points are what irregular data needs: uneven sampling, or several series measured on completely different dates. Set `xAxis.scale = 'time'` (or `'value'`) so the gaps render proportionally; series carrying their own x need no `xAxis.data` at all.
          * @default []
         */
        "series": MdChartXYSeries[];
        /**
          * Label each series at its last point with its name (follows the line end).
          * @default false
         */
        "seriesLabels": boolean;
        /**
          * Zoom to an absolute index range.
         */
        "setZoom": (startIndex: number, endIndex: number) => Promise<void>;
        /**
          * Print each point's value as a data label beside its marker.
          * @default false
         */
        "showLabels": boolean;
        /**
          * Draw the line for each series. Default `true`. Set `false` for a fill-only chart (with `area`) or a marks-only chart — handy when a high `fillOpacity` makes the same-coloured line vanish into the fill. A per-series `series[].stroke` still wins over this.
          * @default true
         */
        "showLine": boolean;
        /**
          * Show data-point markers on every series by default.
          * @default false
         */
        "showMarks": boolean;
        /**
          * Stacking strategy.
          * @default 'none'
         */
        "stack": MdChartStackMode;
        /**
          * Optional sub-title rendered under the title in the muted text colour.
         */
        "subtitle": string | undefined;
        /**
          * Replaces the generated `aria-label` outright. The default summary is assembled in English ("Revenue, Line chart, with 2 series, from Jan to Dec."); rather than translate it piecewise, hand over the whole sentence built in your own language.
          * @default ''
         */
        "summary": string;
        /**
          * Translatable chrome for the screen-reader data table. `%shown%` and `%total%` are substituted in `truncated`.
         */
        "tableLabels"?: { x?: string; index?: string; series?: string; truncated?: string };
        /**
          * Title alignment over the plot: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign": MdChartTitleAlign;
        /**
          * Render the current frame to a PNG data URL.
         */
        "toDataURL": () => Promise<string>;
        /**
          * Tooltip interaction model.
          * @default 'axis'
         */
        "tooltip": MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content with your own. Receives everything the chart knows about the hovered x — the axis value + its formatted label, and per visible series the index, label, colour, raw and formatted value, and whether it is the emphasised one — split into `series` (has a reading here) and `missing` (does not).  Return a DOM `Node` (what React / Vue / Angular renderers produce), a string (inserted as TEXT, never parsed as markup), `{ unsafeHtml }` to opt into raw HTML, `undefined` to fall back to the built-in tooltip, or `null` for no tooltip at this x. The engine keeps positioning it, and it is still the `tooltip` CSS part.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        /**
          * Format raw Y values for tooltips / a11y table.  The tooltip also asks it about series with NO value at the hovered x — `null` when the series has a null datum there, `undefined` when it has no datum at all. Return `''` (or leave those cases unhandled) to keep the default behaviour of omitting the series; return a string to show a row for it — e.g. `(v) => (v === null ? 'no reading' : v === undefined ? '—' : v + ' m')`.
         */
        "valueFormatter"?: (value: number | null | undefined) => string;
        /**
          * X-axis configuration.
         */
        "xAxis": MdChartAxis | undefined;
        /**
          * Multiple independent value (y) axes. When set (non-empty), each series is measured against `yAxes[series.yAxisIndex ?? 0]`, and the axes stack outward from the plot (first → left, rest → right, or per each axis' `position`). Supersedes the single `yAxis`. Each axis keeps its own `min`/`max`/`scale`/ `valueFormatter`/`label`.
         */
        "yAxes": MdChartAxis[] | undefined;
        /**
          * Y-axis configuration.
         */
        "yAxis": MdChartAxis | undefined;
        /**
          * Interactive zoom over the x range.   • `inside` — drag horizontally across the plot to zoom into that span;     double-click anywhere in the plot to reset.   • `slider` — an `md-slider` in range mode under the plot: drag either     thumb to resize the window, click the track to jump the nearest thumb,     drag the window itself to pan. Thumbs are focusable and take     Arrow/Home/End/PageUp/PageDown.   • `both` — both of the above.  Zoom is a *view* over the data: `series`/`xAxis` are untouched, and `mdHover`/`mdMarkerClick` keep reporting absolute indices into your data.
          * @default 'none'
         */
        "zoom": 'none' | 'inside' | 'slider' | 'both';
    }
    interface MdList {
        "activateNext": () => Promise<HTMLMdListItemElement | null>;
        /**
          * Move roving focus to the previous focusable item, wrapping at the start. Returns the newly active item, or `null` when no items are focusable.
         */
        "activatePrevious": () => Promise<HTMLMdListItemElement | null>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Returns the indices of all currently selected items.
         */
        "getSelectedIndices": () => Promise<number[]>;
        /**
          * Row interaction pattern for the whole list. A list uses exactly one interaction mode at a time (per the M3 Lists spec).  - `single-action` (default): each row is one tappable area — leading   icon + label activate together. Use `type="button"` (or selection   mode) on items. - `multi-action`: rows expose a primary action (the row body) plus   optional secondary actions in `slot="trailing"` (e.g. `md-icon-button`).   Clicks on trailing controls do not activate the primary row action;   each secondary control keeps its own focus stop and keyboard behavior.
          * @default 'single-action'
         */
        "interactionMode": MdListInteractionMode;
        /**
          * Optional aria-label for the container.
          * @default ''
         */
        "label": string;
        /**
          * Optional aria-labelledby for the container.
          * @default ''
         */
        "labelledby": string;
        /**
          * Visual style of the list.  - `standard` (default, M3 Expressive): rows share a single rounded   chassis with **no gap** between them. The first row rounds its   top corners, the last rounds its bottom corners, and middle rows   are square so the stack reads as one continuous shape — the M3   "connected" pattern. Best for settings panels, option groups,   and iOS-style sectioned tables. To add hairline separators   between rows, interleave `<md-divider>` elements. - `segmented` (M3 Expressive): rows are individually rounded "tiles"   separated by a small gap. Each tile rounds all four corners. Best   for action sets, quick-action menus, and card-like sections where   each row should read as its own affordance.
          * @default 'standard'
         */
        "listStyle": MdListStyle;
        /**
          * Enable drag-to-reorder. Each row renders a trailing drag handle (grab it to drag the row to a new position); rows can also be moved with `Alt + ArrowUp / ArrowDown` while focused. On drop the list reorders its children in the DOM and emits {@link mdReorder}. Disabled rows can't be picked up. Works alongside `single-action` / `multi-action` and selection modes.
          * @default false
         */
        "reorderable": boolean;
        /**
          * Override the auto-computed ARIA role. Useful when wiring the list into a wider widget pattern (e.g. `role="menu"` for a dropdown). Leave empty to use the role implied by `selectionMode`.
          * @default ''
         */
        "roleOverride": string;
        /**
          * Programmatically toggle selection on the item at `index` according to the current `selection-mode`. Does not emit `mdSelect` (use it to sync external state without a user action).
         */
        "selectItem": (index: number) => Promise<void>;
        /**
          * Selection coordination across child rows.  - `none` (default): each row stands alone and the container is always   `role="list"` (rows are `listitem`s). Interactive rows (`type="button"` /   `type="link"`) expose their widget role on an inner primary element, so   the list validly owns only `listitem`s while still providing the same   roving-tabindex + arrow-key navigation. `type="text"` rows stay   non-focusable. - `single-select`: the container behaves as `role="listbox"`, items   become `role="option"`, and exactly one item carries `aria-selected`. - `multi-select`: same as above plus `aria-multiselectable="true"`.  Switching to a selection mode also auto-promotes plain `type="text"` rows to focusable so users can pick from a list of label-only rows without authoring extra props.
          * @default 'none'
         */
        "selectionMode": MdListSelectionMode;
    }
    interface MdListItem {
        /**
          * Collapse the row. No-op when `expandable` is false.
         */
        "collapse": () => Promise<void>;
        /**
          * The resolved ARIA role of the parent `md-list` container, propagated imperatively by that parent — authors should not touch this. It lets a row adapt its own role to its container: when the container is a `menu` / `menubar` (via `role-override`) an interactive row exposes `role="menuitem"` on the HOST itself (the host IS the widget, like `option`) and skips the inner primary widget, because a `menu` owns its direct light-DOM children (the hosts) and must own `menuitem`s — a `listitem` / `role="none"` host is a disallowed menu child (axe `aria-required-children`). Empty for a plain `list` / `listbox`, where the default `listitem` / `option` roles apply.
          * @default ''
         */
        "containerRole": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disables interaction, removes from tab order, and dims content to 38 % opacity.
          * @default false
         */
        "disabled": boolean;
        /**
          * Expand the row. No-op when `expandable` is false.
         */
        "expand": () => Promise<void>;
        /**
          * Turns the row into a single-level collapsible disclosure. When `true`:   - a trailing **`md-icon-button`** caret renders after any author-supplied     trailing content. It is the disclosure control: it carries     `aria-expanded` + `aria-controls`, gets the icon-button pill/squircle     shape morph when selected (expanded), and its `expand_more` glyph     rotates 180° (points up) when expanded.   - the `expanded-content` slot reveals one level of flat `md-list-item`     child rows (not a nested `md-list` chassis) inline below the parent     with an MD3 expressive open (emphasized-decelerate) / retract     (emphasized-accelerate) height + opacity animation.   - clicking the row body also toggles the panel as a convenience; the     caret button additionally toggles on Enter / Space when focused.   - ArrowRight expands, ArrowLeft collapses (when the row itself is     focusable, e.g. `type="button"` / reorderable rows).
          * @default false
         */
        "expandable": boolean;
        /**
          * Reflects/controls the expanded state of an `expandable` row.
          * @default false
         */
        "expanded": boolean;
        /**
          * Programmatically focus this row (delegates to {@link setFocus}).
         */
        "focusItem": () => Promise<void>;
        /**
          * Headline text. Overridden by `slot="headline"`.
          * @default ''
         */
        "headline": string;
        /**
          * Link href. Honored when `type='link'`.
          * @default ''
         */
        "href": string;
        /**
          * Interaction mode propagated from parent `md-list`. Set imperatively by the parent — authors should not need to touch this directly.  - `single-action`: the whole row is one primary action. - `multi-action`: trailing slot may host secondary controls (icon   buttons, switches) that keep their own focus/click semantics.
          * @default 'single-action'
         */
        "interactionMode": 'single-action' | 'multi-action';
        /**
          * Avatar image URL — forwarded to an internal `md-avatar` rendered in the leading slot. Falls back to `leadingAvatarName` / `leadingAvatarLabel` if the image fails to load.
          * @default ''
         */
        "leadingAvatar": string;
        /**
          * Alt text for `leadingAvatar`. Also surfaced as the avatar's accessible label.
          * @default ''
         */
        "leadingAvatarAlt": string;
        /**
          * Explicit initials shown by the internal `md-avatar`. Overrides the `leadingAvatarName` parsing. Used for "C", "JD" style labels.
          * @default ''
         */
        "leadingAvatarLabel": string;
        /**
          * Full name forwarded to the internal `md-avatar` so it can auto-derive initials when no image is available (`"Ada Lovelace"` → `"AL"`). When both this and `leadingAvatarLabel` are empty the avatar falls back to a `person` icon.
          * @default ''
         */
        "leadingAvatarName": string;
        /**
          * Material Symbols name for the leading slot (24 dp container, 18 dp icon).
          * @default ''
         */
        "leadingIcon": string;
        /**
          * Image thumbnail URL (56 dp square by default).
          * @default ''
         */
        "leadingImage": string;
        /**
          * Alt text for `leadingImage`.
          * @default ''
         */
        "leadingImageAlt": string;
        /**
          * Number of text rows. Auto-detected when 1 + content suggests more (overline → 2-line, overline + supporting → 3-line). Set explicitly to override the auto-promotion.
          * @default 1
         */
        "lines": 1 | 2 | 3;
        /**
          * Overline text shown above the headline. Overridden by `slot="overline"`.
          * @default ''
         */
        "overline": string;
        /**
          * Whether this row can be reordered via drag-and-drop. Set imperatively by the parent `md-list[reorderable]` — authors should not need to touch this directly. When `true` the row renders a trailing drag handle (grab it to drag) and supports `Alt + ArrowUp / ArrowDown` to move when focused. Disabled rows ignore it.
          * @default false
         */
        "reorderable": boolean;
        /**
          * When `true`, paints the keyboard-focus ring even if the row does not hold DOM focus (e.g. toolbar buttons drove `activateNext()`). Set imperatively by `md-list` during programmatic / arrow-key navigation — not on initial load.
          * @default false
         */
        "rovingFocusVisible": boolean;
        /**
          * Selected state. Toggled by `md-list` when `selection-mode` is on.
          * @default false
         */
        "selected": boolean;
        /**
          * Selection mode propagated from parent `md-list`. Set imperatively by the parent — authors should not need to touch this directly. When non-`none`, the item becomes focusable + clickable regardless of `type` and renders with `role="option"` + `aria-selected`.
          * @default 'none'
         */
        "selectionMode": 'none' | 'single-select' | 'multi-select';
        /**
          * Reliable imperative focus used by the parent `md-list`'s roving tabindex / `activateNext` / `activatePrevious`. It does NOT depend on the async Stencil render having flushed the roving `tabindex` onto the target: it ensures the target is focusable *now* (adding a tabindex if the render hasn't written one yet), then focuses it. This fixes the case where `md-list` calls `.focus()` right after toggling `tabbable` — before the re-render — which otherwise left focus on `<body>`.
         */
        "setFocus": (options?: FocusOptions) => Promise<void>;
        /**
          * Visually disabled but remains focusable for discoverability. Does not emit activation events.
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Supporting text below the headline. Overridden by `slot="supporting-text"`.
          * @default ''
         */
        "supportingText": string;
        /**
          * Whether this item is the currently tabbable one in its parent list. Managed by `md-list`'s roving tabindex. Standalone items leave this as `true` and become focusable when `type !== 'text'`.
          * @default true
         */
        "tabbable": boolean;
        /**
          * Link target. Honored when `type='link'`.
          * @default '_self'
         */
        "target": string;
        /**
          * Toggles the `expanded` state. No-op when `expandable` is false.
         */
        "toggle": () => Promise<void>;
        /**
          * Material Symbols name for the trailing slot (24 dp icon).
          * @default ''
         */
        "trailingIcon": string;
        /**
          * Trailing supporting text on the right. Overridden by `slot="trailing-supporting-text"`.
          * @default ''
         */
        "trailingSupportingText": string;
        /**
          * Behavior of the row.  - `text` (default): non-interactive content row. No focus, no ripple,   no click event. Use for static labels and section headers. - `button`: focusable, ripples, fires `mdClick` on activation. - `link`: same as `button` but also navigates to `href` in `target`.  When the row sits inside an `md-list` with `selection-mode` set, the row becomes focusable + clickable regardless of `type` so users can pick from a list of plain rows without authoring extra props.
          * @default 'text'
         */
        "type": MdListItemType;
    }
    interface MdLoadingIndicator {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Accessible label for screen readers
          * @default 'Loading'
         */
        "label": string;
        /**
          * Visual style: uncontained (default) or contained (with background container)
          * @default 'uncontained'
         */
        "variant": 'uncontained' | 'contained';
    }
    interface MdMenu {
        /**
          * ID of the anchor element to position relative to.
          * @default ''
         */
        "anchor": string;
        /**
          * When false, opening the menu initializes roving tabindex without moving focus to the first item. The consumer handles focus (e.g. docked pickers that scroll to the selected option). `show({ autoFocus: false })` still takes precedence for a single open.
          * @default true
         */
        "autoFocus": boolean;
        /**
          * Closes the menu programmatically.
         */
        "close": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Message shown when all menu items are hidden (e.g. no filter results). Empty string disables.
          * @default ''
         */
        "emptyText": string;
        /**
          * The scrollable viewport element (the menu's `.md-menu__scroll-shadow` scroll div), or null when the menu isn't capped/scrollable. A virtualized host attaches its scroll listener and reads `scrollTop`/`clientHeight` from this.
         */
        "getScrollViewport": () => Promise<HTMLElement | null>;
        /**
          * Layout style for vertical menus. 'standard' is flat, 'grouped' uses visual section separators.
          * @default 'standard'
         */
        "layout": 'standard' | 'grouped';
        "listLabel"?: string;
        /**
          * Present the popup as a WAI-ARIA listbox (surface `role="presentation"`, anchor `aria-haspopup="listbox"`) rather than a menu. `md-select` sets this for both its virtual and non-virtual lists so the popup is a listbox of options regardless of size. (The virtual path additionally registers a provider, which adds the aria-activedescendant focus model.)
          * @default false
         */
        "listbox": boolean;
        /**
          * When true, the menu surface min-width matches the anchor element's width. The menu still grows wider if items need more space.
          * @default false
         */
        "matchAnchorWidth": boolean;
        /**
          * Cap the menu surface block-size. When the items exceed it the list scrolls vertically inside a plain scroll viewport. A number is treated as pixels; any CSS length is passed through (e.g. `'50vh'`). Ignored when the menu has submenus — their flyouts must not be clipped by an overflow container.
         */
        "maxHeight"?: number | string;
        /**
          * Whether the menu is open.
          * @default false
         */
        "open": boolean;
        /**
          * When set, the menu does NOT auto-dismiss in response to interaction elsewhere on the page. By default a menu closes when:   1. The user clicks outside the menu and its anchor.   2. Another non-persistent menu opens (single-open coordination —      opening menu B closes menu A so two top-level menus never      overlap each other on screen).  `persistent` opts out of both auto-dismiss paths. The menu then only closes via `close()`, the anchor's toggle, item activation, or Escape. Useful for modal-style pickers, multi-step menus, and any context where the consumer manages dismissal explicitly.  Submenus (an `<md-menu>` rendered inside a parent menu's `submenu` slot) are never affected by sibling-menu dismissal even without this flag — ancestors and descendants are always preserved.
          * @default false
         */
        "persistent": boolean;
        /**
          * Position of the menu relative to the anchor.
          * @default 'bottom-start'
         */
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        /**
          * Skip open/close animation.
          * @default false
         */
        "quick": boolean;
        /**
          * Recompute the menu's position against its anchor immediately. While open, md-menu already tracks scroll, resize, its own surface size AND the anchor moving (a per-frame rect watch — covers transform-animated ancestors like a sliding bottom sheet, and sibling reflow shifting the trigger), so this is rarely needed; call it when the next frame is too late — e.g. to avoid a one-frame lag right after a synchronous layout change.
         */
        "reposition": () => Promise<void>;
        /**
          * Adapt to compact viewports. The menu is *always* clamped within the viewport (it never overflows the screen and pins to the edges with a margin). When `responsive` is set, on viewports at or below the compact breakpoint (600px) the menu additionally expands to near-full-width below the trigger for comfortable touch use.
          * @default false
         */
        "responsive": boolean;
        /**
          * Register (or clear with `null`) the combobox element that holds focus while a virtual provider drives the list. When set, keyboard navigation manages `aria-activedescendant` on it rather than calling `.focus()` on options — the canonical pattern for a listbox whose rows are virtualized/recycled.
         */
        "setComboboxElement": (el: HTMLElement | null) => Promise<void>;
        /**
          * Register (or clear with `null`) a virtual provider. With a provider set, keyboard nav, typeahead, and roving focus run against the data model so a windowed list of millions of options behaves like a normal menu. Without one the menu is unchanged.
         */
        "setVirtualProvider": (provider: VirtualMenuProvider | null) => Promise<void>;
        /**
          * Opens the menu programmatically. Pass `{ autoFocus: false }` to keep focus on the caller (e.g. a text field).
         */
        "show": (opts?: { autoFocus?: boolean; }) => Promise<void>;
        /**
          * Use gap separators instead of dividers between groups (vertical menu only).
          * @default false
         */
        "useGap": boolean;
        /**
          * Visual variant. - 'baseline': MD3 baseline menu (square corners, surface container) - 'standard': M3 Expressive vertical menu (rounded, surface-based colors) - 'vibrant': M3 Expressive vertical menu (rounded, tertiary-based colors)
          * @default 'baseline'
         */
        "variant": 'baseline' | 'standard' | 'vibrant';
    }
    interface MdMenuItem {
        /**
          * Small badge label (e.g. "New") displayed before the trailing area.
          * @default ''
         */
        "badge": string;
        /**
          * Position of the selection checkmark indicator.
          * @default 'end'
         */
        "checkPosition": 'start' | 'end';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Whether the item is disabled.
          * @default false
         */
        "disabled": boolean;
        /**
          * Render a divider line below this item.
          * @default false
         */
        "divider": boolean;
        /**
          * Render a gap (extra space) below this item — an alternative separator to divider.
          * @default false
         */
        "gap": boolean;
        /**
          * Primary label text.
          * @default ''
         */
        "headline": string;
        /**
          * Mixed/partial state for a `checkbox` item — renders a dash in the check slot instead of a tick and exposes `aria-checked="mixed"`. Used for a "select all" affordance where only some children are selected. Ignored for `button`/`radio` types and when `selected` is true.
          * @default false
         */
        "indeterminate": boolean;
        /**
          * Prevent the parent menu from closing when this item is clicked.
          * @default false
         */
        "keepOpen": boolean;
        /**
          * ARIA presentation. `'menuitem'` (default) exposes the item with the menu role family (`menuitem` / `menuitemradio` / `menuitemcheckbox`, state via `aria-checked`). `'option'` exposes it as a listbox option (`role="option"`, state via `aria-selected`) — used by `md-select` so the popup is a proper WAI-ARIA combobox/listbox rather than a menu.
          * @default 'menuitem'
         */
        "presentation": 'menuitem' | 'option';
        /**
          * Override the computed ARIA role. Used when a checkbox/radio item is NOT inside a menu — e.g. a "select all" toggle in a listbox popup, which is semantically a `checkbox` (keeps `aria-checked`, drops the menu-only required-parent). Empty = the default menuitem/option role family.
          * @default ''
         */
        "roleOverride": string;
        /**
          * Whether the item is currently selected.
          * @default false
         */
        "selected": boolean;
        /**
          * Secondary descriptive text below the headline.
          * @default ''
         */
        "supportingText": string;
        /**
          * Trailing text (e.g. keyboard shortcut).
          * @default ''
         */
        "trailingText": string;
        /**
          * Type of item: 'button' (default action), 'checkbox' (toggle), or 'radio' (single-select within group).
          * @default 'button'
         */
        "type": 'button' | 'checkbox' | 'radio';
    }
    interface MdMenuItemGroup {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Optional label shown above the group items.
          * @default ''
         */
        "label": string;
    }
    /**
     * md-meter — a read-only graphical display of a numeric value within a known
     * range (storage used, quota, password strength, battery level).
     * The semantics live on the HOST: `role="meter"` with the full ARIA value
     * contract, a purely presentational track + indicator inside the shadow root
     * (all `aria-hidden`), value clamping into `[min, max]`, and Intl-driven value
     * formatting (locale-aware percent by default, any `Intl.NumberFormatOptions`
     * via `formatOptions`). Keeping the ARIA outside the shadow root is what lets
     * the visuals change shape without touching what is announced.
     * Deliberately NOT a progress indicator: a meter shows a *state* ("how full"),
     * a progress indicator shows an *activity* ("how far along"). It is therefore
     * a plain track + fill — no 4px gap, no stop dot, no indeterminate mode, no
     * completion animation. Nothing here is focusable or interactive and no events
     * are emitted.
     * Two shapes, one contract: `variant="linear"` (default) is the bar;
     * `variant="circular"` is the same reading as a ring, with the value in the
     * middle and the label beneath. The ARIA lives on the host either way, so the
     * shape is presentational and swapping it changes nothing for a screen reader.
     */
    interface MdMeter {
        /**
          * Colour role applied to the fill and track. Any role name the THEME defines is accepted, not just the listed ones — the meter resolves `<name>` to the `--md-sys-color-<name>` / `-<name>-container` pair at runtime, so `color="brand"` works as soon as a theme ships those tokens.  The default (`primary`) keeps md-progress-indicator's neutral look: primary fill on a secondary-container track. Every other role paints `<role>` fill on a `<role>-container` track.
          * @default 'primary'
         */
        "color": | 'primary'
    | 'secondary'
    | 'tertiary'
    | 'error'
    | 'success'
    | 'warning'
    | 'info'
    | (string & {});
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * `Intl.NumberFormatOptions` for the value text (JS-only object prop — set it as a property, not an attribute). When provided, the CLAMPED value is formatted with it (`{ style: 'unit', unit: 'gigabyte' }`, currency, plain number…). When omitted, the value's position in the range is formatted as a locale-aware percentage — the default, because a meter answers "how full", which a bare number cannot say without its bounds alongside it.
         */
        "formatOptions"?: Intl.NumberFormatOptions;
        /**
          * Accessible name (`aria-label`) of the meter. Also the visible header label when `show-label` is set. Pass already-localized text.
          * @default ''
         */
        "label": string;
        /**
          * BCP-47 locale for `Intl.NumberFormat` output (the formatted value text). Empty string = the runtime's default locale. This prop exists ONLY for Intl-computed output; translatable copy (`label`) is a plain text prop.
          * @default ''
         */
        "locale": string;
        /**
          * Upper bound of the range.
          * @default 100
         */
        "max": number;
        /**
          * Lower bound of the range.
          * @default 0
         */
        "min": number;
        /**
          * Renders the `label` text in a header row above the track.
          * @default false
         */
        "showLabel": boolean;
        /**
          * Renders the formatted value in the header row above the track, end-aligned. The visible span is `aria-hidden` — screen readers announce the same string via `aria-valuetext` on the host instead.
          * @default false
         */
        "showValue": boolean;
        /**
          * Ring diameter in dp — circular only, ignored by the bar. Clamped to 24…240 like md-progress-indicator, and tapered by density through the `--md-meter-size` hook.
          * @default 48
         */
        "size": number;
        /**
          * Track thickness in dp. Density-scaled (0.5px per rung), floored at 2px.
          * @default 4
         */
        "thickness": number;
        /**
          * Current value. Clamped into `[min, max]` for ARIA, geometry and formatting.
          * @default 0
         */
        "value": number;
        /**
          * Overrides the formatted value text entirely — both `aria-valuetext` and the visible header value (when `show-value` is set). Use it when neither the default percent format nor `formatOptions` can express the reading (e.g. "3 of 10 seats").
          * @default ''
         */
        "valueText": string;
        /**
          * Shape of the reading.  - `linear` (default) — a horizontal bar, full width of its container. - `circular` — a ring. `show-value` puts the value in the middle of it and   `show-label` captions it underneath, instead of the bar's header row.  The ring does NOT mirror under `dir="rtl"`: it fills clockwise in every locale, matching Material's circular progress (and a clock face).
          * @default 'linear'
         */
        "variant": 'linear' | 'circular';
    }
    /**
     * `md-multi-select` — Material Design 3 multi-value dropdown.
     * Shares its internals with `md-select`: an `md-text-field` trigger and an
     * `md-menu` option surface. Options come from slotted `<md-select-option>`
     * children (primary API) or the programmatic `options` array. Each becomes a
     * `type="checkbox"` `md-menu-item` so several can be selected at once, and the
     * menu uses `keep-open` so multiple toggles don't dismiss it.
     * Selected values render in the trigger as removable chips (`display-mode="chips"`),
     * a "{n} selected" count, or comma-joined text.
     */
    interface MdMultiSelect {
        /**
          * Constraint-validation API, matching md-text-field and the native contract. Validity was already published to the form here, but with no public method a consumer could not ASK this control whether it was valid.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * For `display-mode="chips-inline"`, how the in-field chips handle running out of room:   - `'count'` (default): chips stay on ONE line; the field keeps its normal     height and the chips that don't fit collapse into a trailing `+N` counter     chip (click it — or the field — to open the menu and manage the full set).   - `'wrap'`: chips wrap onto new rows and the field grows to fit them.
          * @default 'count'
         */
        "chipOverflow": 'count' | 'wrap';
        /**
          * Where the removable chips sit relative to the trigger, for `display-mode="chips"` (ignored for `chips-inline`/`count`/`text`): `'bottom'` (default), `'top'`, `'left'`, or `'right'`. `left`/`right` place the chips beside the trigger and are the natural pairing for `trigger="button"`.
          * @default 'bottom'
         */
        "chipPosition": 'bottom' | 'top' | 'left' | 'right';
        /**
          * Material Symbols glyph for the clear button.
          * @default 'close'
         */
        "clearIcon": string;
        /**
          * Accessible label for the clear button (localization).
          * @default 'Clear selection'
         */
        "clearLabel": string;
        /**
          * Show a clear (×) button in the trigger that removes all selections.
          * @default false
         */
        "clearable": boolean;
        /**
          * Close the dropdown programmatically.
         */
        "close": () => Promise<void>;
        /**
          * Custom formatter for `display-mode="count"`.
         */
        "countFormatter"?: (count: number) => string;
        /**
          * Density forwarded to the inner field.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled": boolean;
        /**
          * How selected values appear in the trigger:   - `'chips'` (default): removable chips in a region BELOW the field.   - `'chips-inline'`: removable chips INSIDE the trigger input, on a single     line — the field keeps its normal height and never grows; chips that     overflow are clipped (the open menu is the source of truth).   - `'count'`: "{n} selected".   - `'text'`: comma-joined labels.
          * @default 'chips'
         */
        "displayMode": 'chips' | 'chips-inline' | 'count' | 'text';
        /**
          * Caret glyph (Material Symbol) for the field trigger; rotates 180° when open. Override the whole glyph with the `dropdown-icon` slot.
          * @default 'arrow_drop_down'
         */
        "dropdownIcon": string;
        /**
          * Error state — forwarded to the inner field.
          * @default false
         */
        "error": boolean;
        /**
          * Error text rendered in place of supporting text when `error` is set.
          * @default ''
         */
        "errorText": string;
        /**
          * Accessible label for the filter search input. Defaults to "Filter {label}".
          * @default ''
         */
        "filterLabel": string;
        /**
          * Filter strategy used by `filterable` / `setQuery`.
          * @default 'substring'
         */
        "filterMode": FilterMode;
        /**
          * Show a search field in the menu header that filters via the WASM engine.
          * @default false
         */
        "filterable": boolean;
        /**
          * Move focus to the trigger field.
         */
        "focusTrigger": () => Promise<void>;
        /**
          * Resolve labels for values that may be outside the current filter/window.
         */
        "getLabels": (values: string[]) => Promise<Record<string, string>>;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label": string;
        /**
          * Load a large dataset into the WASM-backed virtualized list. Accepts a materialised `SelectOptionInit[]` or a row factory (`{ count, getRow }`) — the factory streams rows into WASM one at a time, so tens of millions of options never exist as JS objects at once. Falls back to plain DOM rendering when `virtualize="never"` or WebAssembly is unavailable.
         */
        "loadOptions": (source: SelectOptionInit[] | OptionRowSource) => Promise<void>;
        /**
          * Show a busy state: the trigger shows a spinner instead of the caret and the host is `aria-busy`, and the menu shows a wavy progress bar instead of the option list. Use while an async dataset loads (e.g. before/around a large `loadOptions`). Replace the trigger spinner with the `loader` slot.
          * @default false
         */
        "loading": boolean;
        /**
          * Text shown in the trigger / menu while `loading` is true.
          * @default 'Loading…'
         */
        "loadingText": string;
        /**
          * Match the dropdown width to the trigger. When `false` the menu sizes to content. Maps to `md-menu`'s `match-anchor-width`. Default `true`.
          * @default true
         */
        "matchTriggerWidth": boolean;
        /**
          * Max height of the dropdown menu (px), forwarded to `md-menu`. Virtualized lists fall back to 320 when unset (they need a bounded scroll viewport).
         */
        "maxHeight"?: number;
        /**
          * Maximum number of selected items. `0` = unlimited.
          * @default 0
         */
        "maxSelected": number;
        /**
          * Form name. Selected values submit as repeated `name=value` pairs.
          * @default ''
         */
        "name": string;
        /**
          * Text shown when there are no options at all (localization).
          * @default 'No options'
         */
        "noOptionsText": string;
        /**
          * Text shown when the filter matches no options (localization).
          * @default 'No results'
         */
        "noResultsText": string;
        /**
          * Open state of the menu. Reflected so `:host([open])` rules apply.
          * @default false
         */
        "open": boolean;
        /**
          * Programmatic options. Slotted `md-select-option` children take precedence.  Accepts the array (property) or a **JSON string** (attribute), so a plain-HTML page can declare its data inline instead of reaching for a script. Malformed JSON degrades to an empty list with a console warning.
          * @default []
         */
        "options": SelectOptionInit[] | string;
        /**
          * Placeholder shown when nothing is selected.
          * @default ''
         */
        "placeholder": string;
        /**
          * Menu placement relative to the trigger — forwarded to `md-menu`.
          * @default 'bottom-start'
         */
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        "reportValidity": () => Promise<boolean>;
        /**
          * Required (form validation parity).
          * @default false
         */
        "required": boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace": boolean;
        /**
          * Clear all selections.
         */
        "reset": () => Promise<void>;
        /**
          * Fixed virtualized row height (px). Auto-measured from the first row if unset.
         */
        "rowHeight"?: number;
        /**
          * Placeholder for the filter search input (localization).
          * @default 'Search…'
         */
        "searchPlaceholder": string;
        /**
          * Accessible label for the search spinner (localization).
          * @default 'Searching'
         */
        "searchingLabel": string;
        /**
          * Label for the "Select all" item.
          * @default 'Select all'
         */
        "selectAllLabel": string;
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Programmatically filter the virtualized list (mirrors the search field).
         */
        "setQuery": (query: string) => Promise<void>;
        /**
          * Open the dropdown programmatically.
         */
        "show": () => Promise<void>;
        /**
          * Render an inline "Select all" checkbox at the top of the menu.
          * @default false
         */
        "showSelectAll": boolean;
        /**
          * Soft-disabled — disabled visuals but still focusable.
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Supporting / helper text below the field.
          * @default ''
         */
        "supportingText": string;
        /**
          * Trigger style: `'field'` (default) is the readonly md-text-field; `'button'` is a compact button (leading icon + label) that only opens the drawer — the selection shows as removable chips beside it (pair with `chip-position` `left`/`right`). The button always shows chips regardless of `display-mode`.
          * @default 'field'
         */
        "trigger": 'field' | 'button';
        /**
          * Leading icon (Material Symbol name) for `trigger="button"`.
          * @default 'add'
         */
        "triggerIcon": string;
        /**
          * Button text for `trigger="button"`. Falls back to `label`.
          * @default ''
         */
        "triggerLabel": string;
        /**
          * Currently selected values.
          * @default []
         */
        "value": string[];
        /**
          * Localized constraint-validation message shown when `required` is unmet. `errorText` still wins when set, so an app-supplied inline message and the native bubble stay in agreement.
          * @default 'Please select at least one option'
         */
        "valueMissingLabel": string;
        /**
          * Visual variant — forwarded to the inner `md-text-field`.
          * @default 'outlined'
         */
        "variant": 'filled' | 'outlined';
        /**
          * Virtualization mode for large option lists (backed by a WASM data engine):   - `'auto'` (default): virtualize only above `VIRTUALIZE_THRESHOLD` rows.   - `'always'`: always virtualize.   - `'never'`: keep the classic DOM rendering. Virtualized rows render label + selection only (no per-row icon / supporting text), assume a uniform row height, and disable "Select all".
          * @default 'auto'
         */
        "virtualize": 'auto' | 'always' | 'never';
    }
    /**
     * `md-navigation-bar` — Material Design 3 navigation bar.
     * A bottom-anchored row of 3–5 top-level destinations, optimized for
     * compact-width screens (mobile). Each destination is an `<md-navigation-tab>`
     * child; the bar orchestrates selection, label visibility, keyboard
     * navigation (roving tabindex + arrow keys), and emits change events.
     * References:
     *   - {@link https://m3.material.io/components/navigation-bar/overview Overview}
     *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
     *   - {@link https://m3.material.io/components/navigation-bar/guidelines Guidelines}
     *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
     */
    interface MdNavigationBar {
        /**
          * Index of the currently selected destination. Two-way: external writes drive selection, internal selection writes back.
          * @default 0
         */
        "activeIndex": number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Focus the tab at `index` without selecting it. Used by hosts that implement manual activation flows (e.g. wizards).
         */
        "focusTab": (index: number) => Promise<void>;
        /**
          * Label visibility for child tabs.  - `always`   — Show all labels (default; M3 baseline). - `selected` — Show only the selected tab's label. - `none`     — Icon-only mode (no labels rendered).  Per child can be overridden with `label-behavior` on the tab.
          * @default 'always'
         */
        "labelBehavior": MdNavigationBarLabelBehavior;
        /**
          * Keyboard activation policy. When `false` (default), arrow keys move focus AND immediately activate the focused tab (automatic activation; matches the MD3 reference and most nav bar implementations).  When `true`, arrow keys only move focus — the user must press `Enter` / `Space` to activate. Use this when activation triggers destructive or expensive work.
          * @default false
         */
        "manualActivation": boolean;
        /**
          * Programmatically select a destination by index. Out-of-range or disabled targets are clamped to the next enabled tab.
         */
        "select": (index: number) => Promise<void>;
    }
    /**
     * Material Design 3 — Navigation Rail
     * A vertical navigation surface for the left or right edge of medium / large
     * window sizes. Holds 3–7 top-level destinations and optionally a header
     * (menu / brand), a FAB, and a footer.
     * Implements MD3 specs:
     *   - https://m3.material.io/components/navigation-rail/overview
     *   - https://m3.material.io/components/navigation-rail/specs
     *   - https://m3.material.io/components/navigation-rail/guidelines
     *   - https://m3.material.io/components/navigation-rail/accessibility
     */
    interface MdNavigationRail {
        /**
          * Index of the currently active destination. -1 means no destination is active. Two-way bound: updates when the user selects a destination and can be set externally to programmatically change the active destination.
          * @default -1
         */
        "activeIndex": number;
        /**
          * Vertical alignment of destinations relative to the rail.
          * @default 'top'
         */
        "alignment": 'top' | 'middle' | 'bottom';
        /**
          * Programmatically collapse the rail (standard 80px width).
         */
        "collapse": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Set to `true` to disable focus management (roving tabindex + arrow keys). Use when embedding inside a custom focus management system.
          * @default false
         */
        "disableFocusManagement": boolean;
        /**
          * Programmatically expand the rail (shows labels inline with icons).
         */
        "expand": () => Promise<void>;
        /**
          * Render a built-in leading menu button that toggles between the collapsed (`standard`) and `expanded` variants. The icon animates between `menu` (collapsed) and `menu_open` (expanded) per the MD3 navigation rail figure. When you'd rather supply your own control, leave this `false` and place a button in the `header` slot wired to the `expand()` / `collapse()` / `toggle()` methods.
          * @default false
         */
        "expandable": boolean;
        /**
          * Move keyboard focus to the destination at `index`.
         */
        "focusTab": (index: number) => Promise<void>;
        /**
          * Stretch the rail to the full viewport height (`100dvh`) instead of filling its parent container. Opt-in: by default the rail fills whatever height its container gives it (`100%`), which keeps it embeddable in split-pane / nested / modal-docked layouts. Set this when the rail is the app's top-level side navigation and should always span the screen.
          * @default false
         */
        "fullHeight": boolean;
        /**
          * Accessible name announced for the rail's `navigation` landmark. Required for screen reader users (WAI-ARIA APG / WCAG 2.4.1).
          * @default 'Navigation'
         */
        "label": string;
        /**
          * Controls whether destination labels are shown.  - `all`: every destination shows its label (default — recommended by MD3 for 3–7 destinations)  - `selected`: only the active destination shows its label  - `none`: icons only
          * @default 'all'
         */
        "labelVisibility": 'all' | 'selected' | 'none';
        /**
          * Cap on how many destinations stay in the rail. Any beyond it collapse into an overflow trigger that opens a menu of the rest; choosing one activates that destination. Leave unset for no overflow.  The trigger is a `button` with `aria-haspopup="menu"`, deliberately OUTSIDE the `tablist` — a tablist may only contain tabs.
         */
        "maxVisible"?: number;
        /**
          * Modal styling for the expanded variant per spec: `surface-container` background, level-2 elevation, large corner shape. Use when the expanded rail floats over content instead of being docked; positioning and the scrim are the host app's responsibility. No effect while collapsed.
          * @default false
         */
        "modal": boolean;
        /**
          * Layout axis.  - `vertical` (default) — the MD3 rail: a fixed-width column on the leading   edge. - `horizontal` — the same parts laid out as a top-of-page bar: logo   leading, destinations in a row, FAB and footer trailing. For wide layouts   where a side rail would spend width the content wants.  The expand/collapse affordance is vertical-only — a bar has nowhere to grow — so `expandable` is ignored while horizontal.
          * @default 'vertical'
         */
        "orientation": 'vertical' | 'horizontal';
        /**
          * Material Symbols ligature for the overflow trigger.
          * @default 'more_horiz'
         */
        "overflowIcon": string;
        /**
          * Accessible name and label for the overflow trigger. Localize per page.
          * @default 'More'
         */
        "overflowLabel": string;
        /**
          * Toggle between expanded and standard.
         */
        "toggle": () => Promise<void>;
        /**
          * Accessible label for the built-in toggle button (only rendered when `expandable` is set). Announced to assistive tech as the button's name.
          * @default 'Toggle navigation'
         */
        "toggleLabel": string;
        /**
          * Visual variant — `standard` (the spec's *collapsed* rail: 80px wide, stacked icon + label) or `expanded` (icons + label inline, 220–360px wide).
          * @default 'standard'
         */
        "variant": 'standard' | 'expanded';
    }
    /**
     * Material Design 3 — Navigation Rail Destination (Tab)
     * A single destination within a `<md-navigation-rail>`. Renders the MD3
     * active-indicator pill (56×32) around the icon, with an optional label
     * underneath (or inline when the parent rail is expanded). Supports badges,
     * disabled state, link mode (`href`) and slotted custom icons.
     * Spec:
     *   - https://m3.material.io/components/navigation-rail/specs
     *   - https://m3.material.io/components/navigation-rail/guidelines
     *   - https://m3.material.io/components/navigation-rail/accessibility
     */
    interface MdNavigationRailTab {
        /**
          * Active / selected state. Reflected. Managed by `<md-navigation-rail>`.
          * @default false
         */
        "active": boolean;
        /**
          * Show a small dot badge (notification indicator).
          * @default false
         */
        "badge": boolean;
        /**
          * Numeric / textual badge value (renders large badge).
          * @default ''
         */
        "badgeValue": string;
        /**
          * Drop whatever row this destination's dropdown had marked as chosen.  The rail calls this when a *different* destination becomes active. Without it the bar ends up with two highlighted destinations: the newly active one, and the one still wearing the child-selected pill because its menu row was never unmarked.
         */
        "clearSubmenuSelection": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — destination cannot be activated and is removed from tab order.
          * @default false
         */
        "disabled": boolean;
        /**
          * True when the parent rail is expanded; managed by the parent.
          * @default false
         */
        "expanded": boolean;
        /**
          * When set, the tab is rendered as a link (anchor) with the given href. Activates link semantics (uses `aria-current="page"` when active).
          * @default ''
         */
        "href": string;
        /**
          * Material Symbols Outlined icon name (shorthand). Ignored when the `icon` slot is filled.
          * @default ''
         */
        "icon": string;
        /**
          * Visible text label. Recommended for all destinations (MD3 a11y).
          * @default ''
         */
        "label": string;
        /**
          * Controls label visibility. Usually set automatically by the parent `<md-navigation-rail>` based on its `label-visibility` prop.
          * @default 'all'
         */
        "labelVisibility": 'all' | 'selected' | 'none';
        /**
          * Optional target for the link (when `href` is set).
          * @default ''
         */
        "target": string;
        /**
          * Optional value emitted alongside `mdTabClick`.
          * @default ''
         */
        "value": string;
    }
    /**
     * `md-navigation-tab` — a single destination inside `md-navigation-bar`.
     * Renders an icon, an optional label, and an optional badge. Selecting
     * the tab swaps the icon to a filled variant (per M3 spec) and reveals
     * an "active indicator" pill behind the icon.
     * References:
     *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
     *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
     */
    interface MdNavigationTab {
        /**
          * Whether this destination is currently selected.
          * @default false
         */
        "active": boolean;
        /**
          * Material Symbols / Material Icons name shown when active. Per M3 spec, active destinations use the filled variant. Defaults to `icon` when empty (the same glyph in both states).
          * @default ''
         */
        "activeIcon": string;
        /**
          * Show a small "notification dot" badge in the top-right of the icon. Use when there are new items but no count is needed.
          * @default false
         */
        "badge": boolean;
        /**
          * Maximum value rendered when `badgeValue` is purely numeric. Values greater than this render as `"{max}+"` (e.g. `99+`). Set to `0` to disable the cap.
          * @default 999
         */
        "badgeMax": number;
        /**
          * Numeric / textual badge value (e.g. `"3"`, `"99+"`, `"New"`). When set, the badge expands to a pill that accommodates the value (M3 calls this the "large badge" variant).
          * @default ''
         */
        "badgeValue": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable interaction. Disabled tabs are skipped by keyboard nav.
          * @default false
         */
        "disabled": boolean;
        /**
          * Focus the tab programmatically (skip when disabled).
         */
        "focusEl": () => Promise<void>;
        /**
          * When set, activating the tab also navigates to the URL — useful when each destination is a full route. For SPA routing, intercept the native `click` (call `preventDefault()`) or listen to the bar's `mdChange`; navigation only occurs when `href` is truthy and the click isn't prevented.
          * @default ''
         */
        "href": string;
        /**
          * Material Symbols / Material Icons name shown when inactive. Per M3 spec, inactive destinations use the outlined variant.
          * @default ''
         */
        "icon": string;
        /**
          * Destination label rendered below the icon.
          * @default ''
         */
        "label": string;
        /**
          * Label visibility for this tab. Inherits from the parent `md-navigation-bar`'s `label-behavior` when not set.  Not reflected as an attribute — the parent bar uses `getAttribute('label-behavior')` to detect *explicit* author overrides, so reflecting the default would defeat that signal.
          * @default 'always'
         */
        "labelBehavior": MdNavigationTabLabelBehavior;
        /**
          * Soft-disable the destination: it cannot be activated or selected and is announced as `aria-disabled`, but — unlike `disabled` — it stays keyboard-reachable (the bar's arrow-key tour still lands on it) so screen-reader users can discover a temporarily-unavailable destination.
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Link target (forwarded only when `href` is set).
          * @default ''
         */
        "target": string;
    }
    /**
     * `md-number-field` — Material Design 3 number input: an `md-text-field`
     * with locale-aware `Intl.NumberFormat` display, arrow-key / stepper-button /
     * wheel stepping, and native form participation.
     * Behavior contract:
     *   - the visible control is a **plain text input** (`inputmode` numeric or
     *     decimal) — deliberately NOT `role="spinbutton"` and NOT
     *     `type="number"`, so the locale-formatted value stays ordinary readable,
     *     editable text and no native spinner chrome fights the Intl display;
     *     the steppers are separate labeled buttons with `tabindex="-1"` (the
     *     input is the keyboard surface),
     *   - `value` is the raw number (`null` = empty); the formatted string is
     *     display-only state,
     *   - ArrowUp/Down step by `step`, Alt = `smallStep`, Shift = `largeStep`;
     *     Home/End jump to a defined `min`/`max`,
     *   - press-and-hold on a stepper auto-repeats (400ms delay, 60ms interval),
     *   - typing is filtered to the characters of the active locale/format,
     *     parsed leniently and kept verbatim; blur reformats (and clamps unless
     *     `allow-out-of-range`),
     *   - stepping (keys, buttons, wheel) always clamps and commits (`mdChange`).
     * A drag-to-scrub surface (pointer lock over the field) is deliberately out of
     * scope: a niche gesture built on pointer lock, whose behaviour is unreliable
     * in Safari. `allow-wheel-scrub` covers the "nudge without typing" case.
     */
    interface MdNumberField {
        /**
          * Let TYPED text exceed `min`/`max` unclamped (blur does not clamp). Step-based interactions (keys, buttons, wheel) always clamp.
          * @default false
         */
        "allowOutOfRange": boolean;
        /**
          * Enable wheel stepping while the inner input has focus.
          * @default false
         */
        "allowWheelScrub": boolean;
        /**
          * Constraint-validation API, matching md-text-field and the native contract.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Accessible name of the decrement stepper (localizable).
          * @default 'Decrement'
         */
        "decrementLabel": string;
        /**
          * Density forwarded to the text-field and applied to the steppers.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled": boolean;
        /**
          * Error state — forwarded to the text-field.
          * @default false
         */
        "error": boolean;
        /**
          * Error text rendered in place of supporting text when `error` is true.
          * @default ''
         */
        "errorText": string;
        /**
          * `Intl.NumberFormat` options for display formatting (currency, percent, unit, fraction digits…). Percent keeps Intl semantics: displayed 50% ⇔ value 0.5.  Takes either form, so a plain-HTML page needs no script:   - property — `el.formatOptions = { style: 'currency', currency: 'EUR' }`   - attribute — `format-options='{"style":"currency","currency":"EUR"}'`  Malformed JSON warns once and falls back to plain number formatting rather than throwing: a broken attribute must not take the whole field down.
         */
        "formatOptions"?: Intl.NumberFormatOptions | string;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Accessible name of the increment stepper (localizable).
          * @default 'Increment'
         */
        "incrementLabel": string;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label": string;
        /**
          * Step while **Shift** is held.
          * @default 10
         */
        "largeStep": number;
        /**
          * BCP-47 locale for `Intl.NumberFormat` display/parsing ONLY (an Intl-computed value — translatable copy stays in the `*-label` props). Empty = the runtime locale.
          * @default ''
         */
        "locale": string;
        /**
          * Upper bound. Interactive stepping always clamps to it.
         */
        "max"?: number;
        /**
          * Lower bound. Interactive stepping always clamps to it.
         */
        "min"?: number;
        /**
          * Form name. The raw numeric value submits as `String(value)`.
          * @default ''
         */
        "name": string;
        /**
          * Placeholder for the input.
          * @default ''
         */
        "placeholder": string;
        /**
          * Read-only: the value is visible and focusable but cannot be edited or stepped.
          * @default false
         */
        "readOnly": boolean;
        "reportValidity": () => Promise<boolean>;
        /**
          * Required for native form parity (`valueMissing` when empty).
          * @default false
         */
        "required": boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field.
          * @default false
         */
        "reserveSupportingSpace": boolean;
        /**
          * Select the input's full contents.
         */
        "select": () => Promise<void>;
        /**
          * App/server-side validation: a non-empty message marks the field invalid for its form until cleared with an empty string.
         */
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Focus the inner input.
         */
        "setFocus": () => Promise<void>;
        /**
          * Step while **Alt** is held.
          * @default 0.1
         */
        "smallStep": number;
        /**
          * Snap stepped values to multiples of the active step (base = `min` when defined, else 0). Snapping happens BEFORE clamping, so non-aligned bounds stay reachable. Regular steps snap directionally; Alt (`smallStep`) snaps to the nearest multiple.
          * @default false
         */
        "snapOnStep": boolean;
        /**
          * Step for arrows / steppers / wheel.
          * @default 1
         */
        "step": number;
        /**
          * Step down by `step` (× `times`), snapping/clamping like an arrow press.
         */
        "stepDown": (times?: number) => Promise<void>;
        /**
          * Step up by `step` (× `times`), snapping/clamping like an arrow press.
         */
        "stepUp": (times?: number) => Promise<void>;
        /**
          * Which stepper buttons render: inside the field, split around it, or none.
          * @default 'inline'
         */
        "steppers": 'inline' | 'split' | 'none';
        /**
          * Supporting / helper text below the field.
          * @default ''
         */
        "supportingText": string;
        /**
          * The raw numeric value (`null` = empty). Never the formatted string — the display text is derived via `Intl.NumberFormat`.
          * @default null
         */
        "value": number | null;
        /**
          * Localized constraint-validation message shown when `required` is unmet. `errorText` still wins when set, so an app-supplied inline message and the native bubble stay in agreement.
          * @default 'Please enter a number.'
         */
        "valueMissingLabel": string;
        /**
          * Visual variant of the inner text-field.
          * @default 'filled'
         */
        "variant": 'filled' | 'outlined';
    }
    /**
     * `md-organization-chart` — Material Design 3 Expressive organization chart.
     * Visualises hierarchical org data as a top-down tree of avatar cards joined
     * by connector lines. Data-driven (`nodes`), with collapsible subtrees,
     * optional single/multiple selection, and the full WAI-ARIA **tree** pattern
     * (roving tabindex, Arrow/Home/End navigation, Enter/Space to select).
     * The surface pans horizontally when it outgrows its container, so a wide org
     * stays usable on small screens. Direction-aware (RTL mirrors the layout and
     * swaps the horizontal arrow keys); every affordance label is a prop for
     * localisation.
     * ```html
     * <md-organization-chart selection-mode="single"></md-organization-chart>
     * <script>
     *   document.querySelector('md-organization-chart').nodes = [{
     *     id: 'ceo', name: 'Amy Elsner', title: 'Founder & CEO',
     *     children: [{ id: 'prod', name: 'Asiya Javayant', title: 'Product Lead' }],
     *   }];
     * </script>
     * ```
     */
    interface MdOrganizationChart {
        /**
          * Toggler accessible label when a node is expanded (localisable).
          * @default 'Collapse'
         */
        "collapseLabel": string;
        /**
          * Show expand/collapse togglers on nodes that have children.
          * @default true
         */
        "collapsible": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Toggler accessible label when a node is collapsed (localisable).
          * @default 'Expand'
         */
        "expandLabel": string;
        /**
          * Accessible name for the tree (localisable).
          * @default 'Organization chart'
         */
        "label": string;
        /**
          * The organization tree. Accepts an `OrgChartNode[]` (property) or a JSON string (attribute). Multiple roots are supported (rendered side by side).
          * @default []
         */
        "nodes": NodeInput;
        /**
          * Layout direction of the tree:   - `'vertical'`   — top-down (default).   - `'horizontal'` — left-to-right; RTL mirrors it right-to-left.  Only the visual layout changes — the tree's ARIA semantics and keyboard model (Arrow Left/Right = collapse/expand, Up/Down = move) are unchanged.
          * @default 'vertical'
         */
        "orientation": 'vertical' | 'horizontal';
        /**
          * Selected node ids (controlled + initial). Kept in sync as the user selects.
          * @default []
         */
        "selectedIds": string[];
        /**
          * Selection behaviour:   - `'none'`     — nodes are not selectable (default).   - `'single'`   — one node at a time.   - `'multiple'` — any number of nodes.
          * @default 'none'
         */
        "selectionMode": 'none' | 'single' | 'multiple';
    }
    /**
     * `md-otp-field` — Material Design 3 one-time-code field.
     * Renders `length` real `<input>` cells in its own shadow root (it OWNS its
     * inputs, so every cell carries full ARIA), pipes all entry — typing, paste,
     * SMS autofill — through one sanitizing pipeline (strip whitespace →
     * `transform` → `validationType` charset filter → clamp to `length`), and
     * participates in forms via `ElementInternals` (no hidden input).
     * The value is a CONTIGUOUS string: clearing a cell shifts the characters
     * after it left (there are no holes), and a character typed into an empty
     * cell beyond the fill point lands at the first empty position.
     * Normalization is declarative: `transform` names the casing rule as an
     * attribute value rather than taking a callback, so the rule is authorable
     * from plain HTML and survives SSR — an element attribute cannot carry a
     * function.
     */
    interface MdOtpField {
        /**
          * Submit the owning form (`requestSubmit()`) when the code becomes complete.
          * @default false
         */
        "autoSubmit": boolean;
        /**
          * Per-cell `aria-label` template (translatable). `{index}` is 1-based, `{length}` is the cell count.
          * @default 'Character {index} of {length}'
         */
        "cellLabelTemplate": string;
        /**
          * Constraint-validation API, matching md-text-field and the native contract.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Clear the code programmatically (no `mdInput`/`mdChange` — programmatic writes stay silent).
         */
        "clear": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal a global `data-density` ancestor sets; local wins. 0 = default, -4 = compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disables every cell.
          * @default false
         */
        "disabled": boolean;
        /**
          * Error visual state — colors cells and swaps supporting text to `errorText`.
          * @default false
         */
        "error": boolean;
        /**
          * Translatable error message; replaces `supportingText` while `error` is true.
          * @default ''
         */
        "errorText": string;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<MdOtpFieldValidityDetail>;
        /**
          * Chunk the cells into groups of this size with a separator between groups. 0 = ungrouped.
          * @default 0
         */
        "groupSize": number;
        /**
          * When non-empty AND `required`, a started-but-incomplete code is also invalid, reported with this message. Empty (default) = only a fully empty required field blocks submission.
          * @default ''
         */
        "incompleteLabel": string;
        /**
          * Virtual-keyboard hint override. Empty derives from `validationType` (`numeric` → `numeric`, everything else → `text`).
          * @default ''
         */
        "inputMode": string;
        /**
          * Accessible group label (translatable). Rendered as `aria-label` on the cell group.
          * @default 'One-time code'
         */
        "label": string;
        /**
          * Number of character cells.
          * @default 6
         */
        "length": number;
        /**
          * Obscure the entered characters (cells render as `type="password"`).
          * @default false
         */
        "mask": boolean;
        /**
          * Form field name — ElementInternals submits the code under it.
          * @default ''
         */
        "name": string;
        /**
          * Value not user-changeable; cells stay focusable and arrow-navigable.
          * @default false
         */
        "readOnly": boolean;
        /**
          * Like checkValidity(), but also shows the browser's validation message.
         */
        "reportValidity": () => Promise<boolean>;
        /**
          * The code must be complete before the owning form submits.
          * @default false
         */
        "required": boolean;
        /**
          * Always reserve the supporting-text line so error text causes no layout jump.
          * @default false
         */
        "reserveSupportingSpace": boolean;
        /**
          * App/server-side validation: a non-empty message marks the control invalid until cleared with `''`.
         */
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Focus the first empty cell (or the last cell when the code is complete).
         */
        "setFocus": () => Promise<void>;
        /**
          * Translatable supporting text shown under the cells.
          * @default ''
         */
        "supportingText": string;
        /**
          * Case normalization as an attribute value, not a callback (attributes can't carry functions): `uppercase` upper-cases every accepted character (recovery-code entry).
          * @default 'none'
         */
        "transform": 'none' | 'uppercase';
        /**
          * Which characters are accepted. `none` accepts anything (still whitespace-stripped).
          * @default 'numeric'
         */
        "validationType": 'numeric' | 'alpha' | 'alphanumeric' | 'none';
        /**
          * The current code. Mutable, deliberately NOT reflected — entered codes must never appear as a DOM attribute (value privacy, like md-text-field). Programmatic writes run through the same sanitizing pipeline as typing.
          * @default ''
         */
        "value": string;
        /**
          * Translatable constraint-validation message when `required` and empty.
          * @default 'Please enter the complete code.'
         */
        "valueMissingLabel": string;
    }
    interface MdPieChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation": MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Border radius applied to each slice (reserved).
          * @default 4
         */
        "cornerRadius": number;
        /**
          * @default []
         */
        "data": MdPieDatum[];
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Animate the next `data` change as a drill through one slice, so the levels of a hierarchy read as connected rather than as unrelated charts.  AWAIT it, then assign the new `data`. Like every Stencil `@Method` this one resolves through a microtask, so a bare call lands AFTER a `data` assignment on the next line — the swap renders un-animated and the drill arms itself for whatever change comes after.  ```ts await chart.drill(e.detail.dataIndex);          // descend into the clicked slice chart.data = childrenOf(clicked);  await chart.drill(indexOfChildInParent, 'up');  // …and back out of it chart.data = parentLevel; ```  `down` names a slice of the data currently shown; `up` names a slice of the data about to be shown — in both cases, the wedge that connects the two levels. The new ring unfurls out of it, which run in reverse is what makes coming back up read as zooming out of that same wedge. Falls back to a plain swap when the index doesn't resolve, or under `prefers-reduced-motion`.
         */
        "drill": (index: number, direction?: "down" | "up") => Promise<void>;
        /**
          * End angle in degrees.
          * @default -270
         */
        "endAngle": number;
        "getInstance": () => Promise<PieChartEngine | null>;
        /**
          * Fill each slice with a gradient running outward from the centre, for depth. Purely decorative — it changes nothing about what a slice means, which is why it is off by default.
          * @default false
         */
        "gradient": boolean;
        "heightProp"?: string;
        /**
          * Hover highlight scope: slice / series / none.
          * @default 'slice'
         */
        "highlight": 'slice' | 'series' | 'none';
        /**
          * Inner radius (`"0%"` = pie, `"60%"` = donut, or px number).
          * @default '0%'
         */
        "innerRadius": string | number;
        /**
          * @default ''
         */
        "label": string;
        /**
          * Message shown when `data` is empty. The `empty` slot overrides it.
          * @default 'No data to display'
         */
        "labelEmpty": string;
        /**
          * What each slice's label says: its `value`, its `name`, or `both`. Defaults to the value when a legend is present (it already names the slices) and to both when there isn't one.
         */
        "labelMode"?: 'value' | 'name' | 'both';
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the slices; this is what tells them.
          * @default 'Chart data. Use the arrow keys to move between slices, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot": string;
        /**
          * Template for the live announcement as focus moves. `%label%` is the slice name, `%value%` its formatted value and `%percent%` its share.
          * @default '%label%: %value% (%percent%)'
         */
        "labelPoint": string;
        /**
          * @default 'right'
         */
        "legend": MdChartLegendPosition | 'none';
        /**
          * Show the loading overlay instead of the chart.
          * @default false
         */
        "loading": boolean;
        /**
          * Text (and the spinner's accessible name) for the loading overlay.
          * @default 'Loading chart…'
         */
        "loadingLabel": string;
        /**
          * BCP-47 locale for the DEFAULT number formatting (tooltip values, the screen-reader table). Empty follows the browser, which is the right default only until the page has said otherwise; an explicit `valueFormatter` always wins over it.
          * @default ''
         */
        "locale": string;
        /**
          * Render every slice as a shade of ONE colour instead of the categorical palette — darkest first, lightening around the ring.  Worth reaching for when the slices are ordered (a ranking, a funnel) rather than merely different: a single hue stops the reader hunting for meaning in colour that isn't there, and it survives most colour-vision deficiencies, which a five-hue palette does not.  Takes an MD3 role or any CSS colour. Present with no value (`monochrome`) uses `primary`; absent means off — so an explicit empty string is the "on, with the default" case rather than an accident.
         */
        "monochrome"?: string;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation": boolean;
        /**
          * Outer radius. Defaults to `"75%"`.
          * @default '75%'
         */
        "outerRadius": string | number;
        /**
          * Padding angle (degrees) between adjacent slices.
          * @default 0
         */
        "paddingAngle": number;
        /**
          * Re-read the MD3 tokens from the host's computed style and repaint.  The chart does this automatically when the tokens actually change, so reach for this only when a theme is applied in a way the watcher cannot observe — tokens injected into a stylesheet rather than onto an element, for instance. Cheaper and more direct than reassigning `series` to force a rebuild.
         */
        "refreshTheme": () => Promise<void>;
        /**
          * Replay the entry animation from the start (uses the current `animation`).
         */
        "replay": () => Promise<void>;
        "resize": () => Promise<void>;
        /**
          * How the radial band is divided between the rings of a nested chart — relative weights, innermost first. `[2, 1]` gives the inner ring twice the width of the outer one.  The default is not an even split: every ring but the outermost puts its labels INSIDE its own band, so it has to be wide enough to hold a word, while the outermost labels on leaders and needs no more than its arc.
         */
        "ringWidths"?: number[];
        /**
          * Show slice labels around the ring.
          * @default true
         */
        "showLabels": boolean;
        /**
          * Start angle in degrees (90 = 12-o'clock).
          * @default 90
         */
        "startAngle": number;
        /**
          * Sub-title, drawn under the title in the muted text colour.
         */
        "subtitle": string | undefined;
        /**
          * Replaces the generated `aria-label` outright. The default summary is assembled in English; rather than translate it piecewise, hand over the whole sentence built in your own language.
          * @default ''
         */
        "summary": string;
        /**
          * Translatable chrome for the screen-reader data table.
         */
        "tableLabels"?: { category?: string; value?: string; share?: string };
        /**
          * Title alignment over the chart: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign": MdChartTitleAlign;
        "toDataURL": () => Promise<string>;
        /**
          * @default 'item'
         */
        "tooltip": MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content. See `md-line-chart`'s `tooltipRenderer`.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        "valueFormatter"?: (value: number) => string;
    }
    interface MdProgressIndicator {
        /**
          * Set to `true` to trigger the completion ("closing TV") animation. After it plays, the indicator self-hides (`visibility:hidden`) and emits `mdComplete` — EXCEPT the flat (non-wavy) circular variant, which instead rests showing its empty track ring (the active indicator erases away and the track fades in). No-op on indeterminate indicators.
          * @default false
         */
        "complete": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Four-color cycling animation (circular indeterminate only).
          * @default false
         */
        "fourColor": boolean;
        /**
          * Indeterminate mode — looping animation when progress is unknown.
          * @default false
         */
        "indeterminate": boolean;
        /**
          * Accessible label for screen readers.
          * @default 'Progress'
         */
        "label": string;
        /**
          * Maximum progress value.
          * @default 100
         */
        "max": number;
        /**
          * Circular indicator outer size in dp. `0` = auto (non-wavy 40px, wavy 48px per the M3 expressive spec). Clamped to the M3 range 24–240px.
          * @default 0
         */
        "size": number;
        /**
          * Track / stroke thickness in dp.
          * @default 4
         */
        "thickness": number;
        /**
          * Current progress value (0 to max).
          * @default 0
         */
        "value": number;
        /**
          * Visual variant: linear (horizontal bar) or circular (ring).
          * @default 'linear'
         */
        "variant": 'circular' | 'linear';
        /**
          * Enable the wavy (M3 Expressive) active indicator.
          * @default false
         */
        "wave": boolean;
        /**
          * Wave amplitude (center-to-peak) in dp. `0` = auto per M3 spec (linear 3px, circular 1.6px).
          * @default 0
         */
        "waveAmplitude": number;
        /**
          * Wavelength (peak-to-peak) in dp. `0` = auto (linear 40px for both determinate + indeterminate, circular 15px).
          * @default 0
         */
        "waveLength": number;
        /**
          * Wave travel speed in dp/second. `0` = auto (= wavelength, i.e. one wavelength per second per the M3 default).
          * @default 0
         */
        "waveSpeed": number;
    }
    interface MdRadio {
        /**
          * Constraint-validation API, matching md-text-field and the native contract.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Whether the radio button is selected
          * @default false
         */
        "checked": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the radio button
          * @default false
         */
        "disabled": boolean;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Form name — radios sharing a name form an exclusive group
          * @default ''
         */
        "name": string;
        "reportValidity": () => Promise<boolean>;
        /**
          * Whether the radio is required in a form
          * @default false
         */
        "required": boolean;
        /**
          * Programmatically select this radio and uncheck siblings. Emits mdChange like a user interaction.
         */
        "select": () => Promise<void>;
        /**
          * Programmatically blur the radio
         */
        "setBlur": () => Promise<void>;
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Programmatically focus the radio
         */
        "setFocus": () => Promise<void>;
        /**
          * Soft-disabled: disabled visuals but remains focusable for discoverability
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Group-coordination hook. Sibling radios call this so the whole group agrees on validity; exposed as a
          * @Method because a sibling can only reach this component through its public element interface.
         */
        "syncValidityFromGroup": () => Promise<void>;
        /**
          * Form value submitted when checked
          * @default ''
         */
        "value": string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please select one of these options.'
         */
        "valueMissingLabel": string;
    }
    /**
     * `md-rating` — Material Design 3 Expressive Rating control.
     * A star-rating input: configurable max, half-step precision, custom icons
     * (Material Symbols glyphs OR slotted SVG/HTML), per-value icons
     * (filled / empty), hover preview, read-only and disabled modes,
     * keyboard navigation (Arrow + Home/End), and reset to zero.
     * MD3 Expressive: items animate with a brief tonal "scale-up" pop on
     * hover and selection; press uses a state-layer + ripple. The
     * underlying ARIA pattern is a slider so screen readers announce the
     * value as a continuous range.
     */
    interface MdRating {
        /**
          * Default value used when the consumer clears the rating (re-clicking the current value).
          * @default 0
         */
        "defaultValue": number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive and visually dimmed; not focusable.
          * @default false
         */
        "disabled": boolean;
        /**
          * Material Symbols glyph used for *empty* items.
          * @default 'star'
         */
        "emptyIcon": string;
        /**
          * Programmatically focus the rating.
         */
        "focusRating": () => Promise<void>;
        /**
          * Optional label provider — `el.getLabel = (v) => "${v} Stars"`. Used by the hidden screen-reader text and the visible value label when `show-value-label` is set.
         */
        "getLabel"?: (value: number) => string;
        /**
          * Hover-preview behaviour:   - `'preview'` (default) — display the hovered value over the live one.   - `'off'` — no hover preview; `value` is the only signal.
          * @default 'preview'
         */
        "hover": 'preview' | 'off';
        /**
          * Material Symbols glyph used for *filled* items.
          * @default 'star'
         */
        "icon": string;
        /**
          * Maximum rating (number of items rendered).
          * @default 5
         */
        "max": number;
        /**
          * Form name for native form participation.
          * @default ''
         */
        "name": string;
        /**
          * Use the *filled* glyph for empty items as well, drawn with the outline-only color treatment — a solid silhouette outline behind the fill.
          * @default true
         */
        "outlineEmpty": boolean;
        /**
          * Step size — `1` (full only) or `0.5` (half).
          * @default 1
         */
        "precision": 1 | 0.5;
        /**
          * Accessible name for the rating control.  The attribute is `rating-label`, NOT `aria-label-rating`. Anything starting with `aria-` is parsed as an ARIA attribute, and `aria-label-rating` is not one — so the previous name made axe report a CRITICAL `aria-valid-attr` violation on every consumer that used the documented API. The value still ends up as the host's accessible name; only the attribute spelling changed.
          * @default 'Rating'
         */
        "ratingLabel": string;
        /**
          * Read-only — purely decorative; no interaction, no focus.
          * @default false
         */
        "readonly": boolean;
        /**
          * When true, render the current numeric value beside the items.
          * @default false
         */
        "showValueLabel": boolean;
        /**
          * Size of each item.
          * @default 'md'
         */
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Soft-disabled — disabled visuals but remains focusable for discoverability.
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Current numeric rating (0 → max, half-steps when precision = 0.5).
          * @default 0
         */
        "value": number;
    }
    interface MdRipple {
        /**
          * @default false
         */
        "disabled": boolean;
        /**
          * Programmatically trigger a centered ripple (for keyboard activations).
         */
        "trigger": () => Promise<void>;
        /**
          * Resolves when the active press ripple has fully faded out.
         */
        "whenSettled": () => Promise<void>;
    }
    /**
     * Material Design 3 Search.
     * Implements the M3 Search specification
     * (https://m3.material.io/components/search/specs).
     * Two orthogonal axes describe every spec configuration:
     *  - `variant`: `contained` (M3 Expressive default) | `divided` (baseline)
     *  - `layout` : `full-screen` (modal overlay) | `docked` (anchored panel)
     * The component renders a single resting bar plus a results panel. When the
     * `full-screen` layout is `open` the bar pins itself to the top of the
     * viewport via `position: fixed` so consumers don't need a portal — CSS does
     * the work, and the input keeps a stable identity throughout the open/close
     * lifecycle.
     */
    interface MdSearch {
        /**
          * Announce the number of available suggestions / results to assistive technology via a polite live region whenever the slotted results change while the panel is open (M3 "autosuggest" requirement). Set to `false` if you manage your own live region in the results slot.
          * @default true
         */
        "announceResults": boolean;
        /**
          * Close the focused panel.
         */
        "close": () => Promise<void>;
        /**
          * Trailing debounce (ms) applied to the `mdSearch` event so a fetch only fires after the user pauses typing. `0` (default) emits on every change. `mdInput` always fires immediately on every keystroke regardless.
          * @default 0
         */
        "debounce": number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Whether interaction is blocked.
          * @default false
         */
        "disabled": boolean;
        /**
          * Close on outside click (docked layout).
          * @default true
         */
        "dismissOnOutsideClick": boolean;
        /**
          * Resting bar elevation level, mapped to `--md-sys-elevation-{n}`. `0` (the M3 default) renders no shadow; `1`–`5` apply the matching MD3 elevation tier. For a fully custom shadow, set the CSS custom property `--md-search-container-elevation` instead (it overrides this prop).
          * @default 0
         */
        "elevation": 0 | 1 | 2 | 3 | 4 | 5;
        /**
          * Close on Escape key.
          * @default true
         */
        "escapeCloses": boolean;
        /**
          * Programmatically focus the input.
         */
        "focusInput": () => Promise<void>;
        /**
          * Make the docked / inline bar span its container's full inline width with NO side gutters. This zeroes both expand insets for the instance (resting + focused = 0), so there is no 24 → 12px margin animation and the aligned results drawer fills the same full width. Purely an ergonomic switch over the CSS vars — the identical effect is available by setting `--md-search-expand-inset: 0` and `--md-search-expand-focused-inset: 0`. Has no effect on `full-screen` layout (already edge-to-edge).
          * @default false
         */
        "fullWidth": boolean;
        /**
          * Where focus lands when the panel opens. `auto` (default) and `input` focus the text field so the user can type immediately. `leading` focuses the leading button instead (falling back to the input if it isn't focusable).  Note: the M3 "initial focus lands on a leading icon, else the text field" rule applies to the RESTING bar's first tab stop and is handled by DOM order — a slotted interactive leading icon button is the first tabbable element, otherwise the text field. This prop only controls the open view.
          * @default 'auto'
         */
        "initialFocus": 'auto' | 'input' | 'leading';
        /**
          * Accessible label for the search bar / input. Per the M3 labeling guidance the hinted search text (`placeholder`) describes the bar, so when this is left empty the label falls back to `placeholder`. Set it explicitly only when the visible hint and the accessible name should differ.
          * @default ''
         */
        "inputAriaLabel": string;
        /**
          * How the focused panel appears. `full-screen` fills the viewport edge-to-edge with an opaque results surface and a focus trap (no scrim — open/close uses the same translateY + opacity motion as md-dialog fullscreen); `docked` anchors a popup beneath the bar (min 360px / max 720px wide; results drawer shrinks to content up to max ⅔ vh).
          * @default 'full-screen'
         */
        "layout": 'full-screen' | 'docked';
        /**
          * Material Symbols glyph for the resting leading icon.
          * @default 'search'
         */
        "leadingIcon": string;
        /**
          * Whether an async results fetch is in flight. When `true` the M3 loading indicator (the looping shape-morph) shows in the bar's trailing cluster. Controlled by the consumer: set it `true` before awaiting your fetch and back to `false` once results are slotted in. The component never flips this itself — it only reflects the state visually + to assistive tech.
          * @default false
         */
        "loading": boolean;
        /**
          * Accessible label for the in-bar loading indicator.
          * @default 'Searching'
         */
        "loadingLabel": string;
        /**
          * Maximum block-size of the open search surface (`"480px"`, `"66vh"`, etc.). Full-screen: caps the fixed overlay height. Docked: caps the results panel (bar height is unchanged). Prefer the CSS custom property `--md-search-max-block-size` for stylesheet-based theming.
          * @default ''
         */
        "maxBlockSize": string;
        /**
          * Message shown when the panel is open, the user has entered a query, and there are no slotted results — both the on-screen empty state and the polite live-region announcement (unless the visible empty state is shown, in which case the live region defers to it to avoid duplicate speech).
          * @default 'No results available'
         */
        "noResultsLabel": string;
        /**
          * Whether the focused panel is open.
          * @default false
         */
        "open": boolean;
        /**
          * Material Symbols glyph used for the leading button when the bar is open. Defaults are variant-aware: the `contained` (Expressive) bar uses `chevron_left` so the caret matches the bar's softer, pill- shaped silhouette, while the `divided` (baseline) bar uses the classic `arrow_back` to match its squared, app-bar-like chrome. Pass an explicit value to override either default.
         */
        "openLeadingIcon"?: string;
        /**
          * Placeholder text — the spec calls this "supporting text" within the bar anatomy.
          * @default 'Search'
         */
        "placeholder": string;
        /**
          * Template for the results announcement. `{count}` is replaced with the number of detected result items (counts `md-list-item`, `[role="option"]`, or `<li>` descendants of the results slot, falling back to the number of slotted elements).
          * @default '{count} results available'
         */
        "resultsLabel": string;
        /**
          * Retained for API compatibility, but now a no-op: when the results list overflows it always plain-scrolls inside the results viewport (no scroll shadow / edge fades). Setting this has no visual effect.
          * @default true
         */
        "scrollShadow": boolean;
        /**
          * Open the focused panel.
         */
        "show": () => Promise<void>;
        /**
          * Show the trailing clear (×) button when the input has a value.
          * @default true
         */
        "showClearButton": boolean;
        /**
          * Start streaming voice transcription into the input. No-op if unsupported.
         */
        "startVoice": () => Promise<void>;
        /**
          * Stop any active voice transcription.
         */
        "stopVoice": () => Promise<void>;
        /**
          * Maximum wait (ms) before `mdSearch` is forced to fire during sustained typing — a throttle/`maxWait` safety net so the user still sees interim results when they never pause long enough for the debounce to settle. `0` (default) disables it. Only meaningful alongside a non-zero `debounce`.
          * @default 0
         */
        "throttle": number;
        /**
          * Toggle the focused panel.
         */
        "toggle": () => Promise<void>;
        /**
          * What the user sees while the search view is closed. `bar` renders the resting search field (docked default). `icon` renders a compact search icon button that opens the view — the default for `full-screen` layout (contained and divided spec sheets). Docked layouts should keep `bar`.
         */
        "trigger"?: 'bar' | 'icon';
        /**
          * The external trigger as an element, for consumers that hold a reference.
         */
        "triggerElement"?: HTMLElement;
        /**
          * Open the search from an element that lives ELSEWHERE on the page — a button in the app bar, a menu item, a keyboard-shortcut hint — rather than from the built-in trigger or the `trigger` slot, both of which have to sit inside this element.  Takes a CSS selector resolved against the document (`trigger-for="#app-bar-search"`), or an element assigned to the `triggerElement` property when you already hold a reference. The component wires it rather than rendering it: activating it opens the view, `aria-haspopup="dialog"` and `aria-expanded` are kept in step, and closing returns focus to it.  With one set, the built-in trigger is not rendered — otherwise the page would show two ways in.
          * @default ''
         */
        "triggerFor": string;
        /**
          * Material Symbols glyph for the built-in icon trigger (`trigger="icon"`). Ignored when a custom element is slotted in `trigger`.
          * @default 'search'
         */
        "triggerIcon": string;
        /**
          * Current input text, two-way bindable.
          * @default ''
         */
        "value": string;
        /**
          * Visual style. `contained` is the M3 Expressive default with a filled surface and rounded shape; `divided` is the baseline pre-Expressive look that uses a hairline divider between bar and results.
          * @default 'contained'
         */
        "variant": 'contained' | 'divided';
        /**
          * Opt-in voice search. When `true` AND the browser exposes the Web Speech API (`SpeechRecognition` / `webkitSpeechRecognition`), a microphone icon-button is rendered in the trailing cluster. Clicking it starts recognition and streams the interim + final transcript into the input, firing the normal `mdInput` / `mdSearch` flow as if typed. When the API is unavailable the mic is not rendered (graceful no-op). Default `false`.
          * @default false
         */
        "voiceSearch": boolean;
    }
    interface MdSegmentedButton {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable this segment
          * @default false
         */
        "disabled": boolean;
        /**
          * Material Symbols icon name (optional)
          * @default ''
         */
        "icon": string;
        /**
          * Label text
          * @default ''
         */
        "label": string;
        /**
          * Show checkmark icon when selected
          * @default false
         */
        "noCheckmark": boolean;
        /**
          * Enable/disable ripple
          * @default true
         */
        "ripple": boolean;
        /**
          * Density value from parent set (managed by md-segmented-button-set)
          * @default 0
         */
        "segmentDensity": number;
        /**
          * Position index within the set (managed by md-segmented-button-set)
          * @default 0
         */
        "segmentIndex": number;
        /**
          * Whether the parent set is in multiselect mode (managed by md-segmented-button-set)
          * @default false
         */
        "segmentMultiselect": boolean;
        /**
          * Total number of segments in the set (managed by md-segmented-button-set)
          * @default 1
         */
        "segmentTotal": number;
        /**
          * Whether this segment is selected
          * @default false
         */
        "selected": boolean;
        /**
          * Visually disabled but remains focusable for screen readers
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Unique value identifying this segment
          * @default ''
         */
        "value": string;
    }
    interface MdSegmentedButtonSet {
        /**
          * Density: 0 = 40px, -1 = 36px, -2 = 32px, -3 = 28px, -4 = 24px
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Single-select or multi-select mode
          * @default false
         */
        "multiselect": boolean;
    }
    /**
     * `md-select` — Material Design 3 single-select dropdown.
     * Composes the library's own primitives instead of re-implementing them:
     *   - `md-text-field` is the trigger surface, so the picker inherits the
     *     full filled/outlined visuals, density scale, floating label, error
     *     state, supporting text, and focus ring of every other input. It is
     *     `readonly` (not editable) and `appear-focused` while the menu is open
     *     so the field + menu read as one connected control.
     *   - `md-menu` is the option surface, so the picker inherits open/close
     *     motion, viewport-aware positioning, single-open coordination, roving
     *     tabindex, type-ahead, and Escape/Home/End — for free.
     * Options are authored either declaratively as slotted `<md-select-option>`
     * children (primary API) or via the programmatic `options` array. Each
     * becomes a `type="radio"` `md-menu-item` so only one is ever selected.
     */
    interface MdSelect {
        /**
          * Constraint-validation API, matching md-text-field and the native contract.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Icon (Material Symbols name) for the clear button (`clearable`).
          * @default 'close'
         */
        "clearIcon": string;
        /**
          * Accessible label for the clear button (`clearable`).
          * @default 'Clear selection'
         */
        "clearLabel": string;
        /**
          * Show a clear (✕) button in the trigger when a value is selected.
          * @default false
         */
        "clearable": boolean;
        /**
          * Close the dropdown programmatically.
         */
        "close": () => Promise<void>;
        /**
          * Density forwarded to the inner field (0 = comfortable … -3 = compact).
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled": boolean;
        /**
          * Caret icon; rotates 180° when open. Slot `dropdown-icon` overrides it.
          * @default 'arrow_drop_down'
         */
        "dropdownIcon": string;
        /**
          * Error state — forwarded to the inner field.
          * @default false
         */
        "error": boolean;
        /**
          * Error text rendered in place of supporting text when `error` is set.
          * @default ''
         */
        "errorText": string;
        /**
          * Accessible name for the filter field. Falls back to `Filter <label>`.
          * @default ''
         */
        "filterLabel": string;
        /**
          * Filter strategy used by `filterable` / `setQuery`.
          * @default 'substring'
         */
        "filterMode": FilterMode;
        /**
          * Show a search field in the menu header.  A plain list filters client-side on label / supporting text; a virtualized one filters inside the WASM engine. Closing the menu clears the query.
          * @default false
         */
        "filterable": boolean;
        /**
          * Move focus to the trigger field.
         */
        "focusTrigger": () => Promise<void>;
        /**
          * Stretch the control to fill its parent's inline width (otherwise it sizes to a sensible default). The dropdown follows when `match-trigger-width` is set.
          * @default false
         */
        "fullWidth": boolean;
        /**
          * Resolve labels for values that may be outside the current filter/window.
         */
        "getLabels": (values: string[]) => Promise<Record<string, string>>;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label": string;
        /**
          * Load a large dataset into the WASM-backed virtualized list (the high-scale entry point). Accepts a materialised `SelectOptionInit[]` or a row factory (`{ count, getRow }`) — the factory streams rows into WASM one at a time, so tens of millions of options never exist as JS objects at once. Falls back to plain DOM rendering when `virtualize="never"` or WebAssembly is unavailable.
         */
        "loadOptions": (source: SelectOptionInit[] | OptionRowSource) => Promise<void>;
        /**
          * Show a busy state: the trigger shows a spinner instead of the caret and the host is `aria-busy`. Use while an async dataset loads (e.g. before/around a large `loadOptions`). Replace the spinner with the `loader` slot.
          * @default false
         */
        "loading": boolean;
        /**
          * Text shown in the trigger / menu while `loading` is true.
          * @default 'Loading…'
         */
        "loadingText": string;
        /**
          * Match the dropdown width to the trigger width. When `false` the menu sizes to its content (down to its own min-width). Maps to `md-menu`'s `match-anchor-width`. Default `true` — the expected select behaviour.
          * @default true
         */
        "matchTriggerWidth": boolean;
        /**
          * Cap the dropdown height. When the options exceed it the list scrolls vertically with top/bottom edge shadows. A number is pixels; any CSS length is passed through (e.g. `'50vh'`). Forwarded to `md-menu`'s `max-height`.
         */
        "maxHeight"?: number | string;
        /**
          * Form field name. The selected value is submitted under this name.
          * @default ''
         */
        "name": string;
        /**
          * Empty-state text when there are no options at all.
          * @default 'No options'
         */
        "noOptionsText": string;
        /**
          * Empty-state text when a filter matches nothing.
          * @default 'No results'
         */
        "noResultsText": string;
        /**
          * Open state of the dropdown. Reflected so `:host([open])` rules apply.
          * @default false
         */
        "open": boolean;
        /**
          * Programmatic options. Slotted `md-select-option` children take precedence.  Accepts the array (property) or a **JSON string** (attribute), so a plain-HTML page can declare its data inline instead of reaching for a script. Malformed JSON degrades to an empty list with a console warning.
          * @default []
         */
        "options": SelectOptionInit[] | string;
        /**
          * Placeholder shown when no value is selected.
          * @default ''
         */
        "placeholder": string;
        /**
          * Menu placement relative to the trigger — forwarded to `md-menu`.
          * @default 'bottom-start'
         */
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        /**
          * Like checkValidity(), but also shows the browser's validation message.
         */
        "reportValidity": () => Promise<boolean>;
        /**
          * Required (form validation parity).
          * @default false
         */
        "required": boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace": boolean;
        /**
          * Clear the selection.
         */
        "reset": () => Promise<void>;
        /**
          * Fixed virtualized row height (px). Auto-measured from the first row if unset.
         */
        "rowHeight"?: number;
        /**
          * Placeholder for the in-menu filter field (`filterable`).
          * @default 'Search…'
         */
        "searchPlaceholder": string;
        /**
          * Accessible label for the in-menu loading spinner while filtering.
          * @default 'Searching'
         */
        "searchingLabel": string;
        /**
          * App/server-side validation: a non-empty message marks the control invalid for its form until cleared with an empty string.
         */
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Filter the menu exactly as typing into the in-menu search field would.  The field is uncontrolled, so the text is mirrored into it as well — otherwise the list narrows while the box still reads empty, which looks like the call did nothing. The menu may be closed (no field rendered yet), so the text is stashed and applied on the next render.
         */
        "setQuery": (query: string) => Promise<void>;
        /**
          * Open the dropdown programmatically.
         */
        "show": () => Promise<void>;
        /**
          * Supporting / helper text rendered below the field.
          * @default ''
         */
        "supportingText": string;
        /**
          * Selected option value. Empty string = nothing selected.
          * @default ''
         */
        "value": string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please select an item in the list.'
         */
        "valueMissingLabel": string;
        /**
          * Visual variant — forwarded to the inner `md-text-field`.
          * @default 'outlined'
         */
        "variant": 'filled' | 'outlined';
        /**
          * Virtualization mode for large option lists (backed by a WASM data engine):   - `'auto'` (default): virtualize only above `VIRTUALIZE_THRESHOLD` rows.   - `'always'`: always virtualize the `options` / `loadOptions` dataset.   - `'never'`: keep the classic DOM rendering regardless of size. Virtualized rows render label + selection only (no per-row icon / supporting text) and assume a uniform row height.
          * @default 'auto'
         */
        "virtualize": 'auto' | 'always' | 'never';
    }
    /**
     * `md-select-option` — declarative option for `md-select` / `md-multi-select`.
     * This element is a **data carrier**: it renders nothing visible (the host
     * is `display: none`). The parent picker reads its `value`, label (the
     * `label` prop or slotted text), `disabled`, `icon`, and `supporting-text`
     * and projects them into the `md-menu` dropdown as `md-menu-item`s.
     * Authoring:
     * ```html
     * <md-select label="Colour">
     *   <md-select-option value="red" icon="circle">Red</md-select-option>
     *   <md-select-option value="green" disabled>Green</md-select-option>
     * </md-select>
     * ```
     * Whenever an option is added, removed, or has its data changed, it emits a
     * bubbling `mdSelectOptionChange` event so the parent re-reads its option set.
     */
    interface MdSelectOption {
        /**
          * Disabled options stay visible in the menu but cannot be selected.
          * @default false
         */
        "disabled": boolean;
        /**
          * Material Symbols leading-icon glyph rendered in the menu row.
          * @default ''
         */
        "icon": string;
        /**
          * CSS colour for the leading icon (any CSS `<color>` — hex, `rgb()`, or a theme token such as `var(--md-sys-color-primary)`). Ignored without `icon`.
          * @default ''
         */
        "iconColor": string;
        /**
          * Visible label. Falls back to the slotted text content when empty.
          * @default ''
         */
        "label": string;
        /**
          * Initial-selected hint (used by the picker only when its value is unset).
          * @default false
         */
        "selected": boolean;
        /**
          * Secondary text rendered under the label in the menu row.
          * @default ''
         */
        "supportingText": string;
        /**
          * Stable identifier emitted by the picker and used for matching.
          * @default ''
         */
        "value": string;
    }
    /**
     * MD3 Side sheet — anchored panel for secondary content and actions.
     */
    interface MdSideSheet {
        /**
          * Show divider between content and actions
          * @default false
         */
        "bottomDivider": boolean;
        "close": () => Promise<void>;
        /**
          * Show close icon button
          * @default true
         */
        "closeable": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Detached style with rounded corners and margin
          * @default false
         */
        "detached": boolean;
        /**
          * Headline text (or use the headline slot)
          * @default ''
         */
        "headline": string;
        /**
          * Whether the sheet is visible
          * @default false
         */
        "open": boolean;
        /**
          * Whether clicking the scrim closes the sheet (modal only)
          * @default true
         */
        "scrimDismissible": boolean;
        /**
          * Custom aria-label for the container
          * @default ''
         */
        "sheetAriaLabel": string;
        "show": () => Promise<void>;
        /**
          * Show optional back navigation icon (modal only)
          * @default false
         */
        "showBack": boolean;
        /**
          * Which edge the sheet is anchored to
          * @default 'end'
         */
        "side": 'start' | 'end';
        /**
          * Show divider between header and content
          * @default false
         */
        "topDivider": boolean;
        /**
          * `standard` coexists with page content; `modal` overlays with scrim
          * @default 'standard'
         */
        "variant": 'standard' | 'modal';
    }
    interface MdSkeleton {
        /**
          * Animation style: - `pulse` (default) — gentle opacity fade in and out. - `wave` — shimmer gradient sweeps across the surface. - `none` — static block; useful in `prefers-reduced-motion` opt-outs   or when the skeleton is purely decorative.
          * @default 'pulse'
         */
        "animation": 'pulse' | 'wave' | 'none';
        /**
          * Whether to expose the skeleton to assistive tech at all. Set to `false` when the surrounding region already announces its own busy state — duplicate announcements get noisy.
          * @default true
         */
        "announce": boolean;
        /**
          * Accessible label. Defaults to "Loading…". Pass a more specific label when the skeleton represents a recognisable region ("Loading article") for better screen-reader context.
          * @default 'Loading'
         */
        "ariaLabelProp": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Stretch to fill the parent's block size — the placeholder fills its container's height (which must be definite, e.g. a flex/grid cell). Best with the shape variants (`rectangular` / `rounded` / `circular`); pairs with `full-width` for a fully responsive block.
          * @default false
         */
        "fullHeight": boolean;
        /**
          * Stretch to fill the parent's inline size, overriding `width` / `--md-skeleton-width`. (The skeleton is already 100%-wide by default; this makes it explicit and wins over a fixed width — handy in responsive grids.)
          * @default false
         */
        "fullWidth": boolean;
        /**
          * Block size (CSS length). Defaults are `1em` for text, `120px` for rectangular/rounded, and `40px` for circular.
          * @default ''
         */
        "height": string;
        /**
          * Number of text rows to render. Only meaningful for `variant="text"`. The last row is tapered to ~60 % width so it reads as the final line of a paragraph.
          * @default 1
         */
        "lines": number;
        /**
          * Shape of the skeleton placeholder. - `text` — single short rectangle the height of a body line. With   `lines > 1` the last line gets a tapered width so it reads as a   "last line of a paragraph". - `rectangular` — fills the inline + block sizes you give it; no   border-radius unless you set `--md-skeleton-radius`. - `rounded` — same as `rectangular` but with the MD3 medium corner   radius (or your override). Good for cards / images. - `circular` — perfect circle (1:1 aspect). Use for avatar / icon   placeholders; set `--md-skeleton-size` or `width`+`height`.
          * @default 'text'
         */
        "variant": 'text' | 'rectangular' | 'rounded' | 'circular';
        /**
          * Inline size (CSS length). Overrides any `--md-skeleton-width`. Examples: `"100%"`, `"240px"`, `"60ch"`.
          * @default ''
         */
        "width": string;
    }
    /**
     * MD3 Expressive slider: standard (from edge), centered (from midpoint), or range (dual thumb).
     * **Uncontrolled (default)** — the slider manages its own value. Listen to `mdChange` to observe commits.
     * **Controlled** (`controlled`) — the slider never mutates `value`/`valueStart`/`valueEnd`.
     * Listen to events and set the props yourself.  During drag the slider uses internal live
     * state for smooth visuals; on commit it snaps to the prop values.
     */
    interface MdSlider {
        /**
          * Accessible label for the range-end thumb (becomes the thumb input's `aria-label`).
          * @default ''
         */
        "ariaLabelEnd": string;
        /**
          * Accessible label for the range-start thumb (becomes the thumb input's `aria-label`; overrides `aria-label`). NB: a plain (non-`aria-`) attribute name — `aria-label-start` is not a valid ARIA attribute and would just pollute the host with an invalid `aria-*`.
          * @default ''
         */
        "ariaLabelStart": string;
        /**
          * When `true`, the slider never mutates `value` / `valueStart` / `valueEnd` on user interaction. Listen to `mdInput` (continuous) and `mdChange` (committed) events and write the props back yourself. During a drag gesture the slider uses internal live state for smooth visuals.
          * @default false
         */
        "controlled": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled": boolean;
        /**
          * Make a **vertical** slider fill its parent's block size (responsive height) instead of the fixed `--md-slider-track-length`. The parent needs a definite height; otherwise it falls back to the track-length floor so it never collapses. (Horizontal sliders are already full-width by default.)
          * @default false
         */
        "fullHeight": boolean;
        /**
          * Material Symbols name when `inset-icon` and no slotted icon
          * @default ''
         */
        "icon": string;
        /**
          * Standard slider only: inset icon area (use `icon` or `inset-icon` slot)
          * @default false
         */
        "insetIcon": boolean;
        /**
          * @deprecated Use `value-indicator`
          * @default false
         */
        "labeled": boolean;
        /**
          * @default 100
         */
        "max": number;
        /**
          * @default 0
         */
        "min": number;
        /**
          * Form field name. Without it the slider still works, it just submits nothing — same as a native input with no `name`.
          * @default ''
         */
        "name": string;
        /**
          * See `nameStart`.
          * @default ''
         */
        "nameEnd": string;
        /**
          * Range mode submits TWO values, which one name cannot express. By default both go under `name` (read them with `formData.getAll(name)`); set these to give each end its own key. Ignored outside `range`.
          * @default ''
         */
        "nameStart": string;
        /**
          * @default 'horizontal'
         */
        "orientation": 'horizontal' | 'vertical';
        /**
          * Dual-thumb range
          * @default false
         */
        "range": boolean;
        /**
          * Expressive size (default XS per MD3 spec)
          * @default 'xs'
         */
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * @default ''
         */
        "sliderAriaLabel": string;
        /**
          * @default 1
         */
        "step": number;
        /**
          * Stop / tick marks at each step.
          * @default false
         */
        "stops": boolean;
        /**
          * @default 50
         */
        "value": number;
        /**
          * @default 100
         */
        "valueEnd": number;
        /**
          * Human-readable text for the range-end value. Mapped to `aria-valuetext` on the end thumb.
          * @default ''
         */
        "valueEndText": string;
        /**
          * Show value label at thumb(s)
          * @default false
         */
        "valueIndicator": boolean;
        /**
          * @default 0
         */
        "valueStart": number;
        /**
          * Human-readable text for the range-start value. Mapped to `aria-valuetext` on the start thumb.
          * @default ''
         */
        "valueStartText": string;
        /**
          * Human-readable text alternative for the current value (single slider). Mapped to `aria-valuetext`.
          * @default ''
         */
        "valueText": string;
        /**
          * Single-thumb layout: `standard` fills from min; `centered` fills from range midpoint to value.
          * @default 'standard'
         */
        "variant": 'standard' | 'centered';
    }
    interface MdSnackbar {
        /**
          * Action button label (leave empty for no action)
          * @default ''
         */
        "action": string;
        /**
          * Auto-dismiss after `autoHideDuration` ms (dismissive behavior)
          * @default true
         */
        "autoHide": boolean;
        /**
          * Time in ms before auto-dismiss
          * @default 4000
         */
        "autoHideDuration": number;
        /**
          * Show a close (X) icon button
          * @default false
         */
        "closeable": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Accessible label for the close button. Localize per the page locale.
          * @default 'Dismiss'
         */
        "dismissLabel": string;
        /**
          * Hide the snackbar with an optional reason
         */
        "hide": (reason?: "auto" | "action" | "close") => Promise<void>;
        /**
          * Primary supporting text
          * @default ''
         */
        "message": string;
        /**
          * Whether the snackbar is visible
          * @default false
         */
        "open": boolean;
        /**
          * Live-region politeness. `'polite'` (default) → `role="status"` / `aria-live="polite"` (announced when idle). `'assertive'` → `role="alert"` / `aria-live="assertive"` — interrupts the screen reader; use sparingly for important / error messages.
          * @default 'polite'
         */
        "politeness": 'polite' | 'assertive';
        /**
          * Screen position
          * @default 'bottom'
         */
        "position": | 'bottom' | 'bottom-start' | 'bottom-end'
    | 'top' | 'top-start' | 'top-end'
    | 'start' | 'end' | 'center';
        /**
          * Show the snackbar
         */
        "show": () => Promise<void>;
        /**
          * Stack action below text (for longer action labels)
          * @default false
         */
        "stacked": boolean;
    }
    /**
     * md-sparkline — compact inline chart, rendered by the in-house
     * engine (Canvas2D). No ECharts.
     * Sparklines are presentational: no axes, no legend, no title, no
     * tooltip by default (opt in with `show-tooltip`). Line / area drive
     * LineChartEngine in `compact` mode; the bar variant drives
     * BarChartEngine. `show-marks` maps to selective extremes/edges dots.
     */
    interface MdSparkline {
        /**
          * Fixed bar WIDTH in px (bar variant), centred in each slot. Default: auto.
         */
        "barWidth"?: number;
        /**
          * Sparkline tint (MD3 role or raw colour).
          * @default 'primary'
         */
        "color": MdChartColorRole | string;
        /**
          * Bar corner radius (bar variant only).
          * @default 2
         */
        "cornerRadius": number;
        /**
          * Curve type — only meaningful for line / area variants.
          * @default 'smooth'
         */
        "curve": MdChartCurve;
        /**
          * Numeric data series. Pass `null` for gaps.
          * @default []
         */
        "data": MdChartDataPoint[];
        "getInstance": () => Promise<LineChartEngine | BarChartEngine | null>;
        /**
          * Block-size override (defaults to 28px — table-cell friendly).
         */
        "heightProp"?: string;
        /**
          * Optional category labels for tooltips / a11y.
         */
        "labels": (string | number | Date)[] | undefined;
        /**
          * Line stroke width in px (line / area variants). Default 2.5.
         */
        "lineWidth"?: number;
        /**
          * Marker RADIUS in px (line / area variants). Overrides the per-symbol default.
         */
        "markSize"?: number;
        /**
          * Pin the value scale's UPPER bound instead of auto-scaling to the data.
         */
        "max"?: number;
        /**
          * Pin the value scale's LOWER bound instead of auto-scaling to the data. Give several sparklines the same `min` + `max` and they share a comparable scale — a 0–40% metric no longer fills the whole height and reads as maxed out.
         */
        "min"?: number;
        /**
          * Disable all animation.
          * @default false
         */
        "noAnimation": boolean;
        /**
          * Highlight a vertical range (e.g. weekend, anomaly window).
         */
        "referenceAreas": MdSparklineReferenceArea[] | undefined;
        /**
          * Re-read the MD3 tokens from the host's computed style and repaint.  The chart does this automatically when the tokens actually change, so reach for this only when a theme is applied in a way the watcher cannot observe — tokens injected into a stylesheet rather than onto an element, for instance. Cheaper and more direct than reassigning `series` to force a rebuild.
         */
        "refreshTheme": () => Promise<void>;
        "resize": () => Promise<void>;
        /**
          * Show min / max / first / last markers.
          * @default 'extremes'
         */
        "showMarks": 'none' | 'extremes' | 'edges' | 'all';
        /**
          * Show tooltip + crosshair on hover. Defaults to true.
          * @default true
         */
        "showTooltip": boolean;
        /**
          * Format raw values for tooltips / a11y.
         */
        "valueFormatter"?: (value: number | null) => string;
        /**
          * Visual variant.
          * @default 'line'
         */
        "variant": 'line' | 'bar' | 'area';
    }
    interface MdSplitButton {
        /**
          * `id` of the element the trailing button controls (the menu/listbox it opens). Wires `aria-controls` so assistive tech links trigger → popup.
          * @default ''
         */
        "controls": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the entire split button
          * @default false
         */
        "disabled": boolean;
        /**
          * Stretch the split button to fill its container. The leading segment grows and its label truncates with an ellipsis when space is tight; the trailing toggle keeps its intrinsic width.
          * @default false
         */
        "fullWidth": boolean;
        /**
          * `aria-haspopup` value for the trailing button — the kind of popup it opens. Defaults to `'menu'` (the split-button menu-trigger pattern). Set to `'false'` when the trailing button toggles state rather than opening a popup.
          * @default 'menu'
         */
        "haspopup": | 'menu' | 'listbox' | 'dialog' | 'grid' | 'tree' | 'true' | 'false';
        /**
          * Material Symbols icon name for the leading button
          * @default ''
         */
        "icon": string;
        /**
          * Leading button label text
          * @default ''
         */
        "label": string;
        /**
          * Accessible label for the trailing (menu-toggle) button. Localize per the page locale. (WCAG 4.1.2 — the icon-only trailing button needs a name.)
          * @default 'Toggle menu'
         */
        "menuLabel": string;
        /**
          * Enable/disable ripple
          * @default true
         */
        "ripple": boolean;
        /**
          * Size
          * @default 'sm'
         */
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Visually disabled but remains focusable for screen readers
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Whether the trailing button is in "open/checked" state
          * @default false
         */
        "trailingChecked": boolean;
        /**
          * Trailing button icon (default: keyboard_arrow_down)
          * @default 'keyboard_arrow_down'
         */
        "trailingIcon": string;
        /**
          * Color variant
          * @default 'filled'
         */
        "variant": 'elevated' | 'filled' | 'tonal' | 'outlined';
    }
    /**
     * `md-status-dot` — a small absolutely-positioned indicator that pairs
     * with an avatar (or any other tile) to surface presence / state.
     * Designed to drop straight into a `position: relative` parent:
     * ```html
     * <span style="position: relative; display: inline-flex;">
     *   <md-avatar name="Ada Lovelace" label="Ada Lovelace, online"></md-avatar>
     *   <md-status-dot state="online"></md-status-dot>
     * </span>
     * ```
     * Anchors at the bottom-inline-end corner via logical-property insets, so
     * the pip auto-flips in RTL with no extra CSS. A 2 px outline in the
     * surface color keeps the dot visually detached from the avatar tile
     * regardless of the avatar's palette.
     * **Accessibility.** The dot is **decorative by default** — when used to
     * decorate an avatar, the avatar's own `label` should already encode the
     * status (`label="Ada Lovelace, online"`). Setting an explicit `label`
     * exposes the dot to assistive tech:
     * - **static** (`live` off) → `role="img"` + `aria-label` — a labelled image
     *   conveying the current state, announced when focused/encountered.
     * - **live** (`live` on) → `role="status"` (an `aria-live="polite"` region) so
     *   presence *changes* (online → busy) are announced as they happen.
     * **WCAG 1.4.1 (use of colour).** State is conveyed by colour alone, and in
     * Windows High-Contrast every state collapses to a single system colour. Never
     * rely on the dot's colour as the *only* signal of state — pair it with a
     * `label` (or text on the parent) so the status is also available as words.
     * **Live indicator.** When `live` is `true`, the dot pulses outward in its
     * own colour (online pulses green, busy pulses red, etc.). The animation
     * is gated behind `prefers-reduced-motion: no-preference`, so users who
     * have reduced-motion enabled at the OS level see a static dot.
     */
    interface MdStatusDot {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Lay the dot out in normal flow instead of as an absolutely-positioned corner badge.  The default mode is the avatar badge: `position: absolute` pinned 2px outside its positioned parent's inline/block end, with a surface-toned halo separating it from the tile beneath. That is wrong wherever the dot sits BESIDE something — a legend swatch, a "Market open" line, a status label in a table cell — where it needs to flow with the text. Without this prop each such use had to wrap the dot in a `position: relative` span of an explicit size AND null out both inset custom properties; miss one and the dot hangs low and to the right, overlapping its own label.  `inline` makes the host an `inline-block` in normal flow, vertically centred on the text, with no halo (the halo exists to separate the dot from an avatar tile; over ordinary text it reads as a stray ring). Set `--md-status-dot-outline-width` to bring it back.
          * @default false
         */
        "inline": boolean;
        /**
          * Optional accessible label.  - When **empty** (default), the dot is exposed to AT as decorative   (`role="presentation"` + `aria-hidden="true"`) so the parent   element (typically an `md-avatar` or a list-item) can carry the   spoken status without doubling. - When **set** and `live` is **off**, the dot becomes `role="img"` with   the value as its `aria-label` — a static, labelled status image. - When **set** and `live` is **on**, the dot becomes a `role="status"`   live region so presence *changes* are announced as they occur. Use this   for standalone dots that aren't paired with another labelled element.
          * @default ''
         */
        "label": string;
        /**
          * Pulse the dot outward in its own colour. Useful for "live now", "in a call", "recording" affordances. Respects `prefers-reduced-motion: reduce` automatically.
          * @default false
         */
        "live": boolean;
        /**
          * Diameter preset: - `small`  — 8 px (pairs with `md-avatar size="small"`) - `medium` — 12 px (pairs with `md-avatar size="medium"` — default) - `large`  — 16 px (pairs with `md-avatar size="large"`)  Custom diameters are exposed via the `--md-status-dot-size` CSS custom property on the host.
          * @default 'medium'
         */
        "size": MdStatusDotSize;
        /**
          * Presence state. Drives the dot's fill color and its corresponding pulse-halo color (when `live` is `true`).
          * @default 'neutral'
         */
        "state": MdStatusDotState;
    }
    /**
     * `md-step` — one step inside an `<md-stepper>`.
     * Layout, position and i18n are pushed down from the parent stepper via
     * `data-*` attributes, so a step is almost always authored declaratively:
     * ```html
     * <md-stepper active="1">
     *   <md-step label="Account" completed></md-step>
     *   <md-step label="Shipping" description="Address & method"></md-step>
     *   <md-step label="Payment" optional></md-step>
     * </md-stepper>
     * ```
     */
    interface MdStep {
        /**
          * Full override for the announced accessible name (i18n escape hatch).
         */
        "accessibleName"?: string;
        /**
          * Whether this step is the active one. Usually managed by the parent.
          * @default false
         */
        "active": boolean;
        /**
          * Whether this step is completed (shows a check / fills its connector).
          * @default false
         */
        "completed": boolean;
        /**
          * Glyph shown when completed (Material Symbols name).
          * @default 'check'
         */
        "completedIcon": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Supporting text beneath the label.
          * @default ''
         */
        "description": string;
        /**
          * Manually disable the step (parent's `mode="linear"` also gates reachability).
          * @default false
         */
        "disabled": boolean;
        /**
          * Editable: a completed step shows an edit (pencil) affordance, and — with `auto-complete` on — revisiting it preserves downstream completion (review/edit in place). Revisiting a non-editable step restarts progress from there.
          * @default false
         */
        "editable": boolean;
        /**
          * Whether this step is in an error state.
          * @default false
         */
        "error": boolean;
        /**
          * Glyph shown when in error (Material Symbols name).
          * @default 'priority_high'
         */
        "errorIcon": string;
        /**
          * Error message shown beneath the label (overrides `description` on error).
          * @default ''
         */
        "errorText": string;
        /**
          * Hide the built-in Back / Continue actions in the vertical content panel.
          * @default false
         */
        "hideActions": boolean;
        /**
          * Override the indicator glyph (Material Symbols name), e.g. "lock".
          * @default ''
         */
        "icon": string;
        /**
          * Step caption ("Account info", "Shipping", …).
          * @default ''
         */
        "label": string;
        /**
          * Mark the step optional — shows an "Optional" caption and may be skipped.
          * @default false
         */
        "optional": boolean;
    }
    /**
     * `md-stepper` — Material Design 3 stepper. Orchestrates a set of slotted
     * `md-step` children: it owns the active index and navigation, and pushes
     * layout / i18n down to each step.
     * **Controlled, with optional auto-progress.** The stepper owns `active`
     * (updated on click / `next` / `prev` / `goTo`, emitting `mdStepChange`). With
     * `auto-complete` (default), advancing marks the step you leave as `completed`,
     * so the progress line + checks "just work" — no manual wiring. Set
     * `auto-complete="false"` to own `completed` yourself.
     * ```html
     * <md-stepper active="0">
     *   <md-step label="Account"><p>…</p></md-step>
     *   <md-step label="Shipping" optional><p>…</p></md-step>
     *   <md-step label="Payment" editable><p>…</p></md-step>
     * </md-stepper>
     * ```
     */
    interface MdStepper {
        /**
          * Index of the active step. Two-way bindable. Out-of-range values clamp.
          * @default 0
         */
        "active": number;
        /**
          * When advancing (via `next()` or a step's built-in Continue button), mark the step being left as `completed`. Default `true`. Turn off to fully control completion yourself.
          * @default true
         */
        "autoComplete": boolean;
        /**
          * Label for the built-in Back button.
          * @default 'Back'
         */
        "backLabel": string;
        /**
          * Localized word announced for a completed step ("…, completed").
          * @default 'completed'
         */
        "completedWord": string;
        /**
          * Localized word announced for the current step ("…, current").
          * @default 'current'
         */
        "currentWord": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Localized word announced for a step in error ("…, error: message").
          * @default 'error'
         */
        "errorWord": string;
        /**
          * Label for the built-in Continue button on the last step.
          * @default 'Finish'
         */
        "finishLabel": string;
        /**
          * Jump to a step (out-of-range clamps; honors `linear` reachability; emits `mdBeforeChange`).
         */
        "goTo": (index: number) => Promise<void>;
        /**
          * Indicator style: `numbered` circles or minimal `dot`s.
          * @default 'numbered'
         */
        "indicator": 'numbered' | 'dot';
        /**
          * Accessible name for the navigation landmark.
          * @default 'Progress'
         */
        "label": string;
        /**
          * Only mount the **active** vertical step's content panel; inactive panels are removed from layout and the a11y tree (no collapse animation). Use for wizards with heavy per-step content. Note: slotted custom elements stay *connected* in the light DOM — for full teardown, conditionally render the content in your framework.
          * @default false
         */
        "lazy": boolean;
        /**
          * Put the built-in **Continue / Finish** button into a loading state (spinner + interactive-disabled) and disable **Back**, for async submit-on-Continue (prevents double-submit). Toggle it around your `await` — typically inside an `mdBeforeChange` handler that `preventDefault()`s, does async work, then commits by setting `active`.
          * @default false
         */
        "loading": boolean;
        /**
          * - `linear` (default) — steps must be completed in order; future steps are   not reachable until the prior ones are done or optional (an `editable`   completed step can always be revisited). - `non-linear` — any step can be selected.
          * @default 'linear'
         */
        "mode": 'linear' | 'non-linear';
        /**
          * Show the built-in **Back / Continue** navigation (default `true`) so users always have a clear way to move through the flow: - vertical → the buttons sit under each active step (Material wizard style), - horizontal → a single button bar below the stepper. Set `nav="false"` to drive navigation entirely yourself (clicking steps, `next()` / `prev()`, or your own controls).
          * @default true
         */
        "nav": boolean;
        /**
          * Advance to the next non-disabled step (emitting a cancelable `mdBeforeChange`). When `auto-complete` is on, the step you leave is marked completed; past the last reachable step it emits `mdComplete`.
         */
        "next": () => Promise<void>;
        /**
          * Disable the built-in **Continue / Finish** button without disabling Back or the whole step. Bind this to your current step's form validity to gate advancement declaratively (the most common wizard need). Step clicks and the `next()`/`goTo()` methods are unaffected — use `mode="linear"` and/or `mdBeforeChange` for those.
          * @default false
         */
        "nextDisabled": boolean;
        /**
          * Label for the built-in Continue button.
          * @default 'Continue'
         */
        "nextLabel": string;
        /**
          * Localized word for "of" in each step's announced name ("Step 2 of 4").
          * @default 'of'
         */
        "ofWord": string;
        /**
          * Caption + announced word for optional steps.
          * @default 'Optional'
         */
        "optionalWord": string;
        /**
          * Layout orientation.
          * @default 'horizontal'
         */
        "orientation": 'horizontal' | 'vertical';
        /**
          * Step backward to the previous non-disabled step (emitting `mdBeforeChange`).
         */
        "prev": () => Promise<void>;
        /**
          * Reset to the first step and clear completed / error state.
         */
        "reset": () => Promise<void>;
        /**
          * Localized word for "Step" in each step's announced name ("Step 2 of 4").
          * @default 'Step'
         */
        "stepWord": string;
        /**
          * Layout variant: - `default` — the full step row (headers + connectors). - `mobile` — a compact MUI-style bar for narrow screens: **Back**, a centered   progress (dots for `indicator="dot"`, else a "Step N of M" caption), and   **Continue / Finish**. The step headers are hidden; put the active step's   panel in the `content` slot and swap it on `mdStepChange`.
          * @default 'default'
         */
        "variant": 'default' | 'mobile';
    }
    interface MdSubMenuItem {
        /**
          * Small badge label (e.g. "New") displayed before the trailing arrow.
          * @default ''
         */
        "badge": string;
        /**
          * Collapse this row's submenu and reset its visual state. Called by an ancestor menu when it closes, so reopening the menu tree always starts fresh instead of restoring the previously-open submenu and focus ring.
         */
        "collapse": () => Promise<void>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Whether the item is disabled.
          * @default false
         */
        "disabled": boolean;
        /**
          * Render a divider line below this item.
          * @default false
         */
        "divider": boolean;
        /**
          * Render a gap (extra space) below this item — an alternative separator to divider.
          * @default false
         */
        "gap": boolean;
        /**
          * Primary label text.
          * @default ''
         */
        "headline": string;
        /**
          * Secondary descriptive text below the headline.
          * @default ''
         */
        "supportingText": string;
    }
    /**
     * `md-switch` — Material Design 3 switch (on/off toggle).
     * Form-associated: inside a `<form>` it submits its `value` under `name` when
     * selected, and restores on form reset. It is a *labelable* element — wrap it in
     * a `<label>` (clicks on the label text toggle it and name it), or pass
     * `aria-label` / `aria-labelledby` on the host.
     */
    interface MdSwitch {
        /**
          * Constraint-validation API, matching md-text-field and the native contract.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Prevents interaction and removes from tab order
          * @default false
         */
        "disabled": boolean;
        /**
          * Current validity: boolean, message and flags. Mirrors md-text-field.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * Show icons on both selected and unselected states (check / close)
          * @default false
         */
        "icons": boolean;
        /**
          * Name submitted with the owning `<form>`.
          * @default ''
         */
        "name": string;
        /**
          * Like checkValidity(), but also shows the browser's validation message.
         */
        "reportValidity": () => Promise<boolean>;
        /**
          * Mark the control required for form validation / assistive tech.
          * @default false
         */
        "required": boolean;
        /**
          * Whether the switch is on (selected)
          * @default false
         */
        "selected": boolean;
        /**
          * Material Symbols glyph shown on the handle when selected (override the default check). Ignored when the `selected-icon` slot is filled.
          * @default 'check'
         */
        "selectedIcon": string;
        /**
          * App/server-side validation: a non-empty message marks the control invalid for its form until cleared with an empty string.
         */
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Programmatically focus the switch (shows the focus ring).
         */
        "setFocus": () => Promise<void>;
        /**
          * Show icon only when selected
          * @default false
         */
        "showOnlySelectedIcon": boolean;
        /**
          * Visually disabled but remains focusable for discoverability
          * @default false
         */
        "softDisabled": boolean;
        /**
          * Material Symbols glyph shown on the handle when unselected (override the default close). Ignored when the `unselected-icon` slot is filled.
          * @default 'close'
         */
        "unselectedIcon": string;
        /**
          * Value submitted when the switch is selected.
          * @default 'on'
         */
        "value": string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please turn this on if you want to proceed.'
         */
        "valueMissingLabel": string;
    }
    interface MdTab {
        /**
          * Whether this tab is the active / selected tab
          * @default false
         */
        "active": boolean;
        /**
          * Optional badge text (empty string = dot badge)
         */
        "badge": string | undefined;
        /**
          * ID of the associated tabpanel (sets `aria-controls`)
          * @default ''
         */
        "controls": string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Prevents interaction
          * @default false
         */
        "disabled": boolean;
        /**
          * Material Symbols icon name (or use the `icon` slot for custom icons)
          * @default ''
         */
        "icon": string;
        /**
          * Display icon and label side-by-side instead of stacked
          * @default false
         */
        "inlineIcon": boolean;
        /**
          * Tab label text
          * @default ''
         */
        "label": string;
        /**
          * Visual variant — set automatically by parent `md-tabs`
          * @default 'primary'
         */
        "variant": 'primary' | 'secondary';
    }
    /**
     * A single tab panel. Place inside `md-tab-panels`, one per tab, in tab
     * order. The parent manages `active`, `inert`, and the tabs↔panel ARIA
     * wiring; inactive panels stay in layout (hidden) so the region's size
     * never jumps between tabs.
     */
    interface MdTabPanel {
        /**
          * Managed by md-tab-panels — reflects the associated tab's selection.
          * @default false
         */
        "active": boolean;
    }
    /**
     * Panel region for `md-tabs`: place directly AFTER the tablist (or point
     * `for` at its id) with one `md-tab-panel` per tab, in tab order.
     * - Shows the panel matching the tabs' `activeTabIndex`, following
     *   `mdTabChange` automatically.
     * - `sizing="stable"` (default): all panels stack in one grid cell and
     *   inactive ones stay in layout (hidden), so the region — and any
     *   shrink-to-fit card around it — keeps the SAME width/height no matter
     *   which tab is active. `sizing="active"` sizes to the active panel only.
     * - Wires the APG ARIA contract both ways: each tab gets `aria-controls`,
     *   each panel gets `aria-labelledby`, ids auto-assigned where missing.
     */
    interface MdTabPanels {
        /**
          * id of the `md-tabs` to follow. Empty = nearest preceding md-tabs sibling.
          * @default ''
         */
        "for": string;
        /**
          * stable = reserve the largest panel's box; active = size to the active panel.
          * @default 'stable'
         */
        "sizing": 'stable' | 'active';
    }
    /**
     * Material Design 3 — Table
     * The core composable table primitive for hand-authored tables.
     * Layout architecture: the shadow `.md-table__grid` owns the column tracks
     * (`grid-template-columns`), and every slotted `md-table-row` is a REAL grid
     * item spanning all tracks that lays its cells on the shared tracks via
     * `grid-template-columns: subgrid`. Rows being real boxes is what makes row
     * hover, stripes, dividers, sticky headers and focus rings actually work —
     * and a wrong cell count skews one row, never the whole table.
     * Coordination is push-based (the house pattern): the table stamps context
     * onto its rows (`rowgroup`, stripe parity, stickiness) and pushes sort state
     * into `md-table-sort-label`s — no MutationObservers, no `:host-context()`.
     * High-level features:
     *   - Density: `compact` | `standard` | `comfortable`
     *   - Sticky header / footer (pair with `md-table-container` for the scroll box)
     *   - Sticky first / last column via cell `sticky="start" | "end"`
     *   - Sortable columns via `<md-table-sort-label>` (3-state asc → desc → none)
     *   - Selection coordination — emits `mdSelectionChange`; `selectAll()` /
     *     `deselectAll()` / `toggleSelectAll()` for the select-all checkbox
     *   - Horizontal scroll when the content is wider than the container
     *   - Loading + empty-state slots, striped rows, configurable dividers
     */
    interface MdTable {
        /**
          * Snapshot the current row boxes and FLIP-animate whatever the next SYNCHRONOUS DOM mutation does to them. Call it right before you reorder / show-hide rows outside the table's own events (custom filters, async refetch completion, …); sort and pagination are bracketed automatically.
         */
        "animateNextChange": () => Promise<void>;
        /**
          * Optional caption rendered above the table head.
          * @default ''
         */
        "caption": string;
        /**
          * CSS `grid-template-columns` value (e.g. `"auto 1fr 120px"`). Takes precedence over the `columns` prop. Use this for fine control, including using `minmax()`, `fr` units and `auto`.
          * @default ''
         */
        "columnTemplate": string;
        /**
          * Number of columns when no `column-template` is provided. The grid uses `repeat(<columns>, minmax(0, 1fr))`. When neither is set, the column count is derived from the first row's cells.
          * @default 0
         */
        "columns": number;
        /**
          * Layout density. Accepts BOTH vocabularies:  - the semantic sizes `'compact' | 'standard' | 'comfortable'` (row heights   36 / 52 / 60px), which is this component's original API, and - a numeric rung `0 | -1 | -2 | -3 | -4`, the scale every other component in   the library uses.  A numeric value drives `--md-sys-density-scale` on the host (see the DENSITY block in the CSS) exactly as a global `data-density` ancestor would, and takes the `standard` row-height base. The two are not exclusive: the semantic sizes are themselves scale-aware, so `density="compact"` inside a `data-density="-2"` region condenses further.
          * @default 'standard'
         */
        "density": | 'compact'
    | 'standard'
    | 'comfortable'
    | 0
    | -1
    | -2
    | -3
    | -4;
        /**
          * Programmatically deselect every row.
         */
        "deselectAll": () => Promise<void>;
        /**
          * Render the empty-state slot when true.
          * @default false
         */
        "empty": boolean;
        /**
          * Frozen-header architecture: the header is rendered OUTSIDE the vertical scroll area, so the scrollbar spans only the body and runs UNDER the head Pair with `md-table-container[max-height]` to cap the body — max-height controls the SIZE, this prop controls the ARCHITECTURE. Without it, a max-height container scrolls the whole table (use `sticky-header` there for the classic in-flow pinned header).
          * @default false
         */
        "frozenHeader": boolean;
        /**
          * Returns the current selection state (synchronous snapshot).
         */
        "getSelection": () => Promise<MdTableSelectionState>;
        /**
          * Hover surface on rows.
          * @default true
         */
        "hoverable": boolean;
        /**
          * Height ratchet (default ON): once the table has rendered, it never shrinks below that initial height. Paging, filtering to fewer/zero rows, or an async refetch that momentarily empties the body keep the table (and therefore the page) geometrically stable — no content below jumps up, and no scrollbar can flash from a transient collapse (async rebuilds have a few frames where fresh rows haven't hydrated and measure 0). Set `keep-height="false"` for tables that intentionally resize (e.g. a density-toggle demo).
          * @default true
         */
        "keepHeight": boolean;
        /**
          * Accessible name for the `role="table"`. When omitted, the `caption` text names the table (WAI: a caption identifies the table for screen-reader users browsing in tables mode); the generic "Data table" is the last resort. IDREFs cannot cross the shadow boundary, so the caption text is mirrored into `aria-label` rather than referenced via `aria-labelledby`.
          * @default ''
         */
        "label": string;
        /**
          * Show the loading state.
          * @default false
         */
        "loading": boolean;
        /**
          * Loading presentation: - `overlay` (default) — an indeterminate progress line under the header   plus a scrim that dims/disables the body while the header/footer stay put. - `skeleton` — the body is replaced by shimmering skeleton rows; the   header and footer are untouched.
          * @default 'overlay'
         */
        "loadingMode": 'overlay' | 'skeleton';
        /**
          * Number of skeleton rows rendered in `loading-mode="skeleton"`.
          * @default 4
         */
        "loadingRows": number;
        /**
          * Minimum table width (e.g. `"640px"`). When the container is narrower, the grid keeps this width and the container shows a horizontal scrollbar instead of squishing the columns. Also settable via `--md-table-min-width`.
          * @default ''
         */
        "minWidth": string;
        /**
          * Built-in expressive motion. With `expressive` (the default) the table FLIP-animates its rows around sort and page changes: it snapshots the row boxes when the triggering event fires (`mdSortRequest`, or a pagination event heard by the surrounding `md-table-container`), lets the consumer's synchronous handler mutate the rows, then glides survivors to their new positions and fades + rises entrants. Async mutations settle as a clean no-op. `prefers-reduced-motion` always wins; set `none` to opt out — e.g. when driving your own `flipRows` wiring around the same events.
          * @default 'expressive'
         */
        "motion": 'expressive' | 'none';
        /**
          * Hide the dividers between rows.
          * @default false
         */
        "noDividers": boolean;
        /**
          * Pin a column to an edge (or unpin with `'none'`). `column` is the ORIGINAL 0-based column index (its position before any pinning). The table owns the whole mechanic: it physically re-sequences every row's cells (selection and expansion state survive — the elements just move), maintains `sticky` attributes, permutes the column template, and re-floors the header tracks so pin icons / sort arrows never clip in frozen mode. The move itself is an intentional SNAP (no FLIP) — the frozen header re-measures its tracks over the next frames, and animating against that settling would jitter. Resolves to the column's effective side after the call.
         */
        "pinColumn": (column: number, side: "start" | "end" | "none") => Promise<"start" | "end" | null>;
        /**
          * Table-wide custom pin-indicator icon (Material Symbols ligature, e.g. "anchor"). Cast to every pinned header cell so consumers set it once; a cell's own `pin-icon` attribute wins over the cast. Empty = built-in push-pin SVG.
          * @default ''
         */
        "pinIcon": string;
        /**
          * How MULTIPLE pinned columns on the same side behave during horizontal scroll: - `stack` (default) — every pinned column sticks at the same edge, so   later ones slide OVER earlier ones while scrolling (a deck-of-cards   effect; only the top one stays fully visible). - `static` — each pinned column gets a cumulative inset equal to the   widths of the pinned columns before it, so ALL of them stay visible   side-by-side (spreadsheet-style).
          * @default 'stack'
         */
        "pinMode": 'stack' | 'static';
        /**
          * Total row count for assistive tech when only a subset is rendered (pagination / virtualization). 0 = use the rendered count.
          * @default 0
         */
        "rowCount": number;
        /**
          * Absolute 0-based index of the FIRST rendered body row within the full dataset (WAI/ARIA pagination): with `row-count` set, each row gets an `aria-rowindex` so AT reports "row 42 of 5000" instead of a position within the rendered page. Update it on every page change.
          * @default 0
         */
        "rowOffset": number;
        /**
          * Vertical scrollbar presentation (frozen mode): - `overlay` (default) — a custom always-visible thumb FLOATS over the   rows' right edge; no gutter is reserved, so row backgrounds and   dividers run the full width and the bar never takes layout space. - `gutter` — classic inset bar in a reserved gap: `scrollbar-gutter:   stable` keeps a fixed strip inside the table (rows stop short of the   edge) and the native bar renders in it, crossing the row borders.
          * @default 'overlay'
         */
        "scrollbar": 'overlay' | 'gutter';
        /**
          * Programmatically select all selectable body rows (no-op when selection="none").
         */
        "selectAll": () => Promise<void>;
        /**
          * Selection mode. - `none`     — no selection (default) - `single`   — radio-like: selecting a row DESELECTS every other row;                `selectAll()`/the select-all checkbox are inert (disabled) - `multiple` — checkbox-like; select-all checkbox + selectAll() available
          * @default 'none'
         */
        "selection": 'none' | 'single' | 'multiple';
        /**
          * Hide or show a column. `column` is the ORIGINAL 0-based index (same addressing as `pinColumn`). Hidden cells stay in the DOM (state survives — they just stop displaying) and their track leaves the template. The last visible column cannot be hidden. Resolves to the column's effective visibility.
         */
        "setColumnVisibility": (column: number, visible: boolean) => Promise<boolean>;
        /**
          * Set the active sort column / order programmatically.
         */
        "setSort": (column: string, order: MdTableSortOrder) => Promise<void>;
        /**
          * Currently active sort column id. Read by `<md-table-sort-label column="…">` to render its arrow. Empty = no sort.
          * @default ''
         */
        "sortBy": string;
        /**
          * Currently active sort direction.
          * @default 'asc'
         */
        "sortOrder": MdTableSortOrder;
        /**
          * Stick the foot row(s) to the bottom of the scroll container.
          * @default false
         */
        "stickyFooter": boolean;
        /**
          * Stick the head row(s) to the top of the scroll container.
          * @default false
         */
        "stickyHeader": boolean;
        /**
          * Show alternating row colors.
          * @default false
         */
        "striped": boolean;
        /**
          * Long description of a COMPLEX table's structure (WAI caption & summary pattern) — e.g. "Columns are grouped under Profile and Employment". Exposed as `aria-description` on the table; invisible otherwise.
          * @default ''
         */
        "summary": string;
        /**
          * Toggle the "all selected" state (used by the select-all checkbox).
         */
        "toggleSelectAll": () => Promise<void>;
    }
    /**
     * Material Design 3 — Table Body (`<tbody>`).
     * Pure presentational rowgroup. Children flow into the parent grid via
     * `display: contents`.
     */
    interface MdTableBody {
        /**
          * @default false
         */
        "presentational": boolean;
    }
    /**
     * Material Design 3 — Table Cell.
     * The visual surface for a single grid cell. Renders as either a `cell`
     * (default) or `columnheader` / `rowheader` based on `head` / `scope`.
     * Props are intentionally MUI-compatible:
     *   - `align="start" | "center" | "end" | "justify"`
     *   - `numeric` (right-aligns + tabular numbers)
     *   - `padding="none" | "checkbox" | "default"`
     *   - `head` (renders as `<th>` semantics with `scope="col"|"row"`)
     *   - `sticky="start" | "end"` (sticky first / last column)
     *   - `colspan` / `rowspan` (grid spans)
     * The cell is a dumb surface: row state (selected / disabled / header
     * context) arrives as inherited CSS custom properties cast by
     * `<md-table-row>` — no observers, no DOM queries.
     */
    interface MdTableCell {
        /**
          * Text alignment within the cell.
          * @default 'start'
         */
        "align": 'start' | 'center' | 'end' | 'justify';
        /**
          * Number of columns this cell spans (CSS Grid `grid-column: span N`).
          * @default 1
         */
        "colSpan": number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable text-wrap (single-line, ellipsis).
          * @default false
         */
        "ellipsis": boolean;
        /**
          * Render as a column / row header.
          * @default false
         */
        "head": boolean;
        /**
          * Suppress the automatic pin indicator that a sticky header cell shows. By default a frozen (sticky) column always advertises that it's pinned with a small pin icon in its header; set this to opt out (e.g. a sticky checkbox column — which is auto-skipped anyway).
          * @default false
         */
        "noPinIndicator": boolean;
        /**
          * Right-align + tabular figures (for monetary / numeric data).
          * @default false
         */
        "numeric": boolean;
        /**
          * Padding mode (MUI parity).
          * @default 'default'
         */
        "padding": 'default' | 'none' | 'checkbox';
        /**
          * Custom pin-indicator icon: a Material Symbols ligature (e.g. "anchor", "keep"). Empty = the built-in 45° push-pin SVG. Usually set ONCE on md-table via its `pin-icon` prop, which casts the value to every pinned header cell; setting it here directly overrides the cast.
          * @default ''
         */
        "pinIcon": string;
        /**
          * Number of rows this cell spans (CSS Grid `grid-row: span N`).
          * @default 1
         */
        "rowSpan": number;
        /**
          * Header scope (used when `head` is true).
          * @default ''
         */
        "scope": 'col' | 'row' | 'colgroup' | 'rowgroup' | '';
        /**
          * Sticky positioning — `start` for first column, `end` for last column.
          * @default ''
         */
        "sticky": 'start' | 'end' | '';
        /**
          * Optional content variant: `body` | `meta` (smaller, dimmer).
          * @default 'body'
         */
        "variant": 'body' | 'meta';
    }
    /**
     * Material Design 3 — Table Container
     * The outer surface that wraps a `<md-table>`, optional `<md-table-toolbar>`
     * and `<md-table-pagination>`. Provides elevation, shape, and the scroll
     * context (with optional max-height) for sticky headers / columns.
     * Compose freely:
     * ```html
     * <md-table-container variant="elevated" max-height="480px">
     *   <md-table-toolbar headline="Users"></md-table-toolbar>
     *   <md-table>…</md-table>
     *   <md-table-pagination></md-table-pagination>
     * </md-table-container>
     * ```
     */
    interface MdTableContainer {
        /**
          * Explicit MD3 elevation level (0–5), overriding the variant's default shadow. Handy to raise/flatten the surface without changing `variant` (e.g. `variant="outlined" elevation="2"`). Also settable via `--md-table-container-elevation`.
         */
        "elevation"?: 0 | 1 | 2 | 3 | 4 | 5;
        /**
          * If set, the table area becomes scrollable up to this max-height (e.g. `"480px"`, `"60vh"`). Required for sticky header / column.
          * @default ''
         */
        "maxHeight": string;
        /**
          * If set, the table area becomes scrollable up to this min-height (useful to keep an empty-state visible).
          * @default ''
         */
        "minHeight": string;
        /**
          * Container shape — large for dashboards, medium for dense, small for compact. Maps to MD3 `--md-sys-shape-corner-{size}` tokens.
          * @default 'large'
         */
        "shape": 'extra-large' | 'large' | 'medium' | 'small' | 'none';
        /**
          * Visual surface variant. - `elevated` — surface-container-low + box-shadow - `outlined` — surface + 1px outline - `filled`   — surface-container-low, no shadow / outline - `flat`     — transparent (use when nesting inside another surface)
          * @default 'elevated'
         */
        "variant": 'elevated' | 'outlined' | 'filled' | 'flat';
        /**
          * Vibrant tonality: tints the whole table with primary/tertiary container tones (coloured header, primary hover, tertiary selection, primary-tinted surface). Cascades to the contained md-table via CSS custom properties.
          * @default false
         */
        "vibrant": boolean;
    }
    /**
     * Material Design 3 — Table Expand Toggle.
     * The STANDARD expand caret for an expandable `<md-table-row>`: one consistent
     * size (xs / 32px — fits every density incl. compact rows, matching the 32px
     * checkbox precedent), a chevron that rotates 90° on the spatial spring when
     * the row opens, wired click→row.toggle() and aria-expanded out of the box.
     * Slot it INSIDE a cell of the expandable row (never as a bare row child —
     * rows are subgrid containers where any extra child would consume a column
     * track):
     * ```html
     * <md-table-row expandable>
     *   <md-table-cell padding="checkbox"><md-table-expand-toggle></md-table-expand-toggle></md-table-cell>
     *   <md-table-cell>…</md-table-cell>
     *   <div slot="expanded">Details…</div>
     * </md-table-row>
     * ```
     */
    interface MdTableExpandToggle {
        /**
          * Accessible label of the toggle button.
          * @default 'Expand row'
         */
        "buttonLabel": string;
        /**
          * Caret icon (Material Symbols ligature). The expanded rotation still applies (--md-table-expand-toggle-rotation, default 90deg) — pick a "collapsed-pointing" glyph like the default chevron_right, or pair e.g. "expand_circle_right" with a custom rotation.
          * @default 'chevron_right'
         */
        "icon": string;
    }
    /**
     * Material Design 3 — Table Foot (`<tfoot>`).
     * Pure presentational rowgroup for footer rows (totals, summaries).
     * Children flow into the parent grid via `display: contents`.
     */
    interface MdTableFoot {
        /**
          * @default false
         */
        "presentational": boolean;
    }
    /**
     * Material Design 3 — Table Head (`<thead>`).
     * Pure presentational rowgroup used to mark a region of column-header rows.
     * Applies the head background (sticky-friendly) and ensures children flow
     * into the parent `<md-table>`'s CSS Grid via `display: contents`.
     */
    interface MdTableHead {
    }
    /**
     * Material Design 3 — Table Pagination.
     * Drop into a `<md-table-container slot="bottom">` for a complete data table.
     * Mirrors the MUI `<TablePagination>` API surface. Built from house
     * primitives: `md-icon-button` navigation and an `md-select` rows-per-page
     * picker (proper dismissal, keyboard support and theming for free).
     * Emits:
     *   - `mdPageChange` — when the page changes
     *   - `mdRowsPerPageChange` — when the rows-per-page selector changes
     */
    interface MdTablePagination {
        /**
          * Compact pagination layout (good for embedding inside cards).
          * @default false
         */
        "compact": boolean;
        /**
          * Total number of rows in the dataset.
          * @default 0
         */
        "count": number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled state.
          * @default false
         */
        "disabled": boolean;
        /**
          * Programmatically jump to a page.
         */
        "goToPage": (page: number) => Promise<void>;
        /**
          * Label of the "All" rows-per-page option.
          * @default 'All'
         */
        "labelAll": string;
        /**
          * Custom label used to display the current row range (e.g. `"Showing %from%–%to% of %count%"`). Tokens: `%from%`, `%to%`, `%count%`.
          * @default '%from%–%to% of %count%'
         */
        "labelDisplayedRows": string;
        /**
          * Accessible label for the first-page button.
          * @default 'First page'
         */
        "labelFirstPage": string;
        /**
          * Accessible label for the last-page button.
          * @default 'Last page'
         */
        "labelLastPage": string;
        /**
          * Accessible label for the next-page button.
          * @default 'Next page'
         */
        "labelNextPage": string;
        /**
          * Accessible label for the previous-page button.
          * @default 'Previous page'
         */
        "labelPreviousPage": string;
        /**
          * Label for the rows-per-page selector.
          * @default 'Rows per page:'
         */
        "labelRowsPerPage": string;
        /**
          * Current zero-based page.
          * @default 0
         */
        "page": number;
        /**
          * Rows shown per page.
          * @default 10
         */
        "rowsPerPage": number;
        /**
          * Comma-separated list of options for the rows-per-page selector (e.g. `"5,10,25,50"`). Use `"all"` (case-insensitive) to add an "All" option that maps to the total count.
          * @default '5,10,25,50'
         */
        "rowsPerPageOptions": string;
        /**
          * Programmatically set rows-per-page.
         */
        "setRowsPerPage": (rpp: number) => Promise<void>;
        /**
          * Show the first / last page buttons.
          * @default false
         */
        "showFirstLast": boolean;
    }
    /**
     * Material Design 3 — Table Row.
     * A row inside a `<md-table>` that contains `<md-table-cell>` children.
     * The host is a REAL grid box that spans the table's tracks and lays its
     * cells on them via `grid-template-columns: subgrid` — so row hover,
     * stripes, dividers, selection tint, sticky rows and focus rings all work
     * on the row itself, and a wrong cell count skews one row, never the table.
     * The parent `<md-table>` stamps context (`rowgroup`, stripe parity,
     * stickiness) onto the row; the row casts typography/color context down to
     * its cells with `::slotted()` custom properties — no observers.
     * Behaviors:
     * - `selected` — selection state (visual + ARIA)
     * - `disabled` — non-interactive
     * - `clickable` — adds keyboard activation (Enter / Space) + emits `mdRowClick`
     * - `expandable` — toggles an "expanded details" zone (use the `expanded` slot),
     *   spring-animated via the grid-rows height pattern
     */
    interface MdTableRow {
        /**
          * When true, treat the entire row as a button (Enter/Space activates `mdRowClick`).
          * @default false
         */
        "clickable": boolean;
        /**
          * Disabled — row receives no hover/click handlers.
          * @default false
         */
        "disabled": boolean;
        /**
          * Render an "expandable details" zone toggleable via `toggle()`.
          * @default false
         */
        "expandable": boolean;
        /**
          * Whether the expandable zone is open.
          * @default false
         */
        "expanded": boolean;
        /**
          * Highlights the row using primary-container (e.g. for the focused row).
          * @default false
         */
        "highlight": boolean;
        /**
          * Which rowgroup this row belongs to. Stamped automatically by the parent `<md-table>` (head / body / foot) — set it manually only when using rows outside a table.
          * @default 'body'
         */
        "rowgroup": 'head' | 'body' | 'foot';
        /**
          * Whether this row counts as selectable (used by `<md-table>` selection coordination).
          * @default true
         */
        "selectable": boolean;
        /**
          * Selected (highlighted) state. Reflected.
          * @default false
         */
        "selected": boolean;
        /**
          * Toggle the expanded zone.
         */
        "toggle": () => Promise<void>;
        /**
          * Identifier attached to selection / click events. Defaults to row index.
          * @default ''
         */
        "value": string;
    }
    /**
     * Material Design 3 — Table Sort Label.
     * A clickable header label with a sort indicator that reflects the current
     * sort state. Place inside a `<md-table-cell head>`.
     * Wiring:
     *   1. Set `column="<id>"`
     *   2. The parent `<md-table>` listens for `mdSortRequest` (auto-bubbled),
     *      updates its `sort-by` / `sort-order`, and PUSHES the resulting
     *      `active` / `order` back into every sort label — no observers.
     * The arrow is hidden at rest and fades in at 60% opacity on hover or keyboard
     * focus, so an unsorted column advertises sorting only when it is pointed at.
     * Once the column is the sort key the arrow stays fully opaque, tinted with the
     * active colour, and rotates 180° to point down for descending. The rotation
     * runs on the spatial spring, the colour/opacity on the effects spring.
     */
    interface MdTableSortLabel {
        /**
          * Whether this column is the active sort. Pushed by the parent `<md-table>`.
          * @default false
         */
        "active": boolean;
        /**
          * Column id — matched against the `<md-table>`'s `sort-by` value. Required.
          * @default ''
         */
        "column": string;
        /**
          * Direction the arrow points by default (the value used when this column becomes the active sort for the first time).
          * @default 'asc'
         */
        "defaultOrder": 'asc' | 'desc';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled": boolean;
        /**
          * Custom sort-arrow icon: a Material Symbols ligature name (e.g. "north", "expand_less"). Empty = the built-in SVG arrow. The icon still rotates 180deg for descending via the same CSS, so pick an "ascending-pointing" glyph.
          * @default ''
         */
        "icon": string;
        /**
          * Visual position of the arrow icon relative to the label. - `end`   — after the label (default) - `start` — before the label
          * @default 'end'
         */
        "iconPosition": 'start' | 'end';
        /**
          * When the arrow is visible on a column that is NOT the active sort.  - `always` — dimmed but present at rest (default). The arrow is the only   thing distinguishing a sortable column from a fixed one, so hiding it   until hover means the affordance cannot be discovered without hovering   every header — and cannot be discovered at all on a touch device, which   has no hover state to give. - `hover`  — invisible until the header is hovered or focused. Quieter in a   table where nearly every column sorts and the arrows become noise.  The ACTIVE column's arrow is always fully opaque under either setting.
          * @default 'always'
         */
        "inactiveIcon": 'always' | 'hover';
        /**
          * Active sort direction. Pushed by the parent `<md-table>`.
          * @default 'none'
         */
        "order": 'asc' | 'desc' | 'none';
    }
    /**
     * Material Design 3 — Table Toolbar.
     * The header bar above a `<md-table>` (typically dropped into a
     * `<md-table-container slot="top">`). Shows a title + supporting text and
     * a row of action items. Switches into a "selection" mode when one or more
     * rows are selected (matching the MUI / MD3 data-table pattern).
     * Two modes:
     *   - Default — shows the headline + supporting text + actions
     *   - Selection — shows "%count% selected" + selection-actions (when
     *     `num-selected > 0`)
     * The toolbar can listen to its closest `<md-table>`'s `mdSelectionChange`
     * event automatically (via `auto-bind`) — set `auto-bind` on the toolbar
     * and selection state will track the table without any glue code.
     */
    interface MdTableToolbar {
        /**
          * Auto-bind to the closest `<md-table>`'s `mdSelectionChange` event so `numSelected` updates with no glue code.
          * @default false
         */
        "autoBind": boolean;
        /**
          * Compact density for the toolbar (smaller padding / type).
          * @default false
         */
        "compact": boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Title displayed when no rows are selected.
          * @default ''
         */
        "headline": string;
        /**
          * Custom label for the selection-mode caption. Tokens: `%count%`.
          * @default '%count% selected'
         */
        "labelSelected": string;
        /**
          * Number of selected rows (controlled).
          * @default 0
         */
        "numSelected": number;
        /**
          * Optional supporting text under the headline.
          * @default ''
         */
        "supportingText": string;
    }
    interface MdTabs {
        /**
          * Index of the currently active tab
          * @default 0
         */
        "activeTabIndex": number;
        /**
          * Accessible label for the tablist
          * @default ''
         */
        "ariaLabelProp": string;
        /**
          * How far the bottom rule under the strip runs.  - `auto` — each tab draws its own 1px segment, so the rule is exactly as   wide as the tabs collectively are (default; unchanged behaviour). - `full` — one rule across the whole container, drawn by md-tabs itself   and running past the last tab to the container's edge.  `auto` is right when the strip fills its container, which it does at `tab-width="equal"`. It reads as a bug at `tab-width="auto"` in a wide panel, where three content-sized tabs leave the rule stopping a third of the way across and the panel below appearing to hang off the end of it.  In `full` mode the per-tab segments are suppressed by setting `--md-tab-divider-color: transparent` on the host — custom properties inherit through the slotted tabs' shadow roots, so there is no second source of truth about which mode is active.
          * @default 'auto'
         */
        "divider": 'auto' | 'full';
        /**
          * Programmatically select a tab by index
         */
        "selectTab": (index: number) => Promise<void>;
        /**
          * How tabs size along the strip. All modes are layout-stable under dynamic content (labels/badges can change without reshuffling the strip); long labels ellipsize instead of forcing a tab wider. - `equal` (default) — tabs share the parent's FULL width equally;   tracks change only when the PARENT resizes. - `auto` — each tab takes its content width (the strip scrolls), so   tracks follow content by design. - any CSS length (e.g. `"140px"`, `"10rem"`) — every tab gets exactly   that width regardless of parent or content.
          * @default 'equal'
         */
        "tabWidth": string;
        /**
          * Visual variant applied to all child tabs
          * @default 'primary'
         */
        "variant": 'primary' | 'secondary';
        /**
          * Explicit width for the WHOLE strip (any CSS size: `"480px"`, `"40rem"`, `"100%"`). By default the strip is `100%` of its parent — in shrink-to-fit layouts (inline-block cards, auto grid columns, flex items) that parent's width follows the ACTIVE TAB PANEL, so switching to a tab with tiny content collapses the whole tablist. Set `width` to anchor the strip regardless of what the panels do.
          * @default ''
         */
        "width": string;
    }
    interface MdTextField {
        /**
          * Force the field to render in its focused visual state regardless of the underlying input's actual focus. Use when the field acts as an *anchor* for a popover, dialog, or menu, and the OS focus has moved into that surface — for example the `md-time-picker` keeps its trigger field in the focused look while the picker modal is open, so the trigger and the modal read as one connected control.  Visual-only: affects the notched-outline width / colour, the floating label position, and the leading-icon colour through md-text-field's existing `.md-text-field--focused` rules. Does NOT alter formatter / blur-format behaviour (those keep tracking real focus events), so `formatOn="blur"` still flips between formatted and raw on real focus/blur even while `appear-focused` is on.
          * @default false
         */
        "appearFocused": boolean;
        /**
          * @default ''
         */
        "autoCapitalize": string;
        /**
          * Input behavior passthrough (password managers / mobile keyboards).
          * @default ''
         */
        "autocomplete": string;
        /**
          * Constraint-validation API, delegated to the inner input.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * When leading `chips` are slotted, allow them to WRAP onto multiple rows so the field grows to fit them. Default `false`: chips stay on a single line and the field keeps its normal height (the consumer, e.g. md-multi-select, is responsible for handling overflow — clipping or a "+N" counter).
          * @default false
         */
        "chipsWrap": boolean;
        /**
          * Show a clear icon button. 'internal' clears the value directly; 'external' emits mdClear and leaves value control to the consumer.
          * @default false
         */
        "clearable": 'internal' | 'external' | false;
        /**
          * Debounce delay (ms) for the mdSearch event. Emits after the user stops typing for this duration. 0 = disabled.
          * @default 0
         */
        "debounce": number;
        /**
          * Density scale: 0 (default 56px), -1 (52px), -2 (48px), -3 (44px).
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled": boolean;
        /**
          * @default ''
         */
        "enterKeyHint": string;
        /**
          * @default false
         */
        "error": boolean;
        /**
          * @default ''
         */
        "errorText": string;
        /**
          * Border width when focused: 1, 2, or 3 pixels.
          * @default 3
         */
        "focusBorderWidth": number;
        /**
          * When to apply the formatter. - 'blur': format on blur, show raw value on focus (default) - 'input': format on every keystroke (live formatting)
          * @default 'blur'
         */
        "formatOn": 'blur' | 'input';
        /**
          * Formats the raw value for display. Set via JS: `el.formatter = (v) => Number(v).toLocaleString()`.
         */
        "formatter"?: (value: string) => string;
        /**
          * The real `<input>` / `<textarea>` this field renders.  A composite that owns a popup (md-autocomplete, the date/time pickers) needs the actual textbox to wire combobox ARIA onto — its role, its expanded state, and the element references that point at a listbox living in the PARENT's shadow root. Handing it over explicitly replaces the shadow-piercing `querySelector('.md-text-field__input')` those components were doing against a private class name.  Returns null before the first render.
         */
        "getInputElement": () => Promise<HTMLInputElement | HTMLTextAreaElement | null>;
        /**
          * Native-parity validity readback: flags + message + willValidate.
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; flags: Record<string, boolean>; }>;
        /**
          * `aria-autocomplete` for the combobox — how typing relates to the popup.
          * @default ''
         */
        "inputAriaAutocomplete": '' | 'none' | 'inline' | 'list' | 'both';
        /**
          * Expanded state of the owner's popup. Emitted only with `input-role="combobox"`, where `aria-expanded` is an allowed attribute (it is not on a bare textbox).
          * @default false
         */
        "inputExpanded": boolean;
        /**
          * @default ''
         */
        "inputMode": string;
        /**
          * Promote the inner input to a WAI-ARIA **combobox**, for a composite that owns the popup (md-autocomplete). The role has to live on the real textbox — the one AT reads when it has focus — and this field renders it, so the owner cannot put it there itself.  Whitelisted to `combobox`: it is the only role an owner has a legitimate reason to place on a text input, and an open-ended role prop here would let a consumer lie about the control.
          * @default ''
         */
        "inputRole": '' | 'combobox';
        /**
          * @default ''
         */
        "label": string;
        "max": string | number | undefined;
        "maxLength": number | undefined;
        "min": string | number | undefined;
        "minLength": number | undefined;
        /**
          * Multi-line mode. - false: single-line <input> (default) - 'auto-grow': <textarea> that expands as content grows (multi-line field) - 'fixed': <textarea> with fixed height that scrolls (text area)
          * @default false
         */
        "multiline": 'auto-grow' | 'fixed' | false;
        /**
          * Form field name (host attribute — ElementInternals submits under it).
          * @default ''
         */
        "name": string;
        /**
          * Parses the display value back to a raw value. Set via JS: `el.parser = (v) => v.replace(/[^0-9.-]/g, '')`.
         */
        "parser"?: (displayValue: string) => string;
        /**
          * Show a password visibility toggle. 'internal' toggles the input type directly; 'external' emits mdPasswordToggle and leaves type control to the consumer.
          * @default false
         */
        "passwordToggle": 'internal' | 'external' | false;
        /**
          * Constraint validation (forwarded to the inner input; surfaced via ElementInternals).
          * @default ''
         */
        "pattern": string;
        /**
          * @default ''
         */
        "placeholder": string;
        /**
          * @default ''
         */
        "prefixText": string;
        /**
          * @default false
         */
        "readOnly": boolean;
        "reportValidity": () => Promise<boolean>;
        /**
          * @default false
         */
        "required": boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message.  The row is otherwise rendered only when there is something to show, so the first validation error CREATES a line and shoves everything below it down — the whole form jumps as you tab through it. Reserving the space is opt-in because a field with no message would otherwise carry an empty line everywhere, including dense and inline layouts. createFormController turns it on automatically for the fields it manages, since those are exactly the ones whose messages appear and disappear.
          * @default false
         */
        "reserveSupportingSpace": boolean;
        /**
          * Restrict input to allowed characters. Presets: 'numeric', 'integer', 'decimal', 'alpha', 'alphanumeric'. Custom: a regex character class string, e.g. '[0-9+() -]'.
          * @default ''
         */
        "restrict": string;
        /**
          * Number of visible text rows for multiline modes. Defaults to 2 for auto-grow, 4 for fixed.
         */
        "rows": number | undefined;
        /**
          * Select the input's full contents.
         */
        "select": () => Promise<void>;
        /**
          * App/server-side validation: a non-empty message marks the field invalid for its form until cleared with an empty string.
         */
        "setCustomValidity": (message: string) => Promise<void>;
        /**
          * Focus the inner input (host.focus() also works via delegatesFocus).
         */
        "setFocus": () => Promise<void>;
        /**
          * Language/locale for internal speech recognition (e.g. 'en-US', 'de-DE'). Defaults to browser locale.
          * @default ''
         */
        "speechLang": string;
        /**
          * Speech-to-text via Web Speech API. - 'internal': manages recognition internally, appends transcript to value - 'external': emits mdSpeechResult on mic click, letting the consumer handle recognition
          * @default false
         */
        "speechToText": 'internal' | 'external' | false;
        "spellcheck": boolean | undefined;
        "step": string | number | undefined;
        /**
          * @default ''
         */
        "suffixText": string;
        /**
          * @default ''
         */
        "supportingText": string;
        /**
          * Throttle interval (ms) for the mdSearch event. Emits at most once per interval while typing. 0 = disabled.
          * @default 0
         */
        "throttle": number;
        /**
          * @default 'text'
         */
        "type": string;
        /**
          * NOT reflected: typed values (incl. passwords) must never appear as a DOM attribute.
          * @default ''
         */
        "value": string;
        /**
          * @default 'filled'
         */
        "variant": 'filled' | 'outlined';
    }
    interface MdTimePicker {
        /**
          * Localized AM label (12-hour mode).
          * @default 'AM'
         */
        "amLabel": string;
        /**
          * Localized label for the Cancel action.
          * @default 'Cancel'
         */
        "cancelLabel": string;
        /**
          * Constraint-validation parity with native form controls: true when the committed value satisfies `required` / `min` / `max`.
         */
        "checkValidity": () => Promise<boolean>;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the trigger and prevents the dialog from opening.
          * @default false
         */
        "disabled": boolean;
        /**
          * 12-hour (with AM/PM) or 24-hour clock.
          * @default '12h'
         */
        "format": MdTimePickerFormat;
        /**
          * Snapshot of the host's ValidityState + message (ElementInternals).
         */
        "getValidity": () => Promise<{ valid: boolean; validationMessage: string; } & Partial<ValidityState>>;
        /**
          * Headline copy shown above the dial / inputs. Overrides the mode-specific defaults (`headline-input-label` / `headline-dial-label`) when set.
          * @default ''
         */
        "headline": string;
        /**
          * Localized default headline for the dial variant (used when `headline` is empty).
          * @default 'Select time'
         */
        "headlineDialLabel": string;
        /**
          * Localized default headline for the input variant (used when `headline` is empty).
          * @default 'Enter time'
         */
        "headlineInputLabel": string;
        /**
          * Programmatically close the picker dialog (treated as Cancel).
         */
        "hide": () => Promise<void>;
        /**
          * When true the built-in field trigger is not rendered; open the dialog programmatically via `.show()`.
          * @default false
         */
        "hideTrigger": boolean;
        /**
          * Localized accessible label for the Hour input field. Matches the [MD3 Time picker accessibility spec](https://m3.material.io/components/time-pickers/accessibility) which mandates an "Hour" label on the hour text input.
          * @default 'Hour'
         */
        "hourLabel": string;
        /**
          * Trigger field label (acts as the floating label of the text-field-shaped trigger).
          * @default 'Select time'
         */
        "label": string;
        /**
          * Latest selectable time as a 24-hour `HH:MM` string. A committed value after it fails constraint validation with `rangeOverflow`.
          * @default ''
         */
        "max": string;
        /**
          * Earliest selectable time as a 24-hour `HH:MM` string. A committed value before it fails constraint validation with `rangeUnderflow`.
          * @default ''
         */
        "min": string;
        /**
          * Localized accessible label for the Minute input field. Matches the [MD3 Time picker accessibility spec](https://m3.material.io/components/time-pickers/accessibility) which mandates a "Minute" label on the minute text input.
          * @default 'Minute'
         */
        "minuteLabel": string;
        /**
          * Minute snap increment on the dial (1, 5, 10, 15, 30…). Inputs always accept 1-minute granularity.
          * @default 1
         */
        "minuteStep": number;
        /**
          * Form field name — the picker is form-associated and submits its `value` under this name in `FormData`, like a native `<input type="time">`.
          * @default ''
         */
        "name": string;
        /**
          * Localized label for the confirm action.
          * @default 'OK'
         */
        "okLabel": string;
        /**
          * Whether the picker dialog is open. Two-way bindable / reflected.
          * @default false
         */
        "open": boolean;
        /**
          * Dialog orientation for the dial variant.  - `vertical` (default) — single-column portrait layout (~328px wide). - `horizontal` — two-column landscape layout (~572px wide in both   12h and 24h modes since the HH/MM input tiles are 96px in either   format). Forces the AM/PM pill into the 216×38px horizontal   variant since the 52×72px vertical pill cannot share the left   column cleanly. Has no effect when `variant="input"`.
          * @default 'vertical'
         */
        "orientation": MdTimePickerOrientation;
        /**
          * Localized accessible label for the AM/PM radio group container. Wraps the two radio children; individual radio names come from `am-label` / `pm-label`.
          * @default 'Period'
         */
        "periodLabel": string;
        /**
          * Layout for the AM/PM period selector. Has no effect when `format="24h"` (the toggle is not rendered).  - `vertical` — 52×72px stack next to the minute tile (default). - `horizontal` — 216×38px segmented button below HH:MM.
          * @default 'vertical'
         */
        "periodLayout": MdTimePickerPeriodLayout;
        /**
          * Localized PM label (12-hour mode).
          * @default 'PM'
         */
        "pmLabel": string;
        /**
          * Localized validation message for a reversed (overnight) `min`/`max` window when the time falls in the excluded middle. `{min}` and `{max}` are substituted.
          * @default 'Please select a time at or after {min} or at or before {max}.'
         */
        "rangeOutsideLabel": string;
        /**
          * Localized validation message when the committed time is after `max`. `{max}` is substituted.
          * @default 'Please select a time at or before {max}.'
         */
        "rangeOverflowLabel": string;
        /**
          * Localized validation message when the committed time is before `min`. `{min}` is substituted.
          * @default 'Please select a time at or after {min}.'
         */
        "rangeUnderflowLabel": string;
        /**
          * Like `checkValidity()` but also surfaces the browser's validation UI anchored on the trigger field (matches md-text-field).
         */
        "reportValidity": () => Promise<boolean>;
        /**
          * Requires a committed value for the host form to validate (`valueMissing` otherwise).
          * @default false
         */
        "required": boolean;
        /**
          * Adaptive layout policy. Per the MD3 spec the picker should swap orientation or variant based on the viewport so the dial never has to scroll. When `responsive` is enabled the dialog automatically:    • Falls back to `variant="input"` when the viewport height is     too short to fit the 256px dial (default threshold: 460px),     matching the spec line "Time pickers can fallback to the     input time picker when there isn't enough vertical real     estate to present the landscape orientation without     scrolling".   • Promotes the dial variant to `orientation="horizontal"`     when the viewport is wide enough for the 572/608px landscape     dialog (default threshold: width ≥ 720px AND width > height),     matching "the time picker can change to landscape orientation     on larger breakpoints or when viewport height is limited".  The `variant` / `orientation` props remain the *preference* the picker tries to honor — adaptive overrides only kick in when the viewport literally cannot fit them. Default `false` for backward compatibility; flip to `true` on new integrations to get the canonical MD3 adaptive layout.
          * @default false
         */
        "responsive": boolean;
        "show": () => Promise<void>;
        /**
          * Localized accessible label for the mode-toggle icon button when the picker is currently in the *input* variant (clock icon shown — click to switch to the dial). Matches the MD3 accessibility spec entry "Clock button → Toggle dial picker".
          * @default 'Toggle dial picker'
         */
        "toggleDialLabel": string;
        /**
          * Localized accessible label for the mode-toggle icon button when the picker is currently in the *dial* variant (keyboard icon shown — click to switch back to the input). Symmetric extension of the MD3 "Toggle dial picker" naming pattern, since the spec only documents the input-mode label explicitly.
          * @default 'Toggle keyboard input'
         */
        "toggleInputLabel": string;
        /**
          * Selected time as a 24-hour `HH:MM` string (e.g. `"14:30"`).
          * @default ''
         */
        "value": string;
        /**
          * Localized constraint-validation message when `required` and no time is committed.
          * @default 'Please select a time.'
         */
        "valueMissingLabel": string;
        /**
          * Picker mode shown when the dialog first opens. Defaults to `input` (keyboard-first); users can toggle to `dial` from the footer icon.
          * @default 'input'
         */
        "variant": MdTimePickerMode;
    }
    /**
     * The toolbar host has no built-in actions — all behavior comes from slotted controls.
     * On the web, the default is `role="toolbar"` implementing the WAI-ARIA toolbar pattern
     * with an ADAPTIVE roving tabindex:
     *  - **Activator buttons** (`md-icon-button`, `md-button`, native `button`/`a[href]`,
     *    `role=button`/`menuitem`/`link`) form a single roving group — one Tab stop, and
     *    ArrowLeft/Right (or Up/Down when floating+vertical), Home, End move focus between them.
     *  - **Everything else** is left as its OWN Tab stop with its native behavior — text-entry /
     *    value controls (input, textarea, contenteditable, md-text-field, md-select, `select`,
     *    range/spinbutton), ALL toggles (`md-switch`/`md-checkbox`/`md-chip`, native
     *    checkbox/radio, role=checkbox/radio), and self-managing composites (md-button-group,
     *    md-menu, role=group/radiogroup/listbox/menu). Arrows are never stolen from them, and
     *    they never become roving dead-ends.
     * A soft-disabled (`aria-disabled`) group member stays arrow-reachable (APG: disabled
     * items remain focusable for discoverability). The default Tab stop prefers an enabled
     * control, falling back to the first member only when every member is soft-disabled;
     * focusing any member makes it the stop.
     * This keeps every control keyboard-reachable regardless of the mix, per APG guidance for
     * toolbars that contain text fields. Use `generic` for a non-semantic container (no role,
     * no roving).
     */
    interface MdToolbar {
        /**
          * Alignment of items within the container
          * @default 'start'
         */
        "alignment": 'start' | 'center' | 'end' | 'space-between';
        /**
          * Accessible name for the toolbar (`containerSemantics="toolbar"`). Provide a meaningful label describing the toolbar's purpose (e.g. "Text formatting"); prefer `aria-labelledby` when a visible label exists. When neither is set the toolbar is left unnamed (valid ARIA) and a dev-mode warning is logged.
          * @default ''
         */
        "ariaLabelProp": string;
        /**
          * ID(s) of visible label element(s); takes precedence over `aria-label` for the accessible name
          * @default ''
         */
        "ariaLabelledby": string;
        /**
          * Toolbar color scheme (bar chrome). Also applies the MD3 expressive **toolbar-only** token map to slotted `md-icon-button` — does not change standalone icon buttons elsewhere.
          * @default 'standard'
         */
        "color": 'standard' | 'vibrant';
        /**
          * `toolbar` (default): `role="toolbar"`, adaptive roving tabindex, and toolbar ARIA on the host for web. `generic`: no toolbar role — plain container, no roving/arrow navigation. A generic container cannot be named (ARIA name-prohibited): an author-set `aria-label`/ `aria-labelledby` is left in place but ignored by assistive tech, and a dev-mode warning is logged.
          * @default 'toolbar'
         */
        "containerSemantics": 'toolbar' | 'generic';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Layout direction for floating toolbar
          * @default 'horizontal'
         */
        "layout": 'horizontal' | 'vertical';
        /**
          * Focus the toolbar's active roving control. No-op when the roving group is empty.
         */
        "setFocus": () => Promise<void>;
        /**
          * Toolbar type: docked (anchored to bottom) or floating (free-placed)
          * @default 'docked'
         */
        "variant": 'docked' | 'floating';
    }
    interface MdTooltip {
        /**
          * Automatically reposition if overflowing the viewport
          * @default true
         */
        "autoPosition": boolean;
        /**
          * Cross-axis offset in pixels
          * @default 0
         */
        "crossOffset": number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * When true, hover/focus handlers are ignored and any open tooltip is dismissed.
          * @default false
         */
        "disabled": boolean;
        /**
          * Dismiss the tooltip programmatically.
         */
        "hide": () => Promise<void>;
        /**
          * Delay before hiding (ms) — for rich variant hover-out grace period
          * @default 200
         */
        "hideDelay": number;
        /**
          * Distance from the trigger in pixels
          * @default 8
         */
        "offset": number;
        /**
          * Programmatic open state
          * @default false
         */
        "open": boolean;
        /**
          * Preferred placement relative to the trigger
          * @default 'top'
         */
        "position": Placement;
        /**
          * Show the tooltip programmatically (no-op while `disabled`).
         */
        "show": () => Promise<void>;
        /**
          * Delay before showing (ms)
          * @default 500
         */
        "showDelay": number;
        /**
          * Rich tooltip subhead (headline)
          * @default ''
         */
        "subhead": string;
        /**
          * Plain tooltip text, or rich tooltip supporting text
          * @default ''
         */
        "text": string;
        /**
          * Visual variant
          * @default 'plain'
         */
        "variant": 'plain' | 'rich';
    }
    /**
     * `md-transfer-list` — Material Design 3 dual-list selector.
     * Reference parity: https://mui.com/material-ui/react-transfer-list/.
     * Two columns ("source" and "target") with header chips that show the
     * "{n} / {total} selected" count, a search field per side, paired
     * `md-checkbox` rows, and four `md-icon-button` movers in the centre
     * column. Selection inside each side is independent of which side a
     * card lives in; the move buttons only act on items that are both
     * selected AND visible (i.e. matching the search filter).
     * MD3 Expressive: containers are `surface-container-low`, the headers
     * use the `secondary-container` tone for emphasis when the title slot
     * is populated, and movements run on the `motion-emphasized` curve.
     */
    interface MdTransferList {
        /**
          * Template for the header count pill (localisable). `{checked}` and `{total}` are replaced with the live numbers.
          * @default '{checked}/{total} selected'
         */
        "countTemplate": string;
        /**
          * Density scale: 0 (default), -1, -2, -3, -4. Passed to the search text field.
          * @default 0
         */
        "density": 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the entire control.
          * @default false
         */
        "disabled": boolean;
        /**
          * Optional Material Symbols glyph shown above the empty-state text.
          * @default ''
         */
        "emptyIcon": string;
        /**
          * Empty-state text (localisable).
          * @default 'No items'
         */
        "emptyText": string;
        /**
          * The full item pool — both columns are derived from it.  Accepts the array (property) or a **JSON string** (attribute), so a plain-HTML page can declare its data inline instead of reaching for a script. Malformed JSON degrades to an empty list with a console warning.
          * @default []
         */
        "items": MdTransferListItem[] | string;
        /**
          * Material Symbols glyph for the "move all to source" (<<) button.
          * @default 'keyboard_double_arrow_left'
         */
        "moveAllLeftIcon": string;
        /**
          * Accessible label for the << button (localisable).
          * @default 'Move all to source'
         */
        "moveAllLeftLabel": string;
        /**
          * Material Symbols glyph for the "move all to target" (>>) button.
          * @default 'keyboard_double_arrow_right'
         */
        "moveAllRightIcon": string;
        /**
          * Accessible label for the >> button (localisable).
          * @default 'Move all to target'
         */
        "moveAllRightLabel": string;
        /**
          * Material Symbols glyph for the "move selected to source" (<) button.
          * @default 'chevron_left'
         */
        "moveLeftIcon": string;
        /**
          * Accessible label for the < button (localisable).
          * @default 'Move selected to source'
         */
        "moveLeftLabel": string;
        /**
          * Material Symbols glyph for the "move selected to target" (>) button.
          * @default 'chevron_right'
         */
        "moveRightIcon": string;
        /**
          * Accessible label for the > button (localisable). NB: deliberately NOT an `aria-*`-prefixed attribute — a custom attribute in the aria namespace is invalid ARIA (axe aria-valid-attr).
          * @default 'Move selected to target'
         */
        "moveRightLabel": string;
        /**
          * Move selected items to the source side.
         */
        "moveSelectedLeft": () => Promise<void>;
        /**
          * Move selected items to the target side.
         */
        "moveSelectedRight": () => Promise<void>;
        /**
          * Material Symbols glyph for the search fields' leading icon ('' = none).
          * @default 'search'
         */
        "searchIcon": string;
        /**
          * Render search inputs above each list.
          * @default true
         */
        "searchable": boolean;
        /**
          * Show the "select all" select-all checkbox in each header.
          * @default true
         */
        "showSelectAll": boolean;
        /**
          * Show only the >/< buttons (drop >>/<< "move all").
          * @default false
         */
        "singleStepOnly": boolean;
        /**
          * Source-side search placeholder.
          * @default 'Search choices'
         */
        "sourceSearchPlaceholder": string;
        /**
          * Title for the source side.
          * @default 'Choices'
         */
        "sourceTitle": string;
        /**
          * Target-side search placeholder.
          * @default 'Search chosen'
         */
        "targetSearchPlaceholder": string;
        /**
          * Title for the target side.
          * @default 'Chosen'
         */
        "targetTitle": string;
        /**
          * Values currently in the *target* (right) column.
          * @default []
         */
        "value": string[];
    }
}
export interface MdAccordionCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdAccordionElement;
}
export interface MdAccordionItemCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdAccordionItemElement;
}
export interface MdAppBarCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdAppBarElement;
}
export interface MdAreaChartCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdAreaChartElement;
}
export interface MdAutocompleteCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdAutocompleteElement;
}
export interface MdAvatarCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdAvatarElement;
}
export interface MdBarChartCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdBarChartElement;
}
export interface MdBottomSheetCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdBottomSheetElement;
}
export interface MdBreadcrumbItemCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdBreadcrumbItemElement;
}
export interface MdBreadcrumbsCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdBreadcrumbsElement;
}
export interface MdButtonCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdButtonElement;
}
export interface MdButtonGroupCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdButtonGroupElement;
}
export interface MdCardCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdCardElement;
}
export interface MdCheckboxCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdCheckboxElement;
}
export interface MdChipCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdChipElement;
}
export interface MdColorPickerCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdColorPickerElement;
}
export interface MdDatePickerCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdDatePickerElement;
}
export interface MdDialogCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdDialogElement;
}
export interface MdFabCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdFabElement;
}
export interface MdFabMenuCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdFabMenuElement;
}
export interface MdFabMenuItemCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdFabMenuItemElement;
}
export interface MdIconButtonCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdIconButtonElement;
}
export interface MdLineChartCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdLineChartElement;
}
export interface MdListCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdListElement;
}
export interface MdListItemCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdListItemElement;
}
export interface MdMenuCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdMenuElement;
}
export interface MdMenuItemCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdMenuItemElement;
}
export interface MdMultiSelectCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdMultiSelectElement;
}
export interface MdNavigationBarCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdNavigationBarElement;
}
export interface MdNavigationRailCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdNavigationRailElement;
}
export interface MdNavigationRailTabCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdNavigationRailTabElement;
}
export interface MdNavigationTabCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdNavigationTabElement;
}
export interface MdNumberFieldCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdNumberFieldElement;
}
export interface MdOrganizationChartCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdOrganizationChartElement;
}
export interface MdOtpFieldCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdOtpFieldElement;
}
export interface MdPieChartCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdPieChartElement;
}
export interface MdProgressIndicatorCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdProgressIndicatorElement;
}
export interface MdRadioCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdRadioElement;
}
export interface MdRatingCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdRatingElement;
}
export interface MdSearchCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSearchElement;
}
export interface MdSegmentedButtonCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSegmentedButtonElement;
}
export interface MdSegmentedButtonSetCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSegmentedButtonSetElement;
}
export interface MdSelectCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSelectElement;
}
export interface MdSelectOptionCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSelectOptionElement;
}
export interface MdSideSheetCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSideSheetElement;
}
export interface MdSliderCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSliderElement;
}
export interface MdSnackbarCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSnackbarElement;
}
export interface MdSparklineCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSparklineElement;
}
export interface MdSplitButtonCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSplitButtonElement;
}
export interface MdStepCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdStepElement;
}
export interface MdStepperCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdStepperElement;
}
export interface MdSubMenuItemCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSubMenuItemElement;
}
export interface MdSwitchCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdSwitchElement;
}
export interface MdTabCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTabElement;
}
export interface MdTableCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTableElement;
}
export interface MdTablePaginationCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTablePaginationElement;
}
export interface MdTableRowCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTableRowElement;
}
export interface MdTableSortLabelCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTableSortLabelElement;
}
export interface MdTabsCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTabsElement;
}
export interface MdTextFieldCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTextFieldElement;
}
export interface MdTimePickerCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTimePickerElement;
}
export interface MdTooltipCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTooltipElement;
}
export interface MdTransferListCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLMdTransferListElement;
}
declare global {
    interface HTMLMdAccordionElementEventMap {
        "mdToggle": {
    index: number;
    expanded: boolean;
    expandedIndices: number[];
  };
        "mdReorder": {
    from: number;
    to: number;
    order: number[];
  };
        "mdDragStart": MdAccordionDragDetail;
        "mdDragMove": MdAccordionDragDetail;
        "mdDragEnd": MdAccordionDragDetail;
    }
    interface HTMLMdAccordionElement extends Components.MdAccordion, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdAccordionElementEventMap>(type: K, listener: (this: HTMLMdAccordionElement, ev: MdAccordionCustomEvent<HTMLMdAccordionElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdAccordionElementEventMap>(type: K, listener: (this: HTMLMdAccordionElement, ev: MdAccordionCustomEvent<HTMLMdAccordionElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdAccordionElement: {
        prototype: HTMLMdAccordionElement;
        new (): HTMLMdAccordionElement;
    };
    interface HTMLMdAccordionItemElementEventMap {
        "mdItemToggle": { expanded: boolean; index: number };
        "mdItemRequestFocus": {
    direction: 'next' | 'prev' | 'first' | 'last';
    from: number;
  };
        "mdItemRequestReorder": {
    direction: 'up' | 'down';
    from: number;
  };
    }
    interface HTMLMdAccordionItemElement extends Components.MdAccordionItem, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdAccordionItemElementEventMap>(type: K, listener: (this: HTMLMdAccordionItemElement, ev: MdAccordionItemCustomEvent<HTMLMdAccordionItemElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdAccordionItemElementEventMap>(type: K, listener: (this: HTMLMdAccordionItemElement, ev: MdAccordionItemCustomEvent<HTMLMdAccordionItemElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdAccordionItemElement: {
        prototype: HTMLMdAccordionItemElement;
        new (): HTMLMdAccordionItemElement;
    };
    interface HTMLMdAppBarElementEventMap {
        "mdLeadingClick": MouseEvent;
        "mdSearchActivate": { value: string };
        "mdSearchInput": { value: string };
    }
    /**
     * `md-app-bar` — Material Design 3 App Bar.
     * Implements the current (M3 Expressive) app-bars specification:
     *   https://m3.material.io/components/app-bars/specs
     * Size variants: `small` (64px), `medium` (112/136px expanded), `large`
     * (120/152px expanded), and `search` (inline search field in the 64px row).
     * Flexible `medium` / `large` bars always render the expanded headline block.
     * Title alignment (`title-alignment`) is configuration, not a variant.
     * Selecting the search field should open an `md-search` view via `mdSearchActivate`.
     */
    interface HTMLMdAppBarElement extends Components.MdAppBar, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdAppBarElementEventMap>(type: K, listener: (this: HTMLMdAppBarElement, ev: MdAppBarCustomEvent<HTMLMdAppBarElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdAppBarElementEventMap>(type: K, listener: (this: HTMLMdAppBarElement, ev: MdAppBarCustomEvent<HTMLMdAppBarElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdAppBarElement: {
        prototype: HTMLMdAppBarElement;
        new (): HTMLMdAppBarElement;
    };
    interface HTMLMdAreaChartElementEventMap {
        "mdMarkerClick": MdChartClickDetail<MdChartSeries>;
        "mdLineClick": MdChartClickDetail<MdChartSeries>;
        "mdAreaClick": MdChartClickDetail<MdChartSeries>;
        "mdAxisClick": MdChartAxisClickDetail;
        "mdLegendClick": { seriesIndex: number; seriesId?: string; selected: boolean };
        "mdHover": MdChartHoverDetail;
        "mdReady": void;
        "mdZoom": { startIndex: number; endIndex: number; reset: boolean };
    }
    /**
     * md-area-chart — Material Design 3 area / streamgraph chart,
     * rendered by the in-house engine (Canvas2D + a DOM text
     * overlay). No ECharts.
     * Differences from `md-line-chart`:
     *   • the line is always filled with a gradient
     *   • stacking defaults to `'normal'` (areas stack additively)
     *   • supports the `silhouette` streamgraph baseline
     *   • markers are off by default
     */
    interface HTMLMdAreaChartElement extends Components.MdAreaChart, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdAreaChartElementEventMap>(type: K, listener: (this: HTMLMdAreaChartElement, ev: MdAreaChartCustomEvent<HTMLMdAreaChartElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdAreaChartElementEventMap>(type: K, listener: (this: HTMLMdAreaChartElement, ev: MdAreaChartCustomEvent<HTMLMdAreaChartElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdAreaChartElement: {
        prototype: HTMLMdAreaChartElement;
        new (): HTMLMdAreaChartElement;
    };
    interface HTMLMdAutocompleteElementEventMap {
        "mdChange": string | string[];
        "mdInput": string;
        "mdOpen": void;
        "mdClose": void;
        "mdClear": void;
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    /**
     * `md-autocomplete` — Material Design 3 combobox: a text field that filters
     * a dropdown listbox **as you type in the input** (no separate search row),
     * with single or multiple (chips) selection.
     * Built from the same primitives as `md-select` / `md-multi-select`:
     *   - the shared option model (`options` array of `{ value, label,
     *     supportingText?, icon?, iconColor?, disabled? }` or slotted
     *     `<md-select-option>` children),
     *   - `md-text-field` for the trigger (typing filters live),
     *   - `md-menu` as a WAI-ARIA listbox popup (options carry `aria-selected`),
     *   - `VirtualSelectController` for WASM-virtualized huge datasets
     *     (`virtualize`, `loadOptions()`, `filter-mode`) — the same engine as
     *     the selects, so tens of thousands of rows filter smoothly.
     * Keyboard: typing filters; ArrowDown moves focus into the listbox (the
     * focus-moves-into-popup combobox variant — option focus is announced
     * directly); Enter selects; Escape closes back to the input; Backspace on an
     * empty multi input removes the last chip.
     * ARIA: the real textbox — the `<input>` inside md-text-field's shadow root —
     * is promoted to `role="combobox"` with `aria-expanded` / `aria-autocomplete`
     * (handed over via `input-role`, since only that component renders the input),
     * and points at this component's listbox through ARIA element reflection. An
     * IDREF could not: it would have to resolve inside md-text-field's tree, where
     * the listbox is not. See `syncComboboxRefs`.
     * Extras kept from the surface this API mirrors: `free-solo` (commit arbitrary text),
     * `max-selected`, `clearable`, `disable-close-on-select`, `clear-on-blur`,
     * a custom `filterer` and `limit-results` (client-side path only).
     */
    interface HTMLMdAutocompleteElement extends Components.MdAutocomplete, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdAutocompleteElementEventMap>(type: K, listener: (this: HTMLMdAutocompleteElement, ev: MdAutocompleteCustomEvent<HTMLMdAutocompleteElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdAutocompleteElementEventMap>(type: K, listener: (this: HTMLMdAutocompleteElement, ev: MdAutocompleteCustomEvent<HTMLMdAutocompleteElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdAutocompleteElement: {
        prototype: HTMLMdAutocompleteElement;
        new (): HTMLMdAutocompleteElement;
    };
    interface HTMLMdAvatarElementEventMap {
        "mdLoad": { src: string };
        "mdError": { src: string };
    }
    /**
     * `md-avatar` — a presentational identity tile that resolves the first
     * available source from a clear fallback chain:
     *   1. `src` (image)  → `<img>` is rendered.
     *      On error/empty, automatically falls through to step 2.
     *   2. `initials`     → explicit initials string (verbatim, max 3 chars).
     *   3. `name`         → auto-derived initials. Two tokens → first letter
     *                       of each; one token → first letter only. RTL- and
     *                       diacritic-safe via `Intl.Segmenter` when available.
     *   4. default slot   → caller-provided fallback (icon, emoji, SVG).
     *   5. person icon    → material-symbols fallback (always available).
     */
    interface HTMLMdAvatarElement extends Components.MdAvatar, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdAvatarElementEventMap>(type: K, listener: (this: HTMLMdAvatarElement, ev: MdAvatarCustomEvent<HTMLMdAvatarElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdAvatarElementEventMap>(type: K, listener: (this: HTMLMdAvatarElement, ev: MdAvatarCustomEvent<HTMLMdAvatarElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdAvatarElement: {
        prototype: HTMLMdAvatarElement;
        new (): HTMLMdAvatarElement;
    };
    interface HTMLMdBadgeElement extends Components.MdBadge, HTMLStencilElement {
    }
    var HTMLMdBadgeElement: {
        prototype: HTMLMdBadgeElement;
        new (): HTMLMdBadgeElement;
    };
    interface HTMLMdBarChartElementEventMap {
        "mdBarClick": MdChartClickDetail<MdChartSeries>;
        "mdLegendClick": { seriesIndex: number; seriesId?: string; selected: boolean };
        "mdHover": MdChartHoverDetail;
        "mdReady": void;
        "mdZoom": { startIndex: number; endIndex: number; reset: boolean };
    }
    /**
     * md-bar-chart — Material Design 3 Expressive bar chart, rendered
     * by the in-house engine (Canvas2D + a DOM text overlay). No
     * ECharts.
     *   • vertical (default) and horizontal layouts
     *   • grouped / stacked / percentage modes
     *   • category + bar gap configuration
     *   • rounded outer corners (top for vertical, end for horizontal)
     *   • legend toggle, hover tooltip, click events
     *   • role="figure" + screen-reader-only data table
     */
    interface HTMLMdBarChartElement extends Components.MdBarChart, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdBarChartElementEventMap>(type: K, listener: (this: HTMLMdBarChartElement, ev: MdBarChartCustomEvent<HTMLMdBarChartElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdBarChartElementEventMap>(type: K, listener: (this: HTMLMdBarChartElement, ev: MdBarChartCustomEvent<HTMLMdBarChartElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdBarChartElement: {
        prototype: HTMLMdBarChartElement;
        new (): HTMLMdBarChartElement;
    };
    interface HTMLMdBottomSheetElementEventMap {
        "mdOpen": void;
        "mdClose": void;
        "mdCancel": void;
    }
    /**
     * MD3 Bottom sheet — secondary content anchored to the bottom of the screen.
     * Two variants per the M3 spec
     * (https://m3.material.io/components/bottom-sheets/overview). Both are
     * modal dialogs — they always render a scrim, trap focus, lock body
     * scroll, close on Escape, and support drag-to-dismiss.
     * - `standard` (default) — anchored to the bottom edge of the viewport.
     *   Full-width up to a responsive max, with rounded top corners only.
     * - `detached` — floats with a margin on every side and rounded corners
     *   on every side. Best for compact, dialog-like sheets on larger
     *   screens.
     * Width and height are entirely token-driven, so the only thing you
     * change to switch from anchored to floating chrome is the `variant`
     * attribute. The default media query reflows the sheet between
     * full-width on mobile and a centred 640px panel on desktop; consumers
     * can opt out via `--md-bottom-sheet-width` / `--md-bottom-sheet-max-width`.
     */
    interface HTMLMdBottomSheetElement extends Components.MdBottomSheet, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdBottomSheetElementEventMap>(type: K, listener: (this: HTMLMdBottomSheetElement, ev: MdBottomSheetCustomEvent<HTMLMdBottomSheetElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdBottomSheetElementEventMap>(type: K, listener: (this: HTMLMdBottomSheetElement, ev: MdBottomSheetCustomEvent<HTMLMdBottomSheetElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdBottomSheetElement: {
        prototype: HTMLMdBottomSheetElement;
        new (): HTMLMdBottomSheetElement;
    };
    interface HTMLMdBreadcrumbItemElementEventMap {
        "mdSelect": MdBreadcrumbSelectDetail;
    }
    interface HTMLMdBreadcrumbItemElement extends Components.MdBreadcrumbItem, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdBreadcrumbItemElementEventMap>(type: K, listener: (this: HTMLMdBreadcrumbItemElement, ev: MdBreadcrumbItemCustomEvent<HTMLMdBreadcrumbItemElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdBreadcrumbItemElementEventMap>(type: K, listener: (this: HTMLMdBreadcrumbItemElement, ev: MdBreadcrumbItemCustomEvent<HTMLMdBreadcrumbItemElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdBreadcrumbItemElement: {
        prototype: HTMLMdBreadcrumbItemElement;
        new (): HTMLMdBreadcrumbItemElement;
    };
    interface HTMLMdBreadcrumbsElementEventMap {
        "mdExpand": MdBreadcrumbsExpandDetail;
    }
    interface HTMLMdBreadcrumbsElement extends Components.MdBreadcrumbs, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdBreadcrumbsElementEventMap>(type: K, listener: (this: HTMLMdBreadcrumbsElement, ev: MdBreadcrumbsCustomEvent<HTMLMdBreadcrumbsElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdBreadcrumbsElementEventMap>(type: K, listener: (this: HTMLMdBreadcrumbsElement, ev: MdBreadcrumbsCustomEvent<HTMLMdBreadcrumbsElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdBreadcrumbsElement: {
        prototype: HTMLMdBreadcrumbsElement;
        new (): HTMLMdBreadcrumbsElement;
    };
    interface HTMLMdButtonElementEventMap {
        "mdClick": MdButtonClickDetail;
        "mdChange": MdButtonChangeDetail;
    }
    interface HTMLMdButtonElement extends Components.MdButton, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdButtonElementEventMap>(type: K, listener: (this: HTMLMdButtonElement, ev: MdButtonCustomEvent<HTMLMdButtonElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdButtonElementEventMap>(type: K, listener: (this: HTMLMdButtonElement, ev: MdButtonCustomEvent<HTMLMdButtonElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdButtonElement: {
        prototype: HTMLMdButtonElement;
        new (): HTMLMdButtonElement;
    };
    interface HTMLMdButtonGroupElementEventMap {
        "mdSelectionChange": MdButtonGroupChangeDetail;
    }
    interface HTMLMdButtonGroupElement extends Components.MdButtonGroup, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdButtonGroupElementEventMap>(type: K, listener: (this: HTMLMdButtonGroupElement, ev: MdButtonGroupCustomEvent<HTMLMdButtonGroupElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdButtonGroupElementEventMap>(type: K, listener: (this: HTMLMdButtonGroupElement, ev: MdButtonGroupCustomEvent<HTMLMdButtonGroupElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdButtonGroupElement: {
        prototype: HTMLMdButtonGroupElement;
        new (): HTMLMdButtonGroupElement;
    };
    interface HTMLMdCardElementEventMap {
        "mdClick": MouseEvent;
        "mdDragStart": MdCardDragDetail;
        "mdDragMove": MdCardDragDetail;
        "mdDragEnd": MdCardDragDetail;
    }
    interface HTMLMdCardElement extends Components.MdCard, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdCardElementEventMap>(type: K, listener: (this: HTMLMdCardElement, ev: MdCardCustomEvent<HTMLMdCardElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdCardElementEventMap>(type: K, listener: (this: HTMLMdCardElement, ev: MdCardCustomEvent<HTMLMdCardElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdCardElement: {
        prototype: HTMLMdCardElement;
        new (): HTMLMdCardElement;
    };
    interface HTMLMdCheckboxElementEventMap {
        "mdChange": { checked: boolean; indeterminate: boolean };
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    /**
     * `md-checkbox` — Material Design 3 checkbox (with tri-state / indeterminate).
     * Form-associated: inside a `<form>` it submits its `value` under `name` when
     * checked, and restores on form reset. It is a *labelable* element — wrap it in
     * a `<label>` (clicks on the label text toggle it AND give it its accessible
     * name natively), or pass `aria-label` / `aria-labelledby` on the host. A bare,
     * unlabeled checkbox has no accessible name (axe `aria-toggle-field-name`).
     */
    interface HTMLMdCheckboxElement extends Components.MdCheckbox, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdCheckboxElementEventMap>(type: K, listener: (this: HTMLMdCheckboxElement, ev: MdCheckboxCustomEvent<HTMLMdCheckboxElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdCheckboxElementEventMap>(type: K, listener: (this: HTMLMdCheckboxElement, ev: MdCheckboxCustomEvent<HTMLMdCheckboxElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdCheckboxElement: {
        prototype: HTMLMdCheckboxElement;
        new (): HTMLMdCheckboxElement;
    };
    interface HTMLMdChipElementEventMap {
        "mdClick": void;
        "mdSelect": { selected: boolean };
        "mdRemove": void;
    }
    interface HTMLMdChipElement extends Components.MdChip, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdChipElementEventMap>(type: K, listener: (this: HTMLMdChipElement, ev: MdChipCustomEvent<HTMLMdChipElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdChipElementEventMap>(type: K, listener: (this: HTMLMdChipElement, ev: MdChipCustomEvent<HTMLMdChipElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdChipElement: {
        prototype: HTMLMdChipElement;
        new (): HTMLMdChipElement;
    };
    interface HTMLMdColorPickerElementEventMap {
        "mdInput": { value: string };
        "mdChange": { value: string };
        "mdOpenChange": { open: boolean };
    }
    interface HTMLMdColorPickerElement extends Components.MdColorPicker, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdColorPickerElementEventMap>(type: K, listener: (this: HTMLMdColorPickerElement, ev: MdColorPickerCustomEvent<HTMLMdColorPickerElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdColorPickerElementEventMap>(type: K, listener: (this: HTMLMdColorPickerElement, ev: MdColorPickerCustomEvent<HTMLMdColorPickerElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdColorPickerElement: {
        prototype: HTMLMdColorPickerElement;
        new (): HTMLMdColorPickerElement;
    };
    interface HTMLMdDatePickerElementEventMap {
        "mdChange": MdDatePickerChangeDetail;
        "mdSelected": MdDatePickerSelectedDetail;
        "mdInput": { value: string };
        "mdOpen": void;
        "mdClose": void;
        "mdCancel": void;
        "mdViewChange": MdDatePickerViewChangeDetail;
        "mdMenuOpen": MdDatePickerMenuOpenDetail;
        "mdMenuSelect": MdDatePickerMenuSelectDetail;
        "mdModeChange": MdDatePickerModeChangeDetail;
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    /**
     * `md-date-picker` — a Material Design 3 date picker.
     * Variants:
     * - `modal-input` *(default)* — outlined text field + modal calendar dialog.
     * - `modal` — bare modal dialog surfaced via `show()` / `open`.
     * - `docked` — outlined text field + dropdown calendar anchored to the field.
     * The modal dialog can flip to a typed-entry view (date input) via the
     * header toggle, satisfying the MD3 "modal date input" pattern.
     */
    interface HTMLMdDatePickerElement extends Components.MdDatePicker, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdDatePickerElementEventMap>(type: K, listener: (this: HTMLMdDatePickerElement, ev: MdDatePickerCustomEvent<HTMLMdDatePickerElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdDatePickerElementEventMap>(type: K, listener: (this: HTMLMdDatePickerElement, ev: MdDatePickerCustomEvent<HTMLMdDatePickerElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdDatePickerElement: {
        prototype: HTMLMdDatePickerElement;
        new (): HTMLMdDatePickerElement;
    };
    interface HTMLMdDialogElementEventMap {
        "mdOpen": void;
        "mdClose": void;
        "mdCancel": void;
    }
    interface HTMLMdDialogElement extends Components.MdDialog, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdDialogElementEventMap>(type: K, listener: (this: HTMLMdDialogElement, ev: MdDialogCustomEvent<HTMLMdDialogElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdDialogElementEventMap>(type: K, listener: (this: HTMLMdDialogElement, ev: MdDialogCustomEvent<HTMLMdDialogElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdDialogElement: {
        prototype: HTMLMdDialogElement;
        new (): HTMLMdDialogElement;
    };
    interface HTMLMdDividerElement extends Components.MdDivider, HTMLStencilElement {
    }
    var HTMLMdDividerElement: {
        prototype: HTMLMdDividerElement;
        new (): HTMLMdDividerElement;
    };
    interface HTMLMdFabElementEventMap {
        "mdClick": MouseEvent;
    }
    interface HTMLMdFabElement extends Components.MdFab, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdFabElementEventMap>(type: K, listener: (this: HTMLMdFabElement, ev: MdFabCustomEvent<HTMLMdFabElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdFabElementEventMap>(type: K, listener: (this: HTMLMdFabElement, ev: MdFabCustomEvent<HTMLMdFabElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdFabElement: {
        prototype: HTMLMdFabElement;
        new (): HTMLMdFabElement;
    };
    interface HTMLMdFabMenuElementEventMap {
        "mdOpen": void;
        "mdClose": void;
    }
    interface HTMLMdFabMenuElement extends Components.MdFabMenu, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdFabMenuElementEventMap>(type: K, listener: (this: HTMLMdFabMenuElement, ev: MdFabMenuCustomEvent<HTMLMdFabMenuElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdFabMenuElementEventMap>(type: K, listener: (this: HTMLMdFabMenuElement, ev: MdFabMenuCustomEvent<HTMLMdFabMenuElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdFabMenuElement: {
        prototype: HTMLMdFabMenuElement;
        new (): HTMLMdFabMenuElement;
    };
    interface HTMLMdFabMenuItemElementEventMap {
        "mdClick": void;
    }
    interface HTMLMdFabMenuItemElement extends Components.MdFabMenuItem, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdFabMenuItemElementEventMap>(type: K, listener: (this: HTMLMdFabMenuItemElement, ev: MdFabMenuItemCustomEvent<HTMLMdFabMenuItemElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdFabMenuItemElementEventMap>(type: K, listener: (this: HTMLMdFabMenuItemElement, ev: MdFabMenuItemCustomEvent<HTMLMdFabMenuItemElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdFabMenuItemElement: {
        prototype: HTMLMdFabMenuItemElement;
        new (): HTMLMdFabMenuItemElement;
    };
    interface HTMLMdIconButtonElementEventMap {
        "mdClick": { selected: boolean };
    }
    interface HTMLMdIconButtonElement extends Components.MdIconButton, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdIconButtonElementEventMap>(type: K, listener: (this: HTMLMdIconButtonElement, ev: MdIconButtonCustomEvent<HTMLMdIconButtonElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdIconButtonElementEventMap>(type: K, listener: (this: HTMLMdIconButtonElement, ev: MdIconButtonCustomEvent<HTMLMdIconButtonElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdIconButtonElement: {
        prototype: HTMLMdIconButtonElement;
        new (): HTMLMdIconButtonElement;
    };
    interface HTMLMdLineChartElementEventMap {
        "mdMarkerClick": MdChartClickDetail<MdChartXYSeries>;
        "mdLineClick": MdChartClickDetail<MdChartXYSeries>;
        "mdAreaClick": MdChartClickDetail<MdChartXYSeries>;
        "mdAxisClick": MdChartAxisClickDetail;
        "mdLegendClick": { seriesIndex: number; seriesId?: string; selected: boolean };
        "mdHover": MdChartHoverDetail;
        "mdReady": void;
        "mdZoom": { startIndex: number; endIndex: number; reset: boolean };
    }
    /**
     * md-line-chart — Material Design 3 line chart rendered by the
     * in-house chart engine (Canvas2D + a DOM text overlay). No
     * ECharts.
     * Pattern parity with MUI X Charts:
     *   • multi-series with per-series colour / label / curve
     *   • category, time, value, log scales
     *   • curve types: linear / smooth / monotone / step variants
     *   • stacking: normal / percentage
     *   • markers, area fills, gap bridging (`connectNulls`)
     *   • legend with toggle + custom position
     *   • hover crosshair + tooltip
     *   • emits `mdMarkerClick`, `mdLineClick`, `mdAreaClick`, `mdAxisClick`,
     *     `mdLegendClick`, `mdHover`
     * MD3 expressive layer on top:
     *   • palette resolves to `--md-sys-color-*` tokens
     *   • dark theme + brand re-themes via CSS variables only
     *   • a11y: role="figure" + screen-reader-only data table
     *   • fully responsive via ResizeObserver
     */
    interface HTMLMdLineChartElement extends Components.MdLineChart, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdLineChartElementEventMap>(type: K, listener: (this: HTMLMdLineChartElement, ev: MdLineChartCustomEvent<HTMLMdLineChartElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdLineChartElementEventMap>(type: K, listener: (this: HTMLMdLineChartElement, ev: MdLineChartCustomEvent<HTMLMdLineChartElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdLineChartElement: {
        prototype: HTMLMdLineChartElement;
        new (): HTMLMdLineChartElement;
    };
    interface HTMLMdListElementEventMap {
        "mdSelect": MdListSelectDetail;
        "mdActivate": { index: number; item: HTMLMdListItemElement };
        "mdReorder": { from: number; to: number; order: number[] };
    }
    interface HTMLMdListElement extends Components.MdList, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdListElementEventMap>(type: K, listener: (this: HTMLMdListElement, ev: MdListCustomEvent<HTMLMdListElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdListElementEventMap>(type: K, listener: (this: HTMLMdListElement, ev: MdListCustomEvent<HTMLMdListElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdListElement: {
        prototype: HTMLMdListElement;
        new (): HTMLMdListElement;
    };
    interface HTMLMdListItemElementEventMap {
        "mdClick": { item: HTMLElement };
        "mdItemClick": { index: number; selected: boolean };
        "mdItemSelect": {
    index: number;
    value: string;
    selected: boolean;
    item: HTMLMdListItemElement;
  };
        "mdRequestActivation": void;
        "mdItemRequestReorder": {
    direction: 'up' | 'down';
    from: number;
  };
        "mdExpand": MdListItemExpandDetail;
    }
    interface HTMLMdListItemElement extends Components.MdListItem, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdListItemElementEventMap>(type: K, listener: (this: HTMLMdListItemElement, ev: MdListItemCustomEvent<HTMLMdListItemElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdListItemElementEventMap>(type: K, listener: (this: HTMLMdListItemElement, ev: MdListItemCustomEvent<HTMLMdListItemElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdListItemElement: {
        prototype: HTMLMdListItemElement;
        new (): HTMLMdListItemElement;
    };
    interface HTMLMdLoadingIndicatorElement extends Components.MdLoadingIndicator, HTMLStencilElement {
    }
    var HTMLMdLoadingIndicatorElement: {
        prototype: HTMLMdLoadingIndicatorElement;
        new (): HTMLMdLoadingIndicatorElement;
    };
    interface HTMLMdMenuElementEventMap {
        "mdOpen": void;
        "mdClose": void;
    }
    interface HTMLMdMenuElement extends Components.MdMenu, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdMenuElementEventMap>(type: K, listener: (this: HTMLMdMenuElement, ev: MdMenuCustomEvent<HTMLMdMenuElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdMenuElementEventMap>(type: K, listener: (this: HTMLMdMenuElement, ev: MdMenuCustomEvent<HTMLMdMenuElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdMenuElement: {
        prototype: HTMLMdMenuElement;
        new (): HTMLMdMenuElement;
    };
    interface HTMLMdMenuItemElementEventMap {
        "mdClick": void;
    }
    interface HTMLMdMenuItemElement extends Components.MdMenuItem, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdMenuItemElementEventMap>(type: K, listener: (this: HTMLMdMenuItemElement, ev: MdMenuItemCustomEvent<HTMLMdMenuItemElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdMenuItemElementEventMap>(type: K, listener: (this: HTMLMdMenuItemElement, ev: MdMenuItemCustomEvent<HTMLMdMenuItemElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdMenuItemElement: {
        prototype: HTMLMdMenuItemElement;
        new (): HTMLMdMenuItemElement;
    };
    interface HTMLMdMenuItemGroupElement extends Components.MdMenuItemGroup, HTMLStencilElement {
    }
    var HTMLMdMenuItemGroupElement: {
        prototype: HTMLMdMenuItemGroupElement;
        new (): HTMLMdMenuItemGroupElement;
    };
    /**
     * md-meter — a read-only graphical display of a numeric value within a known
     * range (storage used, quota, password strength, battery level).
     * The semantics live on the HOST: `role="meter"` with the full ARIA value
     * contract, a purely presentational track + indicator inside the shadow root
     * (all `aria-hidden`), value clamping into `[min, max]`, and Intl-driven value
     * formatting (locale-aware percent by default, any `Intl.NumberFormatOptions`
     * via `formatOptions`). Keeping the ARIA outside the shadow root is what lets
     * the visuals change shape without touching what is announced.
     * Deliberately NOT a progress indicator: a meter shows a *state* ("how full"),
     * a progress indicator shows an *activity* ("how far along"). It is therefore
     * a plain track + fill — no 4px gap, no stop dot, no indeterminate mode, no
     * completion animation. Nothing here is focusable or interactive and no events
     * are emitted.
     * Two shapes, one contract: `variant="linear"` (default) is the bar;
     * `variant="circular"` is the same reading as a ring, with the value in the
     * middle and the label beneath. The ARIA lives on the host either way, so the
     * shape is presentational and swapping it changes nothing for a screen reader.
     */
    interface HTMLMdMeterElement extends Components.MdMeter, HTMLStencilElement {
    }
    var HTMLMdMeterElement: {
        prototype: HTMLMdMeterElement;
        new (): HTMLMdMeterElement;
    };
    interface HTMLMdMultiSelectElementEventMap {
        "mdChange": string[];
        "mdOpen": void;
        "mdClose": void;
        "mdRemove": string;
        "mdClear": void;
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    /**
     * `md-multi-select` — Material Design 3 multi-value dropdown.
     * Shares its internals with `md-select`: an `md-text-field` trigger and an
     * `md-menu` option surface. Options come from slotted `<md-select-option>`
     * children (primary API) or the programmatic `options` array. Each becomes a
     * `type="checkbox"` `md-menu-item` so several can be selected at once, and the
     * menu uses `keep-open` so multiple toggles don't dismiss it.
     * Selected values render in the trigger as removable chips (`display-mode="chips"`),
     * a "{n} selected" count, or comma-joined text.
     */
    interface HTMLMdMultiSelectElement extends Components.MdMultiSelect, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdMultiSelectElementEventMap>(type: K, listener: (this: HTMLMdMultiSelectElement, ev: MdMultiSelectCustomEvent<HTMLMdMultiSelectElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdMultiSelectElementEventMap>(type: K, listener: (this: HTMLMdMultiSelectElement, ev: MdMultiSelectCustomEvent<HTMLMdMultiSelectElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdMultiSelectElement: {
        prototype: HTMLMdMultiSelectElement;
        new (): HTMLMdMultiSelectElement;
    };
    interface HTMLMdNavigationBarElementEventMap {
        "mdChange": { index: number; previousIndex: number };
    }
    /**
     * `md-navigation-bar` — Material Design 3 navigation bar.
     * A bottom-anchored row of 3–5 top-level destinations, optimized for
     * compact-width screens (mobile). Each destination is an `<md-navigation-tab>`
     * child; the bar orchestrates selection, label visibility, keyboard
     * navigation (roving tabindex + arrow keys), and emits change events.
     * References:
     *   - {@link https://m3.material.io/components/navigation-bar/overview Overview}
     *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
     *   - {@link https://m3.material.io/components/navigation-bar/guidelines Guidelines}
     *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
     */
    interface HTMLMdNavigationBarElement extends Components.MdNavigationBar, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdNavigationBarElementEventMap>(type: K, listener: (this: HTMLMdNavigationBarElement, ev: MdNavigationBarCustomEvent<HTMLMdNavigationBarElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdNavigationBarElementEventMap>(type: K, listener: (this: HTMLMdNavigationBarElement, ev: MdNavigationBarCustomEvent<HTMLMdNavigationBarElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdNavigationBarElement: {
        prototype: HTMLMdNavigationBarElement;
        new (): HTMLMdNavigationBarElement;
    };
    interface HTMLMdNavigationRailElementEventMap {
        "mdTabChange": { index: number; value: string };
        "mdExpand": void;
        "mdCollapse": void;
    }
    /**
     * Material Design 3 — Navigation Rail
     * A vertical navigation surface for the left or right edge of medium / large
     * window sizes. Holds 3–7 top-level destinations and optionally a header
     * (menu / brand), a FAB, and a footer.
     * Implements MD3 specs:
     *   - https://m3.material.io/components/navigation-rail/overview
     *   - https://m3.material.io/components/navigation-rail/specs
     *   - https://m3.material.io/components/navigation-rail/guidelines
     *   - https://m3.material.io/components/navigation-rail/accessibility
     */
    interface HTMLMdNavigationRailElement extends Components.MdNavigationRail, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdNavigationRailElementEventMap>(type: K, listener: (this: HTMLMdNavigationRailElement, ev: MdNavigationRailCustomEvent<HTMLMdNavigationRailElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdNavigationRailElementEventMap>(type: K, listener: (this: HTMLMdNavigationRailElement, ev: MdNavigationRailCustomEvent<HTMLMdNavigationRailElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdNavigationRailElement: {
        prototype: HTMLMdNavigationRailElement;
        new (): HTMLMdNavigationRailElement;
    };
    interface HTMLMdNavigationRailTabElementEventMap {
        "mdTabClick": { value: string };
        "mdSubmenuToggle": { open: boolean };
    }
    /**
     * Material Design 3 — Navigation Rail Destination (Tab)
     * A single destination within a `<md-navigation-rail>`. Renders the MD3
     * active-indicator pill (56×32) around the icon, with an optional label
     * underneath (or inline when the parent rail is expanded). Supports badges,
     * disabled state, link mode (`href`) and slotted custom icons.
     * Spec:
     *   - https://m3.material.io/components/navigation-rail/specs
     *   - https://m3.material.io/components/navigation-rail/guidelines
     *   - https://m3.material.io/components/navigation-rail/accessibility
     */
    interface HTMLMdNavigationRailTabElement extends Components.MdNavigationRailTab, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdNavigationRailTabElementEventMap>(type: K, listener: (this: HTMLMdNavigationRailTabElement, ev: MdNavigationRailTabCustomEvent<HTMLMdNavigationRailTabElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdNavigationRailTabElementEventMap>(type: K, listener: (this: HTMLMdNavigationRailTabElement, ev: MdNavigationRailTabCustomEvent<HTMLMdNavigationRailTabElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdNavigationRailTabElement: {
        prototype: HTMLMdNavigationRailTabElement;
        new (): HTMLMdNavigationRailTabElement;
    };
    interface HTMLMdNavigationTabElementEventMap {
        "mdTabClick": { index: number };
    }
    /**
     * `md-navigation-tab` — a single destination inside `md-navigation-bar`.
     * Renders an icon, an optional label, and an optional badge. Selecting
     * the tab swaps the icon to a filled variant (per M3 spec) and reveals
     * an "active indicator" pill behind the icon.
     * References:
     *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
     *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
     */
    interface HTMLMdNavigationTabElement extends Components.MdNavigationTab, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdNavigationTabElementEventMap>(type: K, listener: (this: HTMLMdNavigationTabElement, ev: MdNavigationTabCustomEvent<HTMLMdNavigationTabElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdNavigationTabElementEventMap>(type: K, listener: (this: HTMLMdNavigationTabElement, ev: MdNavigationTabCustomEvent<HTMLMdNavigationTabElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdNavigationTabElement: {
        prototype: HTMLMdNavigationTabElement;
        new (): HTMLMdNavigationTabElement;
    };
    interface HTMLMdNumberFieldElementEventMap {
        "mdInput": MdNumberFieldChangeDetail;
        "mdChange": MdNumberFieldChangeDetail;
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    /**
     * `md-number-field` — Material Design 3 number input: an `md-text-field`
     * with locale-aware `Intl.NumberFormat` display, arrow-key / stepper-button /
     * wheel stepping, and native form participation.
     * Behavior contract:
     *   - the visible control is a **plain text input** (`inputmode` numeric or
     *     decimal) — deliberately NOT `role="spinbutton"` and NOT
     *     `type="number"`, so the locale-formatted value stays ordinary readable,
     *     editable text and no native spinner chrome fights the Intl display;
     *     the steppers are separate labeled buttons with `tabindex="-1"` (the
     *     input is the keyboard surface),
     *   - `value` is the raw number (`null` = empty); the formatted string is
     *     display-only state,
     *   - ArrowUp/Down step by `step`, Alt = `smallStep`, Shift = `largeStep`;
     *     Home/End jump to a defined `min`/`max`,
     *   - press-and-hold on a stepper auto-repeats (400ms delay, 60ms interval),
     *   - typing is filtered to the characters of the active locale/format,
     *     parsed leniently and kept verbatim; blur reformats (and clamps unless
     *     `allow-out-of-range`),
     *   - stepping (keys, buttons, wheel) always clamps and commits (`mdChange`).
     * A drag-to-scrub surface (pointer lock over the field) is deliberately out of
     * scope: a niche gesture built on pointer lock, whose behaviour is unreliable
     * in Safari. `allow-wheel-scrub` covers the "nudge without typing" case.
     */
    interface HTMLMdNumberFieldElement extends Components.MdNumberField, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdNumberFieldElementEventMap>(type: K, listener: (this: HTMLMdNumberFieldElement, ev: MdNumberFieldCustomEvent<HTMLMdNumberFieldElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdNumberFieldElementEventMap>(type: K, listener: (this: HTMLMdNumberFieldElement, ev: MdNumberFieldCustomEvent<HTMLMdNumberFieldElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdNumberFieldElement: {
        prototype: HTMLMdNumberFieldElement;
        new (): HTMLMdNumberFieldElement;
    };
    interface HTMLMdOrganizationChartElementEventMap {
        "mdSelectionChange": OrgChartSelectionChangeDetail;
        "mdNodeToggle": OrgChartToggleDetail;
    }
    /**
     * `md-organization-chart` — Material Design 3 Expressive organization chart.
     * Visualises hierarchical org data as a top-down tree of avatar cards joined
     * by connector lines. Data-driven (`nodes`), with collapsible subtrees,
     * optional single/multiple selection, and the full WAI-ARIA **tree** pattern
     * (roving tabindex, Arrow/Home/End navigation, Enter/Space to select).
     * The surface pans horizontally when it outgrows its container, so a wide org
     * stays usable on small screens. Direction-aware (RTL mirrors the layout and
     * swaps the horizontal arrow keys); every affordance label is a prop for
     * localisation.
     * ```html
     * <md-organization-chart selection-mode="single"></md-organization-chart>
     * <script>
     *   document.querySelector('md-organization-chart').nodes = [{
     *     id: 'ceo', name: 'Amy Elsner', title: 'Founder & CEO',
     *     children: [{ id: 'prod', name: 'Asiya Javayant', title: 'Product Lead' }],
     *   }];
     * </script>
     * ```
     */
    interface HTMLMdOrganizationChartElement extends Components.MdOrganizationChart, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdOrganizationChartElementEventMap>(type: K, listener: (this: HTMLMdOrganizationChartElement, ev: MdOrganizationChartCustomEvent<HTMLMdOrganizationChartElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdOrganizationChartElementEventMap>(type: K, listener: (this: HTMLMdOrganizationChartElement, ev: MdOrganizationChartCustomEvent<HTMLMdOrganizationChartElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdOrganizationChartElement: {
        prototype: HTMLMdOrganizationChartElement;
        new (): HTMLMdOrganizationChartElement;
    };
    interface HTMLMdOtpFieldElementEventMap {
        "mdInput": string;
        "mdChange": string;
        "mdComplete": MdOtpFieldCompleteDetail;
        "mdInvalidInput": MdOtpFieldInvalidDetail;
        "mdValidityChange": MdOtpFieldValidityDetail;
    }
    /**
     * `md-otp-field` — Material Design 3 one-time-code field.
     * Renders `length` real `<input>` cells in its own shadow root (it OWNS its
     * inputs, so every cell carries full ARIA), pipes all entry — typing, paste,
     * SMS autofill — through one sanitizing pipeline (strip whitespace →
     * `transform` → `validationType` charset filter → clamp to `length`), and
     * participates in forms via `ElementInternals` (no hidden input).
     * The value is a CONTIGUOUS string: clearing a cell shifts the characters
     * after it left (there are no holes), and a character typed into an empty
     * cell beyond the fill point lands at the first empty position.
     * Normalization is declarative: `transform` names the casing rule as an
     * attribute value rather than taking a callback, so the rule is authorable
     * from plain HTML and survives SSR — an element attribute cannot carry a
     * function.
     */
    interface HTMLMdOtpFieldElement extends Components.MdOtpField, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdOtpFieldElementEventMap>(type: K, listener: (this: HTMLMdOtpFieldElement, ev: MdOtpFieldCustomEvent<HTMLMdOtpFieldElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdOtpFieldElementEventMap>(type: K, listener: (this: HTMLMdOtpFieldElement, ev: MdOtpFieldCustomEvent<HTMLMdOtpFieldElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdOtpFieldElement: {
        prototype: HTMLMdOtpFieldElement;
        new (): HTMLMdOtpFieldElement;
    };
    interface HTMLMdPieChartElementEventMap {
        "mdSliceClick": MdChartClickDetail<MdPieDatum>;
        "mdLegendClick": { dataIndex: number; selected: boolean };
        "mdReady": void;
    }
    interface HTMLMdPieChartElement extends Components.MdPieChart, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdPieChartElementEventMap>(type: K, listener: (this: HTMLMdPieChartElement, ev: MdPieChartCustomEvent<HTMLMdPieChartElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdPieChartElementEventMap>(type: K, listener: (this: HTMLMdPieChartElement, ev: MdPieChartCustomEvent<HTMLMdPieChartElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdPieChartElement: {
        prototype: HTMLMdPieChartElement;
        new (): HTMLMdPieChartElement;
    };
    interface HTMLMdProgressIndicatorElementEventMap {
        "mdComplete": void;
    }
    interface HTMLMdProgressIndicatorElement extends Components.MdProgressIndicator, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdProgressIndicatorElementEventMap>(type: K, listener: (this: HTMLMdProgressIndicatorElement, ev: MdProgressIndicatorCustomEvent<HTMLMdProgressIndicatorElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdProgressIndicatorElementEventMap>(type: K, listener: (this: HTMLMdProgressIndicatorElement, ev: MdProgressIndicatorCustomEvent<HTMLMdProgressIndicatorElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdProgressIndicatorElement: {
        prototype: HTMLMdProgressIndicatorElement;
        new (): HTMLMdProgressIndicatorElement;
    };
    interface HTMLMdRadioElementEventMap {
        "mdChange": { checked: boolean; value: string };
        "mdFocus": void;
        "mdBlur": void;
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    interface HTMLMdRadioElement extends Components.MdRadio, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdRadioElementEventMap>(type: K, listener: (this: HTMLMdRadioElement, ev: MdRadioCustomEvent<HTMLMdRadioElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdRadioElementEventMap>(type: K, listener: (this: HTMLMdRadioElement, ev: MdRadioCustomEvent<HTMLMdRadioElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdRadioElement: {
        prototype: HTMLMdRadioElement;
        new (): HTMLMdRadioElement;
    };
    interface HTMLMdRatingElementEventMap {
        "mdHover": number | null;
        "mdChange": number;
    }
    /**
     * `md-rating` — Material Design 3 Expressive Rating control.
     * A star-rating input: configurable max, half-step precision, custom icons
     * (Material Symbols glyphs OR slotted SVG/HTML), per-value icons
     * (filled / empty), hover preview, read-only and disabled modes,
     * keyboard navigation (Arrow + Home/End), and reset to zero.
     * MD3 Expressive: items animate with a brief tonal "scale-up" pop on
     * hover and selection; press uses a state-layer + ripple. The
     * underlying ARIA pattern is a slider so screen readers announce the
     * value as a continuous range.
     */
    interface HTMLMdRatingElement extends Components.MdRating, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdRatingElementEventMap>(type: K, listener: (this: HTMLMdRatingElement, ev: MdRatingCustomEvent<HTMLMdRatingElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdRatingElementEventMap>(type: K, listener: (this: HTMLMdRatingElement, ev: MdRatingCustomEvent<HTMLMdRatingElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdRatingElement: {
        prototype: HTMLMdRatingElement;
        new (): HTMLMdRatingElement;
    };
    interface HTMLMdRippleElement extends Components.MdRipple, HTMLStencilElement {
    }
    var HTMLMdRippleElement: {
        prototype: HTMLMdRippleElement;
        new (): HTMLMdRippleElement;
    };
    interface HTMLMdSearchElementEventMap {
        "mdInput": { value: string };
        "mdSearch": { value: string };
        "mdSubmit": { value: string };
        "mdChange": { value: string };
        "mdOpen": void;
        "mdClose": void;
        "mdClear": void;
        "mdVoice": { value: string; final: boolean };
        "mdLeadingIconClick": MdSearchLeadingIconClickDetail;
        "mdTrailingIconClick": MdSearchTrailingIconClickDetail;
    }
    /**
     * Material Design 3 Search.
     * Implements the M3 Search specification
     * (https://m3.material.io/components/search/specs).
     * Two orthogonal axes describe every spec configuration:
     *  - `variant`: `contained` (M3 Expressive default) | `divided` (baseline)
     *  - `layout` : `full-screen` (modal overlay) | `docked` (anchored panel)
     * The component renders a single resting bar plus a results panel. When the
     * `full-screen` layout is `open` the bar pins itself to the top of the
     * viewport via `position: fixed` so consumers don't need a portal — CSS does
     * the work, and the input keeps a stable identity throughout the open/close
     * lifecycle.
     */
    interface HTMLMdSearchElement extends Components.MdSearch, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSearchElementEventMap>(type: K, listener: (this: HTMLMdSearchElement, ev: MdSearchCustomEvent<HTMLMdSearchElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSearchElementEventMap>(type: K, listener: (this: HTMLMdSearchElement, ev: MdSearchCustomEvent<HTMLMdSearchElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSearchElement: {
        prototype: HTMLMdSearchElement;
        new (): HTMLMdSearchElement;
    };
    interface HTMLMdSegmentedButtonElementEventMap {
        "mdSegmentClick": { value: string; selected: boolean };
    }
    interface HTMLMdSegmentedButtonElement extends Components.MdSegmentedButton, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSegmentedButtonElementEventMap>(type: K, listener: (this: HTMLMdSegmentedButtonElement, ev: MdSegmentedButtonCustomEvent<HTMLMdSegmentedButtonElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSegmentedButtonElementEventMap>(type: K, listener: (this: HTMLMdSegmentedButtonElement, ev: MdSegmentedButtonCustomEvent<HTMLMdSegmentedButtonElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSegmentedButtonElement: {
        prototype: HTMLMdSegmentedButtonElement;
        new (): HTMLMdSegmentedButtonElement;
    };
    interface HTMLMdSegmentedButtonSetElementEventMap {
        "mdChange": string[];
    }
    interface HTMLMdSegmentedButtonSetElement extends Components.MdSegmentedButtonSet, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSegmentedButtonSetElementEventMap>(type: K, listener: (this: HTMLMdSegmentedButtonSetElement, ev: MdSegmentedButtonSetCustomEvent<HTMLMdSegmentedButtonSetElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSegmentedButtonSetElementEventMap>(type: K, listener: (this: HTMLMdSegmentedButtonSetElement, ev: MdSegmentedButtonSetCustomEvent<HTMLMdSegmentedButtonSetElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSegmentedButtonSetElement: {
        prototype: HTMLMdSegmentedButtonSetElement;
        new (): HTMLMdSegmentedButtonSetElement;
    };
    interface HTMLMdSelectElementEventMap {
        "mdChange": string;
        "mdOpen": void;
        "mdClose": void;
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    /**
     * `md-select` — Material Design 3 single-select dropdown.
     * Composes the library's own primitives instead of re-implementing them:
     *   - `md-text-field` is the trigger surface, so the picker inherits the
     *     full filled/outlined visuals, density scale, floating label, error
     *     state, supporting text, and focus ring of every other input. It is
     *     `readonly` (not editable) and `appear-focused` while the menu is open
     *     so the field + menu read as one connected control.
     *   - `md-menu` is the option surface, so the picker inherits open/close
     *     motion, viewport-aware positioning, single-open coordination, roving
     *     tabindex, type-ahead, and Escape/Home/End — for free.
     * Options are authored either declaratively as slotted `<md-select-option>`
     * children (primary API) or via the programmatic `options` array. Each
     * becomes a `type="radio"` `md-menu-item` so only one is ever selected.
     */
    interface HTMLMdSelectElement extends Components.MdSelect, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSelectElementEventMap>(type: K, listener: (this: HTMLMdSelectElement, ev: MdSelectCustomEvent<HTMLMdSelectElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSelectElementEventMap>(type: K, listener: (this: HTMLMdSelectElement, ev: MdSelectCustomEvent<HTMLMdSelectElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSelectElement: {
        prototype: HTMLMdSelectElement;
        new (): HTMLMdSelectElement;
    };
    interface HTMLMdSelectOptionElementEventMap {
        "mdSelectOptionChange": void;
    }
    /**
     * `md-select-option` — declarative option for `md-select` / `md-multi-select`.
     * This element is a **data carrier**: it renders nothing visible (the host
     * is `display: none`). The parent picker reads its `value`, label (the
     * `label` prop or slotted text), `disabled`, `icon`, and `supporting-text`
     * and projects them into the `md-menu` dropdown as `md-menu-item`s.
     * Authoring:
     * ```html
     * <md-select label="Colour">
     *   <md-select-option value="red" icon="circle">Red</md-select-option>
     *   <md-select-option value="green" disabled>Green</md-select-option>
     * </md-select>
     * ```
     * Whenever an option is added, removed, or has its data changed, it emits a
     * bubbling `mdSelectOptionChange` event so the parent re-reads its option set.
     */
    interface HTMLMdSelectOptionElement extends Components.MdSelectOption, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSelectOptionElementEventMap>(type: K, listener: (this: HTMLMdSelectOptionElement, ev: MdSelectOptionCustomEvent<HTMLMdSelectOptionElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSelectOptionElementEventMap>(type: K, listener: (this: HTMLMdSelectOptionElement, ev: MdSelectOptionCustomEvent<HTMLMdSelectOptionElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSelectOptionElement: {
        prototype: HTMLMdSelectOptionElement;
        new (): HTMLMdSelectOptionElement;
    };
    interface HTMLMdSideSheetElementEventMap {
        "mdOpen": void;
        "mdClose": void;
        "mdCancel": void;
        "mdBack": void;
    }
    /**
     * MD3 Side sheet — anchored panel for secondary content and actions.
     */
    interface HTMLMdSideSheetElement extends Components.MdSideSheet, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSideSheetElementEventMap>(type: K, listener: (this: HTMLMdSideSheetElement, ev: MdSideSheetCustomEvent<HTMLMdSideSheetElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSideSheetElementEventMap>(type: K, listener: (this: HTMLMdSideSheetElement, ev: MdSideSheetCustomEvent<HTMLMdSideSheetElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSideSheetElement: {
        prototype: HTMLMdSideSheetElement;
        new (): HTMLMdSideSheetElement;
    };
    interface HTMLMdSkeletonElement extends Components.MdSkeleton, HTMLStencilElement {
    }
    var HTMLMdSkeletonElement: {
        prototype: HTMLMdSkeletonElement;
        new (): HTMLMdSkeletonElement;
    };
    interface HTMLMdSliderElementEventMap {
        "mdInput": MdSliderValueDetail;
        "mdChange": MdSliderValueDetail;
        "mdFocus": MdSliderThumbDetail;
        "mdBlur": MdSliderThumbDetail;
        "mdDragStart": MdSliderDragDetail;
        "mdDragEnd": MdSliderDragDetail;
    }
    /**
     * MD3 Expressive slider: standard (from edge), centered (from midpoint), or range (dual thumb).
     * **Uncontrolled (default)** — the slider manages its own value. Listen to `mdChange` to observe commits.
     * **Controlled** (`controlled`) — the slider never mutates `value`/`valueStart`/`valueEnd`.
     * Listen to events and set the props yourself.  During drag the slider uses internal live
     * state for smooth visuals; on commit it snaps to the prop values.
     */
    interface HTMLMdSliderElement extends Components.MdSlider, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSliderElementEventMap>(type: K, listener: (this: HTMLMdSliderElement, ev: MdSliderCustomEvent<HTMLMdSliderElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSliderElementEventMap>(type: K, listener: (this: HTMLMdSliderElement, ev: MdSliderCustomEvent<HTMLMdSliderElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSliderElement: {
        prototype: HTMLMdSliderElement;
        new (): HTMLMdSliderElement;
    };
    interface HTMLMdSnackbarElementEventMap {
        "mdOpen": void;
        "mdClose": { reason: 'auto' | 'action' | 'close' };
        "mdAction": void;
    }
    interface HTMLMdSnackbarElement extends Components.MdSnackbar, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSnackbarElementEventMap>(type: K, listener: (this: HTMLMdSnackbarElement, ev: MdSnackbarCustomEvent<HTMLMdSnackbarElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSnackbarElementEventMap>(type: K, listener: (this: HTMLMdSnackbarElement, ev: MdSnackbarCustomEvent<HTMLMdSnackbarElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSnackbarElement: {
        prototype: HTMLMdSnackbarElement;
        new (): HTMLMdSnackbarElement;
    };
    interface HTMLMdSparklineElementEventMap {
        "mdSparkClick": { dataIndex: number; value: number | null };
        "mdReady": void;
    }
    /**
     * md-sparkline — compact inline chart, rendered by the in-house
     * engine (Canvas2D). No ECharts.
     * Sparklines are presentational: no axes, no legend, no title, no
     * tooltip by default (opt in with `show-tooltip`). Line / area drive
     * LineChartEngine in `compact` mode; the bar variant drives
     * BarChartEngine. `show-marks` maps to selective extremes/edges dots.
     */
    interface HTMLMdSparklineElement extends Components.MdSparkline, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSparklineElementEventMap>(type: K, listener: (this: HTMLMdSparklineElement, ev: MdSparklineCustomEvent<HTMLMdSparklineElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSparklineElementEventMap>(type: K, listener: (this: HTMLMdSparklineElement, ev: MdSparklineCustomEvent<HTMLMdSparklineElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSparklineElement: {
        prototype: HTMLMdSparklineElement;
        new (): HTMLMdSparklineElement;
    };
    interface HTMLMdSplitButtonElementEventMap {
        "mdLeadingClick": void;
        "mdTrailingClick": { checked: boolean };
    }
    interface HTMLMdSplitButtonElement extends Components.MdSplitButton, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSplitButtonElementEventMap>(type: K, listener: (this: HTMLMdSplitButtonElement, ev: MdSplitButtonCustomEvent<HTMLMdSplitButtonElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSplitButtonElementEventMap>(type: K, listener: (this: HTMLMdSplitButtonElement, ev: MdSplitButtonCustomEvent<HTMLMdSplitButtonElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSplitButtonElement: {
        prototype: HTMLMdSplitButtonElement;
        new (): HTMLMdSplitButtonElement;
    };
    /**
     * `md-status-dot` — a small absolutely-positioned indicator that pairs
     * with an avatar (or any other tile) to surface presence / state.
     * Designed to drop straight into a `position: relative` parent:
     * ```html
     * <span style="position: relative; display: inline-flex;">
     *   <md-avatar name="Ada Lovelace" label="Ada Lovelace, online"></md-avatar>
     *   <md-status-dot state="online"></md-status-dot>
     * </span>
     * ```
     * Anchors at the bottom-inline-end corner via logical-property insets, so
     * the pip auto-flips in RTL with no extra CSS. A 2 px outline in the
     * surface color keeps the dot visually detached from the avatar tile
     * regardless of the avatar's palette.
     * **Accessibility.** The dot is **decorative by default** — when used to
     * decorate an avatar, the avatar's own `label` should already encode the
     * status (`label="Ada Lovelace, online"`). Setting an explicit `label`
     * exposes the dot to assistive tech:
     * - **static** (`live` off) → `role="img"` + `aria-label` — a labelled image
     *   conveying the current state, announced when focused/encountered.
     * - **live** (`live` on) → `role="status"` (an `aria-live="polite"` region) so
     *   presence *changes* (online → busy) are announced as they happen.
     * **WCAG 1.4.1 (use of colour).** State is conveyed by colour alone, and in
     * Windows High-Contrast every state collapses to a single system colour. Never
     * rely on the dot's colour as the *only* signal of state — pair it with a
     * `label` (or text on the parent) so the status is also available as words.
     * **Live indicator.** When `live` is `true`, the dot pulses outward in its
     * own colour (online pulses green, busy pulses red, etc.). The animation
     * is gated behind `prefers-reduced-motion: no-preference`, so users who
     * have reduced-motion enabled at the OS level see a static dot.
     */
    interface HTMLMdStatusDotElement extends Components.MdStatusDot, HTMLStencilElement {
    }
    var HTMLMdStatusDotElement: {
        prototype: HTMLMdStatusDotElement;
        new (): HTMLMdStatusDotElement;
    };
    interface HTMLMdStepElementEventMap {
        "mdStepClick": { index: number };
        "mdStepNext": { index: number };
        "mdStepBack": { index: number };
    }
    /**
     * `md-step` — one step inside an `<md-stepper>`.
     * Layout, position and i18n are pushed down from the parent stepper via
     * `data-*` attributes, so a step is almost always authored declaratively:
     * ```html
     * <md-stepper active="1">
     *   <md-step label="Account" completed></md-step>
     *   <md-step label="Shipping" description="Address & method"></md-step>
     *   <md-step label="Payment" optional></md-step>
     * </md-stepper>
     * ```
     */
    interface HTMLMdStepElement extends Components.MdStep, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdStepElementEventMap>(type: K, listener: (this: HTMLMdStepElement, ev: MdStepCustomEvent<HTMLMdStepElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdStepElementEventMap>(type: K, listener: (this: HTMLMdStepElement, ev: MdStepCustomEvent<HTMLMdStepElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdStepElement: {
        prototype: HTMLMdStepElement;
        new (): HTMLMdStepElement;
    };
    interface HTMLMdStepperElementEventMap {
        "mdBeforeChange": { index: number; previous: number };
        "mdStepChange": { index: number; previous: number };
        "mdComplete": void;
    }
    /**
     * `md-stepper` — Material Design 3 stepper. Orchestrates a set of slotted
     * `md-step` children: it owns the active index and navigation, and pushes
     * layout / i18n down to each step.
     * **Controlled, with optional auto-progress.** The stepper owns `active`
     * (updated on click / `next` / `prev` / `goTo`, emitting `mdStepChange`). With
     * `auto-complete` (default), advancing marks the step you leave as `completed`,
     * so the progress line + checks "just work" — no manual wiring. Set
     * `auto-complete="false"` to own `completed` yourself.
     * ```html
     * <md-stepper active="0">
     *   <md-step label="Account"><p>…</p></md-step>
     *   <md-step label="Shipping" optional><p>…</p></md-step>
     *   <md-step label="Payment" editable><p>…</p></md-step>
     * </md-stepper>
     * ```
     */
    interface HTMLMdStepperElement extends Components.MdStepper, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdStepperElementEventMap>(type: K, listener: (this: HTMLMdStepperElement, ev: MdStepperCustomEvent<HTMLMdStepperElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdStepperElementEventMap>(type: K, listener: (this: HTMLMdStepperElement, ev: MdStepperCustomEvent<HTMLMdStepperElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdStepperElement: {
        prototype: HTMLMdStepperElement;
        new (): HTMLMdStepperElement;
    };
    interface HTMLMdSubMenuItemElementEventMap {
        "mdClick": void;
    }
    interface HTMLMdSubMenuItemElement extends Components.MdSubMenuItem, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSubMenuItemElementEventMap>(type: K, listener: (this: HTMLMdSubMenuItemElement, ev: MdSubMenuItemCustomEvent<HTMLMdSubMenuItemElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSubMenuItemElementEventMap>(type: K, listener: (this: HTMLMdSubMenuItemElement, ev: MdSubMenuItemCustomEvent<HTMLMdSubMenuItemElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSubMenuItemElement: {
        prototype: HTMLMdSubMenuItemElement;
        new (): HTMLMdSubMenuItemElement;
    };
    interface HTMLMdSwitchElementEventMap {
        "mdInput": { selected: boolean };
        "mdChange": { selected: boolean };
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    /**
     * `md-switch` — Material Design 3 switch (on/off toggle).
     * Form-associated: inside a `<form>` it submits its `value` under `name` when
     * selected, and restores on form reset. It is a *labelable* element — wrap it in
     * a `<label>` (clicks on the label text toggle it and name it), or pass
     * `aria-label` / `aria-labelledby` on the host.
     */
    interface HTMLMdSwitchElement extends Components.MdSwitch, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdSwitchElementEventMap>(type: K, listener: (this: HTMLMdSwitchElement, ev: MdSwitchCustomEvent<HTMLMdSwitchElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdSwitchElementEventMap>(type: K, listener: (this: HTMLMdSwitchElement, ev: MdSwitchCustomEvent<HTMLMdSwitchElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdSwitchElement: {
        prototype: HTMLMdSwitchElement;
        new (): HTMLMdSwitchElement;
    };
    interface HTMLMdTabElementEventMap {
        "mdTabClick": void;
    }
    interface HTMLMdTabElement extends Components.MdTab, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTabElementEventMap>(type: K, listener: (this: HTMLMdTabElement, ev: MdTabCustomEvent<HTMLMdTabElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTabElementEventMap>(type: K, listener: (this: HTMLMdTabElement, ev: MdTabCustomEvent<HTMLMdTabElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTabElement: {
        prototype: HTMLMdTabElement;
        new (): HTMLMdTabElement;
    };
    /**
     * A single tab panel. Place inside `md-tab-panels`, one per tab, in tab
     * order. The parent manages `active`, `inert`, and the tabs↔panel ARIA
     * wiring; inactive panels stay in layout (hidden) so the region's size
     * never jumps between tabs.
     */
    interface HTMLMdTabPanelElement extends Components.MdTabPanel, HTMLStencilElement {
    }
    var HTMLMdTabPanelElement: {
        prototype: HTMLMdTabPanelElement;
        new (): HTMLMdTabPanelElement;
    };
    /**
     * Panel region for `md-tabs`: place directly AFTER the tablist (or point
     * `for` at its id) with one `md-tab-panel` per tab, in tab order.
     * - Shows the panel matching the tabs' `activeTabIndex`, following
     *   `mdTabChange` automatically.
     * - `sizing="stable"` (default): all panels stack in one grid cell and
     *   inactive ones stay in layout (hidden), so the region — and any
     *   shrink-to-fit card around it — keeps the SAME width/height no matter
     *   which tab is active. `sizing="active"` sizes to the active panel only.
     * - Wires the APG ARIA contract both ways: each tab gets `aria-controls`,
     *   each panel gets `aria-labelledby`, ids auto-assigned where missing.
     */
    interface HTMLMdTabPanelsElement extends Components.MdTabPanels, HTMLStencilElement {
    }
    var HTMLMdTabPanelsElement: {
        prototype: HTMLMdTabPanelsElement;
        new (): HTMLMdTabPanelsElement;
    };
    interface HTMLMdTableElementEventMap {
        "mdSortChange": MdTableSortState;
        "mdSelectionChange": MdTableSelectionState;
        "mdPinChange": { column: number; side: 'start' | 'end' | null };
        "mdColumnVisibilityChange": { column: number; visible: boolean; hidden: number[] };
        "mdScroll": { scrollLeft: number; scrollTop: number };
    }
    /**
     * Material Design 3 — Table
     * The core composable table primitive for hand-authored tables.
     * Layout architecture: the shadow `.md-table__grid` owns the column tracks
     * (`grid-template-columns`), and every slotted `md-table-row` is a REAL grid
     * item spanning all tracks that lays its cells on the shared tracks via
     * `grid-template-columns: subgrid`. Rows being real boxes is what makes row
     * hover, stripes, dividers, sticky headers and focus rings actually work —
     * and a wrong cell count skews one row, never the whole table.
     * Coordination is push-based (the house pattern): the table stamps context
     * onto its rows (`rowgroup`, stripe parity, stickiness) and pushes sort state
     * into `md-table-sort-label`s — no MutationObservers, no `:host-context()`.
     * High-level features:
     *   - Density: `compact` | `standard` | `comfortable`
     *   - Sticky header / footer (pair with `md-table-container` for the scroll box)
     *   - Sticky first / last column via cell `sticky="start" | "end"`
     *   - Sortable columns via `<md-table-sort-label>` (3-state asc → desc → none)
     *   - Selection coordination — emits `mdSelectionChange`; `selectAll()` /
     *     `deselectAll()` / `toggleSelectAll()` for the select-all checkbox
     *   - Horizontal scroll when the content is wider than the container
     *   - Loading + empty-state slots, striped rows, configurable dividers
     */
    interface HTMLMdTableElement extends Components.MdTable, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTableElementEventMap>(type: K, listener: (this: HTMLMdTableElement, ev: MdTableCustomEvent<HTMLMdTableElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTableElementEventMap>(type: K, listener: (this: HTMLMdTableElement, ev: MdTableCustomEvent<HTMLMdTableElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTableElement: {
        prototype: HTMLMdTableElement;
        new (): HTMLMdTableElement;
    };
    /**
     * Material Design 3 — Table Body (`<tbody>`).
     * Pure presentational rowgroup. Children flow into the parent grid via
     * `display: contents`.
     */
    interface HTMLMdTableBodyElement extends Components.MdTableBody, HTMLStencilElement {
    }
    var HTMLMdTableBodyElement: {
        prototype: HTMLMdTableBodyElement;
        new (): HTMLMdTableBodyElement;
    };
    /**
     * Material Design 3 — Table Cell.
     * The visual surface for a single grid cell. Renders as either a `cell`
     * (default) or `columnheader` / `rowheader` based on `head` / `scope`.
     * Props are intentionally MUI-compatible:
     *   - `align="start" | "center" | "end" | "justify"`
     *   - `numeric` (right-aligns + tabular numbers)
     *   - `padding="none" | "checkbox" | "default"`
     *   - `head` (renders as `<th>` semantics with `scope="col"|"row"`)
     *   - `sticky="start" | "end"` (sticky first / last column)
     *   - `colspan` / `rowspan` (grid spans)
     * The cell is a dumb surface: row state (selected / disabled / header
     * context) arrives as inherited CSS custom properties cast by
     * `<md-table-row>` — no observers, no DOM queries.
     */
    interface HTMLMdTableCellElement extends Components.MdTableCell, HTMLStencilElement {
    }
    var HTMLMdTableCellElement: {
        prototype: HTMLMdTableCellElement;
        new (): HTMLMdTableCellElement;
    };
    /**
     * Material Design 3 — Table Container
     * The outer surface that wraps a `<md-table>`, optional `<md-table-toolbar>`
     * and `<md-table-pagination>`. Provides elevation, shape, and the scroll
     * context (with optional max-height) for sticky headers / columns.
     * Compose freely:
     * ```html
     * <md-table-container variant="elevated" max-height="480px">
     *   <md-table-toolbar headline="Users"></md-table-toolbar>
     *   <md-table>…</md-table>
     *   <md-table-pagination></md-table-pagination>
     * </md-table-container>
     * ```
     */
    interface HTMLMdTableContainerElement extends Components.MdTableContainer, HTMLStencilElement {
    }
    var HTMLMdTableContainerElement: {
        prototype: HTMLMdTableContainerElement;
        new (): HTMLMdTableContainerElement;
    };
    /**
     * Material Design 3 — Table Expand Toggle.
     * The STANDARD expand caret for an expandable `<md-table-row>`: one consistent
     * size (xs / 32px — fits every density incl. compact rows, matching the 32px
     * checkbox precedent), a chevron that rotates 90° on the spatial spring when
     * the row opens, wired click→row.toggle() and aria-expanded out of the box.
     * Slot it INSIDE a cell of the expandable row (never as a bare row child —
     * rows are subgrid containers where any extra child would consume a column
     * track):
     * ```html
     * <md-table-row expandable>
     *   <md-table-cell padding="checkbox"><md-table-expand-toggle></md-table-expand-toggle></md-table-cell>
     *   <md-table-cell>…</md-table-cell>
     *   <div slot="expanded">Details…</div>
     * </md-table-row>
     * ```
     */
    interface HTMLMdTableExpandToggleElement extends Components.MdTableExpandToggle, HTMLStencilElement {
    }
    var HTMLMdTableExpandToggleElement: {
        prototype: HTMLMdTableExpandToggleElement;
        new (): HTMLMdTableExpandToggleElement;
    };
    /**
     * Material Design 3 — Table Foot (`<tfoot>`).
     * Pure presentational rowgroup for footer rows (totals, summaries).
     * Children flow into the parent grid via `display: contents`.
     */
    interface HTMLMdTableFootElement extends Components.MdTableFoot, HTMLStencilElement {
    }
    var HTMLMdTableFootElement: {
        prototype: HTMLMdTableFootElement;
        new (): HTMLMdTableFootElement;
    };
    /**
     * Material Design 3 — Table Head (`<thead>`).
     * Pure presentational rowgroup used to mark a region of column-header rows.
     * Applies the head background (sticky-friendly) and ensures children flow
     * into the parent `<md-table>`'s CSS Grid via `display: contents`.
     */
    interface HTMLMdTableHeadElement extends Components.MdTableHead, HTMLStencilElement {
    }
    var HTMLMdTableHeadElement: {
        prototype: HTMLMdTableHeadElement;
        new (): HTMLMdTableHeadElement;
    };
    interface HTMLMdTablePaginationElementEventMap {
        "mdPageChange": { page: number };
        "mdRowsPerPageChange": { rowsPerPage: number };
    }
    /**
     * Material Design 3 — Table Pagination.
     * Drop into a `<md-table-container slot="bottom">` for a complete data table.
     * Mirrors the MUI `<TablePagination>` API surface. Built from house
     * primitives: `md-icon-button` navigation and an `md-select` rows-per-page
     * picker (proper dismissal, keyboard support and theming for free).
     * Emits:
     *   - `mdPageChange` — when the page changes
     *   - `mdRowsPerPageChange` — when the rows-per-page selector changes
     */
    interface HTMLMdTablePaginationElement extends Components.MdTablePagination, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTablePaginationElementEventMap>(type: K, listener: (this: HTMLMdTablePaginationElement, ev: MdTablePaginationCustomEvent<HTMLMdTablePaginationElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTablePaginationElementEventMap>(type: K, listener: (this: HTMLMdTablePaginationElement, ev: MdTablePaginationCustomEvent<HTMLMdTablePaginationElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTablePaginationElement: {
        prototype: HTMLMdTablePaginationElement;
        new (): HTMLMdTablePaginationElement;
    };
    interface HTMLMdTableRowElementEventMap {
        "mdRowClick": { value: string; row: HTMLElement };
        "mdRowSelectionChange": { selected: boolean; value: string };
        "mdRowExpandedChange": { expanded: boolean };
    }
    /**
     * Material Design 3 — Table Row.
     * A row inside a `<md-table>` that contains `<md-table-cell>` children.
     * The host is a REAL grid box that spans the table's tracks and lays its
     * cells on them via `grid-template-columns: subgrid` — so row hover,
     * stripes, dividers, selection tint, sticky rows and focus rings all work
     * on the row itself, and a wrong cell count skews one row, never the table.
     * The parent `<md-table>` stamps context (`rowgroup`, stripe parity,
     * stickiness) onto the row; the row casts typography/color context down to
     * its cells with `::slotted()` custom properties — no observers.
     * Behaviors:
     * - `selected` — selection state (visual + ARIA)
     * - `disabled` — non-interactive
     * - `clickable` — adds keyboard activation (Enter / Space) + emits `mdRowClick`
     * - `expandable` — toggles an "expanded details" zone (use the `expanded` slot),
     *   spring-animated via the grid-rows height pattern
     */
    interface HTMLMdTableRowElement extends Components.MdTableRow, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTableRowElementEventMap>(type: K, listener: (this: HTMLMdTableRowElement, ev: MdTableRowCustomEvent<HTMLMdTableRowElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTableRowElementEventMap>(type: K, listener: (this: HTMLMdTableRowElement, ev: MdTableRowCustomEvent<HTMLMdTableRowElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTableRowElement: {
        prototype: HTMLMdTableRowElement;
        new (): HTMLMdTableRowElement;
    };
    interface HTMLMdTableSortLabelElementEventMap {
        "mdSortRequest": { column: string; defaultOrder: 'asc' | 'desc' };
    }
    /**
     * Material Design 3 — Table Sort Label.
     * A clickable header label with a sort indicator that reflects the current
     * sort state. Place inside a `<md-table-cell head>`.
     * Wiring:
     *   1. Set `column="<id>"`
     *   2. The parent `<md-table>` listens for `mdSortRequest` (auto-bubbled),
     *      updates its `sort-by` / `sort-order`, and PUSHES the resulting
     *      `active` / `order` back into every sort label — no observers.
     * The arrow is hidden at rest and fades in at 60% opacity on hover or keyboard
     * focus, so an unsorted column advertises sorting only when it is pointed at.
     * Once the column is the sort key the arrow stays fully opaque, tinted with the
     * active colour, and rotates 180° to point down for descending. The rotation
     * runs on the spatial spring, the colour/opacity on the effects spring.
     */
    interface HTMLMdTableSortLabelElement extends Components.MdTableSortLabel, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTableSortLabelElementEventMap>(type: K, listener: (this: HTMLMdTableSortLabelElement, ev: MdTableSortLabelCustomEvent<HTMLMdTableSortLabelElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTableSortLabelElementEventMap>(type: K, listener: (this: HTMLMdTableSortLabelElement, ev: MdTableSortLabelCustomEvent<HTMLMdTableSortLabelElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTableSortLabelElement: {
        prototype: HTMLMdTableSortLabelElement;
        new (): HTMLMdTableSortLabelElement;
    };
    /**
     * Material Design 3 — Table Toolbar.
     * The header bar above a `<md-table>` (typically dropped into a
     * `<md-table-container slot="top">`). Shows a title + supporting text and
     * a row of action items. Switches into a "selection" mode when one or more
     * rows are selected (matching the MUI / MD3 data-table pattern).
     * Two modes:
     *   - Default — shows the headline + supporting text + actions
     *   - Selection — shows "%count% selected" + selection-actions (when
     *     `num-selected > 0`)
     * The toolbar can listen to its closest `<md-table>`'s `mdSelectionChange`
     * event automatically (via `auto-bind`) — set `auto-bind` on the toolbar
     * and selection state will track the table without any glue code.
     */
    interface HTMLMdTableToolbarElement extends Components.MdTableToolbar, HTMLStencilElement {
    }
    var HTMLMdTableToolbarElement: {
        prototype: HTMLMdTableToolbarElement;
        new (): HTMLMdTableToolbarElement;
    };
    interface HTMLMdTabsElementEventMap {
        "mdTabChange": { index: number; previousIndex: number };
    }
    interface HTMLMdTabsElement extends Components.MdTabs, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTabsElementEventMap>(type: K, listener: (this: HTMLMdTabsElement, ev: MdTabsCustomEvent<HTMLMdTabsElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTabsElementEventMap>(type: K, listener: (this: HTMLMdTabsElement, ev: MdTabsCustomEvent<HTMLMdTabsElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTabsElement: {
        prototype: HTMLMdTabsElement;
        new (): HTMLMdTabsElement;
    };
    interface HTMLMdTextFieldElementEventMap {
        "mdInput": string;
        "mdChange": string;
        "mdSearch": string;
        "mdClear": void;
        "mdPasswordToggle": { visible: boolean };
        "mdSpeechResult": { transcript: string; listening: boolean };
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    interface HTMLMdTextFieldElement extends Components.MdTextField, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTextFieldElementEventMap>(type: K, listener: (this: HTMLMdTextFieldElement, ev: MdTextFieldCustomEvent<HTMLMdTextFieldElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTextFieldElementEventMap>(type: K, listener: (this: HTMLMdTextFieldElement, ev: MdTextFieldCustomEvent<HTMLMdTextFieldElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTextFieldElement: {
        prototype: HTMLMdTextFieldElement;
        new (): HTMLMdTextFieldElement;
    };
    interface HTMLMdTimePickerElementEventMap {
        "mdChange": MdTimePickerChangeDetail;
        "mdInput": MdTimePickerChangeDetail;
        "mdOpen": void;
        "mdClose": void;
        "mdCancel": void;
        "mdModeChange": MdTimePickerModeChangeDetail;
        "mdValidityChange": {
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  };
    }
    interface HTMLMdTimePickerElement extends Components.MdTimePicker, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTimePickerElementEventMap>(type: K, listener: (this: HTMLMdTimePickerElement, ev: MdTimePickerCustomEvent<HTMLMdTimePickerElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTimePickerElementEventMap>(type: K, listener: (this: HTMLMdTimePickerElement, ev: MdTimePickerCustomEvent<HTMLMdTimePickerElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTimePickerElement: {
        prototype: HTMLMdTimePickerElement;
        new (): HTMLMdTimePickerElement;
    };
    /**
     * The toolbar host has no built-in actions — all behavior comes from slotted controls.
     * On the web, the default is `role="toolbar"` implementing the WAI-ARIA toolbar pattern
     * with an ADAPTIVE roving tabindex:
     *  - **Activator buttons** (`md-icon-button`, `md-button`, native `button`/`a[href]`,
     *    `role=button`/`menuitem`/`link`) form a single roving group — one Tab stop, and
     *    ArrowLeft/Right (or Up/Down when floating+vertical), Home, End move focus between them.
     *  - **Everything else** is left as its OWN Tab stop with its native behavior — text-entry /
     *    value controls (input, textarea, contenteditable, md-text-field, md-select, `select`,
     *    range/spinbutton), ALL toggles (`md-switch`/`md-checkbox`/`md-chip`, native
     *    checkbox/radio, role=checkbox/radio), and self-managing composites (md-button-group,
     *    md-menu, role=group/radiogroup/listbox/menu). Arrows are never stolen from them, and
     *    they never become roving dead-ends.
     * A soft-disabled (`aria-disabled`) group member stays arrow-reachable (APG: disabled
     * items remain focusable for discoverability). The default Tab stop prefers an enabled
     * control, falling back to the first member only when every member is soft-disabled;
     * focusing any member makes it the stop.
     * This keeps every control keyboard-reachable regardless of the mix, per APG guidance for
     * toolbars that contain text fields. Use `generic` for a non-semantic container (no role,
     * no roving).
     */
    interface HTMLMdToolbarElement extends Components.MdToolbar, HTMLStencilElement {
    }
    var HTMLMdToolbarElement: {
        prototype: HTMLMdToolbarElement;
        new (): HTMLMdToolbarElement;
    };
    interface HTMLMdTooltipElementEventMap {
        "mdOpen": void;
        "mdClose": void;
    }
    interface HTMLMdTooltipElement extends Components.MdTooltip, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTooltipElementEventMap>(type: K, listener: (this: HTMLMdTooltipElement, ev: MdTooltipCustomEvent<HTMLMdTooltipElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTooltipElementEventMap>(type: K, listener: (this: HTMLMdTooltipElement, ev: MdTooltipCustomEvent<HTMLMdTooltipElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTooltipElement: {
        prototype: HTMLMdTooltipElement;
        new (): HTMLMdTooltipElement;
    };
    interface HTMLMdTransferListElementEventMap {
        "mdChange": string[];
        "mdMove": {
    direction: 'left' | 'right';
    moved: string[];
    target: string[];
  };
    }
    /**
     * `md-transfer-list` — Material Design 3 dual-list selector.
     * Reference parity: https://mui.com/material-ui/react-transfer-list/.
     * Two columns ("source" and "target") with header chips that show the
     * "{n} / {total} selected" count, a search field per side, paired
     * `md-checkbox` rows, and four `md-icon-button` movers in the centre
     * column. Selection inside each side is independent of which side a
     * card lives in; the move buttons only act on items that are both
     * selected AND visible (i.e. matching the search filter).
     * MD3 Expressive: containers are `surface-container-low`, the headers
     * use the `secondary-container` tone for emphasis when the title slot
     * is populated, and movements run on the `motion-emphasized` curve.
     */
    interface HTMLMdTransferListElement extends Components.MdTransferList, HTMLStencilElement {
        addEventListener<K extends keyof HTMLMdTransferListElementEventMap>(type: K, listener: (this: HTMLMdTransferListElement, ev: MdTransferListCustomEvent<HTMLMdTransferListElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLMdTransferListElementEventMap>(type: K, listener: (this: HTMLMdTransferListElement, ev: MdTransferListCustomEvent<HTMLMdTransferListElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLMdTransferListElement: {
        prototype: HTMLMdTransferListElement;
        new (): HTMLMdTransferListElement;
    };
    interface HTMLElementTagNameMap {
        "md-accordion": HTMLMdAccordionElement;
        "md-accordion-item": HTMLMdAccordionItemElement;
        "md-app-bar": HTMLMdAppBarElement;
        "md-area-chart": HTMLMdAreaChartElement;
        "md-autocomplete": HTMLMdAutocompleteElement;
        "md-avatar": HTMLMdAvatarElement;
        "md-badge": HTMLMdBadgeElement;
        "md-bar-chart": HTMLMdBarChartElement;
        "md-bottom-sheet": HTMLMdBottomSheetElement;
        "md-breadcrumb-item": HTMLMdBreadcrumbItemElement;
        "md-breadcrumbs": HTMLMdBreadcrumbsElement;
        "md-button": HTMLMdButtonElement;
        "md-button-group": HTMLMdButtonGroupElement;
        "md-card": HTMLMdCardElement;
        "md-checkbox": HTMLMdCheckboxElement;
        "md-chip": HTMLMdChipElement;
        "md-color-picker": HTMLMdColorPickerElement;
        "md-date-picker": HTMLMdDatePickerElement;
        "md-dialog": HTMLMdDialogElement;
        "md-divider": HTMLMdDividerElement;
        "md-fab": HTMLMdFabElement;
        "md-fab-menu": HTMLMdFabMenuElement;
        "md-fab-menu-item": HTMLMdFabMenuItemElement;
        "md-icon-button": HTMLMdIconButtonElement;
        "md-line-chart": HTMLMdLineChartElement;
        "md-list": HTMLMdListElement;
        "md-list-item": HTMLMdListItemElement;
        "md-loading-indicator": HTMLMdLoadingIndicatorElement;
        "md-menu": HTMLMdMenuElement;
        "md-menu-item": HTMLMdMenuItemElement;
        "md-menu-item-group": HTMLMdMenuItemGroupElement;
        "md-meter": HTMLMdMeterElement;
        "md-multi-select": HTMLMdMultiSelectElement;
        "md-navigation-bar": HTMLMdNavigationBarElement;
        "md-navigation-rail": HTMLMdNavigationRailElement;
        "md-navigation-rail-tab": HTMLMdNavigationRailTabElement;
        "md-navigation-tab": HTMLMdNavigationTabElement;
        "md-number-field": HTMLMdNumberFieldElement;
        "md-organization-chart": HTMLMdOrganizationChartElement;
        "md-otp-field": HTMLMdOtpFieldElement;
        "md-pie-chart": HTMLMdPieChartElement;
        "md-progress-indicator": HTMLMdProgressIndicatorElement;
        "md-radio": HTMLMdRadioElement;
        "md-rating": HTMLMdRatingElement;
        "md-ripple": HTMLMdRippleElement;
        "md-search": HTMLMdSearchElement;
        "md-segmented-button": HTMLMdSegmentedButtonElement;
        "md-segmented-button-set": HTMLMdSegmentedButtonSetElement;
        "md-select": HTMLMdSelectElement;
        "md-select-option": HTMLMdSelectOptionElement;
        "md-side-sheet": HTMLMdSideSheetElement;
        "md-skeleton": HTMLMdSkeletonElement;
        "md-slider": HTMLMdSliderElement;
        "md-snackbar": HTMLMdSnackbarElement;
        "md-sparkline": HTMLMdSparklineElement;
        "md-split-button": HTMLMdSplitButtonElement;
        "md-status-dot": HTMLMdStatusDotElement;
        "md-step": HTMLMdStepElement;
        "md-stepper": HTMLMdStepperElement;
        "md-sub-menu-item": HTMLMdSubMenuItemElement;
        "md-switch": HTMLMdSwitchElement;
        "md-tab": HTMLMdTabElement;
        "md-tab-panel": HTMLMdTabPanelElement;
        "md-tab-panels": HTMLMdTabPanelsElement;
        "md-table": HTMLMdTableElement;
        "md-table-body": HTMLMdTableBodyElement;
        "md-table-cell": HTMLMdTableCellElement;
        "md-table-container": HTMLMdTableContainerElement;
        "md-table-expand-toggle": HTMLMdTableExpandToggleElement;
        "md-table-foot": HTMLMdTableFootElement;
        "md-table-head": HTMLMdTableHeadElement;
        "md-table-pagination": HTMLMdTablePaginationElement;
        "md-table-row": HTMLMdTableRowElement;
        "md-table-sort-label": HTMLMdTableSortLabelElement;
        "md-table-toolbar": HTMLMdTableToolbarElement;
        "md-tabs": HTMLMdTabsElement;
        "md-text-field": HTMLMdTextFieldElement;
        "md-time-picker": HTMLMdTimePickerElement;
        "md-toolbar": HTMLMdToolbarElement;
        "md-tooltip": HTMLMdTooltipElement;
        "md-transfer-list": HTMLMdTransferListElement;
    }
}
declare namespace LocalJSX {
    interface MdAccordion {
        /**
          * When a floating accordion starts being dragged, bring it to the front by bumping `z-index`. Disable if you manage stacking yourself.
          * @default true
         */
        "bringToFront"?: boolean;
        /**
          * Comma-separated list of item indexes that should be expanded on first render. Items with `expanded` set in their own attribute also count.
          * @default ''
         */
        "defaultExpanded"?: string;
        /**
          * Density scale: `0` (default, 56 dp headers), `-1` (48 dp), `-2` (40 dp).
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * MD3 elevation level applied to the surface — `0` (default, flat) through `5` (highest). In the `filled` variant each item gets its own shadow; in the `outlined` variant the shadow is drawn on the shared chassis. Expanded items lift to the next elevation level for an expressive "lift off the page" affordance.
          * @default 0
         */
        "elevation"?: 0 | 1 | 2 | 3 | 4 | 5;
        /**
          * Whether only one item may be expanded at a time. When `true`, expanding an item collapses any other expanded item.
          * @default false
         */
        "exclusive"?: boolean;
        /**
          * Render the accordion as a free-floating panel that can be dragged anywhere on the screen via the top drag-bar. The panel uses `position: fixed` and translates via CSS transform, so it lifts out of normal document flow.
          * @default false
         */
        "floating"?: boolean;
        /**
          * ARIA heading level for each item's heading wrapper (`<h1>`–`<h6>`). Pick the value matching the page's information architecture — e.g. `2` inside a top-level page section, `4` deep inside a card. APG: "aria-level that is appropriate for the information architecture of the page". Default `3`.
          * @default 3
         */
        "headingLevel"?: 1 | 2 | 3 | 4 | 5 | 6;
        /**
          * Initial X translation (px) for floating panels — measured in PHYSICAL viewport coordinates from the top-left corner of the viewport, so a positive value always moves the panel to the right regardless of writing direction (LTR or RTL). Default: 24.
          * @default 24
         */
        "initialX"?: number;
        /**
          * Initial Y translation (px) for floating panels — measured in physical viewport coordinates from the top-left corner. Default: 24.
          * @default 24
         */
        "initialY"?: number;
        /**
          * Require at least one item to remain expanded. APG variant: the implementation does not permit the currently-open panel to be collapsed when doing so would leave zero panels open. The locked- open button advertises `aria-disabled="true"`. Pairs naturally with `exclusive` to enforce "exactly one panel is always open".
          * @default false
         */
        "keepOneExpanded"?: boolean;
        /**
          * Emitted when a floating panel is released.
         */
        "onMdDragEnd"?: (event: MdAccordionCustomEvent<MdAccordionDragDetail>) => void;
        /**
          * Emitted continuously while a floating panel is being dragged.
         */
        "onMdDragMove"?: (event: MdAccordionCustomEvent<MdAccordionDragDetail>) => void;
        /**
          * Emitted once a floating panel starts being dragged.
         */
        "onMdDragStart"?: (event: MdAccordionCustomEvent<MdAccordionDragDetail>) => void;
        /**
          * Emitted when items are reordered via drag-and-drop or keyboard (Alt+ArrowUp / Alt+ArrowDown). `order` is the new index sequence relative to the original order, e.g. moving the item that was at index 0 to index 2 yields `order: [1, 2, 0]`.
         */
        "onMdReorder"?: (event: MdAccordionCustomEvent<{
    from: number;
    to: number;
    order: number[];
  }>) => void;
        /**
          * Emitted when an item expands or collapses. Detail carries the full set of expanded indices.
         */
        "onMdToggle"?: (event: MdAccordionCustomEvent<{
    index: number;
    expanded: boolean;
    expandedIndices: number[];
  }>) => void;
        /**
          * Region-role policy for panels: - `auto` (default) — applies `role="region"` only when the item   count is at or below `region-threshold`, otherwise downgrades   to `role="group"` to avoid landmark proliferation (per APG   recommendation for accordions with more than ~6 panels). - `always` — every panel is a landmark region. - `never` — no panel carries a region role (still labelled via   `aria-labelledby` though).
          * @default 'auto'
         */
        "region"?: 'auto' | 'always' | 'never';
        /**
          * Threshold above which `region="auto"` downgrades panel roles from `region` to `group`. APG suggests ~6.
          * @default 6
         */
        "regionThreshold"?: number;
        /**
          * Allow drag-to-reorder of items via the leading drag handles. Items can also be reordered with `Alt + ArrowUp / ArrowDown` while the header is focused. Disabled items can't be moved.
          * @default false
         */
        "reorderable"?: boolean;
        /**
          * Motion preset applied when items expand and collapse. The prop is reflected so the item CSS can pick it up via `:host-context(md-accordion[transition="…"])`.  - `expressive` (default) — full MD3 Expressive choreography:    asymmetric easing (decelerate-on-open, accelerate-on-close),    spring-spatial chevron rotation, container shape & elevation    morph, leading-icon FILL-axis swap. Use for hero surfaces,    settings shells, anything that wants to feel premium. - `standard` — MD3 standard easing (symmetric `emphasized`) with    no spring overshoot. Container morph still happens. The    "neutral" preset — good default for dense product UI. - `fade` — content fades in/out alongside the height animation    using shorter `standard` easing. Chevron rotates without a    spring. Best for content-heavy panels (forms, long copy)    where the motion should be unobtrusive. - `collapse` — pure height collapse with simple `ease-in-out`    timing — no spring, no morph, no fade. The classic    "browser disclosure" look. - `none` — disables every transition (instant snap). Useful for    automated tests, screenshot harnesses, or environments where    motion is undesirable.
          * @default 'expressive'
         */
        "transition"?: 'expressive' | 'standard' | 'fade' | 'collapse' | 'none';
        /**
          * Visual variant. Both share the same expand/collapse mechanics. - `filled` (default) — each item is a tonal surface tile. - `outlined` — items share a single rounded chassis with an outline.
          * @default 'filled'
         */
        "variant"?: 'filled' | 'outlined';
    }
    interface MdAccordionItem {
        /**
          * Whether the item can be collapsed when it is expanded. Set to `false` to implement the APG variant where the accordion does not permit a panel to be collapsed — the open button then advertises `aria-disabled="true"`, and Enter / Space / click no longer toggle it. Typically managed by the parent's `keep-one-expanded` mode.
          * @default true
         */
        "collapsible"?: boolean;
        /**
          * Cap the panel body's block-size (e.g. `"220px"`, `"40vh"`). When set, the content region becomes a scroll container instead of growing to fit — and, crucially, it is made keyboard-focusable (`tabindex="0"`) so keyboard-only users can scroll it. A scrollable region whose content isn't itself focusable is otherwise unreachable without a pointer (WCAG 2.1.1 — axe `scrollable-region-focusable`). Its accessible name comes from the enclosing labelled panel region.  Prefer this over hand-rolling a slotted `overflow:auto` wrapper: an authored scroll `<div>` lives in light DOM the component can't make focusable, so it would trip the same axe rule. Leave empty (default) for a panel that grows to fit its content.
          * @default ''
         */
        "contentMaxHeight"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable interaction.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Whether the item is expanded. Two-way bindable.
          * @default false
         */
        "expanded"?: boolean;
        /**
          * ARIA heading level (`aria-level`) for the item's heading wrapper. Renders a native `<h1>`–`<h6>` so the implicit semantics match the page's information architecture. Per WAI-ARIA APG: "an element with role heading that has a value set for aria-level that is appropriate for the information architecture of the page".  Set per-item, or let the parent `<md-accordion heading-level>` propagate the same value to every item.
          * @default 3
         */
        "headingLevel"?: 1 | 2 | 3 | 4 | 5 | 6;
        /**
          * Header text (or use the `headline` slot).
          * @default ''
         */
        "headline"?: string;
        /**
          * Optional leading-icon (Material Symbols shorthand).
          * @default ''
         */
        "icon"?: string;
        /**
          * Internal event the parent listens to for arrow-key roving focus. Bubbles + composed so the parent receives it through the shadow.
         */
        "onMdItemRequestFocus"?: (event: MdAccordionItemCustomEvent<{
    direction: 'next' | 'prev' | 'first' | 'last';
    from: number;
  }>) => void;
        /**
          * Internal event the parent listens to for Alt+Arrow keyboard reordering. Bubbles + composed so the parent receives it through the shadow.
         */
        "onMdItemRequestReorder"?: (event: MdAccordionItemCustomEvent<{
    direction: 'up' | 'down';
    from: number;
  }>) => void;
        /**
          * Emitted whenever the item toggles. The parent `md-accordion` uses this to coordinate exclusive mode.
         */
        "onMdItemToggle"?: (event: MdAccordionItemCustomEvent<{ expanded: boolean; index: number }>) => void;
        /**
          * Role applied to the accordion panel. APG recommends `region` for structural clarity, but warns against using it when more than ~6 panels can be open at once (landmark proliferation). The parent `<md-accordion region>` orchestrates this automatically.  - `region` (default) — full landmark semantics. - `group` — semantically a group but not a landmark. - `none` — drop the role entirely (still labelled via aria-labelledby).
          * @default 'region'
         */
        "regionRole"?: | 'region'
    | 'group'
    | 'none';
        /**
          * Optional small caption below the headline.
          * @default ''
         */
        "supportingText"?: string;
    }
    /**
     * `md-app-bar` — Material Design 3 App Bar.
     * Implements the current (M3 Expressive) app-bars specification:
     *   https://m3.material.io/components/app-bars/specs
     * Size variants: `small` (64px), `medium` (112/136px expanded), `large`
     * (120/152px expanded), and `search` (inline search field in the 64px row).
     * Flexible `medium` / `large` bars always render the expanded headline block.
     * Title alignment (`title-alignment`) is configuration, not a variant.
     * Selecting the search field should open an `md-search` view via `mdSearchActivate`.
     */
    interface MdAppBar {
        /**
          * Density scale: 0 (default 64px row), -1 (60px), -2 (56px), -3 (52px), -4 (48px). Tightens the action row, icon targets, search field, and expanded heights.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Inline or expanded headline text. Falls back to the `headline` slot when empty.
          * @default ''
         */
        "headline"?: string;
        /**
          * Material Symbols name for the leading navigation icon (slot takes priority).
          * @default ''
         */
        "leadingIcon"?: string;
        /**
          * Accessible label for the prop-based leading icon button. Required when `leading-icon` is set.
          * @default ''
         */
        "leadingIconLabel"?: string;
        /**
          * Emitted when the prop-based leading icon button is activated.
         */
        "onMdLeadingClick"?: (event: MdAppBarCustomEvent<MouseEvent>) => void;
        /**
          * Search variant only: emitted when the inline search field is selected (click, focus, Enter, or Space). Wire this to `md-search.show()` to open the full-screen search view per the MD3 spec.
         */
        "onMdSearchActivate"?: (event: MdAppBarCustomEvent<{ value: string }>) => void;
        /**
          * Search variant only: emitted on every inline search field input change.
         */
        "onMdSearchInput"?: (event: MdAppBarCustomEvent<{ value: string }>) => void;
        /**
          * When true, applies the scrolled container surface colour per M3 app bar common-colors (`surface` → `surface-container`). Wire from your scroll listener; the component does not observe scroll position itself.
          * @default false
         */
        "scrolled"?: boolean;
        /**
          * Search variant only: accessible name for the inline search field. Per M3 labeling guidance, falls back to `search-placeholder` when unset.
          * @default ''
         */
        "searchAriaLabel"?: string;
        /**
          * Search variant only: disable the inline search field.
          * @default false
         */
        "searchDisabled"?: boolean;
        /**
          * Search variant only: hinted search text shown in the inline search field. Falls back to `placeholder` on the optional `search` slot when empty.
          * @default 'Search'
         */
        "searchPlaceholder"?: string;
        /**
          * Search variant only: current search field value (two-way bindable).
          * @default ''
         */
        "searchValue"?: string;
        /**
          * Optional subtitle. On flexible variants it sits below the expanded headline (and grows the container); on `small` it stacks under the inline title. Falls back to the `subtitle` slot when empty.
          * @default ''
         */
        "subtitle"?: string;
        /**
          * Title horizontal alignment — `start` (leading edge, default) or `center`. On search bars it centers the placeholder when empty and unfocused; once focused or when typing, the caret and text align to the inline-start edge.
          * @default 'start'
         */
        "titleAlignment"?: 'start' | 'center';
        /**
          * App bar size variant.  - `small` — 64px action row with inline title-large headline. - `medium` — flexible 112px (136px with subtitle) expanded headline block. - `large` — flexible 120px (152px with subtitle) expanded headline block. - `search` — 64px row with inline search field (M3 search app bar).
          * @default 'small'
         */
        "variant"?: 'small' | 'medium' | 'large' | 'search';
    }
    /**
     * md-area-chart — Material Design 3 area / streamgraph chart,
     * rendered by the in-house engine (Canvas2D + a DOM text
     * overlay). No ECharts.
     * Differences from `md-line-chart`:
     *   • the line is always filled with a gradient
     *   • stacking defaults to `'normal'` (areas stack additively)
     *   • supports the `silhouette` streamgraph baseline
     *   • markers are off by default
     */
    interface MdAreaChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation"?: MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Draw small perpendicular tick marks on the axes.
          * @default false
         */
        "axisTicks"?: boolean;
        /**
          * @default false
         */
        "connectNulls"?: boolean;
        /**
          * @default 'smooth'
         */
        "curve"?: 'linear' | 'smooth' | 'monotone' | 'step' | 'step-before' | 'step-middle';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Fill opacity for the gradient (0..1).
          * @default 0.55
         */
        "fillOpacity"?: number;
        /**
          * Gridlines: horizontal (y ticks), vertical (x ticks), both, or none.
          * @default 'horizontal'
         */
        "grid"?: 'none' | 'horizontal' | 'vertical' | 'both';
        "heightProp"?: string;
        /**
          * Transpose the axes: the category / time axis runs down the side and values run across the bottom. Suits long category names (they read horizontally instead of rotated) and quantities naturally read as depth — altitude, ocean depth, a drill core. `stack` applies as usual, along the value axis.
          * @default false
         */
        "inverted"?: boolean;
        /**
          * @default ''
         */
        "label"?: string;
        /**
          * Message shown when `series` is empty. The `empty` slot overrides it.
          * @default 'No data to display'
         */
        "labelEmpty"?: string;
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the data with the arrow keys; this is what tells them so.
          * @default 'Chart data. Use the arrow keys to move between points, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot"?: string;
        /**
          * Template for the live announcement as keyboard focus moves. `%x%` is the axis value, `%values%` the series readings at it.
          * @default '%x%: %values%'
         */
        "labelPoint"?: string;
        /**
          * Accessible name for the zoom slider's end thumb.
          * @default 'Zoom range end'
         */
        "labelZoomEnd"?: string;
        /**
          * Accessible name for the zoom slider's start thumb.
          * @default 'Zoom range start'
         */
        "labelZoomStart"?: string;
        /**
          * @default 'top-end'
         */
        "legend"?: MdChartLegendPosition | 'none';
        /**
          * Line stroke width in px, for every series. Per-series `series[].lineWidth` wins.
         */
        "lineWidth"?: number;
        /**
          * Show the loading overlay instead of the plot.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Text (and the spinner's accessible name) for the loading overlay.
          * @default 'Loading chart…'
         */
        "loadingLabel"?: string;
        /**
          * BCP-47 locale for the DEFAULT number / date formatting (axis ticks, tooltip values, screen-reader table). Empty follows the browser; an explicit `valueFormatter` always wins.
          * @default ''
         */
        "locale"?: string;
        /**
          * Marker RADIUS in px, for every series. Per-series `series[].markSize` wins.
         */
        "markSize"?: number;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation"?: boolean;
        /**
          * Fires when a series' filled area is clicked — the band between its line and its base (its own band when stacked), excluding its line and points.
         */
        "onMdAreaClick"?: (event: MdAreaChartCustomEvent<MdChartClickDetail<MdChartSeries>>) => void;
        /**
          * Fires when the plot background is clicked (inside the plot, but not on a mark, line or area): the nearest x plus every visible series' value there.
         */
        "onMdAxisClick"?: (event: MdAreaChartCustomEvent<MdChartAxisClickDetail>) => void;
        "onMdHover"?: (event: MdAreaChartCustomEvent<MdChartHoverDetail>) => void;
        "onMdLegendClick"?: (event: MdAreaChartCustomEvent<{ seriesIndex: number; seriesId?: string; selected: boolean }>) => void;
        /**
          * Fires when a series' drawn line is clicked *between* its data points (a click on a point emits `mdMarkerClick` instead).
         */
        "onMdLineClick"?: (event: MdAreaChartCustomEvent<MdChartClickDetail<MdChartSeries>>) => void;
        "onMdMarkerClick"?: (event: MdAreaChartCustomEvent<MdChartClickDetail<MdChartSeries>>) => void;
        "onMdReady"?: (event: MdAreaChartCustomEvent<void>) => void;
        /**
          * Fires when the zoom window changes (drag, slider or `setZoom`/`resetZoom`).
         */
        "onMdZoom"?: (event: MdAreaChartCustomEvent<{ startIndex: number; endIndex: number; reset: boolean }>) => void;
        /**
          * @default []
         */
        "series"?: MdChartSeries[];
        /**
          * Label each series at its last point, so the name follows the end of the band — for racing / progression charts.
          * @default false
         */
        "seriesLabels"?: boolean;
        /**
          * Print each point's value as a data label beside its marker.
          * @default false
         */
        "showLabels"?: boolean;
        /**
          * Draw the line on top of each area's fill. Default `true`. Set `false` for a fill-only area — useful when `fillOpacity` is high (a fully-opaque fill is the same colour as the line, so the line reads as part of it). A per-series `series[].stroke` still wins over this.
          * @default true
         */
        "showLine"?: boolean;
        /**
          * @default false
         */
        "showMarks"?: boolean;
        /**
          * Default stacking is `'normal'` (additive).
          * @default 'normal'
         */
        "stack"?: MdChartStackMode;
        /**
          * Sub-title, drawn under the title in the muted text colour.
         */
        "subtitle"?: string | undefined;
        /**
          * Replaces the generated `aria-label` outright. The default summary is assembled in English ("Traffic, Area chart, with 3 series, from Jan to Dec."); rather than translate it piecewise, hand over the whole sentence built in your own language.
          * @default ''
         */
        "summary"?: string;
        /**
          * Translatable chrome for the screen-reader data table. `%shown%` and `%total%` are substituted in `truncated`.
         */
        "tableLabels"?: { x?: string; index?: string; series?: string; truncated?: string };
        /**
          * Title alignment over the plot: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign"?: MdChartTitleAlign;
        /**
          * @default 'axis'
         */
        "tooltip"?: MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content. See `md-line-chart`'s `tooltipRenderer`.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        "valueFormatter"?: (value: number | null | undefined) => string;
        "xAxis"?: MdChartAxis | undefined;
        "yAxis"?: MdChartAxis | undefined;
        /**
          * @default 'none'
         */
        "zoom"?: 'none' | 'inside' | 'slider' | 'both';
    }
    /**
     * `md-autocomplete` — Material Design 3 combobox: a text field that filters
     * a dropdown listbox **as you type in the input** (no separate search row),
     * with single or multiple (chips) selection.
     * Built from the same primitives as `md-select` / `md-multi-select`:
     *   - the shared option model (`options` array of `{ value, label,
     *     supportingText?, icon?, iconColor?, disabled? }` or slotted
     *     `<md-select-option>` children),
     *   - `md-text-field` for the trigger (typing filters live),
     *   - `md-menu` as a WAI-ARIA listbox popup (options carry `aria-selected`),
     *   - `VirtualSelectController` for WASM-virtualized huge datasets
     *     (`virtualize`, `loadOptions()`, `filter-mode`) — the same engine as
     *     the selects, so tens of thousands of rows filter smoothly.
     * Keyboard: typing filters; ArrowDown moves focus into the listbox (the
     * focus-moves-into-popup combobox variant — option focus is announced
     * directly); Enter selects; Escape closes back to the input; Backspace on an
     * empty multi input removes the last chip.
     * ARIA: the real textbox — the `<input>` inside md-text-field's shadow root —
     * is promoted to `role="combobox"` with `aria-expanded` / `aria-autocomplete`
     * (handed over via `input-role`, since only that component renders the input),
     * and points at this component's listbox through ARIA element reflection. An
     * IDREF could not: it would have to resolve inside md-text-field's tree, where
     * the listbox is not. See `syncComboboxRefs`.
     * Extras kept from the surface this API mirrors: `free-solo` (commit arbitrary text),
     * `max-selected`, `clearable`, `disable-close-on-select`, `clear-on-blur`,
     * a custom `filterer` and `limit-results` (client-side path only).
     */
    interface MdAutocomplete {
        /**
          * Where the selection chips live (`multiple` only): around the field (`'below'` default, `'top'`, `'left'`, `'right'`) or `'inline'` inside it, before the input, wrapping onto new rows as the selection grows (the field grows with them).
          * @default 'below'
         */
        "chipPosition"?: | 'below'
    | 'top'
    | 'left'
    | 'right'
    | 'inline';
        /**
          * Material Symbols glyph for the clear button.
          * @default 'close'
         */
        "clearIcon"?: string;
        /**
          * Clear a non-matching input on blur (strict single mode only).
          * @default false
         */
        "clearOnBlur"?: boolean;
        /**
          * Show a clear (×) button on the trailing edge.
          * @default true
         */
        "clearable"?: boolean;
        /**
          * Density forwarded to the text-field.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Keep the menu open after a selection in **single** mode.  `multiple` already stays open — picking several in a row is the point, and it dismisses on an outside click, on the trigger, or on Escape. This prop gives a single-select the same stickiness when a picker is being used to scan rather than to commit.
          * @default false
         */
        "disableCloseOnSelect"?: boolean;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Caret glyph; rotates 180° when open (matches md-select). Slot `dropdown-icon` overrides it.
          * @default 'arrow_drop_down'
         */
        "dropdownIcon"?: string;
        /**
          * Error state — forwarded to the text-field.
          * @default false
         */
        "error"?: boolean;
        /**
          * Error text rendered in place of supporting text when `error` is true.
          * @default ''
         */
        "errorText"?: string;
        /**
          * Filter strategy for the virtualized (WASM) path.
          * @default 'substring'
         */
        "filterMode"?: FilterMode;
        /**
          * Custom client-side filter (ignored while virtualized — the WASM engine filters by `filter-mode` instead).
         */
        "filterer"?: (
    options: SelectOptionData[],
    state: { inputValue: string; selected: string[] },
  ) => SelectOptionData[];
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Allow committing arbitrary text (not just options) with Enter.
          * @default false
         */
        "freeSolo"?: boolean;
        /**
          * Live text of the input (separate from `value` so typing never commits).
          * @default ''
         */
        "inputValue"?: string;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label"?: string;
        /**
          * Max items shown in the dropdown (`0` = unlimited; client-side path only).
          * @default 0
         */
        "limitResults"?: number;
        /**
          * Busy state: progress bar in the menu while an async dataset loads.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Text shown while `loading` is true.
          * @default 'Loading…'
         */
        "loadingText"?: string;
        /**
          * Match the dropdown width to the trigger.
          * @default true
         */
        "matchTriggerWidth"?: boolean;
        /**
          * Max height of the dropdown menu (px).
         */
        "maxHeight"?: number;
        /**
          * When `multiple`, the max number of selected items (`0` = no limit).
          * @default 0
         */
        "maxSelected"?: number;
        /**
          * Multi-select mode — the selection renders as removable chips.
          * @default false
         */
        "multiple"?: boolean;
        /**
          * Form name. Multi-mode submits as repeated `name=value` pairs.
          * @default ''
         */
        "name"?: string;
        /**
          * Empty-state text when the dataset has no options at all.
          * @default 'No options'
         */
        "noOptionsText"?: string;
        /**
          * Empty-state text when the typed filter matches nothing.
          * @default 'No results'
         */
        "noResultsText"?: string;
        /**
          * Fires whenever the committed selection changes.
         */
        "onMdChange"?: (event: MdAutocompleteCustomEvent<string | string[]>) => void;
        /**
          * Fires when the user clears the value.
         */
        "onMdClear"?: (event: MdAutocompleteCustomEvent<void>) => void;
        /**
          * Fires when the menu closes.
         */
        "onMdClose"?: (event: MdAutocompleteCustomEvent<void>) => void;
        /**
          * Fires whenever the live input string changes.
         */
        "onMdInput"?: (event: MdAutocompleteCustomEvent<string>) => void;
        /**
          * Fires when the menu opens.
         */
        "onMdOpen"?: (event: MdAutocompleteCustomEvent<void>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdAutocompleteCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Open state.
          * @default false
         */
        "open"?: boolean;
        /**
          * Programmatic options (shared select model; `description` is accepted as a legacy alias for `supportingText`). Slotted `<md-select-option>` children take precedence when present.  Accepts either the array (property) or a **JSON string** (attribute), the same contract `md-organization-chart.nodes` uses — so a plain-HTML page can declare its dataset inline instead of reaching for a script:      <md-autocomplete options='[{"value":"a","label":"Apple"}]'></md-autocomplete>  Malformed JSON degrades to an empty list rather than throwing: a broken attribute must not take the whole field down.
          * @default []
         */
        "options"?: MdAutocompleteOption[] | string;
        /**
          * Placeholder for the input.
          * @default ''
         */
        "placeholder"?: string;
        /**
          * Menu placement.
          * @default 'bottom-start'
         */
        "placement"?: 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        /**
          * Required for native form parity.
          * @default false
         */
        "required"?: boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace"?: boolean;
        /**
          * Fixed virtualized row height (px). Auto-measured from the first row if unset.
         */
        "rowHeight"?: number;
        /**
          * Soft-disabled — focusable but non-interactive.
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Template for the polite screen-reader status while the popup is open (localisable). `{count}` is replaced with the visible option count.
          * @default '{count} suggestions available'
         */
        "statusTemplate"?: string;
        /**
          * Supporting / helper text below the field.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Single-mode value — option `value` or free-solo string. Multi-mode value — array of option `value`s.
          * @default ''
         */
        "value"?: string | string[];
        /**
          * Localized constraint-validation message shown when `required` is unmet. `errorText` still wins when set, so an app-supplied inline message and the native bubble stay in agreement.
          * @default 'Please make a selection'
         */
        "valueMissingLabel"?: string;
        /**
          * Visual variant of the inner text-field.
          * @default 'filled'
         */
        "variant"?: 'filled' | 'outlined';
        /**
          * Virtualization strategy (same engine as `md-select`):   - `'auto'` (default): virtualize above 200 rows.   - `'always'`: always virtualize the `options` / `loadOptions` dataset.   - `'never'`: plain DOM rendering.
          * @default 'auto'
         */
        "virtualize"?: 'auto' | 'always' | 'never';
    }
    /**
     * `md-avatar` — a presentational identity tile that resolves the first
     * available source from a clear fallback chain:
     *   1. `src` (image)  → `<img>` is rendered.
     *      On error/empty, automatically falls through to step 2.
     *   2. `initials`     → explicit initials string (verbatim, max 3 chars).
     *   3. `name`         → auto-derived initials. Two tokens → first letter
     *                       of each; one token → first letter only. RTL- and
     *                       diacritic-safe via `Intl.Segmenter` when available.
     *   4. default slot   → caller-provided fallback (icon, emoji, SVG).
     *   5. person icon    → material-symbols fallback (always available).
     */
    interface MdAvatar {
        /**
          * Alternative text for the image. When empty, the image is exposed to AT as decorative (`aria-hidden`); the host carries the accessible label instead (see `label`).
          * @default ''
         */
        "alt"?: string;
        /**
          * When `true` the avatar background is deterministically picked from a tonal palette using the initials/name as a hash seed. When `false`, the avatar uses the neutral on-surface tone.
          * @default true
         */
        "colorFromName"?: boolean;
        /**
          * `<img>` `crossorigin` attribute (forwarded only when set).
          * @default ''
         */
        "crossorigin"?: '' | 'anonymous' | 'use-credentials';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Explicit initials override. Bypasses the `name` parsing entirely. Truncated to the first 3 grapheme clusters at render time so a stray "WXYZ" can't blow out the layout.
          * @default ''
         */
        "initials"?: string;
        /**
          * Accessible label for the avatar. When empty:   - if `alt` is set, that is exposed instead   - if `name` is set, that is exposed instead   - otherwise the avatar is hidden from AT (`role="presentation"`)     so it doesn't add noise next to a labelled control.
          * @default ''
         */
        "label"?: string;
        /**
          * `<img>` `loading` attribute.
          * @default 'lazy'
         */
        "loading"?: 'eager' | 'lazy';
        /**
          * Full name. The component derives initials automatically:   - `"Ada Lovelace"` → `"AL"`   - `"Cher"`         → `"C"`   - `"  john   doe"` → `"JD"`  Honored only when `src` is not set (or fails to load) and `initials` is empty.
          * @default ''
         */
        "name"?: string;
        /**
          * Image failed to load — component fell back to initials/icon.
         */
        "onMdError"?: (event: MdAvatarCustomEvent<{ src: string }>) => void;
        /**
          * Image successfully loaded.
         */
        "onMdLoad"?: (event: MdAvatarCustomEvent<{ src: string }>) => void;
        /**
          * Container shape. - `circle`  — fully rounded (`50%`)             [default] - `rounded` — `--md-sys-shape-corner-medium` (12px) - `square`  — sharp corners (`0`)
          * @default 'circle'
         */
        "shape"?: MdAvatarShape;
        /**
          * Avatar size. Accepts either a named preset or any CSS length.  Presets: - `small`   — 24×24 (typography: label-medium) - `medium`  — 40×40 (typography: title-medium)  [default] - `large`   — 56×56 (typography: title-large)  Custom values: - Unitless number → pixels (`size="72"` → 72px) - Any CSS length  → passes through (`size="4rem"`, `size="10vw"`,   `size="clamp(28px, 4vw, 72px)"`)  Custom values are wired through the existing `--md-avatar-size` CSS custom property, so per-instance overrides via `style` / class still win when set.
          * @default 'medium'
         */
        "size"?: MdAvatarSize;
        /**
          * Image URL. When set, the avatar renders as an `<img>` element.
          * @default ''
         */
        "src"?: string;
    }
    interface MdBadge {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Material Symbols icon name (large variant only, e.g. `"error"`)
          * @default ''
         */
        "icon"?: string;
        /**
          * Numeric threshold. When the parsed value exceeds this, the badge shows `{max}+` (e.g. `999+`). Ignored for non-numeric values.
          * @default 999
         */
        "max"?: number;
        /**
          * Text or number to display (large variant only, max 4 visible characters)
          * @default ''
         */
        "value"?: string;
        /**
          * Badge size — small renders a dot; large renders a label
          * @default 'large'
         */
        "variant"?: 'small' | 'large';
    }
    /**
     * md-bar-chart — Material Design 3 Expressive bar chart, rendered
     * by the in-house engine (Canvas2D + a DOM text overlay). No
     * ECharts.
     *   • vertical (default) and horizontal layouts
     *   • grouped / stacked / percentage modes
     *   • category + bar gap configuration
     *   • rounded outer corners (top for vertical, end for horizontal)
     *   • legend toggle, hover tooltip, click events
     *   • role="figure" + screen-reader-only data table
     */
    interface MdBarChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation"?: MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Draw small perpendicular tick marks on the axes.
          * @default false
         */
        "axisTicks"?: boolean;
        /**
          * Bar gap between sibling grouped bars (`"20%"` or `20`).
          * @default '20%'
         */
        "barGap"?: string | number;
        /**
          * Fixed bar WIDTH in px, centred in each slot. Default: auto — the bar fills its slot (the band minus the category/bar gaps). Clamped to the slot so it can't overrun a neighbour.
         */
        "barWidth"?: number;
        /**
          * Category gap as a percentage of the band width (`"30%"` or `30`).
          * @default '30%'
         */
        "categoryGap"?: string | number;
        /**
          * Draw the bars as CHEVRONS pointing away from the baseline instead of as rectangles. Along a stack they interlock, each segment's point sitting in the next one's notch — a force diagram, where the contributions are handed along in a direction rather than merely piled up.
          * @default false
         */
        "chevron"?: boolean;
        /**
          * Show a hand (`pointer`) cursor over the bars, signalling they're actionable — e.g. clickable to drill into. The chart can't know an interaction is wired, so set this when a `mdBarClick` handler does something (like `drill`). The cursor tracks the bar itself (hit-tested like a click), not the empty column above a short bar.
          * @default false
         */
        "clickable"?: boolean;
        /**
          * Bar corner radius in pixels.
          * @default 6
         */
        "cornerRadius"?: number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        "heightProp"?: string;
        /**
          * @default ''
         */
        "label"?: string;
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the data with the arrow keys; this is what tells them so.
          * @default 'Chart data. Use the arrow keys to move between points, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot"?: string;
        /**
          * Template for the live announcement as keyboard focus moves. `%x%` is the axis value, `%values%` the series readings at it.
          * @default '%x%: %values%'
         */
        "labelPoint"?: string;
        /**
          * Accessible label for the zoom slider's end thumb.
          * @default 'Zoom range end'
         */
        "labelZoomEnd"?: string;
        /**
          * Accessible label for the zoom slider's start thumb.
          * @default 'Zoom range start'
         */
        "labelZoomStart"?: string;
        /**
          * Bar layout — `'horizontal'` swaps categories onto the y axis.
          * @default 'vertical'
         */
        "layout"?: MdChartBarLayout;
        /**
          * @default 'top-end'
         */
        "legend"?: MdChartLegendPosition | 'none';
        /**
          * Data is still on its way — the chart covers the plot with a loader instead of drawing an empty (or stale) axis, and marks itself `aria-busy`.  Set it while the fetch is in flight and clear it when `series` arrives; the entry animation replays on the way out, so the bars draw themselves in rather than appearing fully formed. Slot `loading` to replace the default indicator with your own skeleton.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Accessible + visible text under the loader.
          * @default 'Loading chart…'
         */
        "loadingLabel"?: string;
        /**
          * BCP-47 locale for the DEFAULT number / date formatting (axis ticks, tooltip values, screen-reader table). Empty follows the browser; an explicit `valueFormatter` always wins.
          * @default ''
         */
        "locale"?: string;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation"?: boolean;
        "onMdBarClick"?: (event: MdBarChartCustomEvent<MdChartClickDetail<MdChartSeries>>) => void;
        "onMdHover"?: (event: MdBarChartCustomEvent<MdChartHoverDetail>) => void;
        "onMdLegendClick"?: (event: MdBarChartCustomEvent<{ seriesIndex: number; seriesId?: string; selected: boolean }>) => void;
        "onMdReady"?: (event: MdBarChartCustomEvent<void>) => void;
        /**
          * Fires when the zoom window changes (drag, slider, or `setZoom`/`resetZoom`).
         */
        "onMdZoom"?: (event: MdBarChartCustomEvent<{ startIndex: number; endIndex: number; reset: boolean }>) => void;
        /**
          * Wrap the bars around a circle: one concentric RING per category, with the value axis running as an ANGLE instead of a length. A radial (polar) bar chart.  Stacking, legend hiding and percentage normalisation all work unchanged — only the step from a value to geometry differs, and the value axis deliberately spans less than the full circle, since a ring that closes on itself has no readable start or end. The quarter it gives up is where the category names go; `polar-sweep` changes how much that is.
          * @default false
         */
        "polar"?: boolean;
        /**
          * Radius of the hole at the centre of a polar chart, 0..0.9. Default 0.22.
         */
        "polarHole"?: number;
        /**
          * Fraction of the circle the value axis spans on a polar chart. Default 0.75.
         */
        "polarSweep"?: number;
        /**
          * @default []
         */
        "series"?: MdChartSeries[];
        /**
          * Show numeric data labels on each bar.
          * @default false
         */
        "showLabels"?: boolean;
        /**
          * Print each stack's TOTAL past the end of the column, alongside the per-segment values `show-labels` prints inside it. Stacked charts only — an unstacked bar already IS its total.
          * @default false
         */
        "showTotals"?: boolean;
        /**
          * Stacking strategy: none (grouped) / normal / percentage.
          * @default 'none'
         */
        "stack"?: MdChartStackMode;
        /**
          * A quieter line under the title — a source, a unit, the caveat a title should not have to carry.
         */
        "subtitle"?: string | undefined;
        /**
          * Title alignment over the plot: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign"?: MdChartTitleAlign;
        /**
          * @default 'axis'
         */
        "tooltip"?: MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content. See `md-line-chart`'s `tooltipRenderer`.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        "valueFormatter"?: (value: number | null | undefined) => string;
        /**
          * Category axis configuration.
         */
        "xAxis"?: MdChartAxis | undefined;
        /**
          * Value axis configuration.
         */
        "yAxis"?: MdChartAxis | undefined;
        /**
          * A SECOND value axis, drawn in the opposite gutter and used by any series whose `yAxisIndex` is 1 — two quantities on one category axis that share no scale, like a count and a rate. Vertical charts.  It draws no gridlines of its own: two sets at two spacings turn the plot into a mesh nobody can read a value off.
         */
        "yAxis2"?: MdChartAxis | undefined;
        /**
          * Interactive zoom over the CATEGORY range — the same "zoom is a view" model as `md-line-chart`:   • `inside` — drag horizontally across the plot to zoom into that span of     bars; double-click the plot to reset.   • `slider` — a range slider under the plot; drag its thumbs to resize the     window, or the grip between them to pan it.   • `both` — both gestures.  `series` / `xAxis` are never mutated: the engine is simply fed the sliced window, and hover / click / keyboard indices are rebased so consumers still see ABSOLUTE data indices. Ignored on a `polar` chart (a ring has no span to zoom). For a *value*-range zoom, set `yAxis.min` / `yAxis.max`.
          * @default 'none'
         */
        "zoom"?: 'none' | 'inside' | 'slider' | 'both';
    }
    /**
     * MD3 Bottom sheet — secondary content anchored to the bottom of the screen.
     * Two variants per the M3 spec
     * (https://m3.material.io/components/bottom-sheets/overview). Both are
     * modal dialogs — they always render a scrim, trap focus, lock body
     * scroll, close on Escape, and support drag-to-dismiss.
     * - `standard` (default) — anchored to the bottom edge of the viewport.
     *   Full-width up to a responsive max, with rounded top corners only.
     * - `detached` — floats with a margin on every side and rounded corners
     *   on every side. Best for compact, dialog-like sheets on larger
     *   screens.
     * Width and height are entirely token-driven, so the only thing you
     * change to switch from anchored to floating chrome is the `variant`
     * attribute. The default media query reflows the sheet between
     * full-width on mobile and a centred 640px panel on desktop; consumers
     * can opt out via `--md-bottom-sheet-width` / `--md-bottom-sheet-max-width`.
     */
    interface MdBottomSheet {
        /**
          * Show divider between content and actions
          * @default false
         */
        "bottomDivider"?: boolean;
        /**
          * Show a close icon-button in the top-end corner of the header
          * @default false
         */
        "closeable"?: boolean;
        /**
          * Inline-axis alignment of the slotted content. Inherits to all default-slot children, so prose, lists, and forms all pick it up. `start` (default) reads left in LTR / right in RTL, `end` reads right in LTR / left in RTL, `center` is direction-agnostic. Slotted children that explicitly set their own `text-align` win.
          * @default 'start'
         */
        "contentAlign"?: 'start' | 'center' | 'end';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Headline text (or use the `headline` slot)
          * @default ''
         */
        "headline"?: string;
        /**
          * Inline-axis alignment of the headline text. `start` (default) reads left in LTR / right in RTL, `end` reads right in LTR / left in RTL, `center` is direction-agnostic. Use the matching `::part(headline)` selector for finer control (e.g. `text-align: justify`).
          * @default 'start'
         */
        "headlineAlign"?: 'start' | 'center' | 'end';
        /**
          * Emits when dismissed via scrim click, Escape, drag, or close button
         */
        "onMdCancel"?: (event: MdBottomSheetCustomEvent<void>) => void;
        /**
          * Emits when the sheet closes
         */
        "onMdClose"?: (event: MdBottomSheetCustomEvent<void>) => void;
        /**
          * Emits when the sheet opens
         */
        "onMdOpen"?: (event: MdBottomSheetCustomEvent<void>) => void;
        /**
          * Whether the sheet is visible
          * @default false
         */
        "open"?: boolean;
        /**
          * Whether clicking the scrim closes the sheet
          * @default true
         */
        "scrimDismissible"?: boolean;
        /**
          * Retained for API compatibility, but now a no-op: the content area always plain-scrolls when it overflows (no scroll shadow / edge fades). Setting this has no visual effect.
          * @default true
         */
        "scrollShadow"?: boolean;
        /**
          * Custom aria-label for the container
          * @default ''
         */
        "sheetAriaLabel"?: string;
        /**
          * Show the drag handle indicator
          * @default true
         */
        "showDragHandle"?: boolean;
        /**
          * Show divider between header and content
          * @default false
         */
        "topDivider"?: boolean;
        /**
          * `standard` (default) anchors the sheet to the bottom edge. `detached` floats it with margin on every side. Both variants always render a scrim and trap focus.
          * @default 'standard'
         */
        "variant"?: 'standard' | 'detached';
    }
    interface MdBreadcrumbItem {
        /**
          * Marks this item as part of the collapsed middle section. Usually set by the parent `md-breadcrumbs` when `max-items` is exceeded. Declared as a public prop (instead of just a `data-*` attribute) so Stencil re-renders the item when the parent toggles its visibility.
          * @default false
         */
        "collapsed"?: boolean;
        /**
          * When `true`, mark this item as the current page. Usually the parent `md-breadcrumbs` auto-sets this on the last child, but explicit override is supported.
          * @default false
         */
        "current"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the item — renders without an anchor and is not focusable.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Destination URL. When empty the item renders as a plain span.
          * @default ''
         */
        "href"?: string;
        /**
          * Optional leading icon (Material Symbols shorthand).
          * @default ''
         */
        "icon"?: string;
        /**
          * Position in the trail (0-indexed). Set automatically by the parent `md-breadcrumbs`. Exposed as a reactive prop so children re-render the leading separator when their position changes.
          * @default 0
         */
        "itemIndex"?: number;
        /**
          * Total number of items in the trail. Set automatically by the parent.
          * @default 0
         */
        "itemTotal"?: number;
        /**
          * Fires when the user activates a navigable crumb — i.e. when an item with a non-empty `href` (and not marked `current` / `disabled`) is clicked or activated with `Enter` while focused. The event is **cancelable**: call `event.preventDefault()` from the listener to stop the browser's default navigation. This is the recommended hook for SPA routing — let the breadcrumb render a real `<a>` (so right-click / middle-click / "Open in new tab" still work), then intercept `mdSelect` to push the route through your client-side router instead of a full page reload.  The event bubbles, so consumers can attach a single listener on the parent `<md-breadcrumbs>` for delegation. The event is composed, so listeners outside the shadow tree (the typical case) receive it.  ```ts document.querySelector('md-breadcrumbs')!   .addEventListener('mdSelect', (e) => {     const { href, label, itemIndex } = (e as CustomEvent).detail;     e.preventDefault();              // stop the <a> from navigating     myRouter.push(href);             // SPA navigation   }); ```
         */
        "onMdSelect"?: (event: MdBreadcrumbItemCustomEvent<MdBreadcrumbSelectDetail>) => void;
        /**
          * Anchor `rel` attribute.
          * @default ''
         */
        "rel"?: string;
        /**
          * Anchor `target` attribute (e.g. `_blank`).
          * @default ''
         */
        "target"?: string;
    }
    interface MdBreadcrumbs {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Accessible name for the overflow ("…") toggle button. Surface this in your i18n layer so screen-reader users in non-English locales hear a localized announcement when collapsing is in effect.
          * @default 'Show full breadcrumb trail'
         */
        "expandLabel"?: string;
        /**
          * How many items to ALWAYS show at the end of the trail when collapsed. Default 2.
          * @default 2
         */
        "itemsAfterCollapse"?: number;
        /**
          * How many items to ALWAYS show at the start of the trail when collapsed. Default 1.
          * @default 1
         */
        "itemsBeforeCollapse"?: number;
        /**
          * Accessible name. Defaults to "Breadcrumb".
          * @default 'Breadcrumb'
         */
        "label"?: string;
        /**
          * When the number of crumbs exceeds `maxItems`, the middle ones collapse behind an "…" toggle. Set to `0` to disable collapsing.
          * @default 0
         */
        "maxItems"?: number;
        /**
          * Fires when the user reveals the full breadcrumb trail by activating the overflow ("…") toggle, or when consumers programmatically call `expand()` / `collapse()`. Not cancelable — the visibility change has already happened. Use it for analytics or to react to a user who wants to see the full path.  Bubbles + composed, so consumers outside the shadow tree (the typical case) receive it.
         */
        "onMdExpand"?: (event: MdBreadcrumbsCustomEvent<MdBreadcrumbsExpandDetail>) => void;
        /**
          * Separator glyph placed between items. Use any single character or short string (e.g. `/`, `›`, `→`, `»`). For SVG / icon separators, override `::part(separator)` from the light DOM with a CSS `background-image` — see readme.
          * @default '/'
         */
        "separator"?: string;
    }
    interface MdButton {
        /**
          * @default false
         */
        "connectedLeft"?: boolean;
        /**
          * @default false
         */
        "connectedRight"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled"?: boolean;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Stretch the button to fill its inline-axis container.  Switches the host from `inline-flex` to `flex` and sets `inline-size: 100%` so the button consumes the full width of its parent — the standard pattern for primary CTAs in forms, modals, and bottom sheets. The size's `min-block-size` (touch target) is preserved.  For arbitrary fixed widths or heights, set the `--md-button-width`, `--md-button-height`, `--md-button-min-width`, `--md-button-min-height`, or `--md-button-max-width` CSS custom properties on the host instead.
          * @default false
         */
        "fullWidth"?: boolean;
        /**
          * @default null
         */
        "groupTabindex"?: number | null;
        /**
          * @default ''
         */
        "href"?: string;
        /**
          * @default ''
         */
        "icon"?: string;
        /**
          * @default false
         */
        "loading"?: boolean;
        /**
          * Mirror leading and trailing icons horizontally when the button is rendered in a right-to-left context. Set this on buttons that use **directional** Material Symbols (arrows, chevrons, send, reply…) — non-directional icons (add, search, favorite…) should leave it `false` so the glyph stays semantically correct.
          * @default false
         */
        "mirrorIcon"?: boolean;
        /**
          * The name of the element, used when submitting an HTML form.
         */
        "name"?: string;
        /**
          * Fires when toggle mode flips the `selected` state in response to a user activation. Not cancelable — the state change has already happened. Pair with `mdClick` (cancelable) when you need a veto.  Bubbles and is composed.
         */
        "onMdChange"?: (event: MdButtonCustomEvent<MdButtonChangeDetail>) => void;
        /**
          * Fires every time the user activates the button (mouse click, touch, or `Enter`/`Space` while focused). The event is **cancelable** and the detail payload describes what *would* happen: the post-click toggle state, the navigation `href`, and so on.  Calling `event.preventDefault()` from a listener suppresses the default side effects (toggle flip + `href` navigation) but lets the underlying DOM click bubble. This is the hook to use for SPA routing, "are you sure?" prompts, async confirmation, etc.  The event bubbles and is composed, so listeners outside the shadow tree (the typical case) receive it.  ```ts document.querySelector('md-button')!.addEventListener('mdClick', (e) => {   const { href, selected } = (e as CustomEvent).detail;   if (!confirm('Continue?')) e.preventDefault(); }); ```
         */
        "onMdClick"?: (event: MdButtonCustomEvent<MdButtonClickDetail>) => void;
        /**
          * @default true
         */
        "ripple"?: boolean;
        /**
          * Override the default `role="button"`. Used by composite widgets such as `md-date-picker` day cells (`role="gridcell"`). Leave empty for `button`.
          * @default ''
         */
        "roleOverride"?: string;
        /**
          * Current selected state (toggle mode)
          * @default false
         */
        "selected"?: boolean;
        /**
          * @default 'round'
         */
        "shape"?: 'round' | 'square';
        /**
          * Enable M3 Expressive shape morphing on press and toggle
          * @default true
         */
        "shapeMorph"?: boolean;
        /**
          * @default 'sm'
         */
        "size"?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * When true, suppresses the default 180° trailing-icon rotation while `aria-expanded="true"`. Use when the consumer swaps the glyph (e.g. docked `md-date-picker` month/year triggers use `chevron_right`).
          * @default false
         */
        "suppressExpandIconFlip"?: boolean;
        /**
          * @default '_self'
         */
        "target"?: string;
        /**
          * Enable toggle behavior (selected/unselected)
          * @default false
         */
        "toggle"?: boolean;
        /**
          * @default ''
         */
        "trailingIcon"?: string;
        /**
          * Activation behavior, mirroring the native `<button type>`: - `button` (default): no implicit form action. - `submit`: submits the associated form (`requestSubmit`, so the form's   `submit` event fires and constraint validation runs). - `reset`: resets the associated form.
          * @default 'button'
         */
        "type"?: 'button' | 'submit' | 'reset';
        /**
          * @default ''
         */
        "value"?: string;
        /**
          * @default 'filled'
         */
        "variant"?: 'filled' | 'outlined' | 'text' | 'elevated' | 'tonal';
    }
    interface MdButtonGroup {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * When `true` the group stretches to 100% of its container's inline-size and its children share that space equally (`flex: 1 1 0`). Combine with the `--md-button-group-width` / `--md-button-group-max-width` CSS custom properties for fine-grained control over the final size.
          * @default false
         */
        "fullWidth"?: boolean;
        /**
          * Fires every time a user activation toggles the selection state of a child button. The detail object describes both the new selection snapshot (`values`) and the diff from the previous selection (`added`, `removed`), plus the underlying `originalEvent`.  Distinct from each child button's own `mdChange` (which fires per individual button toggle); the group's `mdSelectionChange` fires once per user activation and reports the *group-level* state.  Bubbles and is composed so listeners outside the shadow tree receive it.
          * @example ```ts group.addEventListener('mdSelectionChange', (e: CustomEvent<MdButtonGroupChangeDetail>) => {   console.log('now selected:', e.detail.values);   console.log('just added:', e.detail.added);   console.log('just removed:', e.detail.removed); }); ```
         */
        "onMdSelectionChange"?: (event: MdButtonGroupCustomEvent<MdButtonGroupChangeDetail>) => void;
        /**
          * When true, at least one button must remain selected
          * @default false
         */
        "required"?: boolean;
        /**
          * Selection mode
          * @default 'single-select'
         */
        "selectionMode"?: 'single-select' | 'multi-select';
        /**
          * Default shape applied to all child buttons
          * @default 'round'
         */
        "shape"?: 'round' | 'square';
        /**
          * Button size — controls gap (standard) and inner corner radii (connected)
          * @default 'sm'
         */
        "size"?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Standard groups add padding between buttons; connected groups fuse them together
          * @default 'standard'
         */
        "variant"?: 'standard' | 'connected';
    }
    interface MdCard {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the card — only meaningful when interactive
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Enables pointer-event-based drag on the card
          * @default false
         */
        "dragEnabled"?: boolean;
        /**
          * When `true` the card stretches to 100% of its container's block-size. The parent must have a definite block-size (e.g. a fixed height row in a CSS grid, or a flex item with `align-items: stretch`) for `100%` to resolve.
          * @default false
         */
        "fullHeight"?: boolean;
        /**
          * When `true` the card stretches to 100% of its container's inline-size. Pair with the `--md-card-max-width` custom property to cap the fluid width (useful inside dashboards and responsive grids).
          * @default false
         */
        "fullWidth"?: boolean;
        /**
          * Makes the card interactive (clickable, with ripple and state layers).  When the card's own content has no focusable controls the host exposes `role="button"` + `tabindex` and is keyboard-activatable. If the content *does* include its own focusable controls (buttons, links, form fields, interactive `md-*` components) the host does not claim `role="button"` — that would be a `nested-interactive` a11y violation — and instead stays a plain container that is still mouse-clickable (emits `mdClick`) while the inner controls remain the keyboard tab stops.
          * @default false
         */
        "interactive"?: boolean;
        /**
          * Emits when an interactive card is clicked or activated via keyboard
         */
        "onMdClick"?: (event: MdCardCustomEvent<MouseEvent>) => void;
        /**
          * Emits when a draggable card is released after dragging
         */
        "onMdDragEnd"?: (event: MdCardCustomEvent<MdCardDragDetail>) => void;
        /**
          * Emits continuously as a dragged card moves — use for drop-zone detection or position tracking
         */
        "onMdDragMove"?: (event: MdCardCustomEvent<MdCardDragDetail>) => void;
        /**
          * Emits once when a draggable card starts being dragged (after crossing the movement threshold)
         */
        "onMdDragStart"?: (event: MdCardCustomEvent<MdCardDragDetail>) => void;
        /**
          * Whether the ripple effect is enabled when interactive
          * @default true
         */
        "ripple"?: boolean;
        /**
          * Soft-disabled: disabled visuals but remains focusable — only meaningful when interactive
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Visual style variant
          * @default 'elevated'
         */
        "variant"?: 'elevated' | 'filled' | 'outlined';
    }
    /**
     * `md-checkbox` — Material Design 3 checkbox (with tri-state / indeterminate).
     * Form-associated: inside a `<form>` it submits its `value` under `name` when
     * checked, and restores on form reset. It is a *labelable* element — wrap it in
     * a `<label>` (clicks on the label text toggle it AND give it its accessible
     * name natively), or pass `aria-label` / `aria-labelledby` on the host. A bare,
     * unlabeled checkbox has no accessible name (axe `aria-toggle-field-name`).
     */
    interface MdCheckbox {
        /**
          * Whether the checkbox is checked
          * @default false
         */
        "checked"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the checkbox
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Show the supporting line in the error colour.
          * @default false
         */
        "error"?: boolean;
        /**
          * Supporting / error message shown beneath the control.  A checkbox previously had nowhere to put a message, so a failed `required` could only surface as the browser's own validation balloon — visually unrelated to the field, and gone the moment you click away. This gives it the same inline supporting line md-text-field has.  The host is a fixed-size touch target, so the line is positioned OUT of that box rather than laid out inside it; see the CSS for why that cannot simply be a flex child.
          * @default ''
         */
        "errorText"?: string;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Whether the checkbox is in the indeterminate (mixed) state
          * @default false
         */
        "indeterminate"?: boolean;
        /**
          * Form name
          * @default ''
         */
        "name"?: string;
        /**
          * Emits when checked or indeterminate state changes
         */
        "onMdChange"?: (event: MdCheckboxCustomEvent<{ checked: boolean; indeterminate: boolean }>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdCheckboxCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Whether the checkbox is required in a form
          * @default false
         */
        "required"?: boolean;
        /**
          * Soft-disabled: disabled visuals but remains focusable for discoverability
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Supporting text shown when there is no error.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Form value submitted when checked
          * @default 'on'
         */
        "value"?: string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please check this box if you want to proceed.'
         */
        "valueMissingLabel"?: string;
    }
    interface MdChip {
        /**
          * Surface style of the chip. Distinct from `variant`, which selects the M3 chip TYPE (assist / filter / input / suggestion); a chip has both.  - `outlined` (default) — transparent container with a 1px outline. - `filled` — tonal container, no outline. - `elevated` — tonal container with elevation and no outline.
          * @default 'outlined'
         */
        "appearance"?: 'outlined' | 'filled' | 'elevated';
        /**
          * Colour role applied to the chip's outline / label / container, depending on `appearance`. Omit for the neutral surface treatment.  Any role name the THEME defines is accepted, not just a fixed list — the chip resolves `<name>` to the `--md-sys-color-<name>` / `-<name>-container` / `-on-<name>-container` family at runtime. So `color="brand"` works as soon as a theme ships those tokens, and a name the theme does not define degrades to the neutral treatment rather than rendering broken.  `primary` | `secondary` | `tertiary` | `error` are the baseline M3 roles; `success` | `warning` | `info` ship in packages/tokens on the same tonal recipe. The union below is a hint for editor autocomplete only — the `string` arm is what makes custom roles work.
         */
        "color"?: | 'primary'
    | 'secondary'
    | 'tertiary'
    | 'error'
    | 'success'
    | 'warning'
    | 'info'
    | (string & {});
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the chip
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Whether the chip is elevated (adds shadow instead of outline).
          * @deprecated Use `appearance="elevated"`. Kept as an alias so existing markup keeps working: when true it forces the elevated appearance and wins over `appearance`, since a consumer setting it is being explicit.
          * @default false
         */
        "elevated"?: boolean;
        /**
          * Material Symbols icon name for the leading icon
          * @default ''
         */
        "icon"?: string;
        /**
          * Label text — alternative to default slot
          * @default ''
         */
        "label"?: string;
        /**
          * Emits when the chip is clicked or activated via keyboard
         */
        "onMdClick"?: (event: MdChipCustomEvent<void>) => void;
        /**
          * Fires when the trailing remove ("✕") button is activated on a removable chip (by click, Enter, Space, Delete, or Backspace).  The event is **cancelable**: by default, after `mdRemove` is dispatched the chip removes itself from the DOM. Call `event.preventDefault()` in a handler to keep the chip mounted (e.g. when your state layer owns the list and will re-render with the chip gone).
         */
        "onMdRemove"?: (event: MdChipCustomEvent<void>) => void;
        /**
          * Emits when the selected state changes (filter/input)
         */
        "onMdSelect"?: (event: MdChipCustomEvent<{ selected: boolean }>) => void;
        /**
          * When `true`, the chip renders a trailing remove ("✕") button and responds to Delete / Backspace from the chip itself. Works on every variant.  `variant="input"` is removable by default (per MD3 spec) — setting `removable="false"` explicitly opts out of that behavior.
         */
        "removable"?: boolean;
        /**
          * Whether clicking the chip body toggles `selected`. Defaults to true for `filter`/`input` variants. Set `selectable="false"` for a token that is only a display of a value (e.g. a multi-select's removable chips) — clicking the body then does nothing and the ✕ remove button is the only action, so the chip doesn't flash a misleading "active" state.
         */
        "selectable"?: boolean;
        /**
          * Whether the chip is selected (filter/input variants)
          * @default false
         */
        "selected"?: boolean;
        /**
          * Soft-disabled: disabled visuals but remains focusable
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Material Symbols icon name for the trailing icon (filter/input only)
          * @default ''
         */
        "trailingIcon"?: string;
        /**
          * Chip variant
          * @default 'assist'
         */
        "variant"?: 'assist' | 'filter' | 'input' | 'suggestion';
    }
    interface MdColorPicker {
        /**
          * Show the alpha slider + alpha hex field.
          * @default false
         */
        "alpha"?: boolean;
        /**
          * Accessible label for the whole picker. Defaults to "Color picker".
          * @default 'Color picker'
         */
        "ariaLabelProp"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the entire control.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * When true and `variant="popover"`, clicking outside the component closes the open panel. Defaults to `false` so existing popovers keep their current behaviour until opted in.
          * @default false
         */
        "dismissOnOutsideClick"?: boolean;
        /**
          * Output format for `value` and `mdChange` events.
          * @default 'hex'
         */
        "format"?: ColorFormat;
        /**
          * Emitted when the user commits a value (release pointer, blur input, Enter, or click a preset).
         */
        "onMdChange"?: (event: MdColorPickerCustomEvent<{ value: string }>) => void;
        /**
          * Emitted on every change while the user drags / types.
         */
        "onMdInput"?: (event: MdColorPickerCustomEvent<{ value: string }>) => void;
        /**
          * Emitted when the popover opens or closes.
         */
        "onMdOpenChange"?: (event: MdColorPickerCustomEvent<{ open: boolean }>) => void;
        /**
          * Whether the popover is open. Only meaningful when variant=`popover`.
          * @default false
         */
        "open"?: boolean;
        /**
          * Swatch presets. When non-empty, a small palette appears below the sliders for one-tap colour selection.  Takes either form, so it behaves like every other list-valued prop in the library rather than being the one exception:   - attribute — `presets="#000000,#FFFFFF,#FF5722"` (comma-separated)   - property  — `el.presets = ['#000000', '#FFFFFF', '#FF5722']`
          * @default ''
         */
        "presets"?: string | string[];
        /**
          * Show the hex input field below the sliders.
          * @default true
         */
        "showHex"?: boolean;
        /**
          * Show the RGB/HSL numeric inputs.
          * @default true
         */
        "showInputs"?: boolean;
        /**
          * Current value. Accepts any 3/4/6/8-digit hex, `rgb()`, `rgba()`, or the formats emitted by this component. Two-way bindable.
          * @default '#6750A4'
         */
        "value"?: string;
        /**
          * Layout variant. - `inline` (default) — picker UI is always visible. - `popover` — opens the picker in a floating panel anchored to itself,   behind a trigger YOU slot into `trigger`. There is no built-in trigger.
          * @default 'inline'
         */
        "variant"?: ColorPickerVariant;
    }
    /**
     * `md-date-picker` — a Material Design 3 date picker.
     * Variants:
     * - `modal-input` *(default)* — outlined text field + modal calendar dialog.
     * - `modal` — bare modal dialog surfaced via `show()` / `open`.
     * - `docked` — outlined text field + dropdown calendar anchored to the field.
     * The modal dialog can flip to a typed-entry view (date input) via the
     * header toggle, satisfying the MD3 "modal date input" pattern.
     */
    interface MdDatePicker {
        /**
          * Material Symbols name for the trailing calendar toggle on the field. Ignored when a `calendar-icon` slot is provided.
          * @default 'calendar_today'
         */
        "calendarIcon"?: string;
        /**
          * Label of the cancel button in the modal action row.
          * @default 'Cancel'
         */
        "cancelLabel"?: string;
        /**
          * Tooltip suffix on the modal month/year nav button.
          * @default 'Choose month and year'
         */
        "chooseMonthAndYearLabel"?: string;
        /**
          * Accessible label for the docked month menu button.
          * @default 'Choose month'
         */
        "chooseMonthLabel"?: string;
        /**
          * Accessible label for the modal month/year toggle button.
          * @default 'Choose a different month and year'
         */
        "chooseMonthYearLabel"?: string;
        /**
          * Accessible label for the docked year menu button.
          * @default 'Choose year'
         */
        "chooseYearLabel"?: string;
        /**
          * Accessible label for the clear button.
          * @default 'Clear date'
         */
        "clearLabel"?: string;
        /**
          * Show a clear ("×") button on the field trigger while a date is selected, which resets the value (same as calling `clear()`). Applies to the `modal-input` and `docked` variants.
          * @default false
         */
        "clearable"?: boolean;
        /**
          * Trigger calendar icon label when the panel is open.
          * @default 'Close calendar'
         */
        "closeCalendarLabel"?: string;
        /**
          * Commit a day the moment it is clicked, instead of staging it behind the Cancel / OK row.  By default every variant STAGES a day click (`mdSelected`) and commits only on OK (`mdChange`). With this set the two collapse into one interaction: the click stages and commits together, the panel dismisses, and the built-in action row is not rendered at all — there is nothing left for it to confirm.  Suits a low-stakes, reversible field. Keep the default when the choice is expensive to get wrong and the user deserves an explicit confirm step.
          * @default false
         */
        "commitOnSelect"?: boolean;
        /**
          * Character between day, month, and year in the text field (display and parsing). Leave empty to use the locale default (e.g. `/` for `en-US`, `.` for `de-DE`). The `value` prop and `mdChange` / `mdSelected` events always use ISO `YYYY-MM-DD` regardless of this setting.
          * @example ```html <md-date-picker date-separator="/" value="2025-06-15"></md-date-picker> <!-- displays 06/15/2025 in en-US --> <md-date-picker date-separator="." locale="de-DE"></md-date-picker> <!-- displays and parses DD.MM.YYYY --> ```
          * @default ''
         */
        "dateSeparator"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — blocks all interaction and removes from tab order.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Large headline shown in typed-entry (input) mode.
          * @default 'Enter dates'
         */
        "enterDatesLabel"?: string;
        /**
          * Error state — paints the field with the error color and shows `error-text`.
          * @default false
         */
        "error"?: boolean;
        /**
          * Error message shown below the field when `error` is true.
          * @default ''
         */
        "errorText"?: string;
        /**
          * Visual variant of the trigger `md-text-field` (`modal-input` and `docked`). Use `filled` with `--md-date-picker-field-container-color` for branded fills.
          * @default 'outlined'
         */
        "fieldVariant"?: 'outlined' | 'filled';
        /**
          * First day of the week (0 = Sunday … 6 = Saturday). `-1` derives it from the locale.
          * @default -1
         */
        "firstDayOfWeek"?: number;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Headline / supporting text shown at the top of the modal header.
          * @default 'Select date'
         */
        "headline"?: string;
        /**
          * Default error when a typed date cannot be parsed or is out of range.
          * @default 'Invalid date'
         */
        "invalidDateLabel"?: string;
        /**
          * Predicate that disables individual dates (return `true` to disable).
         */
        "isDateDisabled"?: (date: Date) => boolean;
        /**
          * Floating label / accessible name for the field.
          * @default 'Date'
         */
        "label"?: string;
        /**
          * BCP-47 locale for month/weekday names and date formatting.
          * @default ''
         */
        "locale"?: string;
        /**
          * Latest selectable date, inclusive, as ISO `YYYY-MM-DD`.
          * @default ''
         */
        "max"?: string;
        /**
          * Earliest selectable date, inclusive, as ISO `YYYY-MM-DD`.
          * @default ''
         */
        "min"?: string;
        /**
          * Form-association name attribute.
          * @default ''
         */
        "name"?: string;
        /**
          * Accessible label for the "next month" nav chevron.
          * @default 'Next month'
         */
        "nextMonthLabel"?: string;
        /**
          * Accessible label for the "next year" nav chevron.
          * @default 'Next year'
         */
        "nextYearLabel"?: string;
        /**
          * Label of the confirm button in the modal action row.
          * @default 'OK'
         */
        "okLabel"?: string;
        /**
          * Emitted when the user dismisses without committing (scrim, Cancel, Esc).
         */
        "onMdCancel"?: (event: MdDatePickerCustomEvent<void>) => void;
        /**
          * Emitted when the user commits a selection.
         */
        "onMdChange"?: (event: MdDatePickerCustomEvent<MdDatePickerChangeDetail>) => void;
        /**
          * Emitted when the picker closes for any reason.
         */
        "onMdClose"?: (event: MdDatePickerCustomEvent<void>) => void;
        /**
          * Emitted on every keystroke in a text-entry field.
         */
        "onMdInput"?: (event: MdDatePickerCustomEvent<{ value: string }>) => void;
        /**
          * Emitted when the user opens the month or year selection menu / grid.
         */
        "onMdMenuOpen"?: (event: MdDatePickerCustomEvent<MdDatePickerMenuOpenDetail>) => void;
        /**
          * Emitted when the user picks a month or year from a selection menu / grid.
         */
        "onMdMenuSelect"?: (event: MdDatePickerCustomEvent<MdDatePickerMenuSelectDetail>) => void;
        /**
          * Emitted when the user toggles between calendar and typed-entry views.
         */
        "onMdModeChange"?: (event: MdDatePickerCustomEvent<MdDatePickerModeChangeDetail>) => void;
        /**
          * Emitted when the picker opens.
         */
        "onMdOpen"?: (event: MdDatePickerCustomEvent<void>) => void;
        /**
          * Emitted when the user selects a day in the calendar (click or keyboard).
         */
        "onMdSelected"?: (event: MdDatePickerCustomEvent<MdDatePickerSelectedDetail>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdDatePickerCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Emitted when the displayed calendar month/year changes from user navigation.
         */
        "onMdViewChange"?: (event: MdDatePickerCustomEvent<MdDatePickerViewChangeDetail>) => void;
        /**
          * Whether the modal / docked panel is open. Two-way bindable.
          * @default false
         */
        "open"?: boolean;
        /**
          * Trigger calendar icon label when the panel is closed.
          * @default 'Open calendar'
         */
        "openCalendarLabel"?: string;
        /**
          * Clicking outside a DOCKED panel dismisses it (like `scrim-dismissible` for the modal, which has a scrim to catch the click and a docked panel does not).  Set `false` to keep the panel open until Cancel / OK / Esc — useful when the picker sits inside another popup whose own outside-click handling would otherwise fight it, or when a stray click should not discard staged input.  Dismissing this way is a cancel: it emits `mdCancel` and discards staging, exactly as the modal scrim does.
          * @default true
         */
        "outsideClickDismissible"?: boolean;
        /**
          * Placeholder shown inside the text-field input.
          * @default ''
         */
        "placeholder"?: string;
        /**
          * Accessible label for the "previous month" nav chevron.
          * @default 'Previous month'
         */
        "previousMonthLabel"?: string;
        /**
          * Accessible label for the "previous year" nav chevron.
          * @default 'Previous year'
         */
        "previousYearLabel"?: string;
        /**
          * Marks the field as required (for form-validation contexts).
          * @default false
         */
        "required"?: boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace"?: boolean;
        /**
          * Clicking the modal scrim closes the picker when true.
          * @default true
         */
        "scrimDismissible"?: boolean;
        /**
          * Large headline when no date is staged in calendar mode.
          * @default 'Select date'
         */
        "selectDateLabel"?: string;
        /**
          * Helper text shown below the field. Hidden while an error is displayed.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Mode-toggle label when in typed-entry mode (switch to calendar).
          * @default 'Switch to calendar input'
         */
        "toggleCalendarLabel"?: string;
        /**
          * Mode-toggle label when in calendar mode (switch to text).
          * @default 'Switch to text input'
         */
        "toggleTextLabel"?: string;
        /**
          * Selected date as ISO `YYYY-MM-DD`. Two-way bindable.
          * @default ''
         */
        "value"?: string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please choose a date.'
         */
        "valueMissingLabel"?: string;
        /**
          * Presentation variant.
          * @default 'modal-input'
         */
        "variant"?: 'modal' | 'modal-input' | 'docked';
        /**
          * Accessible label for the modal year-selection grid.
          * @default 'Year'
         */
        "yearGridLabel"?: string;
    }
    interface MdDialog {
        /**
          * Label for the default cancel action button (slot fallback). Leave empty to derive from `locale`.
          * @default ''
         */
        "cancelLabel"?: string;
        /**
          * Accessible label for the full-screen close button. Leave empty to derive from `locale`.
          * @default ''
         */
        "closeLabel"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Show divider between content and actions
          * @default false
         */
        "divider"?: boolean;
        /**
          * Full-screen variant
          * @default false
         */
        "fullscreen"?: boolean;
        /**
          * Show divider between header and content (full-screen)
          * @default false
         */
        "headerDivider"?: boolean;
        /**
          * Headline text (or use the headline slot)
          * @default ''
         */
        "headline"?: string;
        /**
          * Icon name (Material Symbols shorthand, or use the icon slot)
          * @default ''
         */
        "icon"?: string;
        /**
          * BCP-47 locale for built-in button labels (Close, Cancel, OK).
          * @default 'en-US'
         */
        "locale"?: string;
        /**
          * Label for the default confirm action button (slot fallback). Leave empty to derive from `locale`.
          * @default ''
         */
        "okLabel"?: string;
        /**
          * Emits when dismissed via scrim or Escape
         */
        "onMdCancel"?: (event: MdDialogCustomEvent<void>) => void;
        /**
          * Emits when the dialog closes
         */
        "onMdClose"?: (event: MdDialogCustomEvent<void>) => void;
        /**
          * Emits when the dialog opens
         */
        "onMdOpen"?: (event: MdDialogCustomEvent<void>) => void;
        /**
          * Whether the dialog is open
          * @default false
         */
        "open"?: boolean;
        /**
          * Whether clicking the scrim closes the dialog
          * @default true
         */
        "scrimDismissible"?: boolean;
    }
    interface MdDivider {
        /**
          * Middle-inset: 16px margin on both sides
          * @default false
         */
        "inset"?: boolean;
        /**
          * Inset end only: 0px leading margin, 16px trailing
          * @default false
         */
        "insetEnd"?: boolean;
        /**
          * Inset start only: 16px leading margin, 0px trailing
          * @default false
         */
        "insetStart"?: boolean;
        /**
          * Vertical orientation
          * @default false
         */
        "vertical"?: boolean;
    }
    interface MdFab {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Whether the FAB shows its label (extended) or collapses to an icon-only circle. Leave unset for the default behaviour (extended whenever a `label` is provided). Set explicitly — e.g. by `<md-navigation-rail>` on collapse — to morph between extended and icon-only: the label width/opacity springs closed and the container shrinks to a 56px circle, while the label stays available as the accessible name.
         */
        "extended"?: boolean;
        /**
          * @default ''
         */
        "icon"?: string;
        /**
          * @default ''
         */
        "label"?: string;
        /**
          * @default false
         */
        "lowered"?: boolean;
        /**
          * Fires when the user activates the FAB (mouse click, touch, or `Enter`/`Space` while focused). Not emitted when `disabled` or `soft-disabled`. Bubbles and is composed so a delegated listener on a parent element works.  ```ts document.querySelector('md-fab')!.addEventListener('mdClick', (e) => {   const mouseEvent = (e as CustomEvent<MouseEvent>).detail;   console.log('FAB clicked at', mouseEvent.clientX, mouseEvent.clientY); }); ```
         */
        "onMdClick"?: (event: MdFabCustomEvent<MouseEvent>) => void;
        /**
          * @default true
         */
        "ripple"?: boolean;
        /**
          * @default 'standard'
         */
        "size"?: 'standard' | 'medium' | 'large';
        /**
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * @default 'primary-container'
         */
        "variant"?: 'surface' | 'primary-container' | 'secondary-container' | 'tertiary-container' | 'primary' | 'secondary' | 'tertiary';
    }
    interface MdFabMenu {
        /**
          * ID of the anchor FAB element to position relative to.
          * @default ''
         */
        "anchor"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Accessible name for the popup itself (the `role="menu"` container).  A prop rather than a hardcoded string, matching how every other user-facing string in the library is localised: components stay i18n-engine-agnostic and the consumer resolves the text from its own dictionary. The default is English, so a translated app must set it.
          * @default 'Actions'
         */
        "menuLabel"?: string;
        /**
          * Fires when the menu closes.
         */
        "onMdClose"?: (event: MdFabMenuCustomEvent<void>) => void;
        /**
          * Fires when the menu opens.
         */
        "onMdOpen"?: (event: MdFabMenuCustomEvent<void>) => void;
        /**
          * Whether the menu is open.
          * @default false
         */
        "open"?: boolean;
        /**
          * Direction items fan out from the FAB. - 'up': items stack above the FAB (default, standard bottom-right placement) - 'down': items stack below the FAB - 'auto': detect based on available viewport space
          * @default 'up'
         */
        "placement"?: 'up' | 'down' | 'auto';
        /**
          * Skip open/close animation.
          * @default false
         */
        "quick"?: boolean;
        /**
          * Color set for the menu. Controls the FAB's open-state color (vibrant) and item container color (softer).
          * @default 'primary'
         */
        "variant"?: 'primary' | 'secondary' | 'tertiary';
    }
    interface MdFabMenuItem {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Whether the item is disabled (removed from tab order).
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Material Symbols icon name.
          * @default ''
         */
        "icon"?: string;
        /**
          * Item label text.
          * @default ''
         */
        "label"?: string;
        /**
          * Fires when the item is activated.
         */
        "onMdClick"?: (event: MdFabMenuItemCustomEvent<void>) => void;
        /**
          * When `true`, paints the keyboard-focus ring even if the item does not hold DOM focus (e.g. arrow-key navigation drove `focusItem()`). Set by `md-fab-menu` during programmatic roving — not on initial open.
          * @default false
         */
        "rovingFocusVisible"?: boolean;
        /**
          * Whether the item is soft-disabled (still focusable but not activatable).
          * @default false
         */
        "softDisabled"?: boolean;
    }
    interface MdIconButton {
        /**
          * M3 Expressive width (narrow, default, wide)
          * @default 'default'
         */
        "buttonWidth"?: 'narrow' | 'default' | 'wide';
        /**
          * @default false
         */
        "connectedLeft"?: boolean;
        /**
          * @default false
         */
        "connectedRight"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Prevents interaction and removes from tab order
          * @default false
         */
        "disabled"?: boolean;
        /**
          * @default null
         */
        "groupTabindex"?: number | null;
        /**
          * Turns the button into a link
          * @default ''
         */
        "href"?: string;
        /**
          * Material Symbols icon name
          * @default ''
         */
        "icon"?: string;
        /**
          * Fires on pointer or keyboard activation. Detail includes toggle `selected` state.
         */
        "onMdClick"?: (event: MdIconButtonCustomEvent<{ selected: boolean }>) => void;
        /**
          * Whether to show the ripple effect
          * @default true
         */
        "ripple"?: boolean;
        /**
          * Current selected state (toggle mode)
          * @default false
         */
        "selected"?: boolean;
        /**
          * Icon shown when selected in toggle mode
          * @default ''
         */
        "selectedIcon"?: string;
        /**
          * Container shape
          * @default 'round'
         */
        "shape"?: 'round' | 'square';
        /**
          * Enable M3 Expressive shape morphing on press and toggle
          * @default true
         */
        "shapeMorph"?: boolean;
        /**
          * M3 Expressive size
          * @default 'sm'
         */
        "size"?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Visually disabled but remains focusable for discoverability
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Link target attribute
          * @default '_self'
         */
        "target"?: string;
        /**
          * Enable toggle behavior (selected/unselected)
          * @default false
         */
        "toggle"?: boolean;
        /**
          * @default ''
         */
        "value"?: string;
        /**
          * Visual style variant
          * @default 'standard'
         */
        "variant"?: 'standard' | 'filled' | 'outlined' | 'tonal';
    }
    /**
     * md-line-chart — Material Design 3 line chart rendered by the
     * in-house chart engine (Canvas2D + a DOM text overlay). No
     * ECharts.
     * Pattern parity with MUI X Charts:
     *   • multi-series with per-series colour / label / curve
     *   • category, time, value, log scales
     *   • curve types: linear / smooth / monotone / step variants
     *   • stacking: normal / percentage
     *   • markers, area fills, gap bridging (`connectNulls`)
     *   • legend with toggle + custom position
     *   • hover crosshair + tooltip
     *   • emits `mdMarkerClick`, `mdLineClick`, `mdAreaClick`, `mdAxisClick`,
     *     `mdLegendClick`, `mdHover`
     * MD3 expressive layer on top:
     *   • palette resolves to `--md-sys-color-*` tokens
     *   • dark theme + brand re-themes via CSS variables only
     *   • a11y: role="figure" + screen-reader-only data table
     *   • fully responsive via ResizeObserver
     */
    interface MdLineChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation"?: MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Render the line area as a filled gradient below the line.
          * @default false
         */
        "area"?: boolean;
        /**
          * Draw small perpendicular tick marks on the axes.
          * @default false
         */
        "axisTicks"?: boolean;
        /**
          * Whether `null` values are bridged with a straight line.
          * @default false
         */
        "connectNulls"?: boolean;
        /**
          * Default curve interpolation. Overridable per-series.
          * @default 'smooth'
         */
        "curve"?: 'linear' | 'smooth' | 'monotone' | 'step' | 'step-before' | 'step-middle';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Gridlines: horizontal (y ticks), vertical (x ticks), both, or none.
          * @default 'horizontal'
         */
        "grid"?: 'none' | 'horizontal' | 'vertical' | 'both';
        /**
          * Force a specific height (CSS length).
         */
        "heightProp"?: string;
        /**
          * Transpose the axes — the x-axis data runs vertically and the values run horizontally (e.g. a temperature-by-altitude spline). Best with `curve="smooth"`.
          * @default false
         */
        "inverted"?: boolean;
        /**
          * Optional chart title rendered above the plot.
          * @default ''
         */
        "label"?: string;
        /**
          * Text shown when there is no data. The `empty` slot still overrides it — this is the prop form, for handing a string straight from a dictionary.
          * @default 'No data to display'
         */
        "labelEmpty"?: string;
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the data with the arrow keys; this is what tells them so.
          * @default 'Chart data. Use the arrow keys to move between points, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot"?: string;
        /**
          * Template for the live announcement made as keyboard focus moves. `%x%` is the axis value and `%values%` the series readings at it.
          * @default '%x%: %values%'
         */
        "labelPoint"?: string;
        /**
          * Accessible label for the zoom slider's end thumb.
          * @default 'Zoom range end'
         */
        "labelZoomEnd"?: string;
        /**
          * Accessible label for the zoom slider's start thumb.
          * @default 'Zoom range start'
         */
        "labelZoomStart"?: string;
        /**
          * Legend position. `'none'` hides the legend.
          * @default 'top-end'
         */
        "legend"?: MdChartLegendPosition | 'none';
        /**
          * Line stroke width in px, for every series. Default 2.5; a per-series `series[].lineWidth` overrides it.
         */
        "lineWidth"?: number;
        /**
          * Data is still on its way — the chart covers the plot with a loader instead of drawing an empty (or stale) axis, and marks itself `aria-busy`.  Set it while the fetch is in flight and clear it when `series` arrives; the entry animation replays on the way out, so the data draws itself in rather than appearing fully formed. Slot `loading` to replace the default indicator with your own skeleton.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Accessible + visible text under the loader.
          * @default 'Loading chart…'
         */
        "loadingLabel"?: string;
        /**
          * BCP-47 locale for the DEFAULT number / date formatting — axis ticks, tooltip values and the screen-reader table. Empty follows the browser. An explicit `valueFormatter` / `xAxis.valueFormatter` always wins, so a consumer that formats its own values is unaffected.
          * @default ''
         */
        "locale"?: string;
        /**
          * Vertical reference lines across the plot (e.g. a "current time" divider).
         */
        "markLines"?: MdChartMarkLine[] | undefined;
        /**
          * Marker RADIUS in px, for every series. Overrides the per-symbol default; a per-series `series[].markSize` wins.
         */
        "markSize"?: number;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation"?: boolean;
        /**
          * Fires when a series' filled area is clicked (`area` charts only) — the region between that series' line and its baseline, excluding its line and points. `dataIndex` is the point the click is nearest along x.
         */
        "onMdAreaClick"?: (event: MdLineChartCustomEvent<MdChartClickDetail<MdChartXYSeries>>) => void;
        /**
          * Fires when the plot background is clicked (inside the plot, but not on a mark, line or area): the nearest x position plus every visible series' value there.
         */
        "onMdAxisClick"?: (event: MdLineChartCustomEvent<MdChartAxisClickDetail>) => void;
        /**
          * Fires (throttled to rAF) as the pointer crosses the plot.
         */
        "onMdHover"?: (event: MdLineChartCustomEvent<MdChartHoverDetail>) => void;
        /**
          * Fires when a legend entry is clicked.
         */
        "onMdLegendClick"?: (event: MdLineChartCustomEvent<{ seriesIndex: number; seriesId?: string; selected: boolean }>) => void;
        /**
          * Fires when a series' drawn line is clicked *between* its data points (a click on a point emits `mdMarkerClick` instead). `dataIndex` is the point the click is nearest along x.
         */
        "onMdLineClick"?: (event: MdLineChartCustomEvent<MdChartClickDetail<MdChartXYSeries>>) => void;
        /**
          * Fires on marker click. `dataIndex` addresses the clicked series' own `data` array (its point index, for series that carry their own x).
         */
        "onMdMarkerClick"?: (event: MdLineChartCustomEvent<MdChartClickDetail<MdChartXYSeries>>) => void;
        /**
          * Fires after the chart finishes its initial render.
         */
        "onMdReady"?: (event: MdLineChartCustomEvent<void>) => void;
        /**
          * Fires when the zoom window changes (drag, slider or `setZoom`/`resetZoom`).
         */
        "onMdZoom"?: (event: MdLineChartCustomEvent<{ startIndex: number; endIndex: number; reset: boolean }>) => void;
        /**
          * Series array. Each entry is one line. Empty → empty state.  `data` is either a bare list of y values, positioned by index against `xAxis.data`, or a list of points carrying their own x — `{ x: '2021-11-13', y: 0.12 }` / `['2021-11-13', 0.12]`. Points are what irregular data needs: uneven sampling, or several series measured on completely different dates. Set `xAxis.scale = 'time'` (or `'value'`) so the gaps render proportionally; series carrying their own x need no `xAxis.data` at all.
          * @default []
         */
        "series"?: MdChartXYSeries[];
        /**
          * Label each series at its last point with its name (follows the line end).
          * @default false
         */
        "seriesLabels"?: boolean;
        /**
          * Print each point's value as a data label beside its marker.
          * @default false
         */
        "showLabels"?: boolean;
        /**
          * Draw the line for each series. Default `true`. Set `false` for a fill-only chart (with `area`) or a marks-only chart — handy when a high `fillOpacity` makes the same-coloured line vanish into the fill. A per-series `series[].stroke` still wins over this.
          * @default true
         */
        "showLine"?: boolean;
        /**
          * Show data-point markers on every series by default.
          * @default false
         */
        "showMarks"?: boolean;
        /**
          * Stacking strategy.
          * @default 'none'
         */
        "stack"?: MdChartStackMode;
        /**
          * Optional sub-title rendered under the title in the muted text colour.
         */
        "subtitle"?: string | undefined;
        /**
          * Replaces the generated `aria-label` outright. The default summary is assembled in English ("Revenue, Line chart, with 2 series, from Jan to Dec."); rather than translate it piecewise, hand over the whole sentence built in your own language.
          * @default ''
         */
        "summary"?: string;
        /**
          * Translatable chrome for the screen-reader data table. `%shown%` and `%total%` are substituted in `truncated`.
         */
        "tableLabels"?: { x?: string; index?: string; series?: string; truncated?: string };
        /**
          * Title alignment over the plot: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign"?: MdChartTitleAlign;
        /**
          * Tooltip interaction model.
          * @default 'axis'
         */
        "tooltip"?: MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content with your own. Receives everything the chart knows about the hovered x — the axis value + its formatted label, and per visible series the index, label, colour, raw and formatted value, and whether it is the emphasised one — split into `series` (has a reading here) and `missing` (does not).  Return a DOM `Node` (what React / Vue / Angular renderers produce), a string (inserted as TEXT, never parsed as markup), `{ unsafeHtml }` to opt into raw HTML, `undefined` to fall back to the built-in tooltip, or `null` for no tooltip at this x. The engine keeps positioning it, and it is still the `tooltip` CSS part.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        /**
          * Format raw Y values for tooltips / a11y table.  The tooltip also asks it about series with NO value at the hovered x — `null` when the series has a null datum there, `undefined` when it has no datum at all. Return `''` (or leave those cases unhandled) to keep the default behaviour of omitting the series; return a string to show a row for it — e.g. `(v) => (v === null ? 'no reading' : v === undefined ? '—' : v + ' m')`.
         */
        "valueFormatter"?: (value: number | null | undefined) => string;
        /**
          * X-axis configuration.
         */
        "xAxis"?: MdChartAxis | undefined;
        /**
          * Multiple independent value (y) axes. When set (non-empty), each series is measured against `yAxes[series.yAxisIndex ?? 0]`, and the axes stack outward from the plot (first → left, rest → right, or per each axis' `position`). Supersedes the single `yAxis`. Each axis keeps its own `min`/`max`/`scale`/ `valueFormatter`/`label`.
         */
        "yAxes"?: MdChartAxis[] | undefined;
        /**
          * Y-axis configuration.
         */
        "yAxis"?: MdChartAxis | undefined;
        /**
          * Interactive zoom over the x range.   • `inside` — drag horizontally across the plot to zoom into that span;     double-click anywhere in the plot to reset.   • `slider` — an `md-slider` in range mode under the plot: drag either     thumb to resize the window, click the track to jump the nearest thumb,     drag the window itself to pan. Thumbs are focusable and take     Arrow/Home/End/PageUp/PageDown.   • `both` — both of the above.  Zoom is a *view* over the data: `series`/`xAxis` are untouched, and `mdHover`/`mdMarkerClick` keep reporting absolute indices into your data.
          * @default 'none'
         */
        "zoom"?: 'none' | 'inside' | 'slider' | 'both';
    }
    interface MdList {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Row interaction pattern for the whole list. A list uses exactly one interaction mode at a time (per the M3 Lists spec).  - `single-action` (default): each row is one tappable area — leading   icon + label activate together. Use `type="button"` (or selection   mode) on items. - `multi-action`: rows expose a primary action (the row body) plus   optional secondary actions in `slot="trailing"` (e.g. `md-icon-button`).   Clicks on trailing controls do not activate the primary row action;   each secondary control keeps its own focus stop and keyboard behavior.
          * @default 'single-action'
         */
        "interactionMode"?: MdListInteractionMode;
        /**
          * Optional aria-label for the container.
          * @default ''
         */
        "label"?: string;
        /**
          * Optional aria-labelledby for the container.
          * @default ''
         */
        "labelledby"?: string;
        /**
          * Visual style of the list.  - `standard` (default, M3 Expressive): rows share a single rounded   chassis with **no gap** between them. The first row rounds its   top corners, the last rounds its bottom corners, and middle rows   are square so the stack reads as one continuous shape — the M3   "connected" pattern. Best for settings panels, option groups,   and iOS-style sectioned tables. To add hairline separators   between rows, interleave `<md-divider>` elements. - `segmented` (M3 Expressive): rows are individually rounded "tiles"   separated by a small gap. Each tile rounds all four corners. Best   for action sets, quick-action menus, and card-like sections where   each row should read as its own affordance.
          * @default 'standard'
         */
        "listStyle"?: MdListStyle;
        /**
          * Emitted whenever roving focus lands on a different row.
         */
        "onMdActivate"?: (event: MdListCustomEvent<{ index: number; item: HTMLMdListItemElement }>) => void;
        /**
          * Emitted after a drag (or keyboard) reorder completes.  - `from` / `to`: the moved row's old and new positions (item indices,   ignoring non-item children like dividers). - `order`: the new order expressed as the original (as-authored) indices,   so `order[newPosition] === originalPosition`. Use it to reorder a   backing data array in one pass.
         */
        "onMdReorder"?: (event: MdListCustomEvent<{ from: number; to: number; order: number[] }>) => void;
        /**
          * Emitted when selection changes via user input (click / Enter / Space).
         */
        "onMdSelect"?: (event: MdListCustomEvent<MdListSelectDetail>) => void;
        /**
          * Enable drag-to-reorder. Each row renders a trailing drag handle (grab it to drag the row to a new position); rows can also be moved with `Alt + ArrowUp / ArrowDown` while focused. On drop the list reorders its children in the DOM and emits {@link mdReorder}. Disabled rows can't be picked up. Works alongside `single-action` / `multi-action` and selection modes.
          * @default false
         */
        "reorderable"?: boolean;
        /**
          * Override the auto-computed ARIA role. Useful when wiring the list into a wider widget pattern (e.g. `role="menu"` for a dropdown). Leave empty to use the role implied by `selectionMode`.
          * @default ''
         */
        "roleOverride"?: string;
        /**
          * Selection coordination across child rows.  - `none` (default): each row stands alone and the container is always   `role="list"` (rows are `listitem`s). Interactive rows (`type="button"` /   `type="link"`) expose their widget role on an inner primary element, so   the list validly owns only `listitem`s while still providing the same   roving-tabindex + arrow-key navigation. `type="text"` rows stay   non-focusable. - `single-select`: the container behaves as `role="listbox"`, items   become `role="option"`, and exactly one item carries `aria-selected`. - `multi-select`: same as above plus `aria-multiselectable="true"`.  Switching to a selection mode also auto-promotes plain `type="text"` rows to focusable so users can pick from a list of label-only rows without authoring extra props.
          * @default 'none'
         */
        "selectionMode"?: MdListSelectionMode;
    }
    interface MdListItem {
        /**
          * The resolved ARIA role of the parent `md-list` container, propagated imperatively by that parent — authors should not touch this. It lets a row adapt its own role to its container: when the container is a `menu` / `menubar` (via `role-override`) an interactive row exposes `role="menuitem"` on the HOST itself (the host IS the widget, like `option`) and skips the inner primary widget, because a `menu` owns its direct light-DOM children (the hosts) and must own `menuitem`s — a `listitem` / `role="none"` host is a disallowed menu child (axe `aria-required-children`). Empty for a plain `list` / `listbox`, where the default `listitem` / `option` roles apply.
          * @default ''
         */
        "containerRole"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disables interaction, removes from tab order, and dims content to 38 % opacity.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Turns the row into a single-level collapsible disclosure. When `true`:   - a trailing **`md-icon-button`** caret renders after any author-supplied     trailing content. It is the disclosure control: it carries     `aria-expanded` + `aria-controls`, gets the icon-button pill/squircle     shape morph when selected (expanded), and its `expand_more` glyph     rotates 180° (points up) when expanded.   - the `expanded-content` slot reveals one level of flat `md-list-item`     child rows (not a nested `md-list` chassis) inline below the parent     with an MD3 expressive open (emphasized-decelerate) / retract     (emphasized-accelerate) height + opacity animation.   - clicking the row body also toggles the panel as a convenience; the     caret button additionally toggles on Enter / Space when focused.   - ArrowRight expands, ArrowLeft collapses (when the row itself is     focusable, e.g. `type="button"` / reorderable rows).
          * @default false
         */
        "expandable"?: boolean;
        /**
          * Reflects/controls the expanded state of an `expandable` row.
          * @default false
         */
        "expanded"?: boolean;
        /**
          * Headline text. Overridden by `slot="headline"`.
          * @default ''
         */
        "headline"?: string;
        /**
          * Link href. Honored when `type='link'`.
          * @default ''
         */
        "href"?: string;
        /**
          * Interaction mode propagated from parent `md-list`. Set imperatively by the parent — authors should not need to touch this directly.  - `single-action`: the whole row is one primary action. - `multi-action`: trailing slot may host secondary controls (icon   buttons, switches) that keep their own focus/click semantics.
          * @default 'single-action'
         */
        "interactionMode"?: 'single-action' | 'multi-action';
        /**
          * Avatar image URL — forwarded to an internal `md-avatar` rendered in the leading slot. Falls back to `leadingAvatarName` / `leadingAvatarLabel` if the image fails to load.
          * @default ''
         */
        "leadingAvatar"?: string;
        /**
          * Alt text for `leadingAvatar`. Also surfaced as the avatar's accessible label.
          * @default ''
         */
        "leadingAvatarAlt"?: string;
        /**
          * Explicit initials shown by the internal `md-avatar`. Overrides the `leadingAvatarName` parsing. Used for "C", "JD" style labels.
          * @default ''
         */
        "leadingAvatarLabel"?: string;
        /**
          * Full name forwarded to the internal `md-avatar` so it can auto-derive initials when no image is available (`"Ada Lovelace"` → `"AL"`). When both this and `leadingAvatarLabel` are empty the avatar falls back to a `person` icon.
          * @default ''
         */
        "leadingAvatarName"?: string;
        /**
          * Material Symbols name for the leading slot (24 dp container, 18 dp icon).
          * @default ''
         */
        "leadingIcon"?: string;
        /**
          * Image thumbnail URL (56 dp square by default).
          * @default ''
         */
        "leadingImage"?: string;
        /**
          * Alt text for `leadingImage`.
          * @default ''
         */
        "leadingImageAlt"?: string;
        /**
          * Number of text rows. Auto-detected when 1 + content suggests more (overline → 2-line, overline + supporting → 3-line). Set explicitly to override the auto-promotion.
          * @default 1
         */
        "lines"?: 1 | 2 | 3;
        /**
          * Fired when an interactive row is activated (click, Enter, or Space).
         */
        "onMdClick"?: (event: MdListItemCustomEvent<{ item: HTMLElement }>) => void;
        /**
          * Fires whenever the expanded state of an expandable row changes.
         */
        "onMdExpand"?: (event: MdListItemCustomEvent<MdListItemExpandDetail>) => void;
        /**
          * Bubbles to parent `md-list` so it can manage selection state.
         */
        "onMdItemClick"?: (event: MdListItemCustomEvent<{ index: number; selected: boolean }>) => void;
        /**
          * Internal event the parent `md-list` listens to for keyboard reordering (`Alt + ArrowUp / ArrowDown`). Bubbles + composed so it crosses the shadow boundary up to the list.
         */
        "onMdItemRequestReorder"?: (event: MdListItemCustomEvent<{
    direction: 'up' | 'down';
    from: number;
  }>) => void;
        /**
          * Bubbles to parent `md-list` when an `expanded-content` child row is selected. The list re-emits `mdSelect` with the parent's list index, `childIndex`, `expanded`, and the child in `item`.
         */
        "onMdItemSelect"?: (event: MdListItemCustomEvent<{
    index: number;
    value: string;
    selected: boolean;
    item: HTMLMdListItemElement;
  }>) => void;
        /**
          * Asks the parent `md-list` to give this row roving focus.
         */
        "onMdRequestActivation"?: (event: MdListItemCustomEvent<void>) => void;
        /**
          * Overline text shown above the headline. Overridden by `slot="overline"`.
          * @default ''
         */
        "overline"?: string;
        /**
          * Whether this row can be reordered via drag-and-drop. Set imperatively by the parent `md-list[reorderable]` — authors should not need to touch this directly. When `true` the row renders a trailing drag handle (grab it to drag) and supports `Alt + ArrowUp / ArrowDown` to move when focused. Disabled rows ignore it.
          * @default false
         */
        "reorderable"?: boolean;
        /**
          * When `true`, paints the keyboard-focus ring even if the row does not hold DOM focus (e.g. toolbar buttons drove `activateNext()`). Set imperatively by `md-list` during programmatic / arrow-key navigation — not on initial load.
          * @default false
         */
        "rovingFocusVisible"?: boolean;
        /**
          * Selected state. Toggled by `md-list` when `selection-mode` is on.
          * @default false
         */
        "selected"?: boolean;
        /**
          * Selection mode propagated from parent `md-list`. Set imperatively by the parent — authors should not need to touch this directly. When non-`none`, the item becomes focusable + clickable regardless of `type` and renders with `role="option"` + `aria-selected`.
          * @default 'none'
         */
        "selectionMode"?: 'none' | 'single-select' | 'multi-select';
        /**
          * Visually disabled but remains focusable for discoverability. Does not emit activation events.
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Supporting text below the headline. Overridden by `slot="supporting-text"`.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Whether this item is the currently tabbable one in its parent list. Managed by `md-list`'s roving tabindex. Standalone items leave this as `true` and become focusable when `type !== 'text'`.
          * @default true
         */
        "tabbable"?: boolean;
        /**
          * Link target. Honored when `type='link'`.
          * @default '_self'
         */
        "target"?: string;
        /**
          * Material Symbols name for the trailing slot (24 dp icon).
          * @default ''
         */
        "trailingIcon"?: string;
        /**
          * Trailing supporting text on the right. Overridden by `slot="trailing-supporting-text"`.
          * @default ''
         */
        "trailingSupportingText"?: string;
        /**
          * Behavior of the row.  - `text` (default): non-interactive content row. No focus, no ripple,   no click event. Use for static labels and section headers. - `button`: focusable, ripples, fires `mdClick` on activation. - `link`: same as `button` but also navigates to `href` in `target`.  When the row sits inside an `md-list` with `selection-mode` set, the row becomes focusable + clickable regardless of `type` so users can pick from a list of plain rows without authoring extra props.
          * @default 'text'
         */
        "type"?: MdListItemType;
    }
    interface MdLoadingIndicator {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Accessible label for screen readers
          * @default 'Loading'
         */
        "label"?: string;
        /**
          * Visual style: uncontained (default) or contained (with background container)
          * @default 'uncontained'
         */
        "variant"?: 'uncontained' | 'contained';
    }
    interface MdMenu {
        /**
          * ID of the anchor element to position relative to.
          * @default ''
         */
        "anchor"?: string;
        /**
          * When false, opening the menu initializes roving tabindex without moving focus to the first item. The consumer handles focus (e.g. docked pickers that scroll to the selected option). `show({ autoFocus: false })` still takes precedence for a single open.
          * @default true
         */
        "autoFocus"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Message shown when all menu items are hidden (e.g. no filter results). Empty string disables.
          * @default ''
         */
        "emptyText"?: string;
        /**
          * Layout style for vertical menus. 'standard' is flat, 'grouped' uses visual section separators.
          * @default 'standard'
         */
        "layout"?: 'standard' | 'grouped';
        "listLabel"?: string;
        /**
          * Present the popup as a WAI-ARIA listbox (surface `role="presentation"`, anchor `aria-haspopup="listbox"`) rather than a menu. `md-select` sets this for both its virtual and non-virtual lists so the popup is a listbox of options regardless of size. (The virtual path additionally registers a provider, which adds the aria-activedescendant focus model.)
          * @default false
         */
        "listbox"?: boolean;
        /**
          * When true, the menu surface min-width matches the anchor element's width. The menu still grows wider if items need more space.
          * @default false
         */
        "matchAnchorWidth"?: boolean;
        /**
          * Cap the menu surface block-size. When the items exceed it the list scrolls vertically inside a plain scroll viewport. A number is treated as pixels; any CSS length is passed through (e.g. `'50vh'`). Ignored when the menu has submenus — their flyouts must not be clipped by an overflow container.
         */
        "maxHeight"?: number | string;
        /**
          * Fires when the menu closes. Scoped like `mdOpen` — see its note.
         */
        "onMdClose"?: (event: MdMenuCustomEvent<void>) => void;
        /**
          * Fires when the menu opens. Scoped to the menu element: `bubbles: false` stops it climbing the light-DOM tree, and `composed: false` stops it escaping the shadow root of a host that embeds this menu (md-select, md-multi-select, md-date-picker …). Without `composed: false` a `composed` event still surfaces at the embedding host as an `AT_TARGET` event — so a consumer's `mdOpen` listener on the wrapper would fire for both the wrapper's own event and this inner one (duplicate open/close). Wrappers listen via `onMdOpen` on the menu element itself, which still fires regardless.
         */
        "onMdOpen"?: (event: MdMenuCustomEvent<void>) => void;
        /**
          * Whether the menu is open.
          * @default false
         */
        "open"?: boolean;
        /**
          * When set, the menu does NOT auto-dismiss in response to interaction elsewhere on the page. By default a menu closes when:   1. The user clicks outside the menu and its anchor.   2. Another non-persistent menu opens (single-open coordination —      opening menu B closes menu A so two top-level menus never      overlap each other on screen).  `persistent` opts out of both auto-dismiss paths. The menu then only closes via `close()`, the anchor's toggle, item activation, or Escape. Useful for modal-style pickers, multi-step menus, and any context where the consumer manages dismissal explicitly.  Submenus (an `<md-menu>` rendered inside a parent menu's `submenu` slot) are never affected by sibling-menu dismissal even without this flag — ancestors and descendants are always preserved.
          * @default false
         */
        "persistent"?: boolean;
        /**
          * Position of the menu relative to the anchor.
          * @default 'bottom-start'
         */
        "placement"?: 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        /**
          * Skip open/close animation.
          * @default false
         */
        "quick"?: boolean;
        /**
          * Adapt to compact viewports. The menu is *always* clamped within the viewport (it never overflows the screen and pins to the edges with a margin). When `responsive` is set, on viewports at or below the compact breakpoint (600px) the menu additionally expands to near-full-width below the trigger for comfortable touch use.
          * @default false
         */
        "responsive"?: boolean;
        /**
          * Use gap separators instead of dividers between groups (vertical menu only).
          * @default false
         */
        "useGap"?: boolean;
        /**
          * Visual variant. - 'baseline': MD3 baseline menu (square corners, surface container) - 'standard': M3 Expressive vertical menu (rounded, surface-based colors) - 'vibrant': M3 Expressive vertical menu (rounded, tertiary-based colors)
          * @default 'baseline'
         */
        "variant"?: 'baseline' | 'standard' | 'vibrant';
    }
    interface MdMenuItem {
        /**
          * Small badge label (e.g. "New") displayed before the trailing area.
          * @default ''
         */
        "badge"?: string;
        /**
          * Position of the selection checkmark indicator.
          * @default 'end'
         */
        "checkPosition"?: 'start' | 'end';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Whether the item is disabled.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Render a divider line below this item.
          * @default false
         */
        "divider"?: boolean;
        /**
          * Render a gap (extra space) below this item — an alternative separator to divider.
          * @default false
         */
        "gap"?: boolean;
        /**
          * Primary label text.
          * @default ''
         */
        "headline"?: string;
        /**
          * Mixed/partial state for a `checkbox` item — renders a dash in the check slot instead of a tick and exposes `aria-checked="mixed"`. Used for a "select all" affordance where only some children are selected. Ignored for `button`/`radio` types and when `selected` is true.
          * @default false
         */
        "indeterminate"?: boolean;
        /**
          * Prevent the parent menu from closing when this item is clicked.
          * @default false
         */
        "keepOpen"?: boolean;
        /**
          * Fires when the item is activated.
         */
        "onMdClick"?: (event: MdMenuItemCustomEvent<void>) => void;
        /**
          * ARIA presentation. `'menuitem'` (default) exposes the item with the menu role family (`menuitem` / `menuitemradio` / `menuitemcheckbox`, state via `aria-checked`). `'option'` exposes it as a listbox option (`role="option"`, state via `aria-selected`) — used by `md-select` so the popup is a proper WAI-ARIA combobox/listbox rather than a menu.
          * @default 'menuitem'
         */
        "presentation"?: 'menuitem' | 'option';
        /**
          * Override the computed ARIA role. Used when a checkbox/radio item is NOT inside a menu — e.g. a "select all" toggle in a listbox popup, which is semantically a `checkbox` (keeps `aria-checked`, drops the menu-only required-parent). Empty = the default menuitem/option role family.
          * @default ''
         */
        "roleOverride"?: string;
        /**
          * Whether the item is currently selected.
          * @default false
         */
        "selected"?: boolean;
        /**
          * Secondary descriptive text below the headline.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Trailing text (e.g. keyboard shortcut).
          * @default ''
         */
        "trailingText"?: string;
        /**
          * Type of item: 'button' (default action), 'checkbox' (toggle), or 'radio' (single-select within group).
          * @default 'button'
         */
        "type"?: 'button' | 'checkbox' | 'radio';
    }
    interface MdMenuItemGroup {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Optional label shown above the group items.
          * @default ''
         */
        "label"?: string;
    }
    /**
     * md-meter — a read-only graphical display of a numeric value within a known
     * range (storage used, quota, password strength, battery level).
     * The semantics live on the HOST: `role="meter"` with the full ARIA value
     * contract, a purely presentational track + indicator inside the shadow root
     * (all `aria-hidden`), value clamping into `[min, max]`, and Intl-driven value
     * formatting (locale-aware percent by default, any `Intl.NumberFormatOptions`
     * via `formatOptions`). Keeping the ARIA outside the shadow root is what lets
     * the visuals change shape without touching what is announced.
     * Deliberately NOT a progress indicator: a meter shows a *state* ("how full"),
     * a progress indicator shows an *activity* ("how far along"). It is therefore
     * a plain track + fill — no 4px gap, no stop dot, no indeterminate mode, no
     * completion animation. Nothing here is focusable or interactive and no events
     * are emitted.
     * Two shapes, one contract: `variant="linear"` (default) is the bar;
     * `variant="circular"` is the same reading as a ring, with the value in the
     * middle and the label beneath. The ARIA lives on the host either way, so the
     * shape is presentational and swapping it changes nothing for a screen reader.
     */
    interface MdMeter {
        /**
          * Colour role applied to the fill and track. Any role name the THEME defines is accepted, not just the listed ones — the meter resolves `<name>` to the `--md-sys-color-<name>` / `-<name>-container` pair at runtime, so `color="brand"` works as soon as a theme ships those tokens.  The default (`primary`) keeps md-progress-indicator's neutral look: primary fill on a secondary-container track. Every other role paints `<role>` fill on a `<role>-container` track.
          * @default 'primary'
         */
        "color"?: | 'primary'
    | 'secondary'
    | 'tertiary'
    | 'error'
    | 'success'
    | 'warning'
    | 'info'
    | (string & {});
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * `Intl.NumberFormatOptions` for the value text (JS-only object prop — set it as a property, not an attribute). When provided, the CLAMPED value is formatted with it (`{ style: 'unit', unit: 'gigabyte' }`, currency, plain number…). When omitted, the value's position in the range is formatted as a locale-aware percentage — the default, because a meter answers "how full", which a bare number cannot say without its bounds alongside it.
         */
        "formatOptions"?: Intl.NumberFormatOptions;
        /**
          * Accessible name (`aria-label`) of the meter. Also the visible header label when `show-label` is set. Pass already-localized text.
          * @default ''
         */
        "label"?: string;
        /**
          * BCP-47 locale for `Intl.NumberFormat` output (the formatted value text). Empty string = the runtime's default locale. This prop exists ONLY for Intl-computed output; translatable copy (`label`) is a plain text prop.
          * @default ''
         */
        "locale"?: string;
        /**
          * Upper bound of the range.
          * @default 100
         */
        "max"?: number;
        /**
          * Lower bound of the range.
          * @default 0
         */
        "min"?: number;
        /**
          * Renders the `label` text in a header row above the track.
          * @default false
         */
        "showLabel"?: boolean;
        /**
          * Renders the formatted value in the header row above the track, end-aligned. The visible span is `aria-hidden` — screen readers announce the same string via `aria-valuetext` on the host instead.
          * @default false
         */
        "showValue"?: boolean;
        /**
          * Ring diameter in dp — circular only, ignored by the bar. Clamped to 24…240 like md-progress-indicator, and tapered by density through the `--md-meter-size` hook.
          * @default 48
         */
        "size"?: number;
        /**
          * Track thickness in dp. Density-scaled (0.5px per rung), floored at 2px.
          * @default 4
         */
        "thickness"?: number;
        /**
          * Current value. Clamped into `[min, max]` for ARIA, geometry and formatting.
          * @default 0
         */
        "value"?: number;
        /**
          * Overrides the formatted value text entirely — both `aria-valuetext` and the visible header value (when `show-value` is set). Use it when neither the default percent format nor `formatOptions` can express the reading (e.g. "3 of 10 seats").
          * @default ''
         */
        "valueText"?: string;
        /**
          * Shape of the reading.  - `linear` (default) — a horizontal bar, full width of its container. - `circular` — a ring. `show-value` puts the value in the middle of it and   `show-label` captions it underneath, instead of the bar's header row.  The ring does NOT mirror under `dir="rtl"`: it fills clockwise in every locale, matching Material's circular progress (and a clock face).
          * @default 'linear'
         */
        "variant"?: 'linear' | 'circular';
    }
    /**
     * `md-multi-select` — Material Design 3 multi-value dropdown.
     * Shares its internals with `md-select`: an `md-text-field` trigger and an
     * `md-menu` option surface. Options come from slotted `<md-select-option>`
     * children (primary API) or the programmatic `options` array. Each becomes a
     * `type="checkbox"` `md-menu-item` so several can be selected at once, and the
     * menu uses `keep-open` so multiple toggles don't dismiss it.
     * Selected values render in the trigger as removable chips (`display-mode="chips"`),
     * a "{n} selected" count, or comma-joined text.
     */
    interface MdMultiSelect {
        /**
          * For `display-mode="chips-inline"`, how the in-field chips handle running out of room:   - `'count'` (default): chips stay on ONE line; the field keeps its normal     height and the chips that don't fit collapse into a trailing `+N` counter     chip (click it — or the field — to open the menu and manage the full set).   - `'wrap'`: chips wrap onto new rows and the field grows to fit them.
          * @default 'count'
         */
        "chipOverflow"?: 'count' | 'wrap';
        /**
          * Where the removable chips sit relative to the trigger, for `display-mode="chips"` (ignored for `chips-inline`/`count`/`text`): `'bottom'` (default), `'top'`, `'left'`, or `'right'`. `left`/`right` place the chips beside the trigger and are the natural pairing for `trigger="button"`.
          * @default 'bottom'
         */
        "chipPosition"?: 'bottom' | 'top' | 'left' | 'right';
        /**
          * Material Symbols glyph for the clear button.
          * @default 'close'
         */
        "clearIcon"?: string;
        /**
          * Accessible label for the clear button (localization).
          * @default 'Clear selection'
         */
        "clearLabel"?: string;
        /**
          * Show a clear (×) button in the trigger that removes all selections.
          * @default false
         */
        "clearable"?: boolean;
        /**
          * Custom formatter for `display-mode="count"`.
         */
        "countFormatter"?: (count: number) => string;
        /**
          * Density forwarded to the inner field.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * How selected values appear in the trigger:   - `'chips'` (default): removable chips in a region BELOW the field.   - `'chips-inline'`: removable chips INSIDE the trigger input, on a single     line — the field keeps its normal height and never grows; chips that     overflow are clipped (the open menu is the source of truth).   - `'count'`: "{n} selected".   - `'text'`: comma-joined labels.
          * @default 'chips'
         */
        "displayMode"?: 'chips' | 'chips-inline' | 'count' | 'text';
        /**
          * Caret glyph (Material Symbol) for the field trigger; rotates 180° when open. Override the whole glyph with the `dropdown-icon` slot.
          * @default 'arrow_drop_down'
         */
        "dropdownIcon"?: string;
        /**
          * Error state — forwarded to the inner field.
          * @default false
         */
        "error"?: boolean;
        /**
          * Error text rendered in place of supporting text when `error` is set.
          * @default ''
         */
        "errorText"?: string;
        /**
          * Accessible label for the filter search input. Defaults to "Filter {label}".
          * @default ''
         */
        "filterLabel"?: string;
        /**
          * Filter strategy used by `filterable` / `setQuery`.
          * @default 'substring'
         */
        "filterMode"?: FilterMode;
        /**
          * Show a search field in the menu header that filters via the WASM engine.
          * @default false
         */
        "filterable"?: boolean;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label"?: string;
        /**
          * Show a busy state: the trigger shows a spinner instead of the caret and the host is `aria-busy`, and the menu shows a wavy progress bar instead of the option list. Use while an async dataset loads (e.g. before/around a large `loadOptions`). Replace the trigger spinner with the `loader` slot.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Text shown in the trigger / menu while `loading` is true.
          * @default 'Loading…'
         */
        "loadingText"?: string;
        /**
          * Match the dropdown width to the trigger. When `false` the menu sizes to content. Maps to `md-menu`'s `match-anchor-width`. Default `true`.
          * @default true
         */
        "matchTriggerWidth"?: boolean;
        /**
          * Max height of the dropdown menu (px), forwarded to `md-menu`. Virtualized lists fall back to 320 when unset (they need a bounded scroll viewport).
         */
        "maxHeight"?: number;
        /**
          * Maximum number of selected items. `0` = unlimited.
          * @default 0
         */
        "maxSelected"?: number;
        /**
          * Form name. Selected values submit as repeated `name=value` pairs.
          * @default ''
         */
        "name"?: string;
        /**
          * Text shown when there are no options at all (localization).
          * @default 'No options'
         */
        "noOptionsText"?: string;
        /**
          * Text shown when the filter matches no options (localization).
          * @default 'No results'
         */
        "noResultsText"?: string;
        /**
          * Emits the full selected-value array whenever the selection changes.
         */
        "onMdChange"?: (event: MdMultiSelectCustomEvent<string[]>) => void;
        /**
          * Emits when the clear button empties the selection.
         */
        "onMdClear"?: (event: MdMultiSelectCustomEvent<void>) => void;
        /**
          * Emits when the menu closes.
         */
        "onMdClose"?: (event: MdMultiSelectCustomEvent<void>) => void;
        /**
          * Emits when the menu opens.
         */
        "onMdOpen"?: (event: MdMultiSelectCustomEvent<void>) => void;
        /**
          * Emits the removed value when a chip is dismissed.
         */
        "onMdRemove"?: (event: MdMultiSelectCustomEvent<string>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdMultiSelectCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Open state of the menu. Reflected so `:host([open])` rules apply.
          * @default false
         */
        "open"?: boolean;
        /**
          * Programmatic options. Slotted `md-select-option` children take precedence.  Accepts the array (property) or a **JSON string** (attribute), so a plain-HTML page can declare its data inline instead of reaching for a script. Malformed JSON degrades to an empty list with a console warning.
          * @default []
         */
        "options"?: SelectOptionInit[] | string;
        /**
          * Placeholder shown when nothing is selected.
          * @default ''
         */
        "placeholder"?: string;
        /**
          * Menu placement relative to the trigger — forwarded to `md-menu`.
          * @default 'bottom-start'
         */
        "placement"?: 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        /**
          * Required (form validation parity).
          * @default false
         */
        "required"?: boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace"?: boolean;
        /**
          * Fixed virtualized row height (px). Auto-measured from the first row if unset.
         */
        "rowHeight"?: number;
        /**
          * Placeholder for the filter search input (localization).
          * @default 'Search…'
         */
        "searchPlaceholder"?: string;
        /**
          * Accessible label for the search spinner (localization).
          * @default 'Searching'
         */
        "searchingLabel"?: string;
        /**
          * Label for the "Select all" item.
          * @default 'Select all'
         */
        "selectAllLabel"?: string;
        /**
          * Render an inline "Select all" checkbox at the top of the menu.
          * @default false
         */
        "showSelectAll"?: boolean;
        /**
          * Soft-disabled — disabled visuals but still focusable.
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Supporting / helper text below the field.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Trigger style: `'field'` (default) is the readonly md-text-field; `'button'` is a compact button (leading icon + label) that only opens the drawer — the selection shows as removable chips beside it (pair with `chip-position` `left`/`right`). The button always shows chips regardless of `display-mode`.
          * @default 'field'
         */
        "trigger"?: 'field' | 'button';
        /**
          * Leading icon (Material Symbol name) for `trigger="button"`.
          * @default 'add'
         */
        "triggerIcon"?: string;
        /**
          * Button text for `trigger="button"`. Falls back to `label`.
          * @default ''
         */
        "triggerLabel"?: string;
        /**
          * Currently selected values.
          * @default []
         */
        "value"?: string[];
        /**
          * Localized constraint-validation message shown when `required` is unmet. `errorText` still wins when set, so an app-supplied inline message and the native bubble stay in agreement.
          * @default 'Please select at least one option'
         */
        "valueMissingLabel"?: string;
        /**
          * Visual variant — forwarded to the inner `md-text-field`.
          * @default 'outlined'
         */
        "variant"?: 'filled' | 'outlined';
        /**
          * Virtualization mode for large option lists (backed by a WASM data engine):   - `'auto'` (default): virtualize only above `VIRTUALIZE_THRESHOLD` rows.   - `'always'`: always virtualize.   - `'never'`: keep the classic DOM rendering. Virtualized rows render label + selection only (no per-row icon / supporting text), assume a uniform row height, and disable "Select all".
          * @default 'auto'
         */
        "virtualize"?: 'auto' | 'always' | 'never';
    }
    /**
     * `md-navigation-bar` — Material Design 3 navigation bar.
     * A bottom-anchored row of 3–5 top-level destinations, optimized for
     * compact-width screens (mobile). Each destination is an `<md-navigation-tab>`
     * child; the bar orchestrates selection, label visibility, keyboard
     * navigation (roving tabindex + arrow keys), and emits change events.
     * References:
     *   - {@link https://m3.material.io/components/navigation-bar/overview Overview}
     *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
     *   - {@link https://m3.material.io/components/navigation-bar/guidelines Guidelines}
     *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
     */
    interface MdNavigationBar {
        /**
          * Index of the currently selected destination. Two-way: external writes drive selection, internal selection writes back.
          * @default 0
         */
        "activeIndex"?: number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Label visibility for child tabs.  - `always`   — Show all labels (default; M3 baseline). - `selected` — Show only the selected tab's label. - `none`     — Icon-only mode (no labels rendered).  Per child can be overridden with `label-behavior` on the tab.
          * @default 'always'
         */
        "labelBehavior"?: MdNavigationBarLabelBehavior;
        /**
          * Keyboard activation policy. When `false` (default), arrow keys move focus AND immediately activate the focused tab (automatic activation; matches the MD3 reference and most nav bar implementations).  When `true`, arrow keys only move focus — the user must press `Enter` / `Space` to activate. Use this when activation triggers destructive or expensive work.
          * @default false
         */
        "manualActivation"?: boolean;
        /**
          * Emitted when the selected destination changes (user click, keyboard activation, or programmatic `select()` call). Detail includes both the new and previous indices so listeners can short-circuit no-op reselects.
         */
        "onMdChange"?: (event: MdNavigationBarCustomEvent<{ index: number; previousIndex: number }>) => void;
    }
    /**
     * Material Design 3 — Navigation Rail
     * A vertical navigation surface for the left or right edge of medium / large
     * window sizes. Holds 3–7 top-level destinations and optionally a header
     * (menu / brand), a FAB, and a footer.
     * Implements MD3 specs:
     *   - https://m3.material.io/components/navigation-rail/overview
     *   - https://m3.material.io/components/navigation-rail/specs
     *   - https://m3.material.io/components/navigation-rail/guidelines
     *   - https://m3.material.io/components/navigation-rail/accessibility
     */
    interface MdNavigationRail {
        /**
          * Index of the currently active destination. -1 means no destination is active. Two-way bound: updates when the user selects a destination and can be set externally to programmatically change the active destination.
          * @default -1
         */
        "activeIndex"?: number;
        /**
          * Vertical alignment of destinations relative to the rail.
          * @default 'top'
         */
        "alignment"?: 'top' | 'middle' | 'bottom';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Set to `true` to disable focus management (roving tabindex + arrow keys). Use when embedding inside a custom focus management system.
          * @default false
         */
        "disableFocusManagement"?: boolean;
        /**
          * Render a built-in leading menu button that toggles between the collapsed (`standard`) and `expanded` variants. The icon animates between `menu` (collapsed) and `menu_open` (expanded) per the MD3 navigation rail figure. When you'd rather supply your own control, leave this `false` and place a button in the `header` slot wired to the `expand()` / `collapse()` / `toggle()` methods.
          * @default false
         */
        "expandable"?: boolean;
        /**
          * Stretch the rail to the full viewport height (`100dvh`) instead of filling its parent container. Opt-in: by default the rail fills whatever height its container gives it (`100%`), which keeps it embeddable in split-pane / nested / modal-docked layouts. Set this when the rail is the app's top-level side navigation and should always span the screen.
          * @default false
         */
        "fullHeight"?: boolean;
        /**
          * Accessible name announced for the rail's `navigation` landmark. Required for screen reader users (WAI-ARIA APG / WCAG 2.4.1).
          * @default 'Navigation'
         */
        "label"?: string;
        /**
          * Controls whether destination labels are shown.  - `all`: every destination shows its label (default — recommended by MD3 for 3–7 destinations)  - `selected`: only the active destination shows its label  - `none`: icons only
          * @default 'all'
         */
        "labelVisibility"?: 'all' | 'selected' | 'none';
        /**
          * Cap on how many destinations stay in the rail. Any beyond it collapse into an overflow trigger that opens a menu of the rest; choosing one activates that destination. Leave unset for no overflow.  The trigger is a `button` with `aria-haspopup="menu"`, deliberately OUTSIDE the `tablist` — a tablist may only contain tabs.
         */
        "maxVisible"?: number;
        /**
          * Modal styling for the expanded variant per spec: `surface-container` background, level-2 elevation, large corner shape. Use when the expanded rail floats over content instead of being docked; positioning and the scrim are the host app's responsibility. No effect while collapsed.
          * @default false
         */
        "modal"?: boolean;
        /**
          * Emitted when the rail transitions from expanded → collapsed.
         */
        "onMdCollapse"?: (event: MdNavigationRailCustomEvent<void>) => void;
        /**
          * Emitted when the rail transitions from collapsed → expanded.
         */
        "onMdExpand"?: (event: MdNavigationRailCustomEvent<void>) => void;
        /**
          * Emitted when the user selects a destination.
         */
        "onMdTabChange"?: (event: MdNavigationRailCustomEvent<{ index: number; value: string }>) => void;
        /**
          * Layout axis.  - `vertical` (default) — the MD3 rail: a fixed-width column on the leading   edge. - `horizontal` — the same parts laid out as a top-of-page bar: logo   leading, destinations in a row, FAB and footer trailing. For wide layouts   where a side rail would spend width the content wants.  The expand/collapse affordance is vertical-only — a bar has nowhere to grow — so `expandable` is ignored while horizontal.
          * @default 'vertical'
         */
        "orientation"?: 'vertical' | 'horizontal';
        /**
          * Material Symbols ligature for the overflow trigger.
          * @default 'more_horiz'
         */
        "overflowIcon"?: string;
        /**
          * Accessible name and label for the overflow trigger. Localize per page.
          * @default 'More'
         */
        "overflowLabel"?: string;
        /**
          * Accessible label for the built-in toggle button (only rendered when `expandable` is set). Announced to assistive tech as the button's name.
          * @default 'Toggle navigation'
         */
        "toggleLabel"?: string;
        /**
          * Visual variant — `standard` (the spec's *collapsed* rail: 80px wide, stacked icon + label) or `expanded` (icons + label inline, 220–360px wide).
          * @default 'standard'
         */
        "variant"?: 'standard' | 'expanded';
    }
    /**
     * Material Design 3 — Navigation Rail Destination (Tab)
     * A single destination within a `<md-navigation-rail>`. Renders the MD3
     * active-indicator pill (56×32) around the icon, with an optional label
     * underneath (or inline when the parent rail is expanded). Supports badges,
     * disabled state, link mode (`href`) and slotted custom icons.
     * Spec:
     *   - https://m3.material.io/components/navigation-rail/specs
     *   - https://m3.material.io/components/navigation-rail/guidelines
     *   - https://m3.material.io/components/navigation-rail/accessibility
     */
    interface MdNavigationRailTab {
        /**
          * Active / selected state. Reflected. Managed by `<md-navigation-rail>`.
          * @default false
         */
        "active"?: boolean;
        /**
          * Show a small dot badge (notification indicator).
          * @default false
         */
        "badge"?: boolean;
        /**
          * Numeric / textual badge value (renders large badge).
          * @default ''
         */
        "badgeValue"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — destination cannot be activated and is removed from tab order.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * True when the parent rail is expanded; managed by the parent.
          * @default false
         */
        "expanded"?: boolean;
        /**
          * When set, the tab is rendered as a link (anchor) with the given href. Activates link semantics (uses `aria-current="page"` when active).
          * @default ''
         */
        "href"?: string;
        /**
          * Material Symbols Outlined icon name (shorthand). Ignored when the `icon` slot is filled.
          * @default ''
         */
        "icon"?: string;
        /**
          * Visible text label. Recommended for all destinations (MD3 a11y).
          * @default ''
         */
        "label"?: string;
        /**
          * Controls label visibility. Usually set automatically by the parent `<md-navigation-rail>` based on its `label-visibility` prop.
          * @default 'all'
         */
        "labelVisibility"?: 'all' | 'selected' | 'none';
        /**
          * Fired when this destination's slotted dropdown opens or closes. `md-menu`'s own `mdOpen` / `mdClose` are deliberately non-bubbling and non-composed, so the rail has no way to hear them — this is the signal it listens for (it lifts its stacking context so the fixed-position menu is not trapped).
         */
        "onMdSubmenuToggle"?: (event: MdNavigationRailTabCustomEvent<{ open: boolean }>) => void;
        /**
          * Fired when the user activates the destination (click / Enter / Space).
         */
        "onMdTabClick"?: (event: MdNavigationRailTabCustomEvent<{ value: string }>) => void;
        /**
          * Optional target for the link (when `href` is set).
          * @default ''
         */
        "target"?: string;
        /**
          * Optional value emitted alongside `mdTabClick`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * `md-navigation-tab` — a single destination inside `md-navigation-bar`.
     * Renders an icon, an optional label, and an optional badge. Selecting
     * the tab swaps the icon to a filled variant (per M3 spec) and reveals
     * an "active indicator" pill behind the icon.
     * References:
     *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
     *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
     */
    interface MdNavigationTab {
        /**
          * Whether this destination is currently selected.
          * @default false
         */
        "active"?: boolean;
        /**
          * Material Symbols / Material Icons name shown when active. Per M3 spec, active destinations use the filled variant. Defaults to `icon` when empty (the same glyph in both states).
          * @default ''
         */
        "activeIcon"?: string;
        /**
          * Show a small "notification dot" badge in the top-right of the icon. Use when there are new items but no count is needed.
          * @default false
         */
        "badge"?: boolean;
        /**
          * Maximum value rendered when `badgeValue` is purely numeric. Values greater than this render as `"{max}+"` (e.g. `99+`). Set to `0` to disable the cap.
          * @default 999
         */
        "badgeMax"?: number;
        /**
          * Numeric / textual badge value (e.g. `"3"`, `"99+"`, `"New"`). When set, the badge expands to a pill that accommodates the value (M3 calls this the "large badge" variant).
          * @default ''
         */
        "badgeValue"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable interaction. Disabled tabs are skipped by keyboard nav.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * When set, activating the tab also navigates to the URL — useful when each destination is a full route. For SPA routing, intercept the native `click` (call `preventDefault()`) or listen to the bar's `mdChange`; navigation only occurs when `href` is truthy and the click isn't prevented.
          * @default ''
         */
        "href"?: string;
        /**
          * Material Symbols / Material Icons name shown when inactive. Per M3 spec, inactive destinations use the outlined variant.
          * @default ''
         */
        "icon"?: string;
        /**
          * Destination label rendered below the icon.
          * @default ''
         */
        "label"?: string;
        /**
          * Label visibility for this tab. Inherits from the parent `md-navigation-bar`'s `label-behavior` when not set.  Not reflected as an attribute — the parent bar uses `getAttribute('label-behavior')` to detect *explicit* author overrides, so reflecting the default would defeat that signal.
          * @default 'always'
         */
        "labelBehavior"?: MdNavigationTabLabelBehavior;
        /**
          * Fired when the tab is activated (click, Enter, or Space) and the tab is not disabled. The parent `md-navigation-bar` listens for this to drive selection; external code may also listen to detect per-tab activation (including a re-click of the already-active tab, which does not emit `mdChange` on the bar).
         */
        "onMdTabClick"?: (event: MdNavigationTabCustomEvent<{ index: number }>) => void;
        /**
          * Soft-disable the destination: it cannot be activated or selected and is announced as `aria-disabled`, but — unlike `disabled` — it stays keyboard-reachable (the bar's arrow-key tour still lands on it) so screen-reader users can discover a temporarily-unavailable destination.
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Link target (forwarded only when `href` is set).
          * @default ''
         */
        "target"?: string;
    }
    /**
     * `md-number-field` — Material Design 3 number input: an `md-text-field`
     * with locale-aware `Intl.NumberFormat` display, arrow-key / stepper-button /
     * wheel stepping, and native form participation.
     * Behavior contract:
     *   - the visible control is a **plain text input** (`inputmode` numeric or
     *     decimal) — deliberately NOT `role="spinbutton"` and NOT
     *     `type="number"`, so the locale-formatted value stays ordinary readable,
     *     editable text and no native spinner chrome fights the Intl display;
     *     the steppers are separate labeled buttons with `tabindex="-1"` (the
     *     input is the keyboard surface),
     *   - `value` is the raw number (`null` = empty); the formatted string is
     *     display-only state,
     *   - ArrowUp/Down step by `step`, Alt = `smallStep`, Shift = `largeStep`;
     *     Home/End jump to a defined `min`/`max`,
     *   - press-and-hold on a stepper auto-repeats (400ms delay, 60ms interval),
     *   - typing is filtered to the characters of the active locale/format,
     *     parsed leniently and kept verbatim; blur reformats (and clamps unless
     *     `allow-out-of-range`),
     *   - stepping (keys, buttons, wheel) always clamps and commits (`mdChange`).
     * A drag-to-scrub surface (pointer lock over the field) is deliberately out of
     * scope: a niche gesture built on pointer lock, whose behaviour is unreliable
     * in Safari. `allow-wheel-scrub` covers the "nudge without typing" case.
     */
    interface MdNumberField {
        /**
          * Let TYPED text exceed `min`/`max` unclamped (blur does not clamp). Step-based interactions (keys, buttons, wheel) always clamp.
          * @default false
         */
        "allowOutOfRange"?: boolean;
        /**
          * Enable wheel stepping while the inner input has focus.
          * @default false
         */
        "allowWheelScrub"?: boolean;
        /**
          * Accessible name of the decrement stepper (localizable).
          * @default 'Decrement'
         */
        "decrementLabel"?: string;
        /**
          * Density forwarded to the text-field and applied to the steppers.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Error state — forwarded to the text-field.
          * @default false
         */
        "error"?: boolean;
        /**
          * Error text rendered in place of supporting text when `error` is true.
          * @default ''
         */
        "errorText"?: string;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * `Intl.NumberFormat` options for display formatting (currency, percent, unit, fraction digits…). Percent keeps Intl semantics: displayed 50% ⇔ value 0.5.  Takes either form, so a plain-HTML page needs no script:   - property — `el.formatOptions = { style: 'currency', currency: 'EUR' }`   - attribute — `format-options='{"style":"currency","currency":"EUR"}'`  Malformed JSON warns once and falls back to plain number formatting rather than throwing: a broken attribute must not take the whole field down.
         */
        "formatOptions"?: Intl.NumberFormatOptions | string;
        /**
          * Accessible name of the increment stepper (localizable).
          * @default 'Increment'
         */
        "incrementLabel"?: string;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label"?: string;
        /**
          * Step while **Shift** is held.
          * @default 10
         */
        "largeStep"?: number;
        /**
          * BCP-47 locale for `Intl.NumberFormat` display/parsing ONLY (an Intl-computed value — translatable copy stays in the `*-label` props). Empty = the runtime locale.
          * @default ''
         */
        "locale"?: string;
        /**
          * Upper bound. Interactive stepping always clamps to it.
         */
        "max"?: number;
        /**
          * Lower bound. Interactive stepping always clamps to it.
         */
        "min"?: number;
        /**
          * Form name. The raw numeric value submits as `String(value)`.
          * @default ''
         */
        "name"?: string;
        /**
          * Fires at commit points: blur/Enter reformat, stepping, wheel.
         */
        "onMdChange"?: (event: MdNumberFieldCustomEvent<MdNumberFieldChangeDetail>) => void;
        /**
          * Fires on every value change (typing included).
         */
        "onMdInput"?: (event: MdNumberFieldCustomEvent<MdNumberFieldChangeDetail>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites embedding a field would otherwise hear two events for one logical control; `bubbles: true` still lets a `<form>` or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdNumberFieldCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Placeholder for the input.
          * @default ''
         */
        "placeholder"?: string;
        /**
          * Read-only: the value is visible and focusable but cannot be edited or stepped.
          * @default false
         */
        "readOnly"?: boolean;
        /**
          * Required for native form parity (`valueMissing` when empty).
          * @default false
         */
        "required"?: boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field.
          * @default false
         */
        "reserveSupportingSpace"?: boolean;
        /**
          * Step while **Alt** is held.
          * @default 0.1
         */
        "smallStep"?: number;
        /**
          * Snap stepped values to multiples of the active step (base = `min` when defined, else 0). Snapping happens BEFORE clamping, so non-aligned bounds stay reachable. Regular steps snap directionally; Alt (`smallStep`) snaps to the nearest multiple.
          * @default false
         */
        "snapOnStep"?: boolean;
        /**
          * Step for arrows / steppers / wheel.
          * @default 1
         */
        "step"?: number;
        /**
          * Which stepper buttons render: inside the field, split around it, or none.
          * @default 'inline'
         */
        "steppers"?: 'inline' | 'split' | 'none';
        /**
          * Supporting / helper text below the field.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * The raw numeric value (`null` = empty). Never the formatted string — the display text is derived via `Intl.NumberFormat`.
          * @default null
         */
        "value"?: number | null;
        /**
          * Localized constraint-validation message shown when `required` is unmet. `errorText` still wins when set, so an app-supplied inline message and the native bubble stay in agreement.
          * @default 'Please enter a number.'
         */
        "valueMissingLabel"?: string;
        /**
          * Visual variant of the inner text-field.
          * @default 'filled'
         */
        "variant"?: 'filled' | 'outlined';
    }
    /**
     * `md-organization-chart` — Material Design 3 Expressive organization chart.
     * Visualises hierarchical org data as a top-down tree of avatar cards joined
     * by connector lines. Data-driven (`nodes`), with collapsible subtrees,
     * optional single/multiple selection, and the full WAI-ARIA **tree** pattern
     * (roving tabindex, Arrow/Home/End navigation, Enter/Space to select).
     * The surface pans horizontally when it outgrows its container, so a wide org
     * stays usable on small screens. Direction-aware (RTL mirrors the layout and
     * swaps the horizontal arrow keys); every affordance label is a prop for
     * localisation.
     * ```html
     * <md-organization-chart selection-mode="single"></md-organization-chart>
     * <script>
     *   document.querySelector('md-organization-chart').nodes = [{
     *     id: 'ceo', name: 'Amy Elsner', title: 'Founder & CEO',
     *     children: [{ id: 'prod', name: 'Asiya Javayant', title: 'Product Lead' }],
     *   }];
     * </script>
     * ```
     */
    interface MdOrganizationChart {
        /**
          * Toggler accessible label when a node is expanded (localisable).
          * @default 'Collapse'
         */
        "collapseLabel"?: string;
        /**
          * Show expand/collapse togglers on nodes that have children.
          * @default true
         */
        "collapsible"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Toggler accessible label when a node is collapsed (localisable).
          * @default 'Expand'
         */
        "expandLabel"?: string;
        /**
          * Accessible name for the tree (localisable).
          * @default 'Organization chart'
         */
        "label"?: string;
        /**
          * The organization tree. Accepts an `OrgChartNode[]` (property) or a JSON string (attribute). Multiple roots are supported (rendered side by side).
          * @default []
         */
        "nodes"?: NodeInput;
        /**
          * Fired when a node is expanded or collapsed.
         */
        "onMdNodeToggle"?: (event: MdOrganizationChartCustomEvent<OrgChartToggleDetail>) => void;
        /**
          * Fired when the selection changes.
         */
        "onMdSelectionChange"?: (event: MdOrganizationChartCustomEvent<OrgChartSelectionChangeDetail>) => void;
        /**
          * Layout direction of the tree:   - `'vertical'`   — top-down (default).   - `'horizontal'` — left-to-right; RTL mirrors it right-to-left.  Only the visual layout changes — the tree's ARIA semantics and keyboard model (Arrow Left/Right = collapse/expand, Up/Down = move) are unchanged.
          * @default 'vertical'
         */
        "orientation"?: 'vertical' | 'horizontal';
        /**
          * Selected node ids (controlled + initial). Kept in sync as the user selects.
          * @default []
         */
        "selectedIds"?: string[];
        /**
          * Selection behaviour:   - `'none'`     — nodes are not selectable (default).   - `'single'`   — one node at a time.   - `'multiple'` — any number of nodes.
          * @default 'none'
         */
        "selectionMode"?: 'none' | 'single' | 'multiple';
    }
    /**
     * `md-otp-field` — Material Design 3 one-time-code field.
     * Renders `length` real `<input>` cells in its own shadow root (it OWNS its
     * inputs, so every cell carries full ARIA), pipes all entry — typing, paste,
     * SMS autofill — through one sanitizing pipeline (strip whitespace →
     * `transform` → `validationType` charset filter → clamp to `length`), and
     * participates in forms via `ElementInternals` (no hidden input).
     * The value is a CONTIGUOUS string: clearing a cell shifts the characters
     * after it left (there are no holes), and a character typed into an empty
     * cell beyond the fill point lands at the first empty position.
     * Normalization is declarative: `transform` names the casing rule as an
     * attribute value rather than taking a callback, so the rule is authorable
     * from plain HTML and survives SSR — an element attribute cannot carry a
     * function.
     */
    interface MdOtpField {
        /**
          * Submit the owning form (`requestSubmit()`) when the code becomes complete.
          * @default false
         */
        "autoSubmit"?: boolean;
        /**
          * Per-cell `aria-label` template (translatable). `{index}` is 1-based, `{length}` is the cell count.
          * @default 'Character {index} of {length}'
         */
        "cellLabelTemplate"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal a global `data-density` ancestor sets; local wins. 0 = default, -4 = compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disables every cell.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Error visual state — colors cells and swaps supporting text to `errorText`.
          * @default false
         */
        "error"?: boolean;
        /**
          * Translatable error message; replaces `supportingText` while `error` is true.
          * @default ''
         */
        "errorText"?: string;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Chunk the cells into groups of this size with a separator between groups. 0 = ungrouped.
          * @default 0
         */
        "groupSize"?: number;
        /**
          * When non-empty AND `required`, a started-but-incomplete code is also invalid, reported with this message. Empty (default) = only a fully empty required field blocks submission.
          * @default ''
         */
        "incompleteLabel"?: string;
        /**
          * Virtual-keyboard hint override. Empty derives from `validationType` (`numeric` → `numeric`, everything else → `text`).
          * @default ''
         */
        "inputMode"?: string;
        /**
          * Accessible group label (translatable). Rendered as `aria-label` on the cell group.
          * @default 'One-time code'
         */
        "label"?: string;
        /**
          * Number of character cells.
          * @default 6
         */
        "length"?: number;
        /**
          * Obscure the entered characters (cells render as `type="password"`).
          * @default false
         */
        "mask"?: boolean;
        /**
          * Form field name — ElementInternals submits the code under it.
          * @default ''
         */
        "name"?: string;
        /**
          * Commit event: fires when the code becomes complete and when focus leaves the whole group.
         */
        "onMdChange"?: (event: MdOtpFieldCustomEvent<string>) => void;
        /**
          * Fires when every cell is filled. A complete paste re-fires it even if the value is unchanged.
         */
        "onMdComplete"?: (event: MdOtpFieldCustomEvent<MdOtpFieldCompleteDetail>) => void;
        /**
          * Fires on every value change from user input (typing, clearing, paste, autofill).
         */
        "onMdInput"?: (event: MdOtpFieldCustomEvent<string>) => void;
        /**
          * Fires when typed/pasted characters are rejected by the `validationType` charset.
         */
        "onMdInvalidInput"?: (event: MdOtpFieldCustomEvent<MdOtpFieldInvalidDetail>) => void;
        /**
          * Fires when this control's validity CHANGES — never on mount, never for a re-publish landing on the same state. `composed: false` by the library convention: each control reports only for itself; `bubbles: true` still reaches `<form>`-level listeners in the same DOM tree.
         */
        "onMdValidityChange"?: (event: MdOtpFieldCustomEvent<MdOtpFieldValidityDetail>) => void;
        /**
          * Value not user-changeable; cells stay focusable and arrow-navigable.
          * @default false
         */
        "readOnly"?: boolean;
        /**
          * The code must be complete before the owning form submits.
          * @default false
         */
        "required"?: boolean;
        /**
          * Always reserve the supporting-text line so error text causes no layout jump.
          * @default false
         */
        "reserveSupportingSpace"?: boolean;
        /**
          * Translatable supporting text shown under the cells.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Case normalization as an attribute value, not a callback (attributes can't carry functions): `uppercase` upper-cases every accepted character (recovery-code entry).
          * @default 'none'
         */
        "transform"?: 'none' | 'uppercase';
        /**
          * Which characters are accepted. `none` accepts anything (still whitespace-stripped).
          * @default 'numeric'
         */
        "validationType"?: 'numeric' | 'alpha' | 'alphanumeric' | 'none';
        /**
          * The current code. Mutable, deliberately NOT reflected — entered codes must never appear as a DOM attribute (value privacy, like md-text-field). Programmatic writes run through the same sanitizing pipeline as typing.
          * @default ''
         */
        "value"?: string;
        /**
          * Translatable constraint-validation message when `required` and empty.
          * @default 'Please enter the complete code.'
         */
        "valueMissingLabel"?: string;
    }
    interface MdPieChart {
        /**
          * Entry-animation variant: `expressive` (default), `grow`, `fade`, `draw`, or `none`.
          * @default 'expressive'
         */
        "animation"?: MdChartAnimation;
        /**
          * Entry-animation duration override in ms (≤ 0 disables).
         */
        "animationDuration"?: number;
        /**
          * Border radius applied to each slice (reserved).
          * @default 4
         */
        "cornerRadius"?: number;
        /**
          * @default []
         */
        "data"?: MdPieDatum[];
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * End angle in degrees.
          * @default -270
         */
        "endAngle"?: number;
        /**
          * Fill each slice with a gradient running outward from the centre, for depth. Purely decorative — it changes nothing about what a slice means, which is why it is off by default.
          * @default false
         */
        "gradient"?: boolean;
        "heightProp"?: string;
        /**
          * Hover highlight scope: slice / series / none.
          * @default 'slice'
         */
        "highlight"?: 'slice' | 'series' | 'none';
        /**
          * Inner radius (`"0%"` = pie, `"60%"` = donut, or px number).
          * @default '0%'
         */
        "innerRadius"?: string | number;
        /**
          * @default ''
         */
        "label"?: string;
        /**
          * Message shown when `data` is empty. The `empty` slot overrides it.
          * @default 'No data to display'
         */
        "labelEmpty"?: string;
        /**
          * What each slice's label says: its `value`, its `name`, or `both`. Defaults to the value when a legend is present (it already names the slices) and to both when there isn't one.
         */
        "labelMode"?: 'value' | 'name' | 'both';
        /**
          * Instructions announced when the plot receives keyboard focus. The plot is focusable so a keyboard user can walk the slices; this is what tells them.
          * @default 'Chart data. Use the arrow keys to move between slices, Home and End for the first and last, Escape to leave.'
         */
        "labelPlot"?: string;
        /**
          * Template for the live announcement as focus moves. `%label%` is the slice name, `%value%` its formatted value and `%percent%` its share.
          * @default '%label%: %value% (%percent%)'
         */
        "labelPoint"?: string;
        /**
          * @default 'right'
         */
        "legend"?: MdChartLegendPosition | 'none';
        /**
          * Show the loading overlay instead of the chart.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Text (and the spinner's accessible name) for the loading overlay.
          * @default 'Loading chart…'
         */
        "loadingLabel"?: string;
        /**
          * BCP-47 locale for the DEFAULT number formatting (tooltip values, the screen-reader table). Empty follows the browser, which is the right default only until the page has said otherwise; an explicit `valueFormatter` always wins over it.
          * @default ''
         */
        "locale"?: string;
        /**
          * Render every slice as a shade of ONE colour instead of the categorical palette — darkest first, lightening around the ring.  Worth reaching for when the slices are ordered (a ranking, a funnel) rather than merely different: a single hue stops the reader hunting for meaning in colour that isn't there, and it survives most colour-vision deficiencies, which a five-hue palette does not.  Takes an MD3 role or any CSS colour. Present with no value (`monochrome`) uses `primary`; absent means off — so an explicit empty string is the "on, with the default" case rather than an accident.
         */
        "monochrome"?: string;
        /**
          * Disable all animation (shorthand for `animation="none"`).
          * @default false
         */
        "noAnimation"?: boolean;
        "onMdLegendClick"?: (event: MdPieChartCustomEvent<{ dataIndex: number; selected: boolean }>) => void;
        "onMdReady"?: (event: MdPieChartCustomEvent<void>) => void;
        "onMdSliceClick"?: (event: MdPieChartCustomEvent<MdChartClickDetail<MdPieDatum>>) => void;
        /**
          * Outer radius. Defaults to `"75%"`.
          * @default '75%'
         */
        "outerRadius"?: string | number;
        /**
          * Padding angle (degrees) between adjacent slices.
          * @default 0
         */
        "paddingAngle"?: number;
        /**
          * How the radial band is divided between the rings of a nested chart — relative weights, innermost first. `[2, 1]` gives the inner ring twice the width of the outer one.  The default is not an even split: every ring but the outermost puts its labels INSIDE its own band, so it has to be wide enough to hold a word, while the outermost labels on leaders and needs no more than its arc.
         */
        "ringWidths"?: number[];
        /**
          * Show slice labels around the ring.
          * @default true
         */
        "showLabels"?: boolean;
        /**
          * Start angle in degrees (90 = 12-o'clock).
          * @default 90
         */
        "startAngle"?: number;
        /**
          * Sub-title, drawn under the title in the muted text colour.
         */
        "subtitle"?: string | undefined;
        /**
          * Replaces the generated `aria-label` outright. The default summary is assembled in English; rather than translate it piecewise, hand over the whole sentence built in your own language.
          * @default ''
         */
        "summary"?: string;
        /**
          * Translatable chrome for the screen-reader data table.
         */
        "tableLabels"?: { category?: string; value?: string; share?: string };
        /**
          * Title alignment over the chart: `start` (left), `center`, or `end` (right).
          * @default 'start'
         */
        "titleAlign"?: MdChartTitleAlign;
        /**
          * @default 'item'
         */
        "tooltip"?: MdChartTooltipTrigger;
        /**
          * Replace the tooltip's content. See `md-line-chart`'s `tooltipRenderer`.
         */
        "tooltipRenderer"?: MdChartTooltipRenderer;
        "valueFormatter"?: (value: number) => string;
    }
    interface MdProgressIndicator {
        /**
          * Set to `true` to trigger the completion ("closing TV") animation. After it plays, the indicator self-hides (`visibility:hidden`) and emits `mdComplete` — EXCEPT the flat (non-wavy) circular variant, which instead rests showing its empty track ring (the active indicator erases away and the track fades in). No-op on indeterminate indicators.
          * @default false
         */
        "complete"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Four-color cycling animation (circular indeterminate only).
          * @default false
         */
        "fourColor"?: boolean;
        /**
          * Indeterminate mode — looping animation when progress is unknown.
          * @default false
         */
        "indeterminate"?: boolean;
        /**
          * Accessible label for screen readers.
          * @default 'Progress'
         */
        "label"?: string;
        /**
          * Maximum progress value.
          * @default 100
         */
        "max"?: number;
        /**
          * Fired when the completion animation finishes (and the indicator has hidden itself, or — for flat circular — settled on its empty track ring).
         */
        "onMdComplete"?: (event: MdProgressIndicatorCustomEvent<void>) => void;
        /**
          * Circular indicator outer size in dp. `0` = auto (non-wavy 40px, wavy 48px per the M3 expressive spec). Clamped to the M3 range 24–240px.
          * @default 0
         */
        "size"?: number;
        /**
          * Track / stroke thickness in dp.
          * @default 4
         */
        "thickness"?: number;
        /**
          * Current progress value (0 to max).
          * @default 0
         */
        "value"?: number;
        /**
          * Visual variant: linear (horizontal bar) or circular (ring).
          * @default 'linear'
         */
        "variant"?: 'circular' | 'linear';
        /**
          * Enable the wavy (M3 Expressive) active indicator.
          * @default false
         */
        "wave"?: boolean;
        /**
          * Wave amplitude (center-to-peak) in dp. `0` = auto per M3 spec (linear 3px, circular 1.6px).
          * @default 0
         */
        "waveAmplitude"?: number;
        /**
          * Wavelength (peak-to-peak) in dp. `0` = auto (linear 40px for both determinate + indeterminate, circular 15px).
          * @default 0
         */
        "waveLength"?: number;
        /**
          * Wave travel speed in dp/second. `0` = auto (= wavelength, i.e. one wavelength per second per the M3 default).
          * @default 0
         */
        "waveSpeed"?: number;
    }
    interface MdRadio {
        /**
          * Whether the radio button is selected
          * @default false
         */
        "checked"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the radio button
          * @default false
         */
        "disabled"?: boolean;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Form name — radios sharing a name form an exclusive group
          * @default ''
         */
        "name"?: string;
        /**
          * Emits when the radio loses focus
         */
        "onMdBlur"?: (event: MdRadioCustomEvent<void>) => void;
        /**
          * Emits when the checked state changes via user interaction
         */
        "onMdChange"?: (event: MdRadioCustomEvent<{ checked: boolean; value: string }>) => void;
        /**
          * Emits when the radio receives focus
         */
        "onMdFocus"?: (event: MdRadioCustomEvent<void>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdRadioCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Whether the radio is required in a form
          * @default false
         */
        "required"?: boolean;
        /**
          * Soft-disabled: disabled visuals but remains focusable for discoverability
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Form value submitted when checked
          * @default ''
         */
        "value"?: string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please select one of these options.'
         */
        "valueMissingLabel"?: string;
    }
    /**
     * `md-rating` — Material Design 3 Expressive Rating control.
     * A star-rating input: configurable max, half-step precision, custom icons
     * (Material Symbols glyphs OR slotted SVG/HTML), per-value icons
     * (filled / empty), hover preview, read-only and disabled modes,
     * keyboard navigation (Arrow + Home/End), and reset to zero.
     * MD3 Expressive: items animate with a brief tonal "scale-up" pop on
     * hover and selection; press uses a state-layer + ripple. The
     * underlying ARIA pattern is a slider so screen readers announce the
     * value as a continuous range.
     */
    interface MdRating {
        /**
          * Default value used when the consumer clears the rating (re-clicking the current value).
          * @default 0
         */
        "defaultValue"?: number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive and visually dimmed; not focusable.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Material Symbols glyph used for *empty* items.
          * @default 'star'
         */
        "emptyIcon"?: string;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Optional label provider — `el.getLabel = (v) => "${v} Stars"`. Used by the hidden screen-reader text and the visible value label when `show-value-label` is set.
         */
        "getLabel"?: (value: number) => string;
        /**
          * Hover-preview behaviour:   - `'preview'` (default) — display the hovered value over the live one.   - `'off'` — no hover preview; `value` is the only signal.
          * @default 'preview'
         */
        "hover"?: 'preview' | 'off';
        /**
          * Material Symbols glyph used for *filled* items.
          * @default 'star'
         */
        "icon"?: string;
        /**
          * Maximum rating (number of items rendered).
          * @default 5
         */
        "max"?: number;
        /**
          * Form name for native form participation.
          * @default ''
         */
        "name"?: string;
        /**
          * Fires whenever the value changes via interaction.
         */
        "onMdChange"?: (event: MdRatingCustomEvent<number>) => void;
        /**
          * Fires while the user is hovering (or `null` when the cursor leaves).
         */
        "onMdHover"?: (event: MdRatingCustomEvent<number | null>) => void;
        /**
          * Use the *filled* glyph for empty items as well, drawn with the outline-only color treatment — a solid silhouette outline behind the fill.
          * @default true
         */
        "outlineEmpty"?: boolean;
        /**
          * Step size — `1` (full only) or `0.5` (half).
          * @default 1
         */
        "precision"?: 1 | 0.5;
        /**
          * Accessible name for the rating control.  The attribute is `rating-label`, NOT `aria-label-rating`. Anything starting with `aria-` is parsed as an ARIA attribute, and `aria-label-rating` is not one — so the previous name made axe report a CRITICAL `aria-valid-attr` violation on every consumer that used the documented API. The value still ends up as the host's accessible name; only the attribute spelling changed.
          * @default 'Rating'
         */
        "ratingLabel"?: string;
        /**
          * Read-only — purely decorative; no interaction, no focus.
          * @default false
         */
        "readonly"?: boolean;
        /**
          * When true, render the current numeric value beside the items.
          * @default false
         */
        "showValueLabel"?: boolean;
        /**
          * Size of each item.
          * @default 'md'
         */
        "size"?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Soft-disabled — disabled visuals but remains focusable for discoverability.
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Current numeric rating (0 → max, half-steps when precision = 0.5).
          * @default 0
         */
        "value"?: number;
    }
    interface MdRipple {
        /**
          * @default false
         */
        "disabled"?: boolean;
    }
    /**
     * Material Design 3 Search.
     * Implements the M3 Search specification
     * (https://m3.material.io/components/search/specs).
     * Two orthogonal axes describe every spec configuration:
     *  - `variant`: `contained` (M3 Expressive default) | `divided` (baseline)
     *  - `layout` : `full-screen` (modal overlay) | `docked` (anchored panel)
     * The component renders a single resting bar plus a results panel. When the
     * `full-screen` layout is `open` the bar pins itself to the top of the
     * viewport via `position: fixed` so consumers don't need a portal — CSS does
     * the work, and the input keeps a stable identity throughout the open/close
     * lifecycle.
     */
    interface MdSearch {
        /**
          * Announce the number of available suggestions / results to assistive technology via a polite live region whenever the slotted results change while the panel is open (M3 "autosuggest" requirement). Set to `false` if you manage your own live region in the results slot.
          * @default true
         */
        "announceResults"?: boolean;
        /**
          * Trailing debounce (ms) applied to the `mdSearch` event so a fetch only fires after the user pauses typing. `0` (default) emits on every change. `mdInput` always fires immediately on every keystroke regardless.
          * @default 0
         */
        "debounce"?: number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Whether interaction is blocked.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Close on outside click (docked layout).
          * @default true
         */
        "dismissOnOutsideClick"?: boolean;
        /**
          * Resting bar elevation level, mapped to `--md-sys-elevation-{n}`. `0` (the M3 default) renders no shadow; `1`–`5` apply the matching MD3 elevation tier. For a fully custom shadow, set the CSS custom property `--md-search-container-elevation` instead (it overrides this prop).
          * @default 0
         */
        "elevation"?: 0 | 1 | 2 | 3 | 4 | 5;
        /**
          * Close on Escape key.
          * @default true
         */
        "escapeCloses"?: boolean;
        /**
          * Make the docked / inline bar span its container's full inline width with NO side gutters. This zeroes both expand insets for the instance (resting + focused = 0), so there is no 24 → 12px margin animation and the aligned results drawer fills the same full width. Purely an ergonomic switch over the CSS vars — the identical effect is available by setting `--md-search-expand-inset: 0` and `--md-search-expand-focused-inset: 0`. Has no effect on `full-screen` layout (already edge-to-edge).
          * @default false
         */
        "fullWidth"?: boolean;
        /**
          * Where focus lands when the panel opens. `auto` (default) and `input` focus the text field so the user can type immediately. `leading` focuses the leading button instead (falling back to the input if it isn't focusable).  Note: the M3 "initial focus lands on a leading icon, else the text field" rule applies to the RESTING bar's first tab stop and is handled by DOM order — a slotted interactive leading icon button is the first tabbable element, otherwise the text field. This prop only controls the open view.
          * @default 'auto'
         */
        "initialFocus"?: 'auto' | 'input' | 'leading';
        /**
          * Accessible label for the search bar / input. Per the M3 labeling guidance the hinted search text (`placeholder`) describes the bar, so when this is left empty the label falls back to `placeholder`. Set it explicitly only when the visible hint and the accessible name should differ.
          * @default ''
         */
        "inputAriaLabel"?: string;
        /**
          * How the focused panel appears. `full-screen` fills the viewport edge-to-edge with an opaque results surface and a focus trap (no scrim — open/close uses the same translateY + opacity motion as md-dialog fullscreen); `docked` anchors a popup beneath the bar (min 360px / max 720px wide; results drawer shrinks to content up to max ⅔ vh).
          * @default 'full-screen'
         */
        "layout"?: 'full-screen' | 'docked';
        /**
          * Material Symbols glyph for the resting leading icon.
          * @default 'search'
         */
        "leadingIcon"?: string;
        /**
          * Whether an async results fetch is in flight. When `true` the M3 loading indicator (the looping shape-morph) shows in the bar's trailing cluster. Controlled by the consumer: set it `true` before awaiting your fetch and back to `false` once results are slotted in. The component never flips this itself — it only reflects the state visually + to assistive tech.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Accessible label for the in-bar loading indicator.
          * @default 'Searching'
         */
        "loadingLabel"?: string;
        /**
          * Maximum block-size of the open search surface (`"480px"`, `"66vh"`, etc.). Full-screen: caps the fixed overlay height. Docked: caps the results panel (bar height is unchanged). Prefer the CSS custom property `--md-search-max-block-size` for stylesheet-based theming.
          * @default ''
         */
        "maxBlockSize"?: string;
        /**
          * Message shown when the panel is open, the user has entered a query, and there are no slotted results — both the on-screen empty state and the polite live-region announcement (unless the visible empty state is shown, in which case the live region defers to it to avoid duplicate speech).
          * @default 'No results available'
         */
        "noResultsLabel"?: string;
        /**
          * Fires when the input loses focus with a different value than on focus.
         */
        "onMdChange"?: (event: MdSearchCustomEvent<{ value: string }>) => void;
        /**
          * Fires when the user clicks the clear (×) button.
         */
        "onMdClear"?: (event: MdSearchCustomEvent<void>) => void;
        /**
          * Fires when the panel closes.
         */
        "onMdClose"?: (event: MdSearchCustomEvent<void>) => void;
        /**
          * Fires on every input change (immediate, every keystroke).
         */
        "onMdInput"?: (event: MdSearchCustomEvent<{ value: string }>) => void;
        /**
          * Fires when the interactive leading affordance is clicked — the morphing back / dismiss button while the bar is open, or a custom slotted `leading` icon. The default back button still dismisses the panel as before; this event is additive. The closed resting search glyph is intentionally NOT a click target (a click there focuses the input), so it does not emit.
         */
        "onMdLeadingIconClick"?: (event: MdSearchCustomEvent<MdSearchLeadingIconClickDetail>) => void;
        /**
          * Fires when the panel opens.
         */
        "onMdOpen"?: (event: MdSearchCustomEvent<void>) => void;
        /**
          * Debounced, de-duplicated query event — the one to wire an async results fetch to. Honours `debounce` / `throttle` and applies a distinct-until-changed guard on the trimmed query (so re-typing the same term, or only adding surrounding whitespace, won't re-trigger a fetch). Pressing Enter or clearing the field flushes it immediately.
         */
        "onMdSearch"?: (event: MdSearchCustomEvent<{ value: string }>) => void;
        /**
          * Fires when the user presses Enter to submit a query.
         */
        "onMdSubmit"?: (event: MdSearchCustomEvent<{ value: string }>) => void;
        /**
          * Fires when a slotted `trailing` affordance is clicked. The built-in clear (×) and voice (mic) buttons own their dedicated `mdClear` / `mdVoice` events and do NOT trigger this. Only emitted when slotted trailing content is actually present and was the click target.
         */
        "onMdTrailingIconClick"?: (event: MdSearchCustomEvent<MdSearchTrailingIconClickDetail>) => void;
        /**
          * Fires while voice search is active as the transcript streams in. `value` is the current (interim or final) transcript; `final` is `true` on the recognised final result. The component already mirrors the transcript into the input and fires `mdInput` / `mdSearch`, so listen to this only if you need voice-specific UI (e.g. a transcript preview).
         */
        "onMdVoice"?: (event: MdSearchCustomEvent<{ value: string; final: boolean }>) => void;
        /**
          * Whether the focused panel is open.
          * @default false
         */
        "open"?: boolean;
        /**
          * Material Symbols glyph used for the leading button when the bar is open. Defaults are variant-aware: the `contained` (Expressive) bar uses `chevron_left` so the caret matches the bar's softer, pill- shaped silhouette, while the `divided` (baseline) bar uses the classic `arrow_back` to match its squared, app-bar-like chrome. Pass an explicit value to override either default.
         */
        "openLeadingIcon"?: string;
        /**
          * Placeholder text — the spec calls this "supporting text" within the bar anatomy.
          * @default 'Search'
         */
        "placeholder"?: string;
        /**
          * Template for the results announcement. `{count}` is replaced with the number of detected result items (counts `md-list-item`, `[role="option"]`, or `<li>` descendants of the results slot, falling back to the number of slotted elements).
          * @default '{count} results available'
         */
        "resultsLabel"?: string;
        /**
          * Retained for API compatibility, but now a no-op: when the results list overflows it always plain-scrolls inside the results viewport (no scroll shadow / edge fades). Setting this has no visual effect.
          * @default true
         */
        "scrollShadow"?: boolean;
        /**
          * Show the trailing clear (×) button when the input has a value.
          * @default true
         */
        "showClearButton"?: boolean;
        /**
          * Maximum wait (ms) before `mdSearch` is forced to fire during sustained typing — a throttle/`maxWait` safety net so the user still sees interim results when they never pause long enough for the debounce to settle. `0` (default) disables it. Only meaningful alongside a non-zero `debounce`.
          * @default 0
         */
        "throttle"?: number;
        /**
          * What the user sees while the search view is closed. `bar` renders the resting search field (docked default). `icon` renders a compact search icon button that opens the view — the default for `full-screen` layout (contained and divided spec sheets). Docked layouts should keep `bar`.
         */
        "trigger"?: 'bar' | 'icon';
        /**
          * The external trigger as an element, for consumers that hold a reference.
         */
        "triggerElement"?: HTMLElement;
        /**
          * Open the search from an element that lives ELSEWHERE on the page — a button in the app bar, a menu item, a keyboard-shortcut hint — rather than from the built-in trigger or the `trigger` slot, both of which have to sit inside this element.  Takes a CSS selector resolved against the document (`trigger-for="#app-bar-search"`), or an element assigned to the `triggerElement` property when you already hold a reference. The component wires it rather than rendering it: activating it opens the view, `aria-haspopup="dialog"` and `aria-expanded` are kept in step, and closing returns focus to it.  With one set, the built-in trigger is not rendered — otherwise the page would show two ways in.
          * @default ''
         */
        "triggerFor"?: string;
        /**
          * Material Symbols glyph for the built-in icon trigger (`trigger="icon"`). Ignored when a custom element is slotted in `trigger`.
          * @default 'search'
         */
        "triggerIcon"?: string;
        /**
          * Current input text, two-way bindable.
          * @default ''
         */
        "value"?: string;
        /**
          * Visual style. `contained` is the M3 Expressive default with a filled surface and rounded shape; `divided` is the baseline pre-Expressive look that uses a hairline divider between bar and results.
          * @default 'contained'
         */
        "variant"?: 'contained' | 'divided';
        /**
          * Opt-in voice search. When `true` AND the browser exposes the Web Speech API (`SpeechRecognition` / `webkitSpeechRecognition`), a microphone icon-button is rendered in the trailing cluster. Clicking it starts recognition and streams the interim + final transcript into the input, firing the normal `mdInput` / `mdSearch` flow as if typed. When the API is unavailable the mic is not rendered (graceful no-op). Default `false`.
          * @default false
         */
        "voiceSearch"?: boolean;
    }
    interface MdSegmentedButton {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable this segment
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Material Symbols icon name (optional)
          * @default ''
         */
        "icon"?: string;
        /**
          * Label text
          * @default ''
         */
        "label"?: string;
        /**
          * Show checkmark icon when selected
          * @default false
         */
        "noCheckmark"?: boolean;
        /**
          * Internal event — caught by the set
         */
        "onMdSegmentClick"?: (event: MdSegmentedButtonCustomEvent<{ value: string; selected: boolean }>) => void;
        /**
          * Enable/disable ripple
          * @default true
         */
        "ripple"?: boolean;
        /**
          * Density value from parent set (managed by md-segmented-button-set)
          * @default 0
         */
        "segmentDensity"?: number;
        /**
          * Position index within the set (managed by md-segmented-button-set)
          * @default 0
         */
        "segmentIndex"?: number;
        /**
          * Whether the parent set is in multiselect mode (managed by md-segmented-button-set)
          * @default false
         */
        "segmentMultiselect"?: boolean;
        /**
          * Total number of segments in the set (managed by md-segmented-button-set)
          * @default 1
         */
        "segmentTotal"?: number;
        /**
          * Whether this segment is selected
          * @default false
         */
        "selected"?: boolean;
        /**
          * Visually disabled but remains focusable for screen readers
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Unique value identifying this segment
          * @default ''
         */
        "value"?: string;
    }
    interface MdSegmentedButtonSet {
        /**
          * Density: 0 = 40px, -1 = 36px, -2 = 32px, -3 = 28px, -4 = 24px
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Single-select or multi-select mode
          * @default false
         */
        "multiselect"?: boolean;
        /**
          * Fires when the set of selected segment values changes
         */
        "onMdChange"?: (event: MdSegmentedButtonSetCustomEvent<string[]>) => void;
    }
    /**
     * `md-select` — Material Design 3 single-select dropdown.
     * Composes the library's own primitives instead of re-implementing them:
     *   - `md-text-field` is the trigger surface, so the picker inherits the
     *     full filled/outlined visuals, density scale, floating label, error
     *     state, supporting text, and focus ring of every other input. It is
     *     `readonly` (not editable) and `appear-focused` while the menu is open
     *     so the field + menu read as one connected control.
     *   - `md-menu` is the option surface, so the picker inherits open/close
     *     motion, viewport-aware positioning, single-open coordination, roving
     *     tabindex, type-ahead, and Escape/Home/End — for free.
     * Options are authored either declaratively as slotted `<md-select-option>`
     * children (primary API) or via the programmatic `options` array. Each
     * becomes a `type="radio"` `md-menu-item` so only one is ever selected.
     */
    interface MdSelect {
        /**
          * Icon (Material Symbols name) for the clear button (`clearable`).
          * @default 'close'
         */
        "clearIcon"?: string;
        /**
          * Accessible label for the clear button (`clearable`).
          * @default 'Clear selection'
         */
        "clearLabel"?: string;
        /**
          * Show a clear (✕) button in the trigger when a value is selected.
          * @default false
         */
        "clearable"?: boolean;
        /**
          * Density forwarded to the inner field (0 = comfortable … -3 = compact).
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Caret icon; rotates 180° when open. Slot `dropdown-icon` overrides it.
          * @default 'arrow_drop_down'
         */
        "dropdownIcon"?: string;
        /**
          * Error state — forwarded to the inner field.
          * @default false
         */
        "error"?: boolean;
        /**
          * Error text rendered in place of supporting text when `error` is set.
          * @default ''
         */
        "errorText"?: string;
        /**
          * Accessible name for the filter field. Falls back to `Filter <label>`.
          * @default ''
         */
        "filterLabel"?: string;
        /**
          * Filter strategy used by `filterable` / `setQuery`.
          * @default 'substring'
         */
        "filterMode"?: FilterMode;
        /**
          * Show a search field in the menu header.  A plain list filters client-side on label / supporting text; a virtualized one filters inside the WASM engine. Closing the menu clears the query.
          * @default false
         */
        "filterable"?: boolean;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Stretch the control to fill its parent's inline width (otherwise it sizes to a sensible default). The dropdown follows when `match-trigger-width` is set.
          * @default false
         */
        "fullWidth"?: boolean;
        /**
          * Floating label / accessible name.
          * @default ''
         */
        "label"?: string;
        /**
          * Show a busy state: the trigger shows a spinner instead of the caret and the host is `aria-busy`. Use while an async dataset loads (e.g. before/around a large `loadOptions`). Replace the spinner with the `loader` slot.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Text shown in the trigger / menu while `loading` is true.
          * @default 'Loading…'
         */
        "loadingText"?: string;
        /**
          * Match the dropdown width to the trigger width. When `false` the menu sizes to its content (down to its own min-width). Maps to `md-menu`'s `match-anchor-width`. Default `true` — the expected select behaviour.
          * @default true
         */
        "matchTriggerWidth"?: boolean;
        /**
          * Cap the dropdown height. When the options exceed it the list scrolls vertically with top/bottom edge shadows. A number is pixels; any CSS length is passed through (e.g. `'50vh'`). Forwarded to `md-menu`'s `max-height`.
         */
        "maxHeight"?: number | string;
        /**
          * Form field name. The selected value is submitted under this name.
          * @default ''
         */
        "name"?: string;
        /**
          * Empty-state text when there are no options at all.
          * @default 'No options'
         */
        "noOptionsText"?: string;
        /**
          * Empty-state text when a filter matches nothing.
          * @default 'No results'
         */
        "noResultsText"?: string;
        /**
          * Emits the newly selected value whenever the selection changes.
         */
        "onMdChange"?: (event: MdSelectCustomEvent<string>) => void;
        /**
          * Emits when the dropdown closes.
         */
        "onMdClose"?: (event: MdSelectCustomEvent<void>) => void;
        /**
          * Emits when the dropdown opens.
         */
        "onMdOpen"?: (event: MdSelectCustomEvent<void>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdSelectCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Open state of the dropdown. Reflected so `:host([open])` rules apply.
          * @default false
         */
        "open"?: boolean;
        /**
          * Programmatic options. Slotted `md-select-option` children take precedence.  Accepts the array (property) or a **JSON string** (attribute), so a plain-HTML page can declare its data inline instead of reaching for a script. Malformed JSON degrades to an empty list with a console warning.
          * @default []
         */
        "options"?: SelectOptionInit[] | string;
        /**
          * Placeholder shown when no value is selected.
          * @default ''
         */
        "placeholder"?: string;
        /**
          * Menu placement relative to the trigger — forwarded to `md-menu`.
          * @default 'bottom-start'
         */
        "placement"?: 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        /**
          * Required (form validation parity).
          * @default false
         */
        "required"?: boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message, so a validation error does not push the content below it down. Forwarded to the embedded md-text-field. See that component for why it is opt-in.
          * @default false
         */
        "reserveSupportingSpace"?: boolean;
        /**
          * Fixed virtualized row height (px). Auto-measured from the first row if unset.
         */
        "rowHeight"?: number;
        /**
          * Placeholder for the in-menu filter field (`filterable`).
          * @default 'Search…'
         */
        "searchPlaceholder"?: string;
        /**
          * Accessible label for the in-menu loading spinner while filtering.
          * @default 'Searching'
         */
        "searchingLabel"?: string;
        /**
          * Supporting / helper text rendered below the field.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Selected option value. Empty string = nothing selected.
          * @default ''
         */
        "value"?: string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please select an item in the list.'
         */
        "valueMissingLabel"?: string;
        /**
          * Visual variant — forwarded to the inner `md-text-field`.
          * @default 'outlined'
         */
        "variant"?: 'filled' | 'outlined';
        /**
          * Virtualization mode for large option lists (backed by a WASM data engine):   - `'auto'` (default): virtualize only above `VIRTUALIZE_THRESHOLD` rows.   - `'always'`: always virtualize the `options` / `loadOptions` dataset.   - `'never'`: keep the classic DOM rendering regardless of size. Virtualized rows render label + selection only (no per-row icon / supporting text) and assume a uniform row height.
          * @default 'auto'
         */
        "virtualize"?: 'auto' | 'always' | 'never';
    }
    /**
     * `md-select-option` — declarative option for `md-select` / `md-multi-select`.
     * This element is a **data carrier**: it renders nothing visible (the host
     * is `display: none`). The parent picker reads its `value`, label (the
     * `label` prop or slotted text), `disabled`, `icon`, and `supporting-text`
     * and projects them into the `md-menu` dropdown as `md-menu-item`s.
     * Authoring:
     * ```html
     * <md-select label="Colour">
     *   <md-select-option value="red" icon="circle">Red</md-select-option>
     *   <md-select-option value="green" disabled>Green</md-select-option>
     * </md-select>
     * ```
     * Whenever an option is added, removed, or has its data changed, it emits a
     * bubbling `mdSelectOptionChange` event so the parent re-reads its option set.
     */
    interface MdSelectOption {
        /**
          * Disabled options stay visible in the menu but cannot be selected.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Material Symbols leading-icon glyph rendered in the menu row.
          * @default ''
         */
        "icon"?: string;
        /**
          * CSS colour for the leading icon (any CSS `<color>` — hex, `rgb()`, or a theme token such as `var(--md-sys-color-primary)`). Ignored without `icon`.
          * @default ''
         */
        "iconColor"?: string;
        /**
          * Visible label. Falls back to the slotted text content when empty.
          * @default ''
         */
        "label"?: string;
        /**
          * Fired when the option mounts, unmounts, or its data changes so the parent picker can re-read its option set.
         */
        "onMdSelectOptionChange"?: (event: MdSelectOptionCustomEvent<void>) => void;
        /**
          * Initial-selected hint (used by the picker only when its value is unset).
          * @default false
         */
        "selected"?: boolean;
        /**
          * Secondary text rendered under the label in the menu row.
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Stable identifier emitted by the picker and used for matching.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * MD3 Side sheet — anchored panel for secondary content and actions.
     */
    interface MdSideSheet {
        /**
          * Show divider between content and actions
          * @default false
         */
        "bottomDivider"?: boolean;
        /**
          * Show close icon button
          * @default true
         */
        "closeable"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Detached style with rounded corners and margin
          * @default false
         */
        "detached"?: boolean;
        /**
          * Headline text (or use the headline slot)
          * @default ''
         */
        "headline"?: string;
        /**
          * Emits when the back button is pressed (modal only)
         */
        "onMdBack"?: (event: MdSideSheetCustomEvent<void>) => void;
        /**
          * Emits when dismissed via scrim click or Escape
         */
        "onMdCancel"?: (event: MdSideSheetCustomEvent<void>) => void;
        /**
          * Emits when the sheet closes
         */
        "onMdClose"?: (event: MdSideSheetCustomEvent<void>) => void;
        /**
          * Emits when the sheet opens
         */
        "onMdOpen"?: (event: MdSideSheetCustomEvent<void>) => void;
        /**
          * Whether the sheet is visible
          * @default false
         */
        "open"?: boolean;
        /**
          * Whether clicking the scrim closes the sheet (modal only)
          * @default true
         */
        "scrimDismissible"?: boolean;
        /**
          * Custom aria-label for the container
          * @default ''
         */
        "sheetAriaLabel"?: string;
        /**
          * Show optional back navigation icon (modal only)
          * @default false
         */
        "showBack"?: boolean;
        /**
          * Which edge the sheet is anchored to
          * @default 'end'
         */
        "side"?: 'start' | 'end';
        /**
          * Show divider between header and content
          * @default false
         */
        "topDivider"?: boolean;
        /**
          * `standard` coexists with page content; `modal` overlays with scrim
          * @default 'standard'
         */
        "variant"?: 'standard' | 'modal';
    }
    interface MdSkeleton {
        /**
          * Animation style: - `pulse` (default) — gentle opacity fade in and out. - `wave` — shimmer gradient sweeps across the surface. - `none` — static block; useful in `prefers-reduced-motion` opt-outs   or when the skeleton is purely decorative.
          * @default 'pulse'
         */
        "animation"?: 'pulse' | 'wave' | 'none';
        /**
          * Whether to expose the skeleton to assistive tech at all. Set to `false` when the surrounding region already announces its own busy state — duplicate announcements get noisy.
          * @default true
         */
        "announce"?: boolean;
        /**
          * Accessible label. Defaults to "Loading…". Pass a more specific label when the skeleton represents a recognisable region ("Loading article") for better screen-reader context.
          * @default 'Loading'
         */
        "ariaLabelProp"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Stretch to fill the parent's block size — the placeholder fills its container's height (which must be definite, e.g. a flex/grid cell). Best with the shape variants (`rectangular` / `rounded` / `circular`); pairs with `full-width` for a fully responsive block.
          * @default false
         */
        "fullHeight"?: boolean;
        /**
          * Stretch to fill the parent's inline size, overriding `width` / `--md-skeleton-width`. (The skeleton is already 100%-wide by default; this makes it explicit and wins over a fixed width — handy in responsive grids.)
          * @default false
         */
        "fullWidth"?: boolean;
        /**
          * Block size (CSS length). Defaults are `1em` for text, `120px` for rectangular/rounded, and `40px` for circular.
          * @default ''
         */
        "height"?: string;
        /**
          * Number of text rows to render. Only meaningful for `variant="text"`. The last row is tapered to ~60 % width so it reads as the final line of a paragraph.
          * @default 1
         */
        "lines"?: number;
        /**
          * Shape of the skeleton placeholder. - `text` — single short rectangle the height of a body line. With   `lines > 1` the last line gets a tapered width so it reads as a   "last line of a paragraph". - `rectangular` — fills the inline + block sizes you give it; no   border-radius unless you set `--md-skeleton-radius`. - `rounded` — same as `rectangular` but with the MD3 medium corner   radius (or your override). Good for cards / images. - `circular` — perfect circle (1:1 aspect). Use for avatar / icon   placeholders; set `--md-skeleton-size` or `width`+`height`.
          * @default 'text'
         */
        "variant"?: 'text' | 'rectangular' | 'rounded' | 'circular';
        /**
          * Inline size (CSS length). Overrides any `--md-skeleton-width`. Examples: `"100%"`, `"240px"`, `"60ch"`.
          * @default ''
         */
        "width"?: string;
    }
    /**
     * MD3 Expressive slider: standard (from edge), centered (from midpoint), or range (dual thumb).
     * **Uncontrolled (default)** — the slider manages its own value. Listen to `mdChange` to observe commits.
     * **Controlled** (`controlled`) — the slider never mutates `value`/`valueStart`/`valueEnd`.
     * Listen to events and set the props yourself.  During drag the slider uses internal live
     * state for smooth visuals; on commit it snaps to the prop values.
     */
    interface MdSlider {
        /**
          * Accessible label for the range-end thumb (becomes the thumb input's `aria-label`).
          * @default ''
         */
        "ariaLabelEnd"?: string;
        /**
          * Accessible label for the range-start thumb (becomes the thumb input's `aria-label`; overrides `aria-label`). NB: a plain (non-`aria-`) attribute name — `aria-label-start` is not a valid ARIA attribute and would just pollute the host with an invalid `aria-*`.
          * @default ''
         */
        "ariaLabelStart"?: string;
        /**
          * When `true`, the slider never mutates `value` / `valueStart` / `valueEnd` on user interaction. Listen to `mdInput` (continuous) and `mdChange` (committed) events and write the props back yourself. During a drag gesture the slider uses internal live state for smooth visuals.
          * @default false
         */
        "controlled"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled"?: boolean;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Make a **vertical** slider fill its parent's block size (responsive height) instead of the fixed `--md-slider-track-length`. The parent needs a definite height; otherwise it falls back to the track-length floor so it never collapses. (Horizontal sliders are already full-width by default.)
          * @default false
         */
        "fullHeight"?: boolean;
        /**
          * Material Symbols name when `inset-icon` and no slotted icon
          * @default ''
         */
        "icon"?: string;
        /**
          * Standard slider only: inset icon area (use `icon` or `inset-icon` slot)
          * @default false
         */
        "insetIcon"?: boolean;
        /**
          * @deprecated Use `value-indicator`
          * @default false
         */
        "labeled"?: boolean;
        /**
          * @default 100
         */
        "max"?: number;
        /**
          * @default 0
         */
        "min"?: number;
        /**
          * Form field name. Without it the slider still works, it just submits nothing — same as a native input with no `name`.
          * @default ''
         */
        "name"?: string;
        /**
          * See `nameStart`.
          * @default ''
         */
        "nameEnd"?: string;
        /**
          * Range mode submits TWO values, which one name cannot express. By default both go under `name` (read them with `formData.getAll(name)`); set these to give each end its own key. Ignored outside `range`.
          * @default ''
         */
        "nameStart"?: string;
        /**
          * Fires when a slider thumb loses focus.
         */
        "onMdBlur"?: (event: MdSliderCustomEvent<MdSliderThumbDetail>) => void;
        /**
          * Fires once when the user commits a value (pointer up, keyboard release, change event).
         */
        "onMdChange"?: (event: MdSliderCustomEvent<MdSliderValueDetail>) => void;
        /**
          * Fires when a drag/keyboard interaction ends.
         */
        "onMdDragEnd"?: (event: MdSliderCustomEvent<MdSliderDragDetail>) => void;
        /**
          * Fires when a drag/keyboard interaction begins.
         */
        "onMdDragStart"?: (event: MdSliderCustomEvent<MdSliderDragDetail>) => void;
        /**
          * Fires when a slider thumb receives focus.
         */
        "onMdFocus"?: (event: MdSliderCustomEvent<MdSliderThumbDetail>) => void;
        /**
          * Fires continuously while the user drags or presses arrow keys.
         */
        "onMdInput"?: (event: MdSliderCustomEvent<MdSliderValueDetail>) => void;
        /**
          * @default 'horizontal'
         */
        "orientation"?: 'horizontal' | 'vertical';
        /**
          * Dual-thumb range
          * @default false
         */
        "range"?: boolean;
        /**
          * Expressive size (default XS per MD3 spec)
          * @default 'xs'
         */
        "size"?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * @default ''
         */
        "sliderAriaLabel"?: string;
        /**
          * @default 1
         */
        "step"?: number;
        /**
          * Stop / tick marks at each step.
          * @default false
         */
        "stops"?: boolean;
        /**
          * @default 50
         */
        "value"?: number;
        /**
          * @default 100
         */
        "valueEnd"?: number;
        /**
          * Human-readable text for the range-end value. Mapped to `aria-valuetext` on the end thumb.
          * @default ''
         */
        "valueEndText"?: string;
        /**
          * Show value label at thumb(s)
          * @default false
         */
        "valueIndicator"?: boolean;
        /**
          * @default 0
         */
        "valueStart"?: number;
        /**
          * Human-readable text for the range-start value. Mapped to `aria-valuetext` on the start thumb.
          * @default ''
         */
        "valueStartText"?: string;
        /**
          * Human-readable text alternative for the current value (single slider). Mapped to `aria-valuetext`.
          * @default ''
         */
        "valueText"?: string;
        /**
          * Single-thumb layout: `standard` fills from min; `centered` fills from range midpoint to value.
          * @default 'standard'
         */
        "variant"?: 'standard' | 'centered';
    }
    interface MdSnackbar {
        /**
          * Action button label (leave empty for no action)
          * @default ''
         */
        "action"?: string;
        /**
          * Auto-dismiss after `autoHideDuration` ms (dismissive behavior)
          * @default true
         */
        "autoHide"?: boolean;
        /**
          * Time in ms before auto-dismiss
          * @default 4000
         */
        "autoHideDuration"?: number;
        /**
          * Show a close (X) icon button
          * @default false
         */
        "closeable"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Accessible label for the close button. Localize per the page locale.
          * @default 'Dismiss'
         */
        "dismissLabel"?: string;
        /**
          * Primary supporting text
          * @default ''
         */
        "message"?: string;
        /**
          * Emits when the action button is clicked
         */
        "onMdAction"?: (event: MdSnackbarCustomEvent<void>) => void;
        /**
          * Emits when the snackbar is dismissed, with reason
         */
        "onMdClose"?: (event: MdSnackbarCustomEvent<{ reason: 'auto' | 'action' | 'close' }>) => void;
        /**
          * Emits when the snackbar becomes visible
         */
        "onMdOpen"?: (event: MdSnackbarCustomEvent<void>) => void;
        /**
          * Whether the snackbar is visible
          * @default false
         */
        "open"?: boolean;
        /**
          * Live-region politeness. `'polite'` (default) → `role="status"` / `aria-live="polite"` (announced when idle). `'assertive'` → `role="alert"` / `aria-live="assertive"` — interrupts the screen reader; use sparingly for important / error messages.
          * @default 'polite'
         */
        "politeness"?: 'polite' | 'assertive';
        /**
          * Screen position
          * @default 'bottom'
         */
        "position"?: | 'bottom' | 'bottom-start' | 'bottom-end'
    | 'top' | 'top-start' | 'top-end'
    | 'start' | 'end' | 'center';
        /**
          * Stack action below text (for longer action labels)
          * @default false
         */
        "stacked"?: boolean;
    }
    /**
     * md-sparkline — compact inline chart, rendered by the in-house
     * engine (Canvas2D). No ECharts.
     * Sparklines are presentational: no axes, no legend, no title, no
     * tooltip by default (opt in with `show-tooltip`). Line / area drive
     * LineChartEngine in `compact` mode; the bar variant drives
     * BarChartEngine. `show-marks` maps to selective extremes/edges dots.
     */
    interface MdSparkline {
        /**
          * Fixed bar WIDTH in px (bar variant), centred in each slot. Default: auto.
         */
        "barWidth"?: number;
        /**
          * Sparkline tint (MD3 role or raw colour).
          * @default 'primary'
         */
        "color"?: MdChartColorRole | string;
        /**
          * Bar corner radius (bar variant only).
          * @default 2
         */
        "cornerRadius"?: number;
        /**
          * Curve type — only meaningful for line / area variants.
          * @default 'smooth'
         */
        "curve"?: MdChartCurve;
        /**
          * Numeric data series. Pass `null` for gaps.
          * @default []
         */
        "data"?: MdChartDataPoint[];
        /**
          * Block-size override (defaults to 28px — table-cell friendly).
         */
        "heightProp"?: string;
        /**
          * Optional category labels for tooltips / a11y.
         */
        "labels"?: (string | number | Date)[] | undefined;
        /**
          * Line stroke width in px (line / area variants). Default 2.5.
         */
        "lineWidth"?: number;
        /**
          * Marker RADIUS in px (line / area variants). Overrides the per-symbol default.
         */
        "markSize"?: number;
        /**
          * Pin the value scale's UPPER bound instead of auto-scaling to the data.
         */
        "max"?: number;
        /**
          * Pin the value scale's LOWER bound instead of auto-scaling to the data. Give several sparklines the same `min` + `max` and they share a comparable scale — a 0–40% metric no longer fills the whole height and reads as maxed out.
         */
        "min"?: number;
        /**
          * Disable all animation.
          * @default false
         */
        "noAnimation"?: boolean;
        "onMdReady"?: (event: MdSparklineCustomEvent<void>) => void;
        "onMdSparkClick"?: (event: MdSparklineCustomEvent<{ dataIndex: number; value: number | null }>) => void;
        /**
          * Highlight a vertical range (e.g. weekend, anomaly window).
         */
        "referenceAreas"?: MdSparklineReferenceArea[] | undefined;
        /**
          * Show min / max / first / last markers.
          * @default 'extremes'
         */
        "showMarks"?: 'none' | 'extremes' | 'edges' | 'all';
        /**
          * Show tooltip + crosshair on hover. Defaults to true.
          * @default true
         */
        "showTooltip"?: boolean;
        /**
          * Format raw values for tooltips / a11y.
         */
        "valueFormatter"?: (value: number | null) => string;
        /**
          * Visual variant.
          * @default 'line'
         */
        "variant"?: 'line' | 'bar' | 'area';
    }
    interface MdSplitButton {
        /**
          * `id` of the element the trailing button controls (the menu/listbox it opens). Wires `aria-controls` so assistive tech links trigger → popup.
          * @default ''
         */
        "controls"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the entire split button
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Stretch the split button to fill its container. The leading segment grows and its label truncates with an ellipsis when space is tight; the trailing toggle keeps its intrinsic width.
          * @default false
         */
        "fullWidth"?: boolean;
        /**
          * `aria-haspopup` value for the trailing button — the kind of popup it opens. Defaults to `'menu'` (the split-button menu-trigger pattern). Set to `'false'` when the trailing button toggles state rather than opening a popup.
          * @default 'menu'
         */
        "haspopup"?: | 'menu' | 'listbox' | 'dialog' | 'grid' | 'tree' | 'true' | 'false';
        /**
          * Material Symbols icon name for the leading button
          * @default ''
         */
        "icon"?: string;
        /**
          * Leading button label text
          * @default ''
         */
        "label"?: string;
        /**
          * Accessible label for the trailing (menu-toggle) button. Localize per the page locale. (WCAG 4.1.2 — the icon-only trailing button needs a name.)
          * @default 'Toggle menu'
         */
        "menuLabel"?: string;
        /**
          * Fired when the leading button is activated
         */
        "onMdLeadingClick"?: (event: MdSplitButtonCustomEvent<void>) => void;
        /**
          * Fired when the trailing button is toggled
         */
        "onMdTrailingClick"?: (event: MdSplitButtonCustomEvent<{ checked: boolean }>) => void;
        /**
          * Enable/disable ripple
          * @default true
         */
        "ripple"?: boolean;
        /**
          * Size
          * @default 'sm'
         */
        "size"?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        /**
          * Visually disabled but remains focusable for screen readers
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Whether the trailing button is in "open/checked" state
          * @default false
         */
        "trailingChecked"?: boolean;
        /**
          * Trailing button icon (default: keyboard_arrow_down)
          * @default 'keyboard_arrow_down'
         */
        "trailingIcon"?: string;
        /**
          * Color variant
          * @default 'filled'
         */
        "variant"?: 'elevated' | 'filled' | 'tonal' | 'outlined';
    }
    /**
     * `md-status-dot` — a small absolutely-positioned indicator that pairs
     * with an avatar (or any other tile) to surface presence / state.
     * Designed to drop straight into a `position: relative` parent:
     * ```html
     * <span style="position: relative; display: inline-flex;">
     *   <md-avatar name="Ada Lovelace" label="Ada Lovelace, online"></md-avatar>
     *   <md-status-dot state="online"></md-status-dot>
     * </span>
     * ```
     * Anchors at the bottom-inline-end corner via logical-property insets, so
     * the pip auto-flips in RTL with no extra CSS. A 2 px outline in the
     * surface color keeps the dot visually detached from the avatar tile
     * regardless of the avatar's palette.
     * **Accessibility.** The dot is **decorative by default** — when used to
     * decorate an avatar, the avatar's own `label` should already encode the
     * status (`label="Ada Lovelace, online"`). Setting an explicit `label`
     * exposes the dot to assistive tech:
     * - **static** (`live` off) → `role="img"` + `aria-label` — a labelled image
     *   conveying the current state, announced when focused/encountered.
     * - **live** (`live` on) → `role="status"` (an `aria-live="polite"` region) so
     *   presence *changes* (online → busy) are announced as they happen.
     * **WCAG 1.4.1 (use of colour).** State is conveyed by colour alone, and in
     * Windows High-Contrast every state collapses to a single system colour. Never
     * rely on the dot's colour as the *only* signal of state — pair it with a
     * `label` (or text on the parent) so the status is also available as words.
     * **Live indicator.** When `live` is `true`, the dot pulses outward in its
     * own colour (online pulses green, busy pulses red, etc.). The animation
     * is gated behind `prefers-reduced-motion: no-preference`, so users who
     * have reduced-motion enabled at the OS level see a static dot.
     */
    interface MdStatusDot {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Lay the dot out in normal flow instead of as an absolutely-positioned corner badge.  The default mode is the avatar badge: `position: absolute` pinned 2px outside its positioned parent's inline/block end, with a surface-toned halo separating it from the tile beneath. That is wrong wherever the dot sits BESIDE something — a legend swatch, a "Market open" line, a status label in a table cell — where it needs to flow with the text. Without this prop each such use had to wrap the dot in a `position: relative` span of an explicit size AND null out both inset custom properties; miss one and the dot hangs low and to the right, overlapping its own label.  `inline` makes the host an `inline-block` in normal flow, vertically centred on the text, with no halo (the halo exists to separate the dot from an avatar tile; over ordinary text it reads as a stray ring). Set `--md-status-dot-outline-width` to bring it back.
          * @default false
         */
        "inline"?: boolean;
        /**
          * Optional accessible label.  - When **empty** (default), the dot is exposed to AT as decorative   (`role="presentation"` + `aria-hidden="true"`) so the parent   element (typically an `md-avatar` or a list-item) can carry the   spoken status without doubling. - When **set** and `live` is **off**, the dot becomes `role="img"` with   the value as its `aria-label` — a static, labelled status image. - When **set** and `live` is **on**, the dot becomes a `role="status"`   live region so presence *changes* are announced as they occur. Use this   for standalone dots that aren't paired with another labelled element.
          * @default ''
         */
        "label"?: string;
        /**
          * Pulse the dot outward in its own colour. Useful for "live now", "in a call", "recording" affordances. Respects `prefers-reduced-motion: reduce` automatically.
          * @default false
         */
        "live"?: boolean;
        /**
          * Diameter preset: - `small`  — 8 px (pairs with `md-avatar size="small"`) - `medium` — 12 px (pairs with `md-avatar size="medium"` — default) - `large`  — 16 px (pairs with `md-avatar size="large"`)  Custom diameters are exposed via the `--md-status-dot-size` CSS custom property on the host.
          * @default 'medium'
         */
        "size"?: MdStatusDotSize;
        /**
          * Presence state. Drives the dot's fill color and its corresponding pulse-halo color (when `live` is `true`).
          * @default 'neutral'
         */
        "state"?: MdStatusDotState;
    }
    /**
     * `md-step` — one step inside an `<md-stepper>`.
     * Layout, position and i18n are pushed down from the parent stepper via
     * `data-*` attributes, so a step is almost always authored declaratively:
     * ```html
     * <md-stepper active="1">
     *   <md-step label="Account" completed></md-step>
     *   <md-step label="Shipping" description="Address & method"></md-step>
     *   <md-step label="Payment" optional></md-step>
     * </md-stepper>
     * ```
     */
    interface MdStep {
        /**
          * Full override for the announced accessible name (i18n escape hatch).
         */
        "accessibleName"?: string;
        /**
          * Whether this step is the active one. Usually managed by the parent.
          * @default false
         */
        "active"?: boolean;
        /**
          * Whether this step is completed (shows a check / fills its connector).
          * @default false
         */
        "completed"?: boolean;
        /**
          * Glyph shown when completed (Material Symbols name).
          * @default 'check'
         */
        "completedIcon"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Supporting text beneath the label.
          * @default ''
         */
        "description"?: string;
        /**
          * Manually disable the step (parent's `mode="linear"` also gates reachability).
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Editable: a completed step shows an edit (pencil) affordance, and — with `auto-complete` on — revisiting it preserves downstream completion (review/edit in place). Revisiting a non-editable step restarts progress from there.
          * @default false
         */
        "editable"?: boolean;
        /**
          * Whether this step is in an error state.
          * @default false
         */
        "error"?: boolean;
        /**
          * Glyph shown when in error (Material Symbols name).
          * @default 'priority_high'
         */
        "errorIcon"?: string;
        /**
          * Error message shown beneath the label (overrides `description` on error).
          * @default ''
         */
        "errorText"?: string;
        /**
          * Hide the built-in Back / Continue actions in the vertical content panel.
          * @default false
         */
        "hideActions"?: boolean;
        /**
          * Override the indicator glyph (Material Symbols name), e.g. "lock".
          * @default ''
         */
        "icon"?: string;
        /**
          * Step caption ("Account info", "Shipping", …).
          * @default ''
         */
        "label"?: string;
        /**
          * Back pressed in this step's content panel. Consumed by `md-stepper`.
         */
        "onMdStepBack"?: (event: MdStepCustomEvent<{ index: number }>) => void;
        /**
          * Bubbled to the parent stepper, which decides whether the move is allowed. Internal coordination event — consumed (and stopped) by `md-stepper`.
         */
        "onMdStepClick"?: (event: MdStepCustomEvent<{ index: number }>) => void;
        /**
          * Continue pressed in this step's content panel. Consumed by `md-stepper`.
         */
        "onMdStepNext"?: (event: MdStepCustomEvent<{ index: number }>) => void;
        /**
          * Mark the step optional — shows an "Optional" caption and may be skipped.
          * @default false
         */
        "optional"?: boolean;
    }
    /**
     * `md-stepper` — Material Design 3 stepper. Orchestrates a set of slotted
     * `md-step` children: it owns the active index and navigation, and pushes
     * layout / i18n down to each step.
     * **Controlled, with optional auto-progress.** The stepper owns `active`
     * (updated on click / `next` / `prev` / `goTo`, emitting `mdStepChange`). With
     * `auto-complete` (default), advancing marks the step you leave as `completed`,
     * so the progress line + checks "just work" — no manual wiring. Set
     * `auto-complete="false"` to own `completed` yourself.
     * ```html
     * <md-stepper active="0">
     *   <md-step label="Account"><p>…</p></md-step>
     *   <md-step label="Shipping" optional><p>…</p></md-step>
     *   <md-step label="Payment" editable><p>…</p></md-step>
     * </md-stepper>
     * ```
     */
    interface MdStepper {
        /**
          * Index of the active step. Two-way bindable. Out-of-range values clamp.
          * @default 0
         */
        "active"?: number;
        /**
          * When advancing (via `next()` or a step's built-in Continue button), mark the step being left as `completed`. Default `true`. Turn off to fully control completion yourself.
          * @default true
         */
        "autoComplete"?: boolean;
        /**
          * Label for the built-in Back button.
          * @default 'Back'
         */
        "backLabel"?: string;
        /**
          * Localized word announced for a completed step ("…, completed").
          * @default 'completed'
         */
        "completedWord"?: string;
        /**
          * Localized word announced for the current step ("…, current").
          * @default 'current'
         */
        "currentWord"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Localized word announced for a step in error ("…, error: message").
          * @default 'error'
         */
        "errorWord"?: string;
        /**
          * Label for the built-in Continue button on the last step.
          * @default 'Finish'
         */
        "finishLabel"?: string;
        /**
          * Indicator style: `numbered` circles or minimal `dot`s.
          * @default 'numbered'
         */
        "indicator"?: 'numbered' | 'dot';
        /**
          * Accessible name for the navigation landmark.
          * @default 'Progress'
         */
        "label"?: string;
        /**
          * Only mount the **active** vertical step's content panel; inactive panels are removed from layout and the a11y tree (no collapse animation). Use for wizards with heavy per-step content. Note: slotted custom elements stay *connected* in the light DOM — for full teardown, conditionally render the content in your framework.
          * @default false
         */
        "lazy"?: boolean;
        /**
          * Put the built-in **Continue / Finish** button into a loading state (spinner + interactive-disabled) and disable **Back**, for async submit-on-Continue (prevents double-submit). Toggle it around your `await` — typically inside an `mdBeforeChange` handler that `preventDefault()`s, does async work, then commits by setting `active`.
          * @default false
         */
        "loading"?: boolean;
        /**
          * - `linear` (default) — steps must be completed in order; future steps are   not reachable until the prior ones are done or optional (an `editable`   completed step can always be revisited). - `non-linear` — any step can be selected.
          * @default 'linear'
         */
        "mode"?: 'linear' | 'non-linear';
        /**
          * Show the built-in **Back / Continue** navigation (default `true`) so users always have a clear way to move through the flow: - vertical → the buttons sit under each active step (Material wizard style), - horizontal → a single button bar below the stepper. Set `nav="false"` to drive navigation entirely yourself (clicking steps, `next()` / `prev()`, or your own controls).
          * @default true
         */
        "nav"?: boolean;
        /**
          * Disable the built-in **Continue / Finish** button without disabling Back or the whole step. Bind this to your current step's form validity to gate advancement declaratively (the most common wizard need). Step clicks and the `next()`/`goTo()` methods are unaffected — use `mode="linear"` and/or `mdBeforeChange` for those.
          * @default false
         */
        "nextDisabled"?: boolean;
        /**
          * Label for the built-in Continue button.
          * @default 'Continue'
         */
        "nextLabel"?: string;
        /**
          * Localized word for "of" in each step's announced name ("Step 2 of 4").
          * @default 'of'
         */
        "ofWord"?: string;
        /**
          * Fires BEFORE a user-driven step change commits (step click, built-in nav, or `next()`/`prev()`/`goTo()`). Cancelable: call `event.preventDefault()` to veto the transition (e.g. block Continue until the current step validates). `detail.index` is the requested step, `detail.previous` the current one. Does not fire for a direct `active` property assignment — set `active` yourself to commit authoritatively (e.g. after async validation).
         */
        "onMdBeforeChange"?: (event: MdStepperCustomEvent<{ index: number; previous: number }>) => void;
        /**
          * Continue pressed on the last step (the flow is finished).
         */
        "onMdComplete"?: (event: MdStepperCustomEvent<void>) => void;
        /**
          * Active step changed (after it commits).
         */
        "onMdStepChange"?: (event: MdStepperCustomEvent<{ index: number; previous: number }>) => void;
        /**
          * Caption + announced word for optional steps.
          * @default 'Optional'
         */
        "optionalWord"?: string;
        /**
          * Layout orientation.
          * @default 'horizontal'
         */
        "orientation"?: 'horizontal' | 'vertical';
        /**
          * Localized word for "Step" in each step's announced name ("Step 2 of 4").
          * @default 'Step'
         */
        "stepWord"?: string;
        /**
          * Layout variant: - `default` — the full step row (headers + connectors). - `mobile` — a compact MUI-style bar for narrow screens: **Back**, a centered   progress (dots for `indicator="dot"`, else a "Step N of M" caption), and   **Continue / Finish**. The step headers are hidden; put the active step's   panel in the `content` slot and swap it on `mdStepChange`.
          * @default 'default'
         */
        "variant"?: 'default' | 'mobile';
    }
    interface MdSubMenuItem {
        /**
          * Small badge label (e.g. "New") displayed before the trailing arrow.
          * @default ''
         */
        "badge"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Whether the item is disabled.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Render a divider line below this item.
          * @default false
         */
        "divider"?: boolean;
        /**
          * Render a gap (extra space) below this item — an alternative separator to divider.
          * @default false
         */
        "gap"?: boolean;
        /**
          * Primary label text.
          * @default ''
         */
        "headline"?: string;
        /**
          * Fires when the item row itself is clicked (not the submenu).
         */
        "onMdClick"?: (event: MdSubMenuItemCustomEvent<void>) => void;
        /**
          * Secondary descriptive text below the headline.
          * @default ''
         */
        "supportingText"?: string;
    }
    /**
     * `md-switch` — Material Design 3 switch (on/off toggle).
     * Form-associated: inside a `<form>` it submits its `value` under `name` when
     * selected, and restores on form reset. It is a *labelable* element — wrap it in
     * a `<label>` (clicks on the label text toggle it and name it), or pass
     * `aria-label` / `aria-labelledby` on the host.
     */
    interface MdSwitch {
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Prevents interaction and removes from tab order
          * @default false
         */
        "disabled"?: boolean;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * Show icons on both selected and unselected states (check / close)
          * @default false
         */
        "icons"?: boolean;
        /**
          * Name submitted with the owning `<form>`.
          * @default ''
         */
        "name"?: string;
        /**
          * Fires **after** the internal state has committed. Use for side effects like saving preferences, analytics, or syncing with other components. Not cancelable — the toggle has already happened.  ```ts switch.addEventListener('mdChange', (e) => {   console.log('Switch is now', e.detail.selected ? 'ON' : 'OFF'); }); ```
         */
        "onMdChange"?: (event: MdSwitchCustomEvent<{ selected: boolean }>) => void;
        /**
          * Fires **before** the state changes. Call `event.preventDefault()` to block the internal toggle and manage state externally (controlled mode).  ```ts switch.addEventListener('mdInput', (e) => {   e.preventDefault();          // prevent internal toggle   myState.value = e.detail.selected; // update your own state }); ```
         */
        "onMdInput"?: (event: MdSwitchCustomEvent<{ selected: boolean }>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdSwitchCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Mark the control required for form validation / assistive tech.
          * @default false
         */
        "required"?: boolean;
        /**
          * Whether the switch is on (selected)
          * @default false
         */
        "selected"?: boolean;
        /**
          * Material Symbols glyph shown on the handle when selected (override the default check). Ignored when the `selected-icon` slot is filled.
          * @default 'check'
         */
        "selectedIcon"?: string;
        /**
          * Show icon only when selected
          * @default false
         */
        "showOnlySelectedIcon"?: boolean;
        /**
          * Visually disabled but remains focusable for discoverability
          * @default false
         */
        "softDisabled"?: boolean;
        /**
          * Material Symbols glyph shown on the handle when unselected (override the default close). Ignored when the `unselected-icon` slot is filled.
          * @default 'close'
         */
        "unselectedIcon"?: string;
        /**
          * Value submitted when the switch is selected.
          * @default 'on'
         */
        "value"?: string;
        /**
          * Localized constraint-validation message shown when `required` is unmet.  A prop rather than a hardcoded string, matching md-time-picker's `value-missing-label`: components stay i18n-engine-agnostic and the consumer localizes through its own dictionary. Native inputs get their message from the browser's locale for free; a form-associated custom element supplies its own, so leaving this hardcoded would ship an English-only form to every locale.
          * @default 'Please turn this on if you want to proceed.'
         */
        "valueMissingLabel"?: string;
    }
    interface MdTab {
        /**
          * Whether this tab is the active / selected tab
          * @default false
         */
        "active"?: boolean;
        /**
          * Optional badge text (empty string = dot badge)
         */
        "badge"?: string | undefined;
        /**
          * ID of the associated tabpanel (sets `aria-controls`)
          * @default ''
         */
        "controls"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Prevents interaction
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Material Symbols icon name (or use the `icon` slot for custom icons)
          * @default ''
         */
        "icon"?: string;
        /**
          * Display icon and label side-by-side instead of stacked
          * @default false
         */
        "inlineIcon"?: boolean;
        /**
          * Tab label text
          * @default ''
         */
        "label"?: string;
        /**
          * Emits when the tab is activated by click or keyboard
         */
        "onMdTabClick"?: (event: MdTabCustomEvent<void>) => void;
        /**
          * Visual variant — set automatically by parent `md-tabs`
          * @default 'primary'
         */
        "variant"?: 'primary' | 'secondary';
    }
    /**
     * A single tab panel. Place inside `md-tab-panels`, one per tab, in tab
     * order. The parent manages `active`, `inert`, and the tabs↔panel ARIA
     * wiring; inactive panels stay in layout (hidden) so the region's size
     * never jumps between tabs.
     */
    interface MdTabPanel {
        /**
          * Managed by md-tab-panels — reflects the associated tab's selection.
          * @default false
         */
        "active"?: boolean;
    }
    /**
     * Panel region for `md-tabs`: place directly AFTER the tablist (or point
     * `for` at its id) with one `md-tab-panel` per tab, in tab order.
     * - Shows the panel matching the tabs' `activeTabIndex`, following
     *   `mdTabChange` automatically.
     * - `sizing="stable"` (default): all panels stack in one grid cell and
     *   inactive ones stay in layout (hidden), so the region — and any
     *   shrink-to-fit card around it — keeps the SAME width/height no matter
     *   which tab is active. `sizing="active"` sizes to the active panel only.
     * - Wires the APG ARIA contract both ways: each tab gets `aria-controls`,
     *   each panel gets `aria-labelledby`, ids auto-assigned where missing.
     */
    interface MdTabPanels {
        /**
          * id of the `md-tabs` to follow. Empty = nearest preceding md-tabs sibling.
          * @default ''
         */
        "for"?: string;
        /**
          * stable = reserve the largest panel's box; active = size to the active panel.
          * @default 'stable'
         */
        "sizing"?: 'stable' | 'active';
    }
    /**
     * Material Design 3 — Table
     * The core composable table primitive for hand-authored tables.
     * Layout architecture: the shadow `.md-table__grid` owns the column tracks
     * (`grid-template-columns`), and every slotted `md-table-row` is a REAL grid
     * item spanning all tracks that lays its cells on the shared tracks via
     * `grid-template-columns: subgrid`. Rows being real boxes is what makes row
     * hover, stripes, dividers, sticky headers and focus rings actually work —
     * and a wrong cell count skews one row, never the whole table.
     * Coordination is push-based (the house pattern): the table stamps context
     * onto its rows (`rowgroup`, stripe parity, stickiness) and pushes sort state
     * into `md-table-sort-label`s — no MutationObservers, no `:host-context()`.
     * High-level features:
     *   - Density: `compact` | `standard` | `comfortable`
     *   - Sticky header / footer (pair with `md-table-container` for the scroll box)
     *   - Sticky first / last column via cell `sticky="start" | "end"`
     *   - Sortable columns via `<md-table-sort-label>` (3-state asc → desc → none)
     *   - Selection coordination — emits `mdSelectionChange`; `selectAll()` /
     *     `deselectAll()` / `toggleSelectAll()` for the select-all checkbox
     *   - Horizontal scroll when the content is wider than the container
     *   - Loading + empty-state slots, striped rows, configurable dividers
     */
    interface MdTable {
        /**
          * Optional caption rendered above the table head.
          * @default ''
         */
        "caption"?: string;
        /**
          * CSS `grid-template-columns` value (e.g. `"auto 1fr 120px"`). Takes precedence over the `columns` prop. Use this for fine control, including using `minmax()`, `fr` units and `auto`.
          * @default ''
         */
        "columnTemplate"?: string;
        /**
          * Number of columns when no `column-template` is provided. The grid uses `repeat(<columns>, minmax(0, 1fr))`. When neither is set, the column count is derived from the first row's cells.
          * @default 0
         */
        "columns"?: number;
        /**
          * Layout density. Accepts BOTH vocabularies:  - the semantic sizes `'compact' | 'standard' | 'comfortable'` (row heights   36 / 52 / 60px), which is this component's original API, and - a numeric rung `0 | -1 | -2 | -3 | -4`, the scale every other component in   the library uses.  A numeric value drives `--md-sys-density-scale` on the host (see the DENSITY block in the CSS) exactly as a global `data-density` ancestor would, and takes the `standard` row-height base. The two are not exclusive: the semantic sizes are themselves scale-aware, so `density="compact"` inside a `data-density="-2"` region condenses further.
          * @default 'standard'
         */
        "density"?: | 'compact'
    | 'standard'
    | 'comfortable'
    | 0
    | -1
    | -2
    | -3
    | -4;
        /**
          * Render the empty-state slot when true.
          * @default false
         */
        "empty"?: boolean;
        /**
          * Frozen-header architecture: the header is rendered OUTSIDE the vertical scroll area, so the scrollbar spans only the body and runs UNDER the head Pair with `md-table-container[max-height]` to cap the body — max-height controls the SIZE, this prop controls the ARCHITECTURE. Without it, a max-height container scrolls the whole table (use `sticky-header` there for the classic in-flow pinned header).
          * @default false
         */
        "frozenHeader"?: boolean;
        /**
          * Hover surface on rows.
          * @default true
         */
        "hoverable"?: boolean;
        /**
          * Height ratchet (default ON): once the table has rendered, it never shrinks below that initial height. Paging, filtering to fewer/zero rows, or an async refetch that momentarily empties the body keep the table (and therefore the page) geometrically stable — no content below jumps up, and no scrollbar can flash from a transient collapse (async rebuilds have a few frames where fresh rows haven't hydrated and measure 0). Set `keep-height="false"` for tables that intentionally resize (e.g. a density-toggle demo).
          * @default true
         */
        "keepHeight"?: boolean;
        /**
          * Accessible name for the `role="table"`. When omitted, the `caption` text names the table (WAI: a caption identifies the table for screen-reader users browsing in tables mode); the generic "Data table" is the last resort. IDREFs cannot cross the shadow boundary, so the caption text is mirrored into `aria-label` rather than referenced via `aria-labelledby`.
          * @default ''
         */
        "label"?: string;
        /**
          * Show the loading state.
          * @default false
         */
        "loading"?: boolean;
        /**
          * Loading presentation: - `overlay` (default) — an indeterminate progress line under the header   plus a scrim that dims/disables the body while the header/footer stay put. - `skeleton` — the body is replaced by shimmering skeleton rows; the   header and footer are untouched.
          * @default 'overlay'
         */
        "loadingMode"?: 'overlay' | 'skeleton';
        /**
          * Number of skeleton rows rendered in `loading-mode="skeleton"`.
          * @default 4
         */
        "loadingRows"?: number;
        /**
          * Minimum table width (e.g. `"640px"`). When the container is narrower, the grid keeps this width and the container shows a horizontal scrollbar instead of squishing the columns. Also settable via `--md-table-min-width`.
          * @default ''
         */
        "minWidth"?: string;
        /**
          * Built-in expressive motion. With `expressive` (the default) the table FLIP-animates its rows around sort and page changes: it snapshots the row boxes when the triggering event fires (`mdSortRequest`, or a pagination event heard by the surrounding `md-table-container`), lets the consumer's synchronous handler mutate the rows, then glides survivors to their new positions and fades + rises entrants. Async mutations settle as a clean no-op. `prefers-reduced-motion` always wins; set `none` to opt out — e.g. when driving your own `flipRows` wiring around the same events.
          * @default 'expressive'
         */
        "motion"?: 'expressive' | 'none';
        /**
          * Hide the dividers between rows.
          * @default false
         */
        "noDividers"?: boolean;
        /**
          * Emits when a column is hidden/shown via `setColumnVisibility`.
         */
        "onMdColumnVisibilityChange"?: (event: MdTableCustomEvent<{ column: number; visible: boolean; hidden: number[] }>) => void;
        /**
          * Emits when a column is pinned/unpinned via `pinColumn`.
         */
        "onMdPinChange"?: (event: MdTableCustomEvent<{ column: number; side: 'start' | 'end' | null }>) => void;
        /**
          * rAF-throttled scroll position of the frozen body scroller.
         */
        "onMdScroll"?: (event: MdTableCustomEvent<{ scrollLeft: number; scrollTop: number }>) => void;
        /**
          * Emitted whenever the row selection state changes.
         */
        "onMdSelectionChange"?: (event: MdTableCustomEvent<MdTableSelectionState>) => void;
        /**
          * Emitted when sort changes (after a `md-table-sort-label` is clicked).
         */
        "onMdSortChange"?: (event: MdTableCustomEvent<MdTableSortState>) => void;
        /**
          * Table-wide custom pin-indicator icon (Material Symbols ligature, e.g. "anchor"). Cast to every pinned header cell so consumers set it once; a cell's own `pin-icon` attribute wins over the cast. Empty = built-in push-pin SVG.
          * @default ''
         */
        "pinIcon"?: string;
        /**
          * How MULTIPLE pinned columns on the same side behave during horizontal scroll: - `stack` (default) — every pinned column sticks at the same edge, so   later ones slide OVER earlier ones while scrolling (a deck-of-cards   effect; only the top one stays fully visible). - `static` — each pinned column gets a cumulative inset equal to the   widths of the pinned columns before it, so ALL of them stay visible   side-by-side (spreadsheet-style).
          * @default 'stack'
         */
        "pinMode"?: 'stack' | 'static';
        /**
          * Total row count for assistive tech when only a subset is rendered (pagination / virtualization). 0 = use the rendered count.
          * @default 0
         */
        "rowCount"?: number;
        /**
          * Absolute 0-based index of the FIRST rendered body row within the full dataset (WAI/ARIA pagination): with `row-count` set, each row gets an `aria-rowindex` so AT reports "row 42 of 5000" instead of a position within the rendered page. Update it on every page change.
          * @default 0
         */
        "rowOffset"?: number;
        /**
          * Vertical scrollbar presentation (frozen mode): - `overlay` (default) — a custom always-visible thumb FLOATS over the   rows' right edge; no gutter is reserved, so row backgrounds and   dividers run the full width and the bar never takes layout space. - `gutter` — classic inset bar in a reserved gap: `scrollbar-gutter:   stable` keeps a fixed strip inside the table (rows stop short of the   edge) and the native bar renders in it, crossing the row borders.
          * @default 'overlay'
         */
        "scrollbar"?: 'overlay' | 'gutter';
        /**
          * Selection mode. - `none`     — no selection (default) - `single`   — radio-like: selecting a row DESELECTS every other row;                `selectAll()`/the select-all checkbox are inert (disabled) - `multiple` — checkbox-like; select-all checkbox + selectAll() available
          * @default 'none'
         */
        "selection"?: 'none' | 'single' | 'multiple';
        /**
          * Currently active sort column id. Read by `<md-table-sort-label column="…">` to render its arrow. Empty = no sort.
          * @default ''
         */
        "sortBy"?: string;
        /**
          * Currently active sort direction.
          * @default 'asc'
         */
        "sortOrder"?: MdTableSortOrder;
        /**
          * Stick the foot row(s) to the bottom of the scroll container.
          * @default false
         */
        "stickyFooter"?: boolean;
        /**
          * Stick the head row(s) to the top of the scroll container.
          * @default false
         */
        "stickyHeader"?: boolean;
        /**
          * Show alternating row colors.
          * @default false
         */
        "striped"?: boolean;
        /**
          * Long description of a COMPLEX table's structure (WAI caption & summary pattern) — e.g. "Columns are grouped under Profile and Employment". Exposed as `aria-description` on the table; invisible otherwise.
          * @default ''
         */
        "summary"?: string;
    }
    /**
     * Material Design 3 — Table Body (`<tbody>`).
     * Pure presentational rowgroup. Children flow into the parent grid via
     * `display: contents`.
     */
    interface MdTableBody {
        /**
          * @default false
         */
        "presentational"?: boolean;
    }
    /**
     * Material Design 3 — Table Cell.
     * The visual surface for a single grid cell. Renders as either a `cell`
     * (default) or `columnheader` / `rowheader` based on `head` / `scope`.
     * Props are intentionally MUI-compatible:
     *   - `align="start" | "center" | "end" | "justify"`
     *   - `numeric` (right-aligns + tabular numbers)
     *   - `padding="none" | "checkbox" | "default"`
     *   - `head` (renders as `<th>` semantics with `scope="col"|"row"`)
     *   - `sticky="start" | "end"` (sticky first / last column)
     *   - `colspan` / `rowspan` (grid spans)
     * The cell is a dumb surface: row state (selected / disabled / header
     * context) arrives as inherited CSS custom properties cast by
     * `<md-table-row>` — no observers, no DOM queries.
     */
    interface MdTableCell {
        /**
          * Text alignment within the cell.
          * @default 'start'
         */
        "align"?: 'start' | 'center' | 'end' | 'justify';
        /**
          * Number of columns this cell spans (CSS Grid `grid-column: span N`).
          * @default 1
         */
        "colSpan"?: number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable text-wrap (single-line, ellipsis).
          * @default false
         */
        "ellipsis"?: boolean;
        /**
          * Render as a column / row header.
          * @default false
         */
        "head"?: boolean;
        /**
          * Suppress the automatic pin indicator that a sticky header cell shows. By default a frozen (sticky) column always advertises that it's pinned with a small pin icon in its header; set this to opt out (e.g. a sticky checkbox column — which is auto-skipped anyway).
          * @default false
         */
        "noPinIndicator"?: boolean;
        /**
          * Right-align + tabular figures (for monetary / numeric data).
          * @default false
         */
        "numeric"?: boolean;
        /**
          * Padding mode (MUI parity).
          * @default 'default'
         */
        "padding"?: 'default' | 'none' | 'checkbox';
        /**
          * Custom pin-indicator icon: a Material Symbols ligature (e.g. "anchor", "keep"). Empty = the built-in 45° push-pin SVG. Usually set ONCE on md-table via its `pin-icon` prop, which casts the value to every pinned header cell; setting it here directly overrides the cast.
          * @default ''
         */
        "pinIcon"?: string;
        /**
          * Number of rows this cell spans (CSS Grid `grid-row: span N`).
          * @default 1
         */
        "rowSpan"?: number;
        /**
          * Header scope (used when `head` is true).
          * @default ''
         */
        "scope"?: 'col' | 'row' | 'colgroup' | 'rowgroup' | '';
        /**
          * Sticky positioning — `start` for first column, `end` for last column.
          * @default ''
         */
        "sticky"?: 'start' | 'end' | '';
        /**
          * Optional content variant: `body` | `meta` (smaller, dimmer).
          * @default 'body'
         */
        "variant"?: 'body' | 'meta';
    }
    /**
     * Material Design 3 — Table Container
     * The outer surface that wraps a `<md-table>`, optional `<md-table-toolbar>`
     * and `<md-table-pagination>`. Provides elevation, shape, and the scroll
     * context (with optional max-height) for sticky headers / columns.
     * Compose freely:
     * ```html
     * <md-table-container variant="elevated" max-height="480px">
     *   <md-table-toolbar headline="Users"></md-table-toolbar>
     *   <md-table>…</md-table>
     *   <md-table-pagination></md-table-pagination>
     * </md-table-container>
     * ```
     */
    interface MdTableContainer {
        /**
          * Explicit MD3 elevation level (0–5), overriding the variant's default shadow. Handy to raise/flatten the surface without changing `variant` (e.g. `variant="outlined" elevation="2"`). Also settable via `--md-table-container-elevation`.
         */
        "elevation"?: 0 | 1 | 2 | 3 | 4 | 5;
        /**
          * If set, the table area becomes scrollable up to this max-height (e.g. `"480px"`, `"60vh"`). Required for sticky header / column.
          * @default ''
         */
        "maxHeight"?: string;
        /**
          * If set, the table area becomes scrollable up to this min-height (useful to keep an empty-state visible).
          * @default ''
         */
        "minHeight"?: string;
        /**
          * Container shape — large for dashboards, medium for dense, small for compact. Maps to MD3 `--md-sys-shape-corner-{size}` tokens.
          * @default 'large'
         */
        "shape"?: 'extra-large' | 'large' | 'medium' | 'small' | 'none';
        /**
          * Visual surface variant. - `elevated` — surface-container-low + box-shadow - `outlined` — surface + 1px outline - `filled`   — surface-container-low, no shadow / outline - `flat`     — transparent (use when nesting inside another surface)
          * @default 'elevated'
         */
        "variant"?: 'elevated' | 'outlined' | 'filled' | 'flat';
        /**
          * Vibrant tonality: tints the whole table with primary/tertiary container tones (coloured header, primary hover, tertiary selection, primary-tinted surface). Cascades to the contained md-table via CSS custom properties.
          * @default false
         */
        "vibrant"?: boolean;
    }
    /**
     * Material Design 3 — Table Expand Toggle.
     * The STANDARD expand caret for an expandable `<md-table-row>`: one consistent
     * size (xs / 32px — fits every density incl. compact rows, matching the 32px
     * checkbox precedent), a chevron that rotates 90° on the spatial spring when
     * the row opens, wired click→row.toggle() and aria-expanded out of the box.
     * Slot it INSIDE a cell of the expandable row (never as a bare row child —
     * rows are subgrid containers where any extra child would consume a column
     * track):
     * ```html
     * <md-table-row expandable>
     *   <md-table-cell padding="checkbox"><md-table-expand-toggle></md-table-expand-toggle></md-table-cell>
     *   <md-table-cell>…</md-table-cell>
     *   <div slot="expanded">Details…</div>
     * </md-table-row>
     * ```
     */
    interface MdTableExpandToggle {
        /**
          * Accessible label of the toggle button.
          * @default 'Expand row'
         */
        "buttonLabel"?: string;
        /**
          * Caret icon (Material Symbols ligature). The expanded rotation still applies (--md-table-expand-toggle-rotation, default 90deg) — pick a "collapsed-pointing" glyph like the default chevron_right, or pair e.g. "expand_circle_right" with a custom rotation.
          * @default 'chevron_right'
         */
        "icon"?: string;
    }
    /**
     * Material Design 3 — Table Foot (`<tfoot>`).
     * Pure presentational rowgroup for footer rows (totals, summaries).
     * Children flow into the parent grid via `display: contents`.
     */
    interface MdTableFoot {
        /**
          * @default false
         */
        "presentational"?: boolean;
    }
    /**
     * Material Design 3 — Table Head (`<thead>`).
     * Pure presentational rowgroup used to mark a region of column-header rows.
     * Applies the head background (sticky-friendly) and ensures children flow
     * into the parent `<md-table>`'s CSS Grid via `display: contents`.
     */
    interface MdTableHead {
    }
    /**
     * Material Design 3 — Table Pagination.
     * Drop into a `<md-table-container slot="bottom">` for a complete data table.
     * Mirrors the MUI `<TablePagination>` API surface. Built from house
     * primitives: `md-icon-button` navigation and an `md-select` rows-per-page
     * picker (proper dismissal, keyboard support and theming for free).
     * Emits:
     *   - `mdPageChange` — when the page changes
     *   - `mdRowsPerPageChange` — when the rows-per-page selector changes
     */
    interface MdTablePagination {
        /**
          * Compact pagination layout (good for embedding inside cards).
          * @default false
         */
        "compact"?: boolean;
        /**
          * Total number of rows in the dataset.
          * @default 0
         */
        "count"?: number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled state.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Label of the "All" rows-per-page option.
          * @default 'All'
         */
        "labelAll"?: string;
        /**
          * Custom label used to display the current row range (e.g. `"Showing %from%–%to% of %count%"`). Tokens: `%from%`, `%to%`, `%count%`.
          * @default '%from%–%to% of %count%'
         */
        "labelDisplayedRows"?: string;
        /**
          * Accessible label for the first-page button.
          * @default 'First page'
         */
        "labelFirstPage"?: string;
        /**
          * Accessible label for the last-page button.
          * @default 'Last page'
         */
        "labelLastPage"?: string;
        /**
          * Accessible label for the next-page button.
          * @default 'Next page'
         */
        "labelNextPage"?: string;
        /**
          * Accessible label for the previous-page button.
          * @default 'Previous page'
         */
        "labelPreviousPage"?: string;
        /**
          * Label for the rows-per-page selector.
          * @default 'Rows per page:'
         */
        "labelRowsPerPage"?: string;
        /**
          * Fired when the user changes the page.
         */
        "onMdPageChange"?: (event: MdTablePaginationCustomEvent<{ page: number }>) => void;
        /**
          * Fired when the user changes rows-per-page.
         */
        "onMdRowsPerPageChange"?: (event: MdTablePaginationCustomEvent<{ rowsPerPage: number }>) => void;
        /**
          * Current zero-based page.
          * @default 0
         */
        "page"?: number;
        /**
          * Rows shown per page.
          * @default 10
         */
        "rowsPerPage"?: number;
        /**
          * Comma-separated list of options for the rows-per-page selector (e.g. `"5,10,25,50"`). Use `"all"` (case-insensitive) to add an "All" option that maps to the total count.
          * @default '5,10,25,50'
         */
        "rowsPerPageOptions"?: string;
        /**
          * Show the first / last page buttons.
          * @default false
         */
        "showFirstLast"?: boolean;
    }
    /**
     * Material Design 3 — Table Row.
     * A row inside a `<md-table>` that contains `<md-table-cell>` children.
     * The host is a REAL grid box that spans the table's tracks and lays its
     * cells on them via `grid-template-columns: subgrid` — so row hover,
     * stripes, dividers, selection tint, sticky rows and focus rings all work
     * on the row itself, and a wrong cell count skews one row, never the table.
     * The parent `<md-table>` stamps context (`rowgroup`, stripe parity,
     * stickiness) onto the row; the row casts typography/color context down to
     * its cells with `::slotted()` custom properties — no observers.
     * Behaviors:
     * - `selected` — selection state (visual + ARIA)
     * - `disabled` — non-interactive
     * - `clickable` — adds keyboard activation (Enter / Space) + emits `mdRowClick`
     * - `expandable` — toggles an "expanded details" zone (use the `expanded` slot),
     *   spring-animated via the grid-rows height pattern
     */
    interface MdTableRow {
        /**
          * When true, treat the entire row as a button (Enter/Space activates `mdRowClick`).
          * @default false
         */
        "clickable"?: boolean;
        /**
          * Disabled — row receives no hover/click handlers.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Render an "expandable details" zone toggleable via `toggle()`.
          * @default false
         */
        "expandable"?: boolean;
        /**
          * Whether the expandable zone is open.
          * @default false
         */
        "expanded"?: boolean;
        /**
          * Highlights the row using primary-container (e.g. for the focused row).
          * @default false
         */
        "highlight"?: boolean;
        /**
          * Fired when the user clicks (or Enter/Space-activates) the row.
         */
        "onMdRowClick"?: (event: MdTableRowCustomEvent<{ value: string; row: HTMLElement }>) => void;
        /**
          * Fired when the row's expanded state changes.
         */
        "onMdRowExpandedChange"?: (event: MdTableRowCustomEvent<{ expanded: boolean }>) => void;
        /**
          * Fired when the `selected` state changes (used by `<md-table>` for selection coordination).
         */
        "onMdRowSelectionChange"?: (event: MdTableRowCustomEvent<{ selected: boolean; value: string }>) => void;
        /**
          * Which rowgroup this row belongs to. Stamped automatically by the parent `<md-table>` (head / body / foot) — set it manually only when using rows outside a table.
          * @default 'body'
         */
        "rowgroup"?: 'head' | 'body' | 'foot';
        /**
          * Whether this row counts as selectable (used by `<md-table>` selection coordination).
          * @default true
         */
        "selectable"?: boolean;
        /**
          * Selected (highlighted) state. Reflected.
          * @default false
         */
        "selected"?: boolean;
        /**
          * Identifier attached to selection / click events. Defaults to row index.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * Material Design 3 — Table Sort Label.
     * A clickable header label with a sort indicator that reflects the current
     * sort state. Place inside a `<md-table-cell head>`.
     * Wiring:
     *   1. Set `column="<id>"`
     *   2. The parent `<md-table>` listens for `mdSortRequest` (auto-bubbled),
     *      updates its `sort-by` / `sort-order`, and PUSHES the resulting
     *      `active` / `order` back into every sort label — no observers.
     * The arrow is hidden at rest and fades in at 60% opacity on hover or keyboard
     * focus, so an unsorted column advertises sorting only when it is pointed at.
     * Once the column is the sort key the arrow stays fully opaque, tinted with the
     * active colour, and rotates 180° to point down for descending. The rotation
     * runs on the spatial spring, the colour/opacity on the effects spring.
     */
    interface MdTableSortLabel {
        /**
          * Whether this column is the active sort. Pushed by the parent `<md-table>`.
          * @default false
         */
        "active"?: boolean;
        /**
          * Column id — matched against the `<md-table>`'s `sort-by` value. Required.
          * @default ''
         */
        "column"?: string;
        /**
          * Direction the arrow points by default (the value used when this column becomes the active sort for the first time).
          * @default 'asc'
         */
        "defaultOrder"?: 'asc' | 'desc';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disabled — non-interactive.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Custom sort-arrow icon: a Material Symbols ligature name (e.g. "north", "expand_less"). Empty = the built-in SVG arrow. The icon still rotates 180deg for descending via the same CSS, so pick an "ascending-pointing" glyph.
          * @default ''
         */
        "icon"?: string;
        /**
          * Visual position of the arrow icon relative to the label. - `end`   — after the label (default) - `start` — before the label
          * @default 'end'
         */
        "iconPosition"?: 'start' | 'end';
        /**
          * When the arrow is visible on a column that is NOT the active sort.  - `always` — dimmed but present at rest (default). The arrow is the only   thing distinguishing a sortable column from a fixed one, so hiding it   until hover means the affordance cannot be discovered without hovering   every header — and cannot be discovered at all on a touch device, which   has no hover state to give. - `hover`  — invisible until the header is hovered or focused. Quieter in a   table where nearly every column sorts and the arrows become noise.  The ACTIVE column's arrow is always fully opaque under either setting.
          * @default 'always'
         */
        "inactiveIcon"?: 'always' | 'hover';
        /**
          * Fired when the user requests to sort by this column.
         */
        "onMdSortRequest"?: (event: MdTableSortLabelCustomEvent<{ column: string; defaultOrder: 'asc' | 'desc' }>) => void;
        /**
          * Active sort direction. Pushed by the parent `<md-table>`.
          * @default 'none'
         */
        "order"?: 'asc' | 'desc' | 'none';
    }
    /**
     * Material Design 3 — Table Toolbar.
     * The header bar above a `<md-table>` (typically dropped into a
     * `<md-table-container slot="top">`). Shows a title + supporting text and
     * a row of action items. Switches into a "selection" mode when one or more
     * rows are selected (matching the MUI / MD3 data-table pattern).
     * Two modes:
     *   - Default — shows the headline + supporting text + actions
     *   - Selection — shows "%count% selected" + selection-actions (when
     *     `num-selected > 0`)
     * The toolbar can listen to its closest `<md-table>`'s `mdSelectionChange`
     * event automatically (via `auto-bind`) — set `auto-bind` on the toolbar
     * and selection state will track the table without any glue code.
     */
    interface MdTableToolbar {
        /**
          * Auto-bind to the closest `<md-table>`'s `mdSelectionChange` event so `numSelected` updates with no glue code.
          * @default false
         */
        "autoBind"?: boolean;
        /**
          * Compact density for the toolbar (smaller padding / type).
          * @default false
         */
        "compact"?: boolean;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Title displayed when no rows are selected.
          * @default ''
         */
        "headline"?: string;
        /**
          * Custom label for the selection-mode caption. Tokens: `%count%`.
          * @default '%count% selected'
         */
        "labelSelected"?: string;
        /**
          * Number of selected rows (controlled).
          * @default 0
         */
        "numSelected"?: number;
        /**
          * Optional supporting text under the headline.
          * @default ''
         */
        "supportingText"?: string;
    }
    interface MdTabs {
        /**
          * Index of the currently active tab
          * @default 0
         */
        "activeTabIndex"?: number;
        /**
          * Accessible label for the tablist
          * @default ''
         */
        "ariaLabelProp"?: string;
        /**
          * How far the bottom rule under the strip runs.  - `auto` — each tab draws its own 1px segment, so the rule is exactly as   wide as the tabs collectively are (default; unchanged behaviour). - `full` — one rule across the whole container, drawn by md-tabs itself   and running past the last tab to the container's edge.  `auto` is right when the strip fills its container, which it does at `tab-width="equal"`. It reads as a bug at `tab-width="auto"` in a wide panel, where three content-sized tabs leave the rule stopping a third of the way across and the panel below appearing to hang off the end of it.  In `full` mode the per-tab segments are suppressed by setting `--md-tab-divider-color: transparent` on the host — custom properties inherit through the slotted tabs' shadow roots, so there is no second source of truth about which mode is active.
          * @default 'auto'
         */
        "divider"?: 'auto' | 'full';
        /**
          * Emits when the active tab changes
         */
        "onMdTabChange"?: (event: MdTabsCustomEvent<{ index: number; previousIndex: number }>) => void;
        /**
          * How tabs size along the strip. All modes are layout-stable under dynamic content (labels/badges can change without reshuffling the strip); long labels ellipsize instead of forcing a tab wider. - `equal` (default) — tabs share the parent's FULL width equally;   tracks change only when the PARENT resizes. - `auto` — each tab takes its content width (the strip scrolls), so   tracks follow content by design. - any CSS length (e.g. `"140px"`, `"10rem"`) — every tab gets exactly   that width regardless of parent or content.
          * @default 'equal'
         */
        "tabWidth"?: string;
        /**
          * Visual variant applied to all child tabs
          * @default 'primary'
         */
        "variant"?: 'primary' | 'secondary';
        /**
          * Explicit width for the WHOLE strip (any CSS size: `"480px"`, `"40rem"`, `"100%"`). By default the strip is `100%` of its parent — in shrink-to-fit layouts (inline-block cards, auto grid columns, flex items) that parent's width follows the ACTIVE TAB PANEL, so switching to a tab with tiny content collapses the whole tablist. Set `width` to anchor the strip regardless of what the panels do.
          * @default ''
         */
        "width"?: string;
    }
    interface MdTextField {
        /**
          * Force the field to render in its focused visual state regardless of the underlying input's actual focus. Use when the field acts as an *anchor* for a popover, dialog, or menu, and the OS focus has moved into that surface — for example the `md-time-picker` keeps its trigger field in the focused look while the picker modal is open, so the trigger and the modal read as one connected control.  Visual-only: affects the notched-outline width / colour, the floating label position, and the leading-icon colour through md-text-field's existing `.md-text-field--focused` rules. Does NOT alter formatter / blur-format behaviour (those keep tracking real focus events), so `formatOn="blur"` still flips between formatted and raw on real focus/blur even while `appear-focused` is on.
          * @default false
         */
        "appearFocused"?: boolean;
        /**
          * @default ''
         */
        "autoCapitalize"?: string;
        /**
          * Input behavior passthrough (password managers / mobile keyboards).
          * @default ''
         */
        "autocomplete"?: string;
        /**
          * When leading `chips` are slotted, allow them to WRAP onto multiple rows so the field grows to fit them. Default `false`: chips stay on a single line and the field keeps its normal height (the consumer, e.g. md-multi-select, is responsible for handling overflow — clipping or a "+N" counter).
          * @default false
         */
        "chipsWrap"?: boolean;
        /**
          * Show a clear icon button. 'internal' clears the value directly; 'external' emits mdClear and leaves value control to the consumer.
          * @default false
         */
        "clearable"?: 'internal' | 'external' | false;
        /**
          * Debounce delay (ms) for the mdSearch event. Emits after the user stops typing for this duration. 0 = disabled.
          * @default 0
         */
        "debounce"?: number;
        /**
          * Density scale: 0 (default 56px), -1 (52px), -2 (48px), -3 (44px).
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * @default false
         */
        "disabled"?: boolean;
        /**
          * @default ''
         */
        "enterKeyHint"?: string;
        /**
          * @default false
         */
        "error"?: boolean;
        /**
          * @default ''
         */
        "errorText"?: string;
        /**
          * Border width when focused: 1, 2, or 3 pixels.
          * @default 3
         */
        "focusBorderWidth"?: number;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * When to apply the formatter. - 'blur': format on blur, show raw value on focus (default) - 'input': format on every keystroke (live formatting)
          * @default 'blur'
         */
        "formatOn"?: 'blur' | 'input';
        /**
          * Formats the raw value for display. Set via JS: `el.formatter = (v) => Number(v).toLocaleString()`.
         */
        "formatter"?: (value: string) => string;
        /**
          * `aria-autocomplete` for the combobox — how typing relates to the popup.
          * @default ''
         */
        "inputAriaAutocomplete"?: '' | 'none' | 'inline' | 'list' | 'both';
        /**
          * Expanded state of the owner's popup. Emitted only with `input-role="combobox"`, where `aria-expanded` is an allowed attribute (it is not on a bare textbox).
          * @default false
         */
        "inputExpanded"?: boolean;
        /**
          * @default ''
         */
        "inputMode"?: string;
        /**
          * Promote the inner input to a WAI-ARIA **combobox**, for a composite that owns the popup (md-autocomplete). The role has to live on the real textbox — the one AT reads when it has focus — and this field renders it, so the owner cannot put it there itself.  Whitelisted to `combobox`: it is the only role an owner has a legitimate reason to place on a text input, and an open-ended role prop here would let a consumer lie about the control.
          * @default ''
         */
        "inputRole"?: '' | 'combobox';
        /**
          * @default ''
         */
        "label"?: string;
        "max"?: string | number | undefined;
        "maxLength"?: number | undefined;
        "min"?: string | number | undefined;
        "minLength"?: number | undefined;
        /**
          * Multi-line mode. - false: single-line <input> (default) - 'auto-grow': <textarea> that expands as content grows (multi-line field) - 'fixed': <textarea> with fixed height that scrolls (text area)
          * @default false
         */
        "multiline"?: 'auto-grow' | 'fixed' | false;
        /**
          * Form field name (host attribute — ElementInternals submits under it).
          * @default ''
         */
        "name"?: string;
        "onMdChange"?: (event: MdTextFieldCustomEvent<string>) => void;
        "onMdClear"?: (event: MdTextFieldCustomEvent<void>) => void;
        "onMdInput"?: (event: MdTextFieldCustomEvent<string>) => void;
        "onMdPasswordToggle"?: (event: MdTextFieldCustomEvent<{ visible: boolean }>) => void;
        /**
          * Emitted with debounce/throttle control. Useful for search-as-you-type, API calls, or filtering.
         */
        "onMdSearch"?: (event: MdTextFieldCustomEvent<string>) => void;
        "onMdSpeechResult"?: (event: MdTextFieldCustomEvent<{ transcript: string; listening: boolean }>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdTextFieldCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Parses the display value back to a raw value. Set via JS: `el.parser = (v) => v.replace(/[^0-9.-]/g, '')`.
         */
        "parser"?: (displayValue: string) => string;
        /**
          * Show a password visibility toggle. 'internal' toggles the input type directly; 'external' emits mdPasswordToggle and leaves type control to the consumer.
          * @default false
         */
        "passwordToggle"?: 'internal' | 'external' | false;
        /**
          * Constraint validation (forwarded to the inner input; surfaced via ElementInternals).
          * @default ''
         */
        "pattern"?: string;
        /**
          * @default ''
         */
        "placeholder"?: string;
        /**
          * @default ''
         */
        "prefixText"?: string;
        /**
          * @default false
         */
        "readOnly"?: boolean;
        /**
          * @default false
         */
        "required"?: boolean;
        /**
          * Always occupy the supporting-text line, even when there is no message.  The row is otherwise rendered only when there is something to show, so the first validation error CREATES a line and shoves everything below it down — the whole form jumps as you tab through it. Reserving the space is opt-in because a field with no message would otherwise carry an empty line everywhere, including dense and inline layouts. createFormController turns it on automatically for the fields it manages, since those are exactly the ones whose messages appear and disappear.
          * @default false
         */
        "reserveSupportingSpace"?: boolean;
        /**
          * Restrict input to allowed characters. Presets: 'numeric', 'integer', 'decimal', 'alpha', 'alphanumeric'. Custom: a regex character class string, e.g. '[0-9+() -]'.
          * @default ''
         */
        "restrict"?: string;
        /**
          * Number of visible text rows for multiline modes. Defaults to 2 for auto-grow, 4 for fixed.
         */
        "rows"?: number | undefined;
        /**
          * Language/locale for internal speech recognition (e.g. 'en-US', 'de-DE'). Defaults to browser locale.
          * @default ''
         */
        "speechLang"?: string;
        /**
          * Speech-to-text via Web Speech API. - 'internal': manages recognition internally, appends transcript to value - 'external': emits mdSpeechResult on mic click, letting the consumer handle recognition
          * @default false
         */
        "speechToText"?: 'internal' | 'external' | false;
        "spellcheck"?: boolean | undefined;
        "step"?: string | number | undefined;
        /**
          * @default ''
         */
        "suffixText"?: string;
        /**
          * @default ''
         */
        "supportingText"?: string;
        /**
          * Throttle interval (ms) for the mdSearch event. Emits at most once per interval while typing. 0 = disabled.
          * @default 0
         */
        "throttle"?: number;
        /**
          * @default 'text'
         */
        "type"?: string;
        /**
          * NOT reflected: typed values (incl. passwords) must never appear as a DOM attribute.
          * @default ''
         */
        "value"?: string;
        /**
          * @default 'filled'
         */
        "variant"?: 'filled' | 'outlined';
    }
    interface MdTimePicker {
        /**
          * Localized AM label (12-hour mode).
          * @default 'AM'
         */
        "amLabel"?: string;
        /**
          * Localized label for the Cancel action.
          * @default 'Cancel'
         */
        "cancelLabel"?: string;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disables the trigger and prevents the dialog from opening.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * The `id` of a `<form>` element to associate this element with.
         */
        "form"?: string;
        /**
          * 12-hour (with AM/PM) or 24-hour clock.
          * @default '12h'
         */
        "format"?: MdTimePickerFormat;
        /**
          * Headline copy shown above the dial / inputs. Overrides the mode-specific defaults (`headline-input-label` / `headline-dial-label`) when set.
          * @default ''
         */
        "headline"?: string;
        /**
          * Localized default headline for the dial variant (used when `headline` is empty).
          * @default 'Select time'
         */
        "headlineDialLabel"?: string;
        /**
          * Localized default headline for the input variant (used when `headline` is empty).
          * @default 'Enter time'
         */
        "headlineInputLabel"?: string;
        /**
          * When true the built-in field trigger is not rendered; open the dialog programmatically via `.show()`.
          * @default false
         */
        "hideTrigger"?: boolean;
        /**
          * Localized accessible label for the Hour input field. Matches the [MD3 Time picker accessibility spec](https://m3.material.io/components/time-pickers/accessibility) which mandates an "Hour" label on the hour text input.
          * @default 'Hour'
         */
        "hourLabel"?: string;
        /**
          * Trigger field label (acts as the floating label of the text-field-shaped trigger).
          * @default 'Select time'
         */
        "label"?: string;
        /**
          * Latest selectable time as a 24-hour `HH:MM` string. A committed value after it fails constraint validation with `rangeOverflow`.
          * @default ''
         */
        "max"?: string;
        /**
          * Earliest selectable time as a 24-hour `HH:MM` string. A committed value before it fails constraint validation with `rangeUnderflow`.
          * @default ''
         */
        "min"?: string;
        /**
          * Localized accessible label for the Minute input field. Matches the [MD3 Time picker accessibility spec](https://m3.material.io/components/time-pickers/accessibility) which mandates a "Minute" label on the minute text input.
          * @default 'Minute'
         */
        "minuteLabel"?: string;
        /**
          * Minute snap increment on the dial (1, 5, 10, 15, 30…). Inputs always accept 1-minute granularity.
          * @default 1
         */
        "minuteStep"?: number;
        /**
          * Form field name — the picker is form-associated and submits its `value` under this name in `FormData`, like a native `<input type="time">`.
          * @default ''
         */
        "name"?: string;
        /**
          * Localized label for the confirm action.
          * @default 'OK'
         */
        "okLabel"?: string;
        /**
          * Emitted when the picker is cancelled or dismissed without committing.
         */
        "onMdCancel"?: (event: MdTimePickerCustomEvent<void>) => void;
        /**
          * Emitted when the user confirms the chosen time (OK button, or Enter inside the HH/MM fields while the buffers are valid). This is the canonical "the user has committed a value" event — use it for form integration, persistence, and analytics.  The detail bundle includes both the human-friendly `value` and three industry-standard ISO / Date representations so downstream code never has to re-format the time itself.
         */
        "onMdChange"?: (event: MdTimePickerCustomEvent<MdTimePickerChangeDetail>) => void;
        /**
          * Emitted when the picker closes (whether confirmed, cancelled, or dismissed).
         */
        "onMdClose"?: (event: MdTimePickerCustomEvent<void>) => void;
        /**
          * Emitted on every intermediate change while the dialog is open — dial drag, dial-mode keyboard typing, AM/PM toggle, and input-variant keystrokes that produce a valid HH:MM.  Mirrors the HTML `input` event semantics ("fires for every user-driven mutation") and matches the pattern used by `md-date-picker` / `md-text-field`. Use `mdInput` for live previews and `mdChange` for the final commit. The detail shape is identical to `mdChange` so a previewer can read the same fields regardless of which event arrived.
         */
        "onMdInput"?: (event: MdTimePickerCustomEvent<MdTimePickerChangeDetail>) => void;
        /**
          * Emitted when the user switches between the dial and input variants via the dialog's keyboard / clock toggle. Lets analytics or sibling UI react to the variant change (e.g. announce the new mode to screen readers, or resize a hosting dialog wrapper).
         */
        "onMdModeChange"?: (event: MdTimePickerCustomEvent<MdTimePickerModeChangeDetail>) => void;
        /**
          * Emitted when the picker opens.
         */
        "onMdOpen"?: (event: MdTimePickerCustomEvent<void>) => void;
        /**
          * Fires when this control's validity CHANGES — never on every keystroke, and never for a re-publish that lands on the same state.  `composed: false` is deliberate. Composites like md-select embed an md-text-field, and a composed event escapes that inner shadow root, so a listener on md-select would receive the inner field's event as well as the host's — two events, different payloads, for one logical control. Keeping it uncomposed means each component reports only for itself, while `bubbles: true` still lets a <form> or app root hear every control.
         */
        "onMdValidityChange"?: (event: MdTimePickerCustomEvent<{
    valid: boolean;
    validationMessage: string;
    flags: Record<string, boolean>;
  }>) => void;
        /**
          * Whether the picker dialog is open. Two-way bindable / reflected.
          * @default false
         */
        "open"?: boolean;
        /**
          * Dialog orientation for the dial variant.  - `vertical` (default) — single-column portrait layout (~328px wide). - `horizontal` — two-column landscape layout (~572px wide in both   12h and 24h modes since the HH/MM input tiles are 96px in either   format). Forces the AM/PM pill into the 216×38px horizontal   variant since the 52×72px vertical pill cannot share the left   column cleanly. Has no effect when `variant="input"`.
          * @default 'vertical'
         */
        "orientation"?: MdTimePickerOrientation;
        /**
          * Localized accessible label for the AM/PM radio group container. Wraps the two radio children; individual radio names come from `am-label` / `pm-label`.
          * @default 'Period'
         */
        "periodLabel"?: string;
        /**
          * Layout for the AM/PM period selector. Has no effect when `format="24h"` (the toggle is not rendered).  - `vertical` — 52×72px stack next to the minute tile (default). - `horizontal` — 216×38px segmented button below HH:MM.
          * @default 'vertical'
         */
        "periodLayout"?: MdTimePickerPeriodLayout;
        /**
          * Localized PM label (12-hour mode).
          * @default 'PM'
         */
        "pmLabel"?: string;
        /**
          * Localized validation message for a reversed (overnight) `min`/`max` window when the time falls in the excluded middle. `{min}` and `{max}` are substituted.
          * @default 'Please select a time at or after {min} or at or before {max}.'
         */
        "rangeOutsideLabel"?: string;
        /**
          * Localized validation message when the committed time is after `max`. `{max}` is substituted.
          * @default 'Please select a time at or before {max}.'
         */
        "rangeOverflowLabel"?: string;
        /**
          * Localized validation message when the committed time is before `min`. `{min}` is substituted.
          * @default 'Please select a time at or after {min}.'
         */
        "rangeUnderflowLabel"?: string;
        /**
          * Requires a committed value for the host form to validate (`valueMissing` otherwise).
          * @default false
         */
        "required"?: boolean;
        /**
          * Adaptive layout policy. Per the MD3 spec the picker should swap orientation or variant based on the viewport so the dial never has to scroll. When `responsive` is enabled the dialog automatically:    • Falls back to `variant="input"` when the viewport height is     too short to fit the 256px dial (default threshold: 460px),     matching the spec line "Time pickers can fallback to the     input time picker when there isn't enough vertical real     estate to present the landscape orientation without     scrolling".   • Promotes the dial variant to `orientation="horizontal"`     when the viewport is wide enough for the 572/608px landscape     dialog (default threshold: width ≥ 720px AND width > height),     matching "the time picker can change to landscape orientation     on larger breakpoints or when viewport height is limited".  The `variant` / `orientation` props remain the *preference* the picker tries to honor — adaptive overrides only kick in when the viewport literally cannot fit them. Default `false` for backward compatibility; flip to `true` on new integrations to get the canonical MD3 adaptive layout.
          * @default false
         */
        "responsive"?: boolean;
        /**
          * Localized accessible label for the mode-toggle icon button when the picker is currently in the *input* variant (clock icon shown — click to switch to the dial). Matches the MD3 accessibility spec entry "Clock button → Toggle dial picker".
          * @default 'Toggle dial picker'
         */
        "toggleDialLabel"?: string;
        /**
          * Localized accessible label for the mode-toggle icon button when the picker is currently in the *dial* variant (keyboard icon shown — click to switch back to the input). Symmetric extension of the MD3 "Toggle dial picker" naming pattern, since the spec only documents the input-mode label explicitly.
          * @default 'Toggle keyboard input'
         */
        "toggleInputLabel"?: string;
        /**
          * Selected time as a 24-hour `HH:MM` string (e.g. `"14:30"`).
          * @default ''
         */
        "value"?: string;
        /**
          * Localized constraint-validation message when `required` and no time is committed.
          * @default 'Please select a time.'
         */
        "valueMissingLabel"?: string;
        /**
          * Picker mode shown when the dialog first opens. Defaults to `input` (keyboard-first); users can toggle to `dial` from the footer icon.
          * @default 'input'
         */
        "variant"?: MdTimePickerMode;
    }
    /**
     * The toolbar host has no built-in actions — all behavior comes from slotted controls.
     * On the web, the default is `role="toolbar"` implementing the WAI-ARIA toolbar pattern
     * with an ADAPTIVE roving tabindex:
     *  - **Activator buttons** (`md-icon-button`, `md-button`, native `button`/`a[href]`,
     *    `role=button`/`menuitem`/`link`) form a single roving group — one Tab stop, and
     *    ArrowLeft/Right (or Up/Down when floating+vertical), Home, End move focus between them.
     *  - **Everything else** is left as its OWN Tab stop with its native behavior — text-entry /
     *    value controls (input, textarea, contenteditable, md-text-field, md-select, `select`,
     *    range/spinbutton), ALL toggles (`md-switch`/`md-checkbox`/`md-chip`, native
     *    checkbox/radio, role=checkbox/radio), and self-managing composites (md-button-group,
     *    md-menu, role=group/radiogroup/listbox/menu). Arrows are never stolen from them, and
     *    they never become roving dead-ends.
     * A soft-disabled (`aria-disabled`) group member stays arrow-reachable (APG: disabled
     * items remain focusable for discoverability). The default Tab stop prefers an enabled
     * control, falling back to the first member only when every member is soft-disabled;
     * focusing any member makes it the stop.
     * This keeps every control keyboard-reachable regardless of the mix, per APG guidance for
     * toolbars that contain text fields. Use `generic` for a non-semantic container (no role,
     * no roving).
     */
    interface MdToolbar {
        /**
          * Alignment of items within the container
          * @default 'start'
         */
        "alignment"?: 'start' | 'center' | 'end' | 'space-between';
        /**
          * Accessible name for the toolbar (`containerSemantics="toolbar"`). Provide a meaningful label describing the toolbar's purpose (e.g. "Text formatting"); prefer `aria-labelledby` when a visible label exists. When neither is set the toolbar is left unnamed (valid ARIA) and a dev-mode warning is logged.
          * @default ''
         */
        "ariaLabelProp"?: string;
        /**
          * ID(s) of visible label element(s); takes precedence over `aria-label` for the accessible name
          * @default ''
         */
        "ariaLabelledby"?: string;
        /**
          * Toolbar color scheme (bar chrome). Also applies the MD3 expressive **toolbar-only** token map to slotted `md-icon-button` — does not change standalone icon buttons elsewhere.
          * @default 'standard'
         */
        "color"?: 'standard' | 'vibrant';
        /**
          * `toolbar` (default): `role="toolbar"`, adaptive roving tabindex, and toolbar ARIA on the host for web. `generic`: no toolbar role — plain container, no roving/arrow navigation. A generic container cannot be named (ARIA name-prohibited): an author-set `aria-label`/ `aria-labelledby` is left in place but ignored by assistive tech, and a dev-mode warning is logged.
          * @default 'toolbar'
         */
        "containerSemantics"?: 'toolbar' | 'generic';
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Layout direction for floating toolbar
          * @default 'horizontal'
         */
        "layout"?: 'horizontal' | 'vertical';
        /**
          * Toolbar type: docked (anchored to bottom) or floating (free-placed)
          * @default 'docked'
         */
        "variant"?: 'docked' | 'floating';
    }
    interface MdTooltip {
        /**
          * Automatically reposition if overflowing the viewport
          * @default true
         */
        "autoPosition"?: boolean;
        /**
          * Cross-axis offset in pixels
          * @default 0
         */
        "crossOffset"?: number;
        /**
          * Local density rung. Drives the same `--md-sys-density-scale` signal that a global `data-density` ancestor sets, so a local value simply overrides the inherited one. 0 = default, -4 = ultra-compact.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * When true, hover/focus handlers are ignored and any open tooltip is dismissed.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Delay before hiding (ms) — for rich variant hover-out grace period
          * @default 200
         */
        "hideDelay"?: number;
        /**
          * Distance from the trigger in pixels
          * @default 8
         */
        "offset"?: number;
        /**
          * Emits when the tooltip is dismissed (does not bubble or cross shadow boundaries).
         */
        "onMdClose"?: (event: MdTooltipCustomEvent<void>) => void;
        /**
          * Emits when the tooltip becomes visible. `bubbles:false` + `composed:false` keep it from crossing the shadow boundary and retargeting onto an embedding host (the repo's documented composed-event-leak class — see md-date-picker's swallow guards).
         */
        "onMdOpen"?: (event: MdTooltipCustomEvent<void>) => void;
        /**
          * Programmatic open state
          * @default false
         */
        "open"?: boolean;
        /**
          * Preferred placement relative to the trigger
          * @default 'top'
         */
        "position"?: Placement;
        /**
          * Delay before showing (ms)
          * @default 500
         */
        "showDelay"?: number;
        /**
          * Rich tooltip subhead (headline)
          * @default ''
         */
        "subhead"?: string;
        /**
          * Plain tooltip text, or rich tooltip supporting text
          * @default ''
         */
        "text"?: string;
        /**
          * Visual variant
          * @default 'plain'
         */
        "variant"?: 'plain' | 'rich';
    }
    /**
     * `md-transfer-list` — Material Design 3 dual-list selector.
     * Reference parity: https://mui.com/material-ui/react-transfer-list/.
     * Two columns ("source" and "target") with header chips that show the
     * "{n} / {total} selected" count, a search field per side, paired
     * `md-checkbox` rows, and four `md-icon-button` movers in the centre
     * column. Selection inside each side is independent of which side a
     * card lives in; the move buttons only act on items that are both
     * selected AND visible (i.e. matching the search filter).
     * MD3 Expressive: containers are `surface-container-low`, the headers
     * use the `secondary-container` tone for emphasis when the title slot
     * is populated, and movements run on the `motion-emphasized` curve.
     */
    interface MdTransferList {
        /**
          * Template for the header count pill (localisable). `{checked}` and `{total}` are replaced with the live numbers.
          * @default '{checked}/{total} selected'
         */
        "countTemplate"?: string;
        /**
          * Density scale: 0 (default), -1, -2, -3, -4. Passed to the search text field.
          * @default 0
         */
        "density"?: 0 | -1 | -2 | -3 | -4;
        /**
          * Disable the entire control.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Optional Material Symbols glyph shown above the empty-state text.
          * @default ''
         */
        "emptyIcon"?: string;
        /**
          * Empty-state text (localisable).
          * @default 'No items'
         */
        "emptyText"?: string;
        /**
          * The full item pool — both columns are derived from it.  Accepts the array (property) or a **JSON string** (attribute), so a plain-HTML page can declare its data inline instead of reaching for a script. Malformed JSON degrades to an empty list with a console warning.
          * @default []
         */
        "items"?: MdTransferListItem[] | string;
        /**
          * Material Symbols glyph for the "move all to source" (<<) button.
          * @default 'keyboard_double_arrow_left'
         */
        "moveAllLeftIcon"?: string;
        /**
          * Accessible label for the << button (localisable).
          * @default 'Move all to source'
         */
        "moveAllLeftLabel"?: string;
        /**
          * Material Symbols glyph for the "move all to target" (>>) button.
          * @default 'keyboard_double_arrow_right'
         */
        "moveAllRightIcon"?: string;
        /**
          * Accessible label for the >> button (localisable).
          * @default 'Move all to target'
         */
        "moveAllRightLabel"?: string;
        /**
          * Material Symbols glyph for the "move selected to source" (<) button.
          * @default 'chevron_left'
         */
        "moveLeftIcon"?: string;
        /**
          * Accessible label for the < button (localisable).
          * @default 'Move selected to source'
         */
        "moveLeftLabel"?: string;
        /**
          * Material Symbols glyph for the "move selected to target" (>) button.
          * @default 'chevron_right'
         */
        "moveRightIcon"?: string;
        /**
          * Accessible label for the > button (localisable). NB: deliberately NOT an `aria-*`-prefixed attribute — a custom attribute in the aria namespace is invalid ARIA (axe aria-valid-attr).
          * @default 'Move selected to target'
         */
        "moveRightLabel"?: string;
        /**
          * Fires on every change to the target value.
         */
        "onMdChange"?: (event: MdTransferListCustomEvent<string[]>) => void;
        /**
          * Fires after items are moved. detail describes the operation.
         */
        "onMdMove"?: (event: MdTransferListCustomEvent<{
    direction: 'left' | 'right';
    moved: string[];
    target: string[];
  }>) => void;
        /**
          * Material Symbols glyph for the search fields' leading icon ('' = none).
          * @default 'search'
         */
        "searchIcon"?: string;
        /**
          * Render search inputs above each list.
          * @default true
         */
        "searchable"?: boolean;
        /**
          * Show the "select all" select-all checkbox in each header.
          * @default true
         */
        "showSelectAll"?: boolean;
        /**
          * Show only the >/< buttons (drop >>/<< "move all").
          * @default false
         */
        "singleStepOnly"?: boolean;
        /**
          * Source-side search placeholder.
          * @default 'Search choices'
         */
        "sourceSearchPlaceholder"?: string;
        /**
          * Title for the source side.
          * @default 'Choices'
         */
        "sourceTitle"?: string;
        /**
          * Target-side search placeholder.
          * @default 'Search chosen'
         */
        "targetSearchPlaceholder"?: string;
        /**
          * Title for the target side.
          * @default 'Chosen'
         */
        "targetTitle"?: string;
        /**
          * Values currently in the *target* (right) column.
          * @default []
         */
        "value"?: string[];
    }

    interface MdAccordionAttributes {
        "variant": 'filled' | 'outlined';
        "density": 0 | -1 | -2 | -3 | -4;
        "elevation": 0 | 1 | 2 | 3 | 4 | 5;
        "reorderable": boolean;
        "floating": boolean;
        "initialX": number;
        "initialY": number;
        "bringToFront": boolean;
        "exclusive": boolean;
        "keepOneExpanded": boolean;
        "headingLevel": 1 | 2 | 3 | 4 | 5 | 6;
        "region": 'auto' | 'always' | 'never';
        "regionThreshold": number;
        "transition": 'expressive' | 'standard' | 'fade' | 'collapse' | 'none';
        "defaultExpanded": string;
    }
    interface MdAccordionItemAttributes {
        "headline": string;
        "supportingText": string;
        "icon": string;
        "expanded": boolean;
        "disabled": boolean;
        "headingLevel": 1 | 2 | 3 | 4 | 5 | 6;
        "collapsible": boolean;
        "regionRole": | 'region'
    | 'group'
    | 'none';
        "contentMaxHeight": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdAppBarAttributes {
        "variant": 'small' | 'medium' | 'large' | 'search';
        "titleAlignment": 'start' | 'center';
        "searchPlaceholder": string;
        "searchValue": string;
        "searchAriaLabel": string;
        "searchDisabled": boolean;
        "headline": string;
        "subtitle": string;
        "scrolled": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
        "leadingIcon": string;
        "leadingIconLabel": string;
    }
    interface MdAreaChartAttributes {
        "label": string;
        "subtitle": string | undefined;
        "titleAlign": MdChartTitleAlign;
        "curve": 'linear' | 'smooth' | 'monotone' | 'step' | 'step-before' | 'step-middle';
        "stack": MdChartStackMode;
        "connectNulls": boolean;
        "showMarks": boolean;
        "lineWidth": number;
        "markSize": number;
        "legend": MdChartLegendPosition | 'none';
        "tooltip": MdChartTooltipTrigger;
        "zoom": 'none' | 'inside' | 'slider' | 'both';
        "locale": string;
        "heightProp": string;
        "noAnimation": boolean;
        "animation": MdChartAnimation;
        "animationDuration": number;
        "fillOpacity": number;
        "showLine": boolean;
        "grid": 'none' | 'horizontal' | 'vertical' | 'both';
        "inverted": boolean;
        "seriesLabels": boolean;
        "summary": string;
        "labelEmpty": string;
        "loading": boolean;
        "loadingLabel": string;
        "labelZoomStart": string;
        "labelZoomEnd": string;
        "axisTicks": boolean;
        "showLabels": boolean;
        "labelPlot": string;
        "labelPoint": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdAutocompleteAttributes {
        "valueMissingLabel": string;
        "reserveSupportingSpace": boolean;
        "variant": 'filled' | 'outlined';
        "label": string;
        "placeholder": string;
        "supportingText": string;
        "error": boolean;
        "errorText": string;
        "disabled": boolean;
        "softDisabled": boolean;
        "required": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
        "options": MdAutocompleteOption[] | string;
        "multiple": boolean;
        "chipPosition": | 'below'
    | 'top'
    | 'left'
    | 'right'
    | 'inline';
        "value": string | string[];
        "inputValue": string;
        "freeSolo": boolean;
        "maxSelected": number;
        "loading": boolean;
        "noOptionsText": string;
        "noResultsText": string;
        "loadingText": string;
        "statusTemplate": string;
        "disableCloseOnSelect": boolean;
        "clearOnBlur": boolean;
        "clearable": boolean;
        "clearIcon": string;
        "dropdownIcon": string;
        "limitResults": number;
        "open": boolean;
        "virtualize": 'auto' | 'always' | 'never';
        "filterMode": FilterMode;
        "rowHeight": number;
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        "matchTriggerWidth": boolean;
        "maxHeight": number;
        "name": string;
    }
    interface MdAvatarAttributes {
        "src": string;
        "alt": string;
        "name": string;
        "initials": string;
        "size": MdAvatarSize;
        "shape": MdAvatarShape;
        "label": string;
        "colorFromName": boolean;
        "loading": 'eager' | 'lazy';
        "crossorigin": '' | 'anonymous' | 'use-credentials';
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdBadgeAttributes {
        "variant": 'small' | 'large';
        "value": string;
        "max": number;
        "icon": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdBarChartAttributes {
        "label": string;
        "subtitle": string | undefined;
        "titleAlign": MdChartTitleAlign;
        "layout": MdChartBarLayout;
        "stack": MdChartStackMode;
        "categoryGap": string;
        "barGap": string;
        "barWidth": number;
        "cornerRadius": number;
        "legend": MdChartLegendPosition | 'none';
        "tooltip": MdChartTooltipTrigger;
        "zoom": 'none' | 'inside' | 'slider' | 'both';
        "locale": string;
        "heightProp": string;
        "noAnimation": boolean;
        "animation": MdChartAnimation;
        "animationDuration": number;
        "showLabels": boolean;
        "showTotals": boolean;
        "chevron": boolean;
        "polar": boolean;
        "polarHole": number;
        "polarSweep": number;
        "axisTicks": boolean;
        "clickable": boolean;
        "labelZoomStart": string;
        "labelZoomEnd": string;
        "labelPlot": string;
        "labelPoint": string;
        "loading": boolean;
        "loadingLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdBottomSheetAttributes {
        "open": boolean;
        "variant": 'standard' | 'detached';
        "headline": string;
        "showDragHandle": boolean;
        "closeable": boolean;
        "scrimDismissible": boolean;
        "topDivider": boolean;
        "bottomDivider": boolean;
        "sheetAriaLabel": string;
        "scrollShadow": boolean;
        "headlineAlign": 'start' | 'center' | 'end';
        "contentAlign": 'start' | 'center' | 'end';
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdBreadcrumbItemAttributes {
        "href": string;
        "target": string;
        "rel": string;
        "current": boolean;
        "icon": string;
        "disabled": boolean;
        "collapsed": boolean;
        "itemIndex": number;
        "itemTotal": number;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdBreadcrumbsAttributes {
        "label": string;
        "expandLabel": string;
        "separator": string;
        "maxItems": number;
        "itemsBeforeCollapse": number;
        "itemsAfterCollapse": number;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdButtonAttributes {
        "type": 'button' | 'submit' | 'reset';
        "variant": 'filled' | 'outlined' | 'text' | 'elevated' | 'tonal';
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        "shape": 'round' | 'square';
        "disabled": boolean;
        "softDisabled": boolean;
        "ripple": boolean;
        "loading": boolean;
        "icon": string;
        "trailingIcon": string;
        "href": string;
        "target": string;
        "toggle": boolean;
        "selected": boolean;
        "shapeMorph": boolean;
        "mirrorIcon": boolean;
        "suppressExpandIconFlip": boolean;
        "fullWidth": boolean;
        "connectedLeft": boolean;
        "connectedRight": boolean;
        "value": string;
        "groupTabindex": number | null;
        "roleOverride": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdButtonGroupAttributes {
        "variant": 'standard' | 'connected';
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        "shape": 'round' | 'square';
        "selectionMode": 'single-select' | 'multi-select';
        "required": boolean;
        "fullWidth": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdCardAttributes {
        "variant": 'elevated' | 'filled' | 'outlined';
        "interactive": boolean;
        "dragEnabled": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "ripple": boolean;
        "fullWidth": boolean;
        "fullHeight": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdCheckboxAttributes {
        "valueMissingLabel": string;
        "error": boolean;
        "errorText": string;
        "supportingText": string;
        "checked": boolean;
        "indeterminate": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "required": boolean;
        "name": string;
        "value": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdChipAttributes {
        "variant": 'assist' | 'filter' | 'input' | 'suggestion';
        "icon": string;
        "trailingIcon": string;
        "selected": boolean;
        "selectable": boolean;
        "appearance": 'outlined' | 'filled' | 'elevated';
        "color": | 'primary'
    | 'secondary'
    | 'tertiary'
    | 'error'
    | 'success'
    | 'warning'
    | 'info'
    | (string & {});
        "elevated": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "removable": boolean;
        "label": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdColorPickerAttributes {
        "variant": ColorPickerVariant;
        "value": string;
        "format": ColorFormat;
        "alpha": boolean;
        "showHex": boolean;
        "showInputs": boolean;
        "disabled": boolean;
        "presets": string | string[];
        "ariaLabelProp": string;
        "open": boolean;
        "dismissOnOutsideClick": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdDatePickerAttributes {
        "valueMissingLabel": string;
        "reserveSupportingSpace": boolean;
        "variant": 'modal' | 'modal-input' | 'docked';
        "value": string;
        "label": string;
        "placeholder": string;
        "min": string;
        "max": string;
        "open": boolean;
        "disabled": boolean;
        "required": boolean;
        "clearable": boolean;
        "clearLabel": string;
        "error": boolean;
        "errorText": string;
        "supportingText": string;
        "locale": string;
        "dateSeparator": string;
        "fieldVariant": 'outlined' | 'filled';
        "firstDayOfWeek": number;
        "headline": string;
        "cancelLabel": string;
        "okLabel": string;
        "selectDateLabel": string;
        "enterDatesLabel": string;
        "invalidDateLabel": string;
        "previousMonthLabel": string;
        "nextMonthLabel": string;
        "previousYearLabel": string;
        "nextYearLabel": string;
        "chooseMonthLabel": string;
        "chooseYearLabel": string;
        "chooseMonthYearLabel": string;
        "chooseMonthAndYearLabel": string;
        "toggleCalendarLabel": string;
        "toggleTextLabel": string;
        "openCalendarLabel": string;
        "closeCalendarLabel": string;
        "calendarIcon": string;
        "yearGridLabel": string;
        "scrimDismissible": boolean;
        "outsideClickDismissible": boolean;
        "commitOnSelect": boolean;
        "name": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdDialogAttributes {
        "open": boolean;
        "headline": string;
        "icon": string;
        "fullscreen": boolean;
        "scrimDismissible": boolean;
        "locale": string;
        "closeLabel": string;
        "cancelLabel": string;
        "okLabel": string;
        "divider": boolean;
        "headerDivider": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdDividerAttributes {
        "inset": boolean;
        "insetStart": boolean;
        "insetEnd": boolean;
        "vertical": boolean;
    }
    interface MdFabAttributes {
        "variant": 'surface' | 'primary-container' | 'secondary-container' | 'tertiary-container' | 'primary' | 'secondary' | 'tertiary';
        "size": 'standard' | 'medium' | 'large';
        "disabled": boolean;
        "softDisabled": boolean;
        "lowered": boolean;
        "ripple": boolean;
        "icon": string;
        "label": string;
        "extended": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdFabMenuAttributes {
        "open": boolean;
        "anchor": string;
        "variant": 'primary' | 'secondary' | 'tertiary';
        "placement": 'up' | 'down' | 'auto';
        "quick": boolean;
        "menuLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdFabMenuItemAttributes {
        "icon": string;
        "label": string;
        "disabled": boolean;
        "softDisabled": boolean;
        "rovingFocusVisible": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdIconButtonAttributes {
        "variant": 'standard' | 'filled' | 'outlined' | 'tonal';
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        "shape": 'round' | 'square';
        "buttonWidth": 'narrow' | 'default' | 'wide';
        "disabled": boolean;
        "softDisabled": boolean;
        "toggle": boolean;
        "selected": boolean;
        "shapeMorph": boolean;
        "ripple": boolean;
        "icon": string;
        "selectedIcon": string;
        "href": string;
        "target": string;
        "connectedLeft": boolean;
        "connectedRight": boolean;
        "value": string;
        "groupTabindex": number | null;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdLineChartAttributes {
        "label": string;
        "subtitle": string | undefined;
        "titleAlign": MdChartTitleAlign;
        "curve": 'linear' | 'smooth' | 'monotone' | 'step' | 'step-before' | 'step-middle';
        "stack": MdChartStackMode;
        "connectNulls": boolean;
        "showMarks": boolean;
        "lineWidth": number;
        "markSize": number;
        "area": boolean;
        "showLine": boolean;
        "legend": MdChartLegendPosition | 'none';
        "tooltip": MdChartTooltipTrigger;
        "grid": 'none' | 'horizontal' | 'vertical' | 'both';
        "axisTicks": boolean;
        "inverted": boolean;
        "showLabels": boolean;
        "seriesLabels": boolean;
        "zoom": 'none' | 'inside' | 'slider' | 'both';
        "locale": string;
        "labelEmpty": string;
        "labelPlot": string;
        "labelPoint": string;
        "labelZoomStart": string;
        "labelZoomEnd": string;
        "summary": string;
        "heightProp": string;
        "noAnimation": boolean;
        "animation": MdChartAnimation;
        "animationDuration": number;
        "loading": boolean;
        "loadingLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdListAttributes {
        "listStyle": MdListStyle;
        "selectionMode": MdListSelectionMode;
        "interactionMode": MdListInteractionMode;
        "reorderable": boolean;
        "label": string;
        "labelledby": string;
        "roleOverride": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdListItemAttributes {
        "type": MdListItemType;
        "headline": string;
        "overline": string;
        "supportingText": string;
        "trailingSupportingText": string;
        "leadingIcon": string;
        "trailingIcon": string;
        "leadingAvatar": string;
        "leadingAvatarAlt": string;
        "leadingAvatarName": string;
        "leadingAvatarLabel": string;
        "leadingImage": string;
        "leadingImageAlt": string;
        "lines": 1 | 2 | 3;
        "href": string;
        "target": string;
        "disabled": boolean;
        "softDisabled": boolean;
        "selected": boolean;
        "expandable": boolean;
        "expanded": boolean;
        "tabbable": boolean;
        "rovingFocusVisible": boolean;
        "selectionMode": 'none' | 'single-select' | 'multi-select';
        "interactionMode": 'single-action' | 'multi-action';
        "containerRole": string;
        "reorderable": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdLoadingIndicatorAttributes {
        "variant": 'uncontained' | 'contained';
        "label": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdMenuAttributes {
        "open": boolean;
        "anchor": string;
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        "quick": boolean;
        "variant": 'baseline' | 'standard' | 'vibrant';
        "layout": 'standard' | 'grouped';
        "useGap": boolean;
        "emptyText": string;
        "matchAnchorWidth": boolean;
        "responsive": boolean;
        "persistent": boolean;
        "autoFocus": boolean;
        "maxHeight": string;
        "listLabel": string;
        "listbox": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdMenuItemAttributes {
        "headline": string;
        "supportingText": string;
        "trailingText": string;
        "disabled": boolean;
        "selected": boolean;
        "indeterminate": boolean;
        "keepOpen": boolean;
        "divider": boolean;
        "gap": boolean;
        "badge": string;
        "type": 'button' | 'checkbox' | 'radio';
        "checkPosition": 'start' | 'end';
        "presentation": 'menuitem' | 'option';
        "roleOverride": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdMenuItemGroupAttributes {
        "label": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdMeterAttributes {
        "value": number;
        "min": number;
        "max": number;
        "label": string;
        "showLabel": boolean;
        "showValue": boolean;
        "valueText": string;
        "locale": string;
        "color": | 'primary'
    | 'secondary'
    | 'tertiary'
    | 'error'
    | 'success'
    | 'warning'
    | 'info'
    | (string & {});
        "variant": 'linear' | 'circular';
        "size": number;
        "thickness": number;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdMultiSelectAttributes {
        "valueMissingLabel": string;
        "reserveSupportingSpace": boolean;
        "variant": 'filled' | 'outlined';
        "label": string;
        "placeholder": string;
        "supportingText": string;
        "error": boolean;
        "errorText": string;
        "disabled": boolean;
        "softDisabled": boolean;
        "required": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
        "options": SelectOptionInit[] | string;
        "virtualize": 'auto' | 'always' | 'never';
        "filterable": boolean;
        "filterMode": FilterMode;
        "rowHeight": number;
        "maxSelected": number;
        "showSelectAll": boolean;
        "selectAllLabel": string;
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        "maxHeight": number;
        "matchTriggerWidth": boolean;
        "displayMode": 'chips' | 'chips-inline' | 'count' | 'text';
        "chipOverflow": 'count' | 'wrap';
        "chipPosition": 'bottom' | 'top' | 'left' | 'right';
        "trigger": 'field' | 'button';
        "triggerIcon": string;
        "triggerLabel": string;
        "searchPlaceholder": string;
        "filterLabel": string;
        "noResultsText": string;
        "noOptionsText": string;
        "searchingLabel": string;
        "loadingText": string;
        "loading": boolean;
        "clearable": boolean;
        "clearIcon": string;
        "clearLabel": string;
        "dropdownIcon": string;
        "name": string;
        "open": boolean;
    }
    interface MdNavigationBarAttributes {
        "activeIndex": number;
        "labelBehavior": MdNavigationBarLabelBehavior;
        "manualActivation": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdNavigationRailAttributes {
        "variant": 'standard' | 'expanded';
        "modal": boolean;
        "fullHeight": boolean;
        "alignment": 'top' | 'middle' | 'bottom';
        "expandable": boolean;
        "toggleLabel": string;
        "labelVisibility": 'all' | 'selected' | 'none';
        "activeIndex": number;
        "label": string;
        "disableFocusManagement": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
        "orientation": 'vertical' | 'horizontal';
        "maxVisible": number;
        "overflowLabel": string;
        "overflowIcon": string;
    }
    interface MdNavigationRailTabAttributes {
        "icon": string;
        "label": string;
        "active": boolean;
        "disabled": boolean;
        "badge": boolean;
        "badgeValue": string;
        "href": string;
        "target": string;
        "value": string;
        "labelVisibility": 'all' | 'selected' | 'none';
        "expanded": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdNavigationTabAttributes {
        "active": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "label": string;
        "icon": string;
        "activeIcon": string;
        "badge": boolean;
        "badgeValue": string;
        "badgeMax": number;
        "href": string;
        "target": string;
        "labelBehavior": MdNavigationTabLabelBehavior;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdNumberFieldAttributes {
        "variant": 'filled' | 'outlined';
        "label": string;
        "placeholder": string;
        "supportingText": string;
        "error": boolean;
        "errorText": string;
        "disabled": boolean;
        "readOnly": boolean;
        "required": boolean;
        "name": string;
        "density": 0 | -1 | -2 | -3 | -4;
        "value": number | null;
        "locale": string;
        "formatOptions": Intl.NumberFormatOptions | string;
        "min": number;
        "max": number;
        "step": number;
        "smallStep": number;
        "largeStep": number;
        "snapOnStep": boolean;
        "allowOutOfRange": boolean;
        "allowWheelScrub": boolean;
        "steppers": 'inline' | 'split' | 'none';
        "incrementLabel": string;
        "decrementLabel": string;
        "valueMissingLabel": string;
        "reserveSupportingSpace": boolean;
    }
    interface MdOrganizationChartAttributes {
        "nodes": NodeInput;
        "selectionMode": 'none' | 'single' | 'multiple';
        "collapsible": boolean;
        "orientation": 'vertical' | 'horizontal';
        "label": string;
        "expandLabel": string;
        "collapseLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdOtpFieldAttributes {
        "length": number;
        "value": string;
        "validationType": 'numeric' | 'alpha' | 'alphanumeric' | 'none';
        "transform": 'none' | 'uppercase';
        "mask": boolean;
        "autoSubmit": boolean;
        "groupSize": number;
        "inputMode": string;
        "name": string;
        "disabled": boolean;
        "readOnly": boolean;
        "required": boolean;
        "error": boolean;
        "errorText": string;
        "supportingText": string;
        "label": string;
        "cellLabelTemplate": string;
        "valueMissingLabel": string;
        "incompleteLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
        "reserveSupportingSpace": boolean;
    }
    interface MdPieChartAttributes {
        "label": string;
        "titleAlign": MdChartTitleAlign;
        "innerRadius": string;
        "outerRadius": string;
        "startAngle": number;
        "endAngle": number;
        "paddingAngle": number;
        "cornerRadius": number;
        "showLabels": boolean;
        "labelMode": 'value' | 'name' | 'both';
        "monochrome": string;
        "gradient": boolean;
        "highlight": 'slice' | 'series' | 'none';
        "legend": MdChartLegendPosition | 'none';
        "tooltip": MdChartTooltipTrigger;
        "locale": string;
        "subtitle": string | undefined;
        "summary": string;
        "labelEmpty": string;
        "loading": boolean;
        "loadingLabel": string;
        "labelPlot": string;
        "labelPoint": string;
        "heightProp": string;
        "noAnimation": boolean;
        "animation": MdChartAnimation;
        "animationDuration": number;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdProgressIndicatorAttributes {
        "variant": 'circular' | 'linear';
        "value": number;
        "max": number;
        "indeterminate": boolean;
        "thickness": number;
        "wave": boolean;
        "waveAmplitude": number;
        "waveLength": number;
        "waveSpeed": number;
        "size": number;
        "fourColor": boolean;
        "label": string;
        "complete": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdRadioAttributes {
        "valueMissingLabel": string;
        "checked": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "required": boolean;
        "name": string;
        "value": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdRatingAttributes {
        "value": number;
        "defaultValue": number;
        "max": number;
        "precision": 1 | 0.5;
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        "readonly": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "icon": string;
        "emptyIcon": string;
        "outlineEmpty": boolean;
        "showValueLabel": boolean;
        "ratingLabel": string;
        "name": string;
        "hover": 'preview' | 'off';
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdRippleAttributes {
        "disabled": boolean;
    }
    interface MdSearchAttributes {
        "variant": 'contained' | 'divided';
        "layout": 'full-screen' | 'docked';
        "trigger": 'bar' | 'icon';
        "triggerIcon": string;
        "triggerFor": string;
        "fullWidth": boolean;
        "value": string;
        "placeholder": string;
        "open": boolean;
        "disabled": boolean;
        "elevation": 0 | 1 | 2 | 3 | 4 | 5;
        "leadingIcon": string;
        "openLeadingIcon": string;
        "showClearButton": boolean;
        "voiceSearch": boolean;
        "escapeCloses": boolean;
        "dismissOnOutsideClick": boolean;
        "scrollShadow": boolean;
        "maxBlockSize": string;
        "inputAriaLabel": string;
        "initialFocus": 'auto' | 'input' | 'leading';
        "announceResults": boolean;
        "resultsLabel": string;
        "noResultsLabel": string;
        "loading": boolean;
        "debounce": number;
        "throttle": number;
        "loadingLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSegmentedButtonAttributes {
        "value": string;
        "selected": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "icon": string;
        "label": string;
        "noCheckmark": boolean;
        "ripple": boolean;
        "segmentIndex": number;
        "segmentTotal": number;
        "segmentDensity": number;
        "segmentMultiselect": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSegmentedButtonSetAttributes {
        "multiselect": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSelectAttributes {
        "valueMissingLabel": string;
        "reserveSupportingSpace": boolean;
        "variant": 'filled' | 'outlined';
        "label": string;
        "placeholder": string;
        "value": string;
        "disabled": boolean;
        "required": boolean;
        "error": boolean;
        "errorText": string;
        "supportingText": string;
        "density": 0 | -1 | -2 | -3 | -4;
        "clearable": boolean;
        "matchTriggerWidth": boolean;
        "placement": 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
        "maxHeight": string;
        "options": SelectOptionInit[] | string;
        "virtualize": 'auto' | 'always' | 'never';
        "filterable": boolean;
        "filterMode": FilterMode;
        "rowHeight": number;
        "name": string;
        "searchPlaceholder": string;
        "filterLabel": string;
        "noResultsText": string;
        "noOptionsText": string;
        "clearLabel": string;
        "searchingLabel": string;
        "loadingText": string;
        "clearIcon": string;
        "dropdownIcon": string;
        "loading": boolean;
        "fullWidth": boolean;
        "open": boolean;
    }
    interface MdSelectOptionAttributes {
        "value": string;
        "label": string;
        "disabled": boolean;
        "selected": boolean;
        "icon": string;
        "iconColor": string;
        "supportingText": string;
    }
    interface MdSideSheetAttributes {
        "open": boolean;
        "variant": 'standard' | 'modal';
        "side": 'start' | 'end';
        "headline": string;
        "closeable": boolean;
        "showBack": boolean;
        "scrimDismissible": boolean;
        "topDivider": boolean;
        "bottomDivider": boolean;
        "detached": boolean;
        "sheetAriaLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSkeletonAttributes {
        "variant": 'text' | 'rectangular' | 'rounded' | 'circular';
        "animation": 'pulse' | 'wave' | 'none';
        "lines": number;
        "width": string;
        "height": string;
        "fullWidth": boolean;
        "fullHeight": boolean;
        "ariaLabelProp": string;
        "announce": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSliderAttributes {
        "min": number;
        "max": number;
        "value": number;
        "step": number;
        "disabled": boolean;
        "name": string;
        "nameStart": string;
        "nameEnd": string;
        "variant": 'standard' | 'centered';
        "range": boolean;
        "stops": boolean;
        "valueStart": number;
        "valueEnd": number;
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        "orientation": 'horizontal' | 'vertical';
        "fullHeight": boolean;
        "valueIndicator": boolean;
        "labeled": boolean;
        "insetIcon": boolean;
        "icon": string;
        "sliderAriaLabel": string;
        "ariaLabelStart": string;
        "ariaLabelEnd": string;
        "valueText": string;
        "valueStartText": string;
        "valueEndText": string;
        "controlled": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSnackbarAttributes {
        "open": boolean;
        "message": string;
        "action": string;
        "closeable": boolean;
        "autoHide": boolean;
        "autoHideDuration": number;
        "position": | 'bottom' | 'bottom-start' | 'bottom-end'
    | 'top' | 'top-start' | 'top-end'
    | 'start' | 'end' | 'center';
        "stacked": boolean;
        "politeness": 'polite' | 'assertive';
        "dismissLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSparklineAttributes {
        "variant": 'line' | 'bar' | 'area';
        "color": MdChartColorRole | string;
        "curve": MdChartCurve;
        "showTooltip": boolean;
        "showMarks": 'none' | 'extremes' | 'edges' | 'all';
        "heightProp": string;
        "noAnimation": boolean;
        "cornerRadius": number;
        "min": number;
        "max": number;
        "lineWidth": number;
        "markSize": number;
        "barWidth": number;
    }
    interface MdSplitButtonAttributes {
        "variant": 'elevated' | 'filled' | 'tonal' | 'outlined';
        "size": 'xs' | 'sm' | 'md' | 'lg' | 'xl';
        "disabled": boolean;
        "softDisabled": boolean;
        "icon": string;
        "label": string;
        "trailingIcon": string;
        "trailingChecked": boolean;
        "menuLabel": string;
        "haspopup": | 'menu' | 'listbox' | 'dialog' | 'grid' | 'tree' | 'true' | 'false';
        "controls": string;
        "fullWidth": boolean;
        "ripple": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdStatusDotAttributes {
        "state": MdStatusDotState;
        "size": MdStatusDotSize;
        "inline": boolean;
        "live": boolean;
        "label": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdStepAttributes {
        "label": string;
        "description": string;
        "completed": boolean;
        "active": boolean;
        "error": boolean;
        "errorText": string;
        "disabled": boolean;
        "optional": boolean;
        "editable": boolean;
        "icon": string;
        "completedIcon": string;
        "errorIcon": string;
        "accessibleName": string;
        "hideActions": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdStepperAttributes {
        "orientation": 'horizontal' | 'vertical';
        "indicator": 'numbered' | 'dot';
        "variant": 'default' | 'mobile';
        "mode": 'linear' | 'non-linear';
        "active": number;
        "autoComplete": boolean;
        "nav": boolean;
        "nextDisabled": boolean;
        "loading": boolean;
        "lazy": boolean;
        "label": string;
        "stepWord": string;
        "ofWord": string;
        "completedWord": string;
        "currentWord": string;
        "errorWord": string;
        "optionalWord": string;
        "nextLabel": string;
        "backLabel": string;
        "finishLabel": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSubMenuItemAttributes {
        "headline": string;
        "supportingText": string;
        "disabled": boolean;
        "divider": boolean;
        "gap": boolean;
        "badge": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdSwitchAttributes {
        "valueMissingLabel": string;
        "selected": boolean;
        "disabled": boolean;
        "softDisabled": boolean;
        "required": boolean;
        "name": string;
        "value": string;
        "icons": boolean;
        "showOnlySelectedIcon": boolean;
        "selectedIcon": string;
        "unselectedIcon": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTabAttributes {
        "label": string;
        "icon": string;
        "active": boolean;
        "disabled": boolean;
        "inlineIcon": boolean;
        "variant": 'primary' | 'secondary';
        "badge": string | undefined;
        "controls": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTabPanelAttributes {
        "active": boolean;
    }
    interface MdTabPanelsAttributes {
        "for": string;
        "sizing": 'stable' | 'active';
    }
    interface MdTableAttributes {
        "density": string;
        "columnTemplate": string;
        "columns": number;
        "minWidth": string;
        "stickyHeader": boolean;
        "frozenHeader": boolean;
        "keepHeight": boolean;
        "scrollbar": 'overlay' | 'gutter';
        "pinMode": 'stack' | 'static';
        "pinIcon": string;
        "stickyFooter": boolean;
        "striped": boolean;
        "noDividers": boolean;
        "hoverable": boolean;
        "motion": 'expressive' | 'none';
        "selection": 'none' | 'single' | 'multiple';
        "sortBy": string;
        "sortOrder": MdTableSortOrder;
        "loading": boolean;
        "loadingMode": 'overlay' | 'skeleton';
        "loadingRows": number;
        "empty": boolean;
        "label": string;
        "caption": string;
        "summary": string;
        "rowOffset": number;
        "rowCount": number;
    }
    interface MdTableBodyAttributes {
        "presentational": boolean;
    }
    interface MdTableCellAttributes {
        "align": 'start' | 'center' | 'end' | 'justify';
        "numeric": boolean;
        "padding": 'default' | 'none' | 'checkbox';
        "head": boolean;
        "scope": 'col' | 'row' | 'colgroup' | 'rowgroup' | '';
        "sticky": 'start' | 'end' | '';
        "colSpan": number;
        "rowSpan": number;
        "ellipsis": boolean;
        "variant": 'body' | 'meta';
        "noPinIndicator": boolean;
        "pinIcon": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTableContainerAttributes {
        "variant": 'elevated' | 'outlined' | 'filled' | 'flat';
        "shape": 'extra-large' | 'large' | 'medium' | 'small' | 'none';
        "maxHeight": string;
        "minHeight": string;
        "vibrant": boolean;
        "elevation": 0 | 1 | 2 | 3 | 4 | 5;
    }
    interface MdTableExpandToggleAttributes {
        "buttonLabel": string;
        "icon": string;
    }
    interface MdTableFootAttributes {
        "presentational": boolean;
    }
    interface MdTablePaginationAttributes {
        "count": number;
        "page": number;
        "rowsPerPage": number;
        "rowsPerPageOptions": string;
        "showFirstLast": boolean;
        "labelDisplayedRows": string;
        "labelRowsPerPage": string;
        "labelFirstPage": string;
        "labelPreviousPage": string;
        "labelNextPage": string;
        "labelLastPage": string;
        "labelAll": string;
        "compact": boolean;
        "disabled": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTableRowAttributes {
        "rowgroup": 'head' | 'body' | 'foot';
        "selected": boolean;
        "disabled": boolean;
        "selectable": boolean;
        "clickable": boolean;
        "value": string;
        "expandable": boolean;
        "expanded": boolean;
        "highlight": boolean;
    }
    interface MdTableSortLabelAttributes {
        "column": string;
        "disabled": boolean;
        "defaultOrder": 'asc' | 'desc';
        "iconPosition": 'start' | 'end';
        "icon": string;
        "inactiveIcon": 'always' | 'hover';
        "active": boolean;
        "order": 'asc' | 'desc' | 'none';
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTableToolbarAttributes {
        "headline": string;
        "supportingText": string;
        "numSelected": number;
        "autoBind": boolean;
        "labelSelected": string;
        "compact": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTabsAttributes {
        "activeTabIndex": number;
        "variant": 'primary' | 'secondary';
        "divider": 'auto' | 'full';
        "ariaLabelProp": string;
        "tabWidth": string;
        "width": string;
    }
    interface MdTextFieldAttributes {
        "reserveSupportingSpace": boolean;
        "variant": 'filled' | 'outlined';
        "label": string;
        "value": string;
        "placeholder": string;
        "type": string;
        "name": string;
        "pattern": string;
        "minLength": number | undefined;
        "min": string;
        "max": string;
        "step": string;
        "autocomplete": string;
        "inputMode": string;
        "enterKeyHint": string;
        "autoCapitalize": string;
        "spellcheck": boolean | undefined;
        "disabled": boolean;
        "readOnly": boolean;
        "required": boolean;
        "error": boolean;
        "errorText": string;
        "supportingText": string;
        "prefixText": string;
        "suffixText": string;
        "maxLength": number | undefined;
        "clearable": string;
        "passwordToggle": string;
        "density": 0 | -1 | -2 | -3 | -4;
        "multiline": string;
        "rows": number | undefined;
        "speechToText": string;
        "speechLang": string;
        "restrict": string;
        "formatOn": 'blur' | 'input';
        "focusBorderWidth": number;
        "appearFocused": boolean;
        "chipsWrap": boolean;
        "debounce": number;
        "throttle": number;
        "inputRole": '' | 'combobox';
        "inputExpanded": boolean;
        "inputAriaAutocomplete": '' | 'none' | 'inline' | 'list' | 'both';
    }
    interface MdTimePickerAttributes {
        "variant": MdTimePickerMode;
        "format": MdTimePickerFormat;
        "value": string;
        "name": string;
        "min": string;
        "max": string;
        "required": boolean;
        "label": string;
        "headline": string;
        "headlineInputLabel": string;
        "headlineDialLabel": string;
        "valueMissingLabel": string;
        "rangeUnderflowLabel": string;
        "rangeOverflowLabel": string;
        "rangeOutsideLabel": string;
        "disabled": boolean;
        "open": boolean;
        "minuteStep": number;
        "cancelLabel": string;
        "okLabel": string;
        "hideTrigger": boolean;
        "amLabel": string;
        "pmLabel": string;
        "hourLabel": string;
        "minuteLabel": string;
        "toggleDialLabel": string;
        "toggleInputLabel": string;
        "periodLabel": string;
        "periodLayout": MdTimePickerPeriodLayout;
        "orientation": MdTimePickerOrientation;
        "responsive": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdToolbarAttributes {
        "variant": 'docked' | 'floating';
        "color": 'standard' | 'vibrant';
        "layout": 'horizontal' | 'vertical';
        "alignment": 'start' | 'center' | 'end' | 'space-between';
        "containerSemantics": 'toolbar' | 'generic';
        "ariaLabelProp": string;
        "ariaLabelledby": string;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTooltipAttributes {
        "text": string;
        "subhead": string;
        "variant": 'plain' | 'rich';
        "position": Placement;
        "autoPosition": boolean;
        "offset": number;
        "crossOffset": number;
        "open": boolean;
        "showDelay": number;
        "hideDelay": number;
        "disabled": boolean;
        "density": 0 | -1 | -2 | -3 | -4;
    }
    interface MdTransferListAttributes {
        "items": MdTransferListItem[] | string;
        "sourceTitle": string;
        "targetTitle": string;
        "searchable": boolean;
        "sourceSearchPlaceholder": string;
        "targetSearchPlaceholder": string;
        "showSelectAll": boolean;
        "searchIcon": string;
        "moveRightIcon": string;
        "moveLeftIcon": string;
        "moveAllRightIcon": string;
        "moveAllLeftIcon": string;
        "emptyIcon": string;
        "emptyText": string;
        "countTemplate": string;
        "density": 0 | -1 | -2 | -3 | -4;
        "disabled": boolean;
        "singleStepOnly": boolean;
        "moveRightLabel": string;
        "moveLeftLabel": string;
        "moveAllRightLabel": string;
        "moveAllLeftLabel": string;
    }

    interface IntrinsicElements {
        "md-accordion": Omit<MdAccordion, keyof MdAccordionAttributes> & { [K in keyof MdAccordion & keyof MdAccordionAttributes]?: MdAccordion[K] } & { [K in keyof MdAccordion & keyof MdAccordionAttributes as `attr:${K}`]?: MdAccordionAttributes[K] } & { [K in keyof MdAccordion & keyof MdAccordionAttributes as `prop:${K}`]?: MdAccordion[K] };
        "md-accordion-item": Omit<MdAccordionItem, keyof MdAccordionItemAttributes> & { [K in keyof MdAccordionItem & keyof MdAccordionItemAttributes]?: MdAccordionItem[K] } & { [K in keyof MdAccordionItem & keyof MdAccordionItemAttributes as `attr:${K}`]?: MdAccordionItemAttributes[K] } & { [K in keyof MdAccordionItem & keyof MdAccordionItemAttributes as `prop:${K}`]?: MdAccordionItem[K] };
        "md-app-bar": Omit<MdAppBar, keyof MdAppBarAttributes> & { [K in keyof MdAppBar & keyof MdAppBarAttributes]?: MdAppBar[K] } & { [K in keyof MdAppBar & keyof MdAppBarAttributes as `attr:${K}`]?: MdAppBarAttributes[K] } & { [K in keyof MdAppBar & keyof MdAppBarAttributes as `prop:${K}`]?: MdAppBar[K] };
        "md-area-chart": Omit<MdAreaChart, keyof MdAreaChartAttributes> & { [K in keyof MdAreaChart & keyof MdAreaChartAttributes]?: MdAreaChart[K] } & { [K in keyof MdAreaChart & keyof MdAreaChartAttributes as `attr:${K}`]?: MdAreaChartAttributes[K] } & { [K in keyof MdAreaChart & keyof MdAreaChartAttributes as `prop:${K}`]?: MdAreaChart[K] };
        "md-autocomplete": Omit<MdAutocomplete, keyof MdAutocompleteAttributes> & { [K in keyof MdAutocomplete & keyof MdAutocompleteAttributes]?: MdAutocomplete[K] } & { [K in keyof MdAutocomplete & keyof MdAutocompleteAttributes as `attr:${K}`]?: MdAutocompleteAttributes[K] } & { [K in keyof MdAutocomplete & keyof MdAutocompleteAttributes as `prop:${K}`]?: MdAutocomplete[K] };
        "md-avatar": Omit<MdAvatar, keyof MdAvatarAttributes> & { [K in keyof MdAvatar & keyof MdAvatarAttributes]?: MdAvatar[K] } & { [K in keyof MdAvatar & keyof MdAvatarAttributes as `attr:${K}`]?: MdAvatarAttributes[K] } & { [K in keyof MdAvatar & keyof MdAvatarAttributes as `prop:${K}`]?: MdAvatar[K] };
        "md-badge": Omit<MdBadge, keyof MdBadgeAttributes> & { [K in keyof MdBadge & keyof MdBadgeAttributes]?: MdBadge[K] } & { [K in keyof MdBadge & keyof MdBadgeAttributes as `attr:${K}`]?: MdBadgeAttributes[K] } & { [K in keyof MdBadge & keyof MdBadgeAttributes as `prop:${K}`]?: MdBadge[K] };
        "md-bar-chart": Omit<MdBarChart, keyof MdBarChartAttributes> & { [K in keyof MdBarChart & keyof MdBarChartAttributes]?: MdBarChart[K] } & { [K in keyof MdBarChart & keyof MdBarChartAttributes as `attr:${K}`]?: MdBarChartAttributes[K] } & { [K in keyof MdBarChart & keyof MdBarChartAttributes as `prop:${K}`]?: MdBarChart[K] };
        "md-bottom-sheet": Omit<MdBottomSheet, keyof MdBottomSheetAttributes> & { [K in keyof MdBottomSheet & keyof MdBottomSheetAttributes]?: MdBottomSheet[K] } & { [K in keyof MdBottomSheet & keyof MdBottomSheetAttributes as `attr:${K}`]?: MdBottomSheetAttributes[K] } & { [K in keyof MdBottomSheet & keyof MdBottomSheetAttributes as `prop:${K}`]?: MdBottomSheet[K] };
        "md-breadcrumb-item": Omit<MdBreadcrumbItem, keyof MdBreadcrumbItemAttributes> & { [K in keyof MdBreadcrumbItem & keyof MdBreadcrumbItemAttributes]?: MdBreadcrumbItem[K] } & { [K in keyof MdBreadcrumbItem & keyof MdBreadcrumbItemAttributes as `attr:${K}`]?: MdBreadcrumbItemAttributes[K] } & { [K in keyof MdBreadcrumbItem & keyof MdBreadcrumbItemAttributes as `prop:${K}`]?: MdBreadcrumbItem[K] };
        "md-breadcrumbs": Omit<MdBreadcrumbs, keyof MdBreadcrumbsAttributes> & { [K in keyof MdBreadcrumbs & keyof MdBreadcrumbsAttributes]?: MdBreadcrumbs[K] } & { [K in keyof MdBreadcrumbs & keyof MdBreadcrumbsAttributes as `attr:${K}`]?: MdBreadcrumbsAttributes[K] } & { [K in keyof MdBreadcrumbs & keyof MdBreadcrumbsAttributes as `prop:${K}`]?: MdBreadcrumbs[K] };
        "md-button": Omit<MdButton, keyof MdButtonAttributes> & { [K in keyof MdButton & keyof MdButtonAttributes]?: MdButton[K] } & { [K in keyof MdButton & keyof MdButtonAttributes as `attr:${K}`]?: MdButtonAttributes[K] } & { [K in keyof MdButton & keyof MdButtonAttributes as `prop:${K}`]?: MdButton[K] };
        "md-button-group": Omit<MdButtonGroup, keyof MdButtonGroupAttributes> & { [K in keyof MdButtonGroup & keyof MdButtonGroupAttributes]?: MdButtonGroup[K] } & { [K in keyof MdButtonGroup & keyof MdButtonGroupAttributes as `attr:${K}`]?: MdButtonGroupAttributes[K] } & { [K in keyof MdButtonGroup & keyof MdButtonGroupAttributes as `prop:${K}`]?: MdButtonGroup[K] };
        "md-card": Omit<MdCard, keyof MdCardAttributes> & { [K in keyof MdCard & keyof MdCardAttributes]?: MdCard[K] } & { [K in keyof MdCard & keyof MdCardAttributes as `attr:${K}`]?: MdCardAttributes[K] } & { [K in keyof MdCard & keyof MdCardAttributes as `prop:${K}`]?: MdCard[K] };
        "md-checkbox": Omit<MdCheckbox, keyof MdCheckboxAttributes> & { [K in keyof MdCheckbox & keyof MdCheckboxAttributes]?: MdCheckbox[K] } & { [K in keyof MdCheckbox & keyof MdCheckboxAttributes as `attr:${K}`]?: MdCheckboxAttributes[K] } & { [K in keyof MdCheckbox & keyof MdCheckboxAttributes as `prop:${K}`]?: MdCheckbox[K] };
        "md-chip": Omit<MdChip, keyof MdChipAttributes> & { [K in keyof MdChip & keyof MdChipAttributes]?: MdChip[K] } & { [K in keyof MdChip & keyof MdChipAttributes as `attr:${K}`]?: MdChipAttributes[K] } & { [K in keyof MdChip & keyof MdChipAttributes as `prop:${K}`]?: MdChip[K] };
        "md-color-picker": Omit<MdColorPicker, keyof MdColorPickerAttributes> & { [K in keyof MdColorPicker & keyof MdColorPickerAttributes]?: MdColorPicker[K] } & { [K in keyof MdColorPicker & keyof MdColorPickerAttributes as `attr:${K}`]?: MdColorPickerAttributes[K] } & { [K in keyof MdColorPicker & keyof MdColorPickerAttributes as `prop:${K}`]?: MdColorPicker[K] };
        "md-date-picker": Omit<MdDatePicker, keyof MdDatePickerAttributes> & { [K in keyof MdDatePicker & keyof MdDatePickerAttributes]?: MdDatePicker[K] } & { [K in keyof MdDatePicker & keyof MdDatePickerAttributes as `attr:${K}`]?: MdDatePickerAttributes[K] } & { [K in keyof MdDatePicker & keyof MdDatePickerAttributes as `prop:${K}`]?: MdDatePicker[K] };
        "md-dialog": Omit<MdDialog, keyof MdDialogAttributes> & { [K in keyof MdDialog & keyof MdDialogAttributes]?: MdDialog[K] } & { [K in keyof MdDialog & keyof MdDialogAttributes as `attr:${K}`]?: MdDialogAttributes[K] } & { [K in keyof MdDialog & keyof MdDialogAttributes as `prop:${K}`]?: MdDialog[K] };
        "md-divider": Omit<MdDivider, keyof MdDividerAttributes> & { [K in keyof MdDivider & keyof MdDividerAttributes]?: MdDivider[K] } & { [K in keyof MdDivider & keyof MdDividerAttributes as `attr:${K}`]?: MdDividerAttributes[K] } & { [K in keyof MdDivider & keyof MdDividerAttributes as `prop:${K}`]?: MdDivider[K] };
        "md-fab": Omit<MdFab, keyof MdFabAttributes> & { [K in keyof MdFab & keyof MdFabAttributes]?: MdFab[K] } & { [K in keyof MdFab & keyof MdFabAttributes as `attr:${K}`]?: MdFabAttributes[K] } & { [K in keyof MdFab & keyof MdFabAttributes as `prop:${K}`]?: MdFab[K] };
        "md-fab-menu": Omit<MdFabMenu, keyof MdFabMenuAttributes> & { [K in keyof MdFabMenu & keyof MdFabMenuAttributes]?: MdFabMenu[K] } & { [K in keyof MdFabMenu & keyof MdFabMenuAttributes as `attr:${K}`]?: MdFabMenuAttributes[K] } & { [K in keyof MdFabMenu & keyof MdFabMenuAttributes as `prop:${K}`]?: MdFabMenu[K] };
        "md-fab-menu-item": Omit<MdFabMenuItem, keyof MdFabMenuItemAttributes> & { [K in keyof MdFabMenuItem & keyof MdFabMenuItemAttributes]?: MdFabMenuItem[K] } & { [K in keyof MdFabMenuItem & keyof MdFabMenuItemAttributes as `attr:${K}`]?: MdFabMenuItemAttributes[K] } & { [K in keyof MdFabMenuItem & keyof MdFabMenuItemAttributes as `prop:${K}`]?: MdFabMenuItem[K] };
        "md-icon-button": Omit<MdIconButton, keyof MdIconButtonAttributes> & { [K in keyof MdIconButton & keyof MdIconButtonAttributes]?: MdIconButton[K] } & { [K in keyof MdIconButton & keyof MdIconButtonAttributes as `attr:${K}`]?: MdIconButtonAttributes[K] } & { [K in keyof MdIconButton & keyof MdIconButtonAttributes as `prop:${K}`]?: MdIconButton[K] };
        "md-line-chart": Omit<MdLineChart, keyof MdLineChartAttributes> & { [K in keyof MdLineChart & keyof MdLineChartAttributes]?: MdLineChart[K] } & { [K in keyof MdLineChart & keyof MdLineChartAttributes as `attr:${K}`]?: MdLineChartAttributes[K] } & { [K in keyof MdLineChart & keyof MdLineChartAttributes as `prop:${K}`]?: MdLineChart[K] };
        "md-list": Omit<MdList, keyof MdListAttributes> & { [K in keyof MdList & keyof MdListAttributes]?: MdList[K] } & { [K in keyof MdList & keyof MdListAttributes as `attr:${K}`]?: MdListAttributes[K] } & { [K in keyof MdList & keyof MdListAttributes as `prop:${K}`]?: MdList[K] };
        "md-list-item": Omit<MdListItem, keyof MdListItemAttributes> & { [K in keyof MdListItem & keyof MdListItemAttributes]?: MdListItem[K] } & { [K in keyof MdListItem & keyof MdListItemAttributes as `attr:${K}`]?: MdListItemAttributes[K] } & { [K in keyof MdListItem & keyof MdListItemAttributes as `prop:${K}`]?: MdListItem[K] };
        "md-loading-indicator": Omit<MdLoadingIndicator, keyof MdLoadingIndicatorAttributes> & { [K in keyof MdLoadingIndicator & keyof MdLoadingIndicatorAttributes]?: MdLoadingIndicator[K] } & { [K in keyof MdLoadingIndicator & keyof MdLoadingIndicatorAttributes as `attr:${K}`]?: MdLoadingIndicatorAttributes[K] } & { [K in keyof MdLoadingIndicator & keyof MdLoadingIndicatorAttributes as `prop:${K}`]?: MdLoadingIndicator[K] };
        "md-menu": Omit<MdMenu, keyof MdMenuAttributes> & { [K in keyof MdMenu & keyof MdMenuAttributes]?: MdMenu[K] } & { [K in keyof MdMenu & keyof MdMenuAttributes as `attr:${K}`]?: MdMenuAttributes[K] } & { [K in keyof MdMenu & keyof MdMenuAttributes as `prop:${K}`]?: MdMenu[K] };
        "md-menu-item": Omit<MdMenuItem, keyof MdMenuItemAttributes> & { [K in keyof MdMenuItem & keyof MdMenuItemAttributes]?: MdMenuItem[K] } & { [K in keyof MdMenuItem & keyof MdMenuItemAttributes as `attr:${K}`]?: MdMenuItemAttributes[K] } & { [K in keyof MdMenuItem & keyof MdMenuItemAttributes as `prop:${K}`]?: MdMenuItem[K] };
        "md-menu-item-group": Omit<MdMenuItemGroup, keyof MdMenuItemGroupAttributes> & { [K in keyof MdMenuItemGroup & keyof MdMenuItemGroupAttributes]?: MdMenuItemGroup[K] } & { [K in keyof MdMenuItemGroup & keyof MdMenuItemGroupAttributes as `attr:${K}`]?: MdMenuItemGroupAttributes[K] } & { [K in keyof MdMenuItemGroup & keyof MdMenuItemGroupAttributes as `prop:${K}`]?: MdMenuItemGroup[K] };
        "md-meter": Omit<MdMeter, keyof MdMeterAttributes> & { [K in keyof MdMeter & keyof MdMeterAttributes]?: MdMeter[K] } & { [K in keyof MdMeter & keyof MdMeterAttributes as `attr:${K}`]?: MdMeterAttributes[K] } & { [K in keyof MdMeter & keyof MdMeterAttributes as `prop:${K}`]?: MdMeter[K] };
        "md-multi-select": Omit<MdMultiSelect, keyof MdMultiSelectAttributes> & { [K in keyof MdMultiSelect & keyof MdMultiSelectAttributes]?: MdMultiSelect[K] } & { [K in keyof MdMultiSelect & keyof MdMultiSelectAttributes as `attr:${K}`]?: MdMultiSelectAttributes[K] } & { [K in keyof MdMultiSelect & keyof MdMultiSelectAttributes as `prop:${K}`]?: MdMultiSelect[K] };
        "md-navigation-bar": Omit<MdNavigationBar, keyof MdNavigationBarAttributes> & { [K in keyof MdNavigationBar & keyof MdNavigationBarAttributes]?: MdNavigationBar[K] } & { [K in keyof MdNavigationBar & keyof MdNavigationBarAttributes as `attr:${K}`]?: MdNavigationBarAttributes[K] } & { [K in keyof MdNavigationBar & keyof MdNavigationBarAttributes as `prop:${K}`]?: MdNavigationBar[K] };
        "md-navigation-rail": Omit<MdNavigationRail, keyof MdNavigationRailAttributes> & { [K in keyof MdNavigationRail & keyof MdNavigationRailAttributes]?: MdNavigationRail[K] } & { [K in keyof MdNavigationRail & keyof MdNavigationRailAttributes as `attr:${K}`]?: MdNavigationRailAttributes[K] } & { [K in keyof MdNavigationRail & keyof MdNavigationRailAttributes as `prop:${K}`]?: MdNavigationRail[K] };
        "md-navigation-rail-tab": Omit<MdNavigationRailTab, keyof MdNavigationRailTabAttributes> & { [K in keyof MdNavigationRailTab & keyof MdNavigationRailTabAttributes]?: MdNavigationRailTab[K] } & { [K in keyof MdNavigationRailTab & keyof MdNavigationRailTabAttributes as `attr:${K}`]?: MdNavigationRailTabAttributes[K] } & { [K in keyof MdNavigationRailTab & keyof MdNavigationRailTabAttributes as `prop:${K}`]?: MdNavigationRailTab[K] };
        "md-navigation-tab": Omit<MdNavigationTab, keyof MdNavigationTabAttributes> & { [K in keyof MdNavigationTab & keyof MdNavigationTabAttributes]?: MdNavigationTab[K] } & { [K in keyof MdNavigationTab & keyof MdNavigationTabAttributes as `attr:${K}`]?: MdNavigationTabAttributes[K] } & { [K in keyof MdNavigationTab & keyof MdNavigationTabAttributes as `prop:${K}`]?: MdNavigationTab[K] };
        "md-number-field": Omit<MdNumberField, keyof MdNumberFieldAttributes> & { [K in keyof MdNumberField & keyof MdNumberFieldAttributes]?: MdNumberField[K] } & { [K in keyof MdNumberField & keyof MdNumberFieldAttributes as `attr:${K}`]?: MdNumberFieldAttributes[K] } & { [K in keyof MdNumberField & keyof MdNumberFieldAttributes as `prop:${K}`]?: MdNumberField[K] };
        "md-organization-chart": Omit<MdOrganizationChart, keyof MdOrganizationChartAttributes> & { [K in keyof MdOrganizationChart & keyof MdOrganizationChartAttributes]?: MdOrganizationChart[K] } & { [K in keyof MdOrganizationChart & keyof MdOrganizationChartAttributes as `attr:${K}`]?: MdOrganizationChartAttributes[K] } & { [K in keyof MdOrganizationChart & keyof MdOrganizationChartAttributes as `prop:${K}`]?: MdOrganizationChart[K] };
        "md-otp-field": Omit<MdOtpField, keyof MdOtpFieldAttributes> & { [K in keyof MdOtpField & keyof MdOtpFieldAttributes]?: MdOtpField[K] } & { [K in keyof MdOtpField & keyof MdOtpFieldAttributes as `attr:${K}`]?: MdOtpFieldAttributes[K] } & { [K in keyof MdOtpField & keyof MdOtpFieldAttributes as `prop:${K}`]?: MdOtpField[K] };
        "md-pie-chart": Omit<MdPieChart, keyof MdPieChartAttributes> & { [K in keyof MdPieChart & keyof MdPieChartAttributes]?: MdPieChart[K] } & { [K in keyof MdPieChart & keyof MdPieChartAttributes as `attr:${K}`]?: MdPieChartAttributes[K] } & { [K in keyof MdPieChart & keyof MdPieChartAttributes as `prop:${K}`]?: MdPieChart[K] };
        "md-progress-indicator": Omit<MdProgressIndicator, keyof MdProgressIndicatorAttributes> & { [K in keyof MdProgressIndicator & keyof MdProgressIndicatorAttributes]?: MdProgressIndicator[K] } & { [K in keyof MdProgressIndicator & keyof MdProgressIndicatorAttributes as `attr:${K}`]?: MdProgressIndicatorAttributes[K] } & { [K in keyof MdProgressIndicator & keyof MdProgressIndicatorAttributes as `prop:${K}`]?: MdProgressIndicator[K] };
        "md-radio": Omit<MdRadio, keyof MdRadioAttributes> & { [K in keyof MdRadio & keyof MdRadioAttributes]?: MdRadio[K] } & { [K in keyof MdRadio & keyof MdRadioAttributes as `attr:${K}`]?: MdRadioAttributes[K] } & { [K in keyof MdRadio & keyof MdRadioAttributes as `prop:${K}`]?: MdRadio[K] };
        "md-rating": Omit<MdRating, keyof MdRatingAttributes> & { [K in keyof MdRating & keyof MdRatingAttributes]?: MdRating[K] } & { [K in keyof MdRating & keyof MdRatingAttributes as `attr:${K}`]?: MdRatingAttributes[K] } & { [K in keyof MdRating & keyof MdRatingAttributes as `prop:${K}`]?: MdRating[K] };
        "md-ripple": Omit<MdRipple, keyof MdRippleAttributes> & { [K in keyof MdRipple & keyof MdRippleAttributes]?: MdRipple[K] } & { [K in keyof MdRipple & keyof MdRippleAttributes as `attr:${K}`]?: MdRippleAttributes[K] } & { [K in keyof MdRipple & keyof MdRippleAttributes as `prop:${K}`]?: MdRipple[K] };
        "md-search": Omit<MdSearch, keyof MdSearchAttributes> & { [K in keyof MdSearch & keyof MdSearchAttributes]?: MdSearch[K] } & { [K in keyof MdSearch & keyof MdSearchAttributes as `attr:${K}`]?: MdSearchAttributes[K] } & { [K in keyof MdSearch & keyof MdSearchAttributes as `prop:${K}`]?: MdSearch[K] };
        "md-segmented-button": Omit<MdSegmentedButton, keyof MdSegmentedButtonAttributes> & { [K in keyof MdSegmentedButton & keyof MdSegmentedButtonAttributes]?: MdSegmentedButton[K] } & { [K in keyof MdSegmentedButton & keyof MdSegmentedButtonAttributes as `attr:${K}`]?: MdSegmentedButtonAttributes[K] } & { [K in keyof MdSegmentedButton & keyof MdSegmentedButtonAttributes as `prop:${K}`]?: MdSegmentedButton[K] };
        "md-segmented-button-set": Omit<MdSegmentedButtonSet, keyof MdSegmentedButtonSetAttributes> & { [K in keyof MdSegmentedButtonSet & keyof MdSegmentedButtonSetAttributes]?: MdSegmentedButtonSet[K] } & { [K in keyof MdSegmentedButtonSet & keyof MdSegmentedButtonSetAttributes as `attr:${K}`]?: MdSegmentedButtonSetAttributes[K] } & { [K in keyof MdSegmentedButtonSet & keyof MdSegmentedButtonSetAttributes as `prop:${K}`]?: MdSegmentedButtonSet[K] };
        "md-select": Omit<MdSelect, keyof MdSelectAttributes> & { [K in keyof MdSelect & keyof MdSelectAttributes]?: MdSelect[K] } & { [K in keyof MdSelect & keyof MdSelectAttributes as `attr:${K}`]?: MdSelectAttributes[K] } & { [K in keyof MdSelect & keyof MdSelectAttributes as `prop:${K}`]?: MdSelect[K] };
        "md-select-option": Omit<MdSelectOption, keyof MdSelectOptionAttributes> & { [K in keyof MdSelectOption & keyof MdSelectOptionAttributes]?: MdSelectOption[K] } & { [K in keyof MdSelectOption & keyof MdSelectOptionAttributes as `attr:${K}`]?: MdSelectOptionAttributes[K] } & { [K in keyof MdSelectOption & keyof MdSelectOptionAttributes as `prop:${K}`]?: MdSelectOption[K] };
        "md-side-sheet": Omit<MdSideSheet, keyof MdSideSheetAttributes> & { [K in keyof MdSideSheet & keyof MdSideSheetAttributes]?: MdSideSheet[K] } & { [K in keyof MdSideSheet & keyof MdSideSheetAttributes as `attr:${K}`]?: MdSideSheetAttributes[K] } & { [K in keyof MdSideSheet & keyof MdSideSheetAttributes as `prop:${K}`]?: MdSideSheet[K] };
        "md-skeleton": Omit<MdSkeleton, keyof MdSkeletonAttributes> & { [K in keyof MdSkeleton & keyof MdSkeletonAttributes]?: MdSkeleton[K] } & { [K in keyof MdSkeleton & keyof MdSkeletonAttributes as `attr:${K}`]?: MdSkeletonAttributes[K] } & { [K in keyof MdSkeleton & keyof MdSkeletonAttributes as `prop:${K}`]?: MdSkeleton[K] };
        "md-slider": Omit<MdSlider, keyof MdSliderAttributes> & { [K in keyof MdSlider & keyof MdSliderAttributes]?: MdSlider[K] } & { [K in keyof MdSlider & keyof MdSliderAttributes as `attr:${K}`]?: MdSliderAttributes[K] } & { [K in keyof MdSlider & keyof MdSliderAttributes as `prop:${K}`]?: MdSlider[K] };
        "md-snackbar": Omit<MdSnackbar, keyof MdSnackbarAttributes> & { [K in keyof MdSnackbar & keyof MdSnackbarAttributes]?: MdSnackbar[K] } & { [K in keyof MdSnackbar & keyof MdSnackbarAttributes as `attr:${K}`]?: MdSnackbarAttributes[K] } & { [K in keyof MdSnackbar & keyof MdSnackbarAttributes as `prop:${K}`]?: MdSnackbar[K] };
        "md-sparkline": Omit<MdSparkline, keyof MdSparklineAttributes> & { [K in keyof MdSparkline & keyof MdSparklineAttributes]?: MdSparkline[K] } & { [K in keyof MdSparkline & keyof MdSparklineAttributes as `attr:${K}`]?: MdSparklineAttributes[K] } & { [K in keyof MdSparkline & keyof MdSparklineAttributes as `prop:${K}`]?: MdSparkline[K] };
        "md-split-button": Omit<MdSplitButton, keyof MdSplitButtonAttributes> & { [K in keyof MdSplitButton & keyof MdSplitButtonAttributes]?: MdSplitButton[K] } & { [K in keyof MdSplitButton & keyof MdSplitButtonAttributes as `attr:${K}`]?: MdSplitButtonAttributes[K] } & { [K in keyof MdSplitButton & keyof MdSplitButtonAttributes as `prop:${K}`]?: MdSplitButton[K] };
        "md-status-dot": Omit<MdStatusDot, keyof MdStatusDotAttributes> & { [K in keyof MdStatusDot & keyof MdStatusDotAttributes]?: MdStatusDot[K] } & { [K in keyof MdStatusDot & keyof MdStatusDotAttributes as `attr:${K}`]?: MdStatusDotAttributes[K] } & { [K in keyof MdStatusDot & keyof MdStatusDotAttributes as `prop:${K}`]?: MdStatusDot[K] };
        "md-step": Omit<MdStep, keyof MdStepAttributes> & { [K in keyof MdStep & keyof MdStepAttributes]?: MdStep[K] } & { [K in keyof MdStep & keyof MdStepAttributes as `attr:${K}`]?: MdStepAttributes[K] } & { [K in keyof MdStep & keyof MdStepAttributes as `prop:${K}`]?: MdStep[K] };
        "md-stepper": Omit<MdStepper, keyof MdStepperAttributes> & { [K in keyof MdStepper & keyof MdStepperAttributes]?: MdStepper[K] } & { [K in keyof MdStepper & keyof MdStepperAttributes as `attr:${K}`]?: MdStepperAttributes[K] } & { [K in keyof MdStepper & keyof MdStepperAttributes as `prop:${K}`]?: MdStepper[K] };
        "md-sub-menu-item": Omit<MdSubMenuItem, keyof MdSubMenuItemAttributes> & { [K in keyof MdSubMenuItem & keyof MdSubMenuItemAttributes]?: MdSubMenuItem[K] } & { [K in keyof MdSubMenuItem & keyof MdSubMenuItemAttributes as `attr:${K}`]?: MdSubMenuItemAttributes[K] } & { [K in keyof MdSubMenuItem & keyof MdSubMenuItemAttributes as `prop:${K}`]?: MdSubMenuItem[K] };
        "md-switch": Omit<MdSwitch, keyof MdSwitchAttributes> & { [K in keyof MdSwitch & keyof MdSwitchAttributes]?: MdSwitch[K] } & { [K in keyof MdSwitch & keyof MdSwitchAttributes as `attr:${K}`]?: MdSwitchAttributes[K] } & { [K in keyof MdSwitch & keyof MdSwitchAttributes as `prop:${K}`]?: MdSwitch[K] };
        "md-tab": Omit<MdTab, keyof MdTabAttributes> & { [K in keyof MdTab & keyof MdTabAttributes]?: MdTab[K] } & { [K in keyof MdTab & keyof MdTabAttributes as `attr:${K}`]?: MdTabAttributes[K] } & { [K in keyof MdTab & keyof MdTabAttributes as `prop:${K}`]?: MdTab[K] };
        "md-tab-panel": Omit<MdTabPanel, keyof MdTabPanelAttributes> & { [K in keyof MdTabPanel & keyof MdTabPanelAttributes]?: MdTabPanel[K] } & { [K in keyof MdTabPanel & keyof MdTabPanelAttributes as `attr:${K}`]?: MdTabPanelAttributes[K] } & { [K in keyof MdTabPanel & keyof MdTabPanelAttributes as `prop:${K}`]?: MdTabPanel[K] };
        "md-tab-panels": Omit<MdTabPanels, keyof MdTabPanelsAttributes> & { [K in keyof MdTabPanels & keyof MdTabPanelsAttributes]?: MdTabPanels[K] } & { [K in keyof MdTabPanels & keyof MdTabPanelsAttributes as `attr:${K}`]?: MdTabPanelsAttributes[K] } & { [K in keyof MdTabPanels & keyof MdTabPanelsAttributes as `prop:${K}`]?: MdTabPanels[K] };
        "md-table": Omit<MdTable, keyof MdTableAttributes> & { [K in keyof MdTable & keyof MdTableAttributes]?: MdTable[K] } & { [K in keyof MdTable & keyof MdTableAttributes as `attr:${K}`]?: MdTableAttributes[K] } & { [K in keyof MdTable & keyof MdTableAttributes as `prop:${K}`]?: MdTable[K] };
        "md-table-body": Omit<MdTableBody, keyof MdTableBodyAttributes> & { [K in keyof MdTableBody & keyof MdTableBodyAttributes]?: MdTableBody[K] } & { [K in keyof MdTableBody & keyof MdTableBodyAttributes as `attr:${K}`]?: MdTableBodyAttributes[K] } & { [K in keyof MdTableBody & keyof MdTableBodyAttributes as `prop:${K}`]?: MdTableBody[K] };
        "md-table-cell": Omit<MdTableCell, keyof MdTableCellAttributes> & { [K in keyof MdTableCell & keyof MdTableCellAttributes]?: MdTableCell[K] } & { [K in keyof MdTableCell & keyof MdTableCellAttributes as `attr:${K}`]?: MdTableCellAttributes[K] } & { [K in keyof MdTableCell & keyof MdTableCellAttributes as `prop:${K}`]?: MdTableCell[K] };
        "md-table-container": Omit<MdTableContainer, keyof MdTableContainerAttributes> & { [K in keyof MdTableContainer & keyof MdTableContainerAttributes]?: MdTableContainer[K] } & { [K in keyof MdTableContainer & keyof MdTableContainerAttributes as `attr:${K}`]?: MdTableContainerAttributes[K] } & { [K in keyof MdTableContainer & keyof MdTableContainerAttributes as `prop:${K}`]?: MdTableContainer[K] };
        "md-table-expand-toggle": Omit<MdTableExpandToggle, keyof MdTableExpandToggleAttributes> & { [K in keyof MdTableExpandToggle & keyof MdTableExpandToggleAttributes]?: MdTableExpandToggle[K] } & { [K in keyof MdTableExpandToggle & keyof MdTableExpandToggleAttributes as `attr:${K}`]?: MdTableExpandToggleAttributes[K] } & { [K in keyof MdTableExpandToggle & keyof MdTableExpandToggleAttributes as `prop:${K}`]?: MdTableExpandToggle[K] };
        "md-table-foot": Omit<MdTableFoot, keyof MdTableFootAttributes> & { [K in keyof MdTableFoot & keyof MdTableFootAttributes]?: MdTableFoot[K] } & { [K in keyof MdTableFoot & keyof MdTableFootAttributes as `attr:${K}`]?: MdTableFootAttributes[K] } & { [K in keyof MdTableFoot & keyof MdTableFootAttributes as `prop:${K}`]?: MdTableFoot[K] };
        "md-table-head": MdTableHead;
        "md-table-pagination": Omit<MdTablePagination, keyof MdTablePaginationAttributes> & { [K in keyof MdTablePagination & keyof MdTablePaginationAttributes]?: MdTablePagination[K] } & { [K in keyof MdTablePagination & keyof MdTablePaginationAttributes as `attr:${K}`]?: MdTablePaginationAttributes[K] } & { [K in keyof MdTablePagination & keyof MdTablePaginationAttributes as `prop:${K}`]?: MdTablePagination[K] };
        "md-table-row": Omit<MdTableRow, keyof MdTableRowAttributes> & { [K in keyof MdTableRow & keyof MdTableRowAttributes]?: MdTableRow[K] } & { [K in keyof MdTableRow & keyof MdTableRowAttributes as `attr:${K}`]?: MdTableRowAttributes[K] } & { [K in keyof MdTableRow & keyof MdTableRowAttributes as `prop:${K}`]?: MdTableRow[K] };
        "md-table-sort-label": Omit<MdTableSortLabel, keyof MdTableSortLabelAttributes> & { [K in keyof MdTableSortLabel & keyof MdTableSortLabelAttributes]?: MdTableSortLabel[K] } & { [K in keyof MdTableSortLabel & keyof MdTableSortLabelAttributes as `attr:${K}`]?: MdTableSortLabelAttributes[K] } & { [K in keyof MdTableSortLabel & keyof MdTableSortLabelAttributes as `prop:${K}`]?: MdTableSortLabel[K] };
        "md-table-toolbar": Omit<MdTableToolbar, keyof MdTableToolbarAttributes> & { [K in keyof MdTableToolbar & keyof MdTableToolbarAttributes]?: MdTableToolbar[K] } & { [K in keyof MdTableToolbar & keyof MdTableToolbarAttributes as `attr:${K}`]?: MdTableToolbarAttributes[K] } & { [K in keyof MdTableToolbar & keyof MdTableToolbarAttributes as `prop:${K}`]?: MdTableToolbar[K] };
        "md-tabs": Omit<MdTabs, keyof MdTabsAttributes> & { [K in keyof MdTabs & keyof MdTabsAttributes]?: MdTabs[K] } & { [K in keyof MdTabs & keyof MdTabsAttributes as `attr:${K}`]?: MdTabsAttributes[K] } & { [K in keyof MdTabs & keyof MdTabsAttributes as `prop:${K}`]?: MdTabs[K] };
        "md-text-field": Omit<MdTextField, keyof MdTextFieldAttributes> & { [K in keyof MdTextField & keyof MdTextFieldAttributes]?: MdTextField[K] } & { [K in keyof MdTextField & keyof MdTextFieldAttributes as `attr:${K}`]?: MdTextFieldAttributes[K] } & { [K in keyof MdTextField & keyof MdTextFieldAttributes as `prop:${K}`]?: MdTextField[K] };
        "md-time-picker": Omit<MdTimePicker, keyof MdTimePickerAttributes> & { [K in keyof MdTimePicker & keyof MdTimePickerAttributes]?: MdTimePicker[K] } & { [K in keyof MdTimePicker & keyof MdTimePickerAttributes as `attr:${K}`]?: MdTimePickerAttributes[K] } & { [K in keyof MdTimePicker & keyof MdTimePickerAttributes as `prop:${K}`]?: MdTimePicker[K] };
        "md-toolbar": Omit<MdToolbar, keyof MdToolbarAttributes> & { [K in keyof MdToolbar & keyof MdToolbarAttributes]?: MdToolbar[K] } & { [K in keyof MdToolbar & keyof MdToolbarAttributes as `attr:${K}`]?: MdToolbarAttributes[K] } & { [K in keyof MdToolbar & keyof MdToolbarAttributes as `prop:${K}`]?: MdToolbar[K] };
        "md-tooltip": Omit<MdTooltip, keyof MdTooltipAttributes> & { [K in keyof MdTooltip & keyof MdTooltipAttributes]?: MdTooltip[K] } & { [K in keyof MdTooltip & keyof MdTooltipAttributes as `attr:${K}`]?: MdTooltipAttributes[K] } & { [K in keyof MdTooltip & keyof MdTooltipAttributes as `prop:${K}`]?: MdTooltip[K] };
        "md-transfer-list": Omit<MdTransferList, keyof MdTransferListAttributes> & { [K in keyof MdTransferList & keyof MdTransferListAttributes]?: MdTransferList[K] } & { [K in keyof MdTransferList & keyof MdTransferListAttributes as `attr:${K}`]?: MdTransferListAttributes[K] } & { [K in keyof MdTransferList & keyof MdTransferListAttributes as `prop:${K}`]?: MdTransferList[K] };
    }
}
export { LocalJSX as JSX };
declare module "@stencil/core" {
    export namespace JSX {
        interface IntrinsicElements {
            "md-accordion": LocalJSX.IntrinsicElements["md-accordion"] & JSXBase.HTMLAttributes<HTMLMdAccordionElement>;
            "md-accordion-item": LocalJSX.IntrinsicElements["md-accordion-item"] & JSXBase.HTMLAttributes<HTMLMdAccordionItemElement>;
            /**
             * `md-app-bar` — Material Design 3 App Bar.
             * Implements the current (M3 Expressive) app-bars specification:
             *   https://m3.material.io/components/app-bars/specs
             * Size variants: `small` (64px), `medium` (112/136px expanded), `large`
             * (120/152px expanded), and `search` (inline search field in the 64px row).
             * Flexible `medium` / `large` bars always render the expanded headline block.
             * Title alignment (`title-alignment`) is configuration, not a variant.
             * Selecting the search field should open an `md-search` view via `mdSearchActivate`.
             */
            "md-app-bar": LocalJSX.IntrinsicElements["md-app-bar"] & JSXBase.HTMLAttributes<HTMLMdAppBarElement>;
            /**
             * md-area-chart — Material Design 3 area / streamgraph chart,
             * rendered by the in-house engine (Canvas2D + a DOM text
             * overlay). No ECharts.
             * Differences from `md-line-chart`:
             *   • the line is always filled with a gradient
             *   • stacking defaults to `'normal'` (areas stack additively)
             *   • supports the `silhouette` streamgraph baseline
             *   • markers are off by default
             */
            "md-area-chart": LocalJSX.IntrinsicElements["md-area-chart"] & JSXBase.HTMLAttributes<HTMLMdAreaChartElement>;
            /**
             * `md-autocomplete` — Material Design 3 combobox: a text field that filters
             * a dropdown listbox **as you type in the input** (no separate search row),
             * with single or multiple (chips) selection.
             * Built from the same primitives as `md-select` / `md-multi-select`:
             *   - the shared option model (`options` array of `{ value, label,
             *     supportingText?, icon?, iconColor?, disabled? }` or slotted
             *     `<md-select-option>` children),
             *   - `md-text-field` for the trigger (typing filters live),
             *   - `md-menu` as a WAI-ARIA listbox popup (options carry `aria-selected`),
             *   - `VirtualSelectController` for WASM-virtualized huge datasets
             *     (`virtualize`, `loadOptions()`, `filter-mode`) — the same engine as
             *     the selects, so tens of thousands of rows filter smoothly.
             * Keyboard: typing filters; ArrowDown moves focus into the listbox (the
             * focus-moves-into-popup combobox variant — option focus is announced
             * directly); Enter selects; Escape closes back to the input; Backspace on an
             * empty multi input removes the last chip.
             * ARIA: the real textbox — the `<input>` inside md-text-field's shadow root —
             * is promoted to `role="combobox"` with `aria-expanded` / `aria-autocomplete`
             * (handed over via `input-role`, since only that component renders the input),
             * and points at this component's listbox through ARIA element reflection. An
             * IDREF could not: it would have to resolve inside md-text-field's tree, where
             * the listbox is not. See `syncComboboxRefs`.
             * Extras kept from the surface this API mirrors: `free-solo` (commit arbitrary text),
             * `max-selected`, `clearable`, `disable-close-on-select`, `clear-on-blur`,
             * a custom `filterer` and `limit-results` (client-side path only).
             */
            "md-autocomplete": LocalJSX.IntrinsicElements["md-autocomplete"] & JSXBase.HTMLAttributes<HTMLMdAutocompleteElement>;
            /**
             * `md-avatar` — a presentational identity tile that resolves the first
             * available source from a clear fallback chain:
             *   1. `src` (image)  → `<img>` is rendered.
             *      On error/empty, automatically falls through to step 2.
             *   2. `initials`     → explicit initials string (verbatim, max 3 chars).
             *   3. `name`         → auto-derived initials. Two tokens → first letter
             *                       of each; one token → first letter only. RTL- and
             *                       diacritic-safe via `Intl.Segmenter` when available.
             *   4. default slot   → caller-provided fallback (icon, emoji, SVG).
             *   5. person icon    → material-symbols fallback (always available).
             */
            "md-avatar": LocalJSX.IntrinsicElements["md-avatar"] & JSXBase.HTMLAttributes<HTMLMdAvatarElement>;
            "md-badge": LocalJSX.IntrinsicElements["md-badge"] & JSXBase.HTMLAttributes<HTMLMdBadgeElement>;
            /**
             * md-bar-chart — Material Design 3 Expressive bar chart, rendered
             * by the in-house engine (Canvas2D + a DOM text overlay). No
             * ECharts.
             *   • vertical (default) and horizontal layouts
             *   • grouped / stacked / percentage modes
             *   • category + bar gap configuration
             *   • rounded outer corners (top for vertical, end for horizontal)
             *   • legend toggle, hover tooltip, click events
             *   • role="figure" + screen-reader-only data table
             */
            "md-bar-chart": LocalJSX.IntrinsicElements["md-bar-chart"] & JSXBase.HTMLAttributes<HTMLMdBarChartElement>;
            /**
             * MD3 Bottom sheet — secondary content anchored to the bottom of the screen.
             * Two variants per the M3 spec
             * (https://m3.material.io/components/bottom-sheets/overview). Both are
             * modal dialogs — they always render a scrim, trap focus, lock body
             * scroll, close on Escape, and support drag-to-dismiss.
             * - `standard` (default) — anchored to the bottom edge of the viewport.
             *   Full-width up to a responsive max, with rounded top corners only.
             * - `detached` — floats with a margin on every side and rounded corners
             *   on every side. Best for compact, dialog-like sheets on larger
             *   screens.
             * Width and height are entirely token-driven, so the only thing you
             * change to switch from anchored to floating chrome is the `variant`
             * attribute. The default media query reflows the sheet between
             * full-width on mobile and a centred 640px panel on desktop; consumers
             * can opt out via `--md-bottom-sheet-width` / `--md-bottom-sheet-max-width`.
             */
            "md-bottom-sheet": LocalJSX.IntrinsicElements["md-bottom-sheet"] & JSXBase.HTMLAttributes<HTMLMdBottomSheetElement>;
            "md-breadcrumb-item": LocalJSX.IntrinsicElements["md-breadcrumb-item"] & JSXBase.HTMLAttributes<HTMLMdBreadcrumbItemElement>;
            "md-breadcrumbs": LocalJSX.IntrinsicElements["md-breadcrumbs"] & JSXBase.HTMLAttributes<HTMLMdBreadcrumbsElement>;
            "md-button": LocalJSX.IntrinsicElements["md-button"] & JSXBase.HTMLAttributes<HTMLMdButtonElement>;
            "md-button-group": LocalJSX.IntrinsicElements["md-button-group"] & JSXBase.HTMLAttributes<HTMLMdButtonGroupElement>;
            "md-card": LocalJSX.IntrinsicElements["md-card"] & JSXBase.HTMLAttributes<HTMLMdCardElement>;
            /**
             * `md-checkbox` — Material Design 3 checkbox (with tri-state / indeterminate).
             * Form-associated: inside a `<form>` it submits its `value` under `name` when
             * checked, and restores on form reset. It is a *labelable* element — wrap it in
             * a `<label>` (clicks on the label text toggle it AND give it its accessible
             * name natively), or pass `aria-label` / `aria-labelledby` on the host. A bare,
             * unlabeled checkbox has no accessible name (axe `aria-toggle-field-name`).
             */
            "md-checkbox": LocalJSX.IntrinsicElements["md-checkbox"] & JSXBase.HTMLAttributes<HTMLMdCheckboxElement>;
            "md-chip": LocalJSX.IntrinsicElements["md-chip"] & JSXBase.HTMLAttributes<HTMLMdChipElement>;
            "md-color-picker": LocalJSX.IntrinsicElements["md-color-picker"] & JSXBase.HTMLAttributes<HTMLMdColorPickerElement>;
            /**
             * `md-date-picker` — a Material Design 3 date picker.
             * Variants:
             * - `modal-input` *(default)* — outlined text field + modal calendar dialog.
             * - `modal` — bare modal dialog surfaced via `show()` / `open`.
             * - `docked` — outlined text field + dropdown calendar anchored to the field.
             * The modal dialog can flip to a typed-entry view (date input) via the
             * header toggle, satisfying the MD3 "modal date input" pattern.
             */
            "md-date-picker": LocalJSX.IntrinsicElements["md-date-picker"] & JSXBase.HTMLAttributes<HTMLMdDatePickerElement>;
            "md-dialog": LocalJSX.IntrinsicElements["md-dialog"] & JSXBase.HTMLAttributes<HTMLMdDialogElement>;
            "md-divider": LocalJSX.IntrinsicElements["md-divider"] & JSXBase.HTMLAttributes<HTMLMdDividerElement>;
            "md-fab": LocalJSX.IntrinsicElements["md-fab"] & JSXBase.HTMLAttributes<HTMLMdFabElement>;
            "md-fab-menu": LocalJSX.IntrinsicElements["md-fab-menu"] & JSXBase.HTMLAttributes<HTMLMdFabMenuElement>;
            "md-fab-menu-item": LocalJSX.IntrinsicElements["md-fab-menu-item"] & JSXBase.HTMLAttributes<HTMLMdFabMenuItemElement>;
            "md-icon-button": LocalJSX.IntrinsicElements["md-icon-button"] & JSXBase.HTMLAttributes<HTMLMdIconButtonElement>;
            /**
             * md-line-chart — Material Design 3 line chart rendered by the
             * in-house chart engine (Canvas2D + a DOM text overlay). No
             * ECharts.
             * Pattern parity with MUI X Charts:
             *   • multi-series with per-series colour / label / curve
             *   • category, time, value, log scales
             *   • curve types: linear / smooth / monotone / step variants
             *   • stacking: normal / percentage
             *   • markers, area fills, gap bridging (`connectNulls`)
             *   • legend with toggle + custom position
             *   • hover crosshair + tooltip
             *   • emits `mdMarkerClick`, `mdLineClick`, `mdAreaClick`, `mdAxisClick`,
             *     `mdLegendClick`, `mdHover`
             * MD3 expressive layer on top:
             *   • palette resolves to `--md-sys-color-*` tokens
             *   • dark theme + brand re-themes via CSS variables only
             *   • a11y: role="figure" + screen-reader-only data table
             *   • fully responsive via ResizeObserver
             */
            "md-line-chart": LocalJSX.IntrinsicElements["md-line-chart"] & JSXBase.HTMLAttributes<HTMLMdLineChartElement>;
            "md-list": LocalJSX.IntrinsicElements["md-list"] & JSXBase.HTMLAttributes<HTMLMdListElement>;
            "md-list-item": LocalJSX.IntrinsicElements["md-list-item"] & JSXBase.HTMLAttributes<HTMLMdListItemElement>;
            "md-loading-indicator": LocalJSX.IntrinsicElements["md-loading-indicator"] & JSXBase.HTMLAttributes<HTMLMdLoadingIndicatorElement>;
            "md-menu": LocalJSX.IntrinsicElements["md-menu"] & JSXBase.HTMLAttributes<HTMLMdMenuElement>;
            "md-menu-item": LocalJSX.IntrinsicElements["md-menu-item"] & JSXBase.HTMLAttributes<HTMLMdMenuItemElement>;
            "md-menu-item-group": LocalJSX.IntrinsicElements["md-menu-item-group"] & JSXBase.HTMLAttributes<HTMLMdMenuItemGroupElement>;
            /**
             * md-meter — a read-only graphical display of a numeric value within a known
             * range (storage used, quota, password strength, battery level).
             * The semantics live on the HOST: `role="meter"` with the full ARIA value
             * contract, a purely presentational track + indicator inside the shadow root
             * (all `aria-hidden`), value clamping into `[min, max]`, and Intl-driven value
             * formatting (locale-aware percent by default, any `Intl.NumberFormatOptions`
             * via `formatOptions`). Keeping the ARIA outside the shadow root is what lets
             * the visuals change shape without touching what is announced.
             * Deliberately NOT a progress indicator: a meter shows a *state* ("how full"),
             * a progress indicator shows an *activity* ("how far along"). It is therefore
             * a plain track + fill — no 4px gap, no stop dot, no indeterminate mode, no
             * completion animation. Nothing here is focusable or interactive and no events
             * are emitted.
             * Two shapes, one contract: `variant="linear"` (default) is the bar;
             * `variant="circular"` is the same reading as a ring, with the value in the
             * middle and the label beneath. The ARIA lives on the host either way, so the
             * shape is presentational and swapping it changes nothing for a screen reader.
             */
            "md-meter": LocalJSX.IntrinsicElements["md-meter"] & JSXBase.HTMLAttributes<HTMLMdMeterElement>;
            /**
             * `md-multi-select` — Material Design 3 multi-value dropdown.
             * Shares its internals with `md-select`: an `md-text-field` trigger and an
             * `md-menu` option surface. Options come from slotted `<md-select-option>`
             * children (primary API) or the programmatic `options` array. Each becomes a
             * `type="checkbox"` `md-menu-item` so several can be selected at once, and the
             * menu uses `keep-open` so multiple toggles don't dismiss it.
             * Selected values render in the trigger as removable chips (`display-mode="chips"`),
             * a "{n} selected" count, or comma-joined text.
             */
            "md-multi-select": LocalJSX.IntrinsicElements["md-multi-select"] & JSXBase.HTMLAttributes<HTMLMdMultiSelectElement>;
            /**
             * `md-navigation-bar` — Material Design 3 navigation bar.
             * A bottom-anchored row of 3–5 top-level destinations, optimized for
             * compact-width screens (mobile). Each destination is an `<md-navigation-tab>`
             * child; the bar orchestrates selection, label visibility, keyboard
             * navigation (roving tabindex + arrow keys), and emits change events.
             * References:
             *   - {@link https://m3.material.io/components/navigation-bar/overview Overview}
             *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
             *   - {@link https://m3.material.io/components/navigation-bar/guidelines Guidelines}
             *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
             */
            "md-navigation-bar": LocalJSX.IntrinsicElements["md-navigation-bar"] & JSXBase.HTMLAttributes<HTMLMdNavigationBarElement>;
            /**
             * Material Design 3 — Navigation Rail
             * A vertical navigation surface for the left or right edge of medium / large
             * window sizes. Holds 3–7 top-level destinations and optionally a header
             * (menu / brand), a FAB, and a footer.
             * Implements MD3 specs:
             *   - https://m3.material.io/components/navigation-rail/overview
             *   - https://m3.material.io/components/navigation-rail/specs
             *   - https://m3.material.io/components/navigation-rail/guidelines
             *   - https://m3.material.io/components/navigation-rail/accessibility
             */
            "md-navigation-rail": LocalJSX.IntrinsicElements["md-navigation-rail"] & JSXBase.HTMLAttributes<HTMLMdNavigationRailElement>;
            /**
             * Material Design 3 — Navigation Rail Destination (Tab)
             * A single destination within a `<md-navigation-rail>`. Renders the MD3
             * active-indicator pill (56×32) around the icon, with an optional label
             * underneath (or inline when the parent rail is expanded). Supports badges,
             * disabled state, link mode (`href`) and slotted custom icons.
             * Spec:
             *   - https://m3.material.io/components/navigation-rail/specs
             *   - https://m3.material.io/components/navigation-rail/guidelines
             *   - https://m3.material.io/components/navigation-rail/accessibility
             */
            "md-navigation-rail-tab": LocalJSX.IntrinsicElements["md-navigation-rail-tab"] & JSXBase.HTMLAttributes<HTMLMdNavigationRailTabElement>;
            /**
             * `md-navigation-tab` — a single destination inside `md-navigation-bar`.
             * Renders an icon, an optional label, and an optional badge. Selecting
             * the tab swaps the icon to a filled variant (per M3 spec) and reveals
             * an "active indicator" pill behind the icon.
             * References:
             *   - {@link https://m3.material.io/components/navigation-bar/specs Specs}
             *   - {@link https://m3.material.io/components/navigation-bar/accessibility Accessibility}
             */
            "md-navigation-tab": LocalJSX.IntrinsicElements["md-navigation-tab"] & JSXBase.HTMLAttributes<HTMLMdNavigationTabElement>;
            /**
             * `md-number-field` — Material Design 3 number input: an `md-text-field`
             * with locale-aware `Intl.NumberFormat` display, arrow-key / stepper-button /
             * wheel stepping, and native form participation.
             * Behavior contract:
             *   - the visible control is a **plain text input** (`inputmode` numeric or
             *     decimal) — deliberately NOT `role="spinbutton"` and NOT
             *     `type="number"`, so the locale-formatted value stays ordinary readable,
             *     editable text and no native spinner chrome fights the Intl display;
             *     the steppers are separate labeled buttons with `tabindex="-1"` (the
             *     input is the keyboard surface),
             *   - `value` is the raw number (`null` = empty); the formatted string is
             *     display-only state,
             *   - ArrowUp/Down step by `step`, Alt = `smallStep`, Shift = `largeStep`;
             *     Home/End jump to a defined `min`/`max`,
             *   - press-and-hold on a stepper auto-repeats (400ms delay, 60ms interval),
             *   - typing is filtered to the characters of the active locale/format,
             *     parsed leniently and kept verbatim; blur reformats (and clamps unless
             *     `allow-out-of-range`),
             *   - stepping (keys, buttons, wheel) always clamps and commits (`mdChange`).
             * A drag-to-scrub surface (pointer lock over the field) is deliberately out of
             * scope: a niche gesture built on pointer lock, whose behaviour is unreliable
             * in Safari. `allow-wheel-scrub` covers the "nudge without typing" case.
             */
            "md-number-field": LocalJSX.IntrinsicElements["md-number-field"] & JSXBase.HTMLAttributes<HTMLMdNumberFieldElement>;
            /**
             * `md-organization-chart` — Material Design 3 Expressive organization chart.
             * Visualises hierarchical org data as a top-down tree of avatar cards joined
             * by connector lines. Data-driven (`nodes`), with collapsible subtrees,
             * optional single/multiple selection, and the full WAI-ARIA **tree** pattern
             * (roving tabindex, Arrow/Home/End navigation, Enter/Space to select).
             * The surface pans horizontally when it outgrows its container, so a wide org
             * stays usable on small screens. Direction-aware (RTL mirrors the layout and
             * swaps the horizontal arrow keys); every affordance label is a prop for
             * localisation.
             * ```html
             * <md-organization-chart selection-mode="single"></md-organization-chart>
             * <script>
             *   document.querySelector('md-organization-chart').nodes = [{
             *     id: 'ceo', name: 'Amy Elsner', title: 'Founder & CEO',
             *     children: [{ id: 'prod', name: 'Asiya Javayant', title: 'Product Lead' }],
             *   }];
             * </script>
             * ```
             */
            "md-organization-chart": LocalJSX.IntrinsicElements["md-organization-chart"] & JSXBase.HTMLAttributes<HTMLMdOrganizationChartElement>;
            /**
             * `md-otp-field` — Material Design 3 one-time-code field.
             * Renders `length` real `<input>` cells in its own shadow root (it OWNS its
             * inputs, so every cell carries full ARIA), pipes all entry — typing, paste,
             * SMS autofill — through one sanitizing pipeline (strip whitespace →
             * `transform` → `validationType` charset filter → clamp to `length`), and
             * participates in forms via `ElementInternals` (no hidden input).
             * The value is a CONTIGUOUS string: clearing a cell shifts the characters
             * after it left (there are no holes), and a character typed into an empty
             * cell beyond the fill point lands at the first empty position.
             * Normalization is declarative: `transform` names the casing rule as an
             * attribute value rather than taking a callback, so the rule is authorable
             * from plain HTML and survives SSR — an element attribute cannot carry a
             * function.
             */
            "md-otp-field": LocalJSX.IntrinsicElements["md-otp-field"] & JSXBase.HTMLAttributes<HTMLMdOtpFieldElement>;
            "md-pie-chart": LocalJSX.IntrinsicElements["md-pie-chart"] & JSXBase.HTMLAttributes<HTMLMdPieChartElement>;
            "md-progress-indicator": LocalJSX.IntrinsicElements["md-progress-indicator"] & JSXBase.HTMLAttributes<HTMLMdProgressIndicatorElement>;
            "md-radio": LocalJSX.IntrinsicElements["md-radio"] & JSXBase.HTMLAttributes<HTMLMdRadioElement>;
            /**
             * `md-rating` — Material Design 3 Expressive Rating control.
             * A star-rating input: configurable max, half-step precision, custom icons
             * (Material Symbols glyphs OR slotted SVG/HTML), per-value icons
             * (filled / empty), hover preview, read-only and disabled modes,
             * keyboard navigation (Arrow + Home/End), and reset to zero.
             * MD3 Expressive: items animate with a brief tonal "scale-up" pop on
             * hover and selection; press uses a state-layer + ripple. The
             * underlying ARIA pattern is a slider so screen readers announce the
             * value as a continuous range.
             */
            "md-rating": LocalJSX.IntrinsicElements["md-rating"] & JSXBase.HTMLAttributes<HTMLMdRatingElement>;
            "md-ripple": LocalJSX.IntrinsicElements["md-ripple"] & JSXBase.HTMLAttributes<HTMLMdRippleElement>;
            /**
             * Material Design 3 Search.
             * Implements the M3 Search specification
             * (https://m3.material.io/components/search/specs).
             * Two orthogonal axes describe every spec configuration:
             *  - `variant`: `contained` (M3 Expressive default) | `divided` (baseline)
             *  - `layout` : `full-screen` (modal overlay) | `docked` (anchored panel)
             * The component renders a single resting bar plus a results panel. When the
             * `full-screen` layout is `open` the bar pins itself to the top of the
             * viewport via `position: fixed` so consumers don't need a portal — CSS does
             * the work, and the input keeps a stable identity throughout the open/close
             * lifecycle.
             */
            "md-search": LocalJSX.IntrinsicElements["md-search"] & JSXBase.HTMLAttributes<HTMLMdSearchElement>;
            "md-segmented-button": LocalJSX.IntrinsicElements["md-segmented-button"] & JSXBase.HTMLAttributes<HTMLMdSegmentedButtonElement>;
            "md-segmented-button-set": LocalJSX.IntrinsicElements["md-segmented-button-set"] & JSXBase.HTMLAttributes<HTMLMdSegmentedButtonSetElement>;
            /**
             * `md-select` — Material Design 3 single-select dropdown.
             * Composes the library's own primitives instead of re-implementing them:
             *   - `md-text-field` is the trigger surface, so the picker inherits the
             *     full filled/outlined visuals, density scale, floating label, error
             *     state, supporting text, and focus ring of every other input. It is
             *     `readonly` (not editable) and `appear-focused` while the menu is open
             *     so the field + menu read as one connected control.
             *   - `md-menu` is the option surface, so the picker inherits open/close
             *     motion, viewport-aware positioning, single-open coordination, roving
             *     tabindex, type-ahead, and Escape/Home/End — for free.
             * Options are authored either declaratively as slotted `<md-select-option>`
             * children (primary API) or via the programmatic `options` array. Each
             * becomes a `type="radio"` `md-menu-item` so only one is ever selected.
             */
            "md-select": LocalJSX.IntrinsicElements["md-select"] & JSXBase.HTMLAttributes<HTMLMdSelectElement>;
            /**
             * `md-select-option` — declarative option for `md-select` / `md-multi-select`.
             * This element is a **data carrier**: it renders nothing visible (the host
             * is `display: none`). The parent picker reads its `value`, label (the
             * `label` prop or slotted text), `disabled`, `icon`, and `supporting-text`
             * and projects them into the `md-menu` dropdown as `md-menu-item`s.
             * Authoring:
             * ```html
             * <md-select label="Colour">
             *   <md-select-option value="red" icon="circle">Red</md-select-option>
             *   <md-select-option value="green" disabled>Green</md-select-option>
             * </md-select>
             * ```
             * Whenever an option is added, removed, or has its data changed, it emits a
             * bubbling `mdSelectOptionChange` event so the parent re-reads its option set.
             */
            "md-select-option": LocalJSX.IntrinsicElements["md-select-option"] & JSXBase.HTMLAttributes<HTMLMdSelectOptionElement>;
            /**
             * MD3 Side sheet — anchored panel for secondary content and actions.
             */
            "md-side-sheet": LocalJSX.IntrinsicElements["md-side-sheet"] & JSXBase.HTMLAttributes<HTMLMdSideSheetElement>;
            "md-skeleton": LocalJSX.IntrinsicElements["md-skeleton"] & JSXBase.HTMLAttributes<HTMLMdSkeletonElement>;
            /**
             * MD3 Expressive slider: standard (from edge), centered (from midpoint), or range (dual thumb).
             * **Uncontrolled (default)** — the slider manages its own value. Listen to `mdChange` to observe commits.
             * **Controlled** (`controlled`) — the slider never mutates `value`/`valueStart`/`valueEnd`.
             * Listen to events and set the props yourself.  During drag the slider uses internal live
             * state for smooth visuals; on commit it snaps to the prop values.
             */
            "md-slider": LocalJSX.IntrinsicElements["md-slider"] & JSXBase.HTMLAttributes<HTMLMdSliderElement>;
            "md-snackbar": LocalJSX.IntrinsicElements["md-snackbar"] & JSXBase.HTMLAttributes<HTMLMdSnackbarElement>;
            /**
             * md-sparkline — compact inline chart, rendered by the in-house
             * engine (Canvas2D). No ECharts.
             * Sparklines are presentational: no axes, no legend, no title, no
             * tooltip by default (opt in with `show-tooltip`). Line / area drive
             * LineChartEngine in `compact` mode; the bar variant drives
             * BarChartEngine. `show-marks` maps to selective extremes/edges dots.
             */
            "md-sparkline": LocalJSX.IntrinsicElements["md-sparkline"] & JSXBase.HTMLAttributes<HTMLMdSparklineElement>;
            "md-split-button": LocalJSX.IntrinsicElements["md-split-button"] & JSXBase.HTMLAttributes<HTMLMdSplitButtonElement>;
            /**
             * `md-status-dot` — a small absolutely-positioned indicator that pairs
             * with an avatar (or any other tile) to surface presence / state.
             * Designed to drop straight into a `position: relative` parent:
             * ```html
             * <span style="position: relative; display: inline-flex;">
             *   <md-avatar name="Ada Lovelace" label="Ada Lovelace, online"></md-avatar>
             *   <md-status-dot state="online"></md-status-dot>
             * </span>
             * ```
             * Anchors at the bottom-inline-end corner via logical-property insets, so
             * the pip auto-flips in RTL with no extra CSS. A 2 px outline in the
             * surface color keeps the dot visually detached from the avatar tile
             * regardless of the avatar's palette.
             * **Accessibility.** The dot is **decorative by default** — when used to
             * decorate an avatar, the avatar's own `label` should already encode the
             * status (`label="Ada Lovelace, online"`). Setting an explicit `label`
             * exposes the dot to assistive tech:
             * - **static** (`live` off) → `role="img"` + `aria-label` — a labelled image
             *   conveying the current state, announced when focused/encountered.
             * - **live** (`live` on) → `role="status"` (an `aria-live="polite"` region) so
             *   presence *changes* (online → busy) are announced as they happen.
             * **WCAG 1.4.1 (use of colour).** State is conveyed by colour alone, and in
             * Windows High-Contrast every state collapses to a single system colour. Never
             * rely on the dot's colour as the *only* signal of state — pair it with a
             * `label` (or text on the parent) so the status is also available as words.
             * **Live indicator.** When `live` is `true`, the dot pulses outward in its
             * own colour (online pulses green, busy pulses red, etc.). The animation
             * is gated behind `prefers-reduced-motion: no-preference`, so users who
             * have reduced-motion enabled at the OS level see a static dot.
             */
            "md-status-dot": LocalJSX.IntrinsicElements["md-status-dot"] & JSXBase.HTMLAttributes<HTMLMdStatusDotElement>;
            /**
             * `md-step` — one step inside an `<md-stepper>`.
             * Layout, position and i18n are pushed down from the parent stepper via
             * `data-*` attributes, so a step is almost always authored declaratively:
             * ```html
             * <md-stepper active="1">
             *   <md-step label="Account" completed></md-step>
             *   <md-step label="Shipping" description="Address & method"></md-step>
             *   <md-step label="Payment" optional></md-step>
             * </md-stepper>
             * ```
             */
            "md-step": LocalJSX.IntrinsicElements["md-step"] & JSXBase.HTMLAttributes<HTMLMdStepElement>;
            /**
             * `md-stepper` — Material Design 3 stepper. Orchestrates a set of slotted
             * `md-step` children: it owns the active index and navigation, and pushes
             * layout / i18n down to each step.
             * **Controlled, with optional auto-progress.** The stepper owns `active`
             * (updated on click / `next` / `prev` / `goTo`, emitting `mdStepChange`). With
             * `auto-complete` (default), advancing marks the step you leave as `completed`,
             * so the progress line + checks "just work" — no manual wiring. Set
             * `auto-complete="false"` to own `completed` yourself.
             * ```html
             * <md-stepper active="0">
             *   <md-step label="Account"><p>…</p></md-step>
             *   <md-step label="Shipping" optional><p>…</p></md-step>
             *   <md-step label="Payment" editable><p>…</p></md-step>
             * </md-stepper>
             * ```
             */
            "md-stepper": LocalJSX.IntrinsicElements["md-stepper"] & JSXBase.HTMLAttributes<HTMLMdStepperElement>;
            "md-sub-menu-item": LocalJSX.IntrinsicElements["md-sub-menu-item"] & JSXBase.HTMLAttributes<HTMLMdSubMenuItemElement>;
            /**
             * `md-switch` — Material Design 3 switch (on/off toggle).
             * Form-associated: inside a `<form>` it submits its `value` under `name` when
             * selected, and restores on form reset. It is a *labelable* element — wrap it in
             * a `<label>` (clicks on the label text toggle it and name it), or pass
             * `aria-label` / `aria-labelledby` on the host.
             */
            "md-switch": LocalJSX.IntrinsicElements["md-switch"] & JSXBase.HTMLAttributes<HTMLMdSwitchElement>;
            "md-tab": LocalJSX.IntrinsicElements["md-tab"] & JSXBase.HTMLAttributes<HTMLMdTabElement>;
            /**
             * A single tab panel. Place inside `md-tab-panels`, one per tab, in tab
             * order. The parent manages `active`, `inert`, and the tabs↔panel ARIA
             * wiring; inactive panels stay in layout (hidden) so the region's size
             * never jumps between tabs.
             */
            "md-tab-panel": LocalJSX.IntrinsicElements["md-tab-panel"] & JSXBase.HTMLAttributes<HTMLMdTabPanelElement>;
            /**
             * Panel region for `md-tabs`: place directly AFTER the tablist (or point
             * `for` at its id) with one `md-tab-panel` per tab, in tab order.
             * - Shows the panel matching the tabs' `activeTabIndex`, following
             *   `mdTabChange` automatically.
             * - `sizing="stable"` (default): all panels stack in one grid cell and
             *   inactive ones stay in layout (hidden), so the region — and any
             *   shrink-to-fit card around it — keeps the SAME width/height no matter
             *   which tab is active. `sizing="active"` sizes to the active panel only.
             * - Wires the APG ARIA contract both ways: each tab gets `aria-controls`,
             *   each panel gets `aria-labelledby`, ids auto-assigned where missing.
             */
            "md-tab-panels": LocalJSX.IntrinsicElements["md-tab-panels"] & JSXBase.HTMLAttributes<HTMLMdTabPanelsElement>;
            /**
             * Material Design 3 — Table
             * The core composable table primitive for hand-authored tables.
             * Layout architecture: the shadow `.md-table__grid` owns the column tracks
             * (`grid-template-columns`), and every slotted `md-table-row` is a REAL grid
             * item spanning all tracks that lays its cells on the shared tracks via
             * `grid-template-columns: subgrid`. Rows being real boxes is what makes row
             * hover, stripes, dividers, sticky headers and focus rings actually work —
             * and a wrong cell count skews one row, never the whole table.
             * Coordination is push-based (the house pattern): the table stamps context
             * onto its rows (`rowgroup`, stripe parity, stickiness) and pushes sort state
             * into `md-table-sort-label`s — no MutationObservers, no `:host-context()`.
             * High-level features:
             *   - Density: `compact` | `standard` | `comfortable`
             *   - Sticky header / footer (pair with `md-table-container` for the scroll box)
             *   - Sticky first / last column via cell `sticky="start" | "end"`
             *   - Sortable columns via `<md-table-sort-label>` (3-state asc → desc → none)
             *   - Selection coordination — emits `mdSelectionChange`; `selectAll()` /
             *     `deselectAll()` / `toggleSelectAll()` for the select-all checkbox
             *   - Horizontal scroll when the content is wider than the container
             *   - Loading + empty-state slots, striped rows, configurable dividers
             */
            "md-table": LocalJSX.IntrinsicElements["md-table"] & JSXBase.HTMLAttributes<HTMLMdTableElement>;
            /**
             * Material Design 3 — Table Body (`<tbody>`).
             * Pure presentational rowgroup. Children flow into the parent grid via
             * `display: contents`.
             */
            "md-table-body": LocalJSX.IntrinsicElements["md-table-body"] & JSXBase.HTMLAttributes<HTMLMdTableBodyElement>;
            /**
             * Material Design 3 — Table Cell.
             * The visual surface for a single grid cell. Renders as either a `cell`
             * (default) or `columnheader` / `rowheader` based on `head` / `scope`.
             * Props are intentionally MUI-compatible:
             *   - `align="start" | "center" | "end" | "justify"`
             *   - `numeric` (right-aligns + tabular numbers)
             *   - `padding="none" | "checkbox" | "default"`
             *   - `head` (renders as `<th>` semantics with `scope="col"|"row"`)
             *   - `sticky="start" | "end"` (sticky first / last column)
             *   - `colspan` / `rowspan` (grid spans)
             * The cell is a dumb surface: row state (selected / disabled / header
             * context) arrives as inherited CSS custom properties cast by
             * `<md-table-row>` — no observers, no DOM queries.
             */
            "md-table-cell": LocalJSX.IntrinsicElements["md-table-cell"] & JSXBase.HTMLAttributes<HTMLMdTableCellElement>;
            /**
             * Material Design 3 — Table Container
             * The outer surface that wraps a `<md-table>`, optional `<md-table-toolbar>`
             * and `<md-table-pagination>`. Provides elevation, shape, and the scroll
             * context (with optional max-height) for sticky headers / columns.
             * Compose freely:
             * ```html
             * <md-table-container variant="elevated" max-height="480px">
             *   <md-table-toolbar headline="Users"></md-table-toolbar>
             *   <md-table>…</md-table>
             *   <md-table-pagination></md-table-pagination>
             * </md-table-container>
             * ```
             */
            "md-table-container": LocalJSX.IntrinsicElements["md-table-container"] & JSXBase.HTMLAttributes<HTMLMdTableContainerElement>;
            /**
             * Material Design 3 — Table Expand Toggle.
             * The STANDARD expand caret for an expandable `<md-table-row>`: one consistent
             * size (xs / 32px — fits every density incl. compact rows, matching the 32px
             * checkbox precedent), a chevron that rotates 90° on the spatial spring when
             * the row opens, wired click→row.toggle() and aria-expanded out of the box.
             * Slot it INSIDE a cell of the expandable row (never as a bare row child —
             * rows are subgrid containers where any extra child would consume a column
             * track):
             * ```html
             * <md-table-row expandable>
             *   <md-table-cell padding="checkbox"><md-table-expand-toggle></md-table-expand-toggle></md-table-cell>
             *   <md-table-cell>…</md-table-cell>
             *   <div slot="expanded">Details…</div>
             * </md-table-row>
             * ```
             */
            "md-table-expand-toggle": LocalJSX.IntrinsicElements["md-table-expand-toggle"] & JSXBase.HTMLAttributes<HTMLMdTableExpandToggleElement>;
            /**
             * Material Design 3 — Table Foot (`<tfoot>`).
             * Pure presentational rowgroup for footer rows (totals, summaries).
             * Children flow into the parent grid via `display: contents`.
             */
            "md-table-foot": LocalJSX.IntrinsicElements["md-table-foot"] & JSXBase.HTMLAttributes<HTMLMdTableFootElement>;
            /**
             * Material Design 3 — Table Head (`<thead>`).
             * Pure presentational rowgroup used to mark a region of column-header rows.
             * Applies the head background (sticky-friendly) and ensures children flow
             * into the parent `<md-table>`'s CSS Grid via `display: contents`.
             */
            "md-table-head": LocalJSX.IntrinsicElements["md-table-head"] & JSXBase.HTMLAttributes<HTMLMdTableHeadElement>;
            /**
             * Material Design 3 — Table Pagination.
             * Drop into a `<md-table-container slot="bottom">` for a complete data table.
             * Mirrors the MUI `<TablePagination>` API surface. Built from house
             * primitives: `md-icon-button` navigation and an `md-select` rows-per-page
             * picker (proper dismissal, keyboard support and theming for free).
             * Emits:
             *   - `mdPageChange` — when the page changes
             *   - `mdRowsPerPageChange` — when the rows-per-page selector changes
             */
            "md-table-pagination": LocalJSX.IntrinsicElements["md-table-pagination"] & JSXBase.HTMLAttributes<HTMLMdTablePaginationElement>;
            /**
             * Material Design 3 — Table Row.
             * A row inside a `<md-table>` that contains `<md-table-cell>` children.
             * The host is a REAL grid box that spans the table's tracks and lays its
             * cells on them via `grid-template-columns: subgrid` — so row hover,
             * stripes, dividers, selection tint, sticky rows and focus rings all work
             * on the row itself, and a wrong cell count skews one row, never the table.
             * The parent `<md-table>` stamps context (`rowgroup`, stripe parity,
             * stickiness) onto the row; the row casts typography/color context down to
             * its cells with `::slotted()` custom properties — no observers.
             * Behaviors:
             * - `selected` — selection state (visual + ARIA)
             * - `disabled` — non-interactive
             * - `clickable` — adds keyboard activation (Enter / Space) + emits `mdRowClick`
             * - `expandable` — toggles an "expanded details" zone (use the `expanded` slot),
             *   spring-animated via the grid-rows height pattern
             */
            "md-table-row": LocalJSX.IntrinsicElements["md-table-row"] & JSXBase.HTMLAttributes<HTMLMdTableRowElement>;
            /**
             * Material Design 3 — Table Sort Label.
             * A clickable header label with a sort indicator that reflects the current
             * sort state. Place inside a `<md-table-cell head>`.
             * Wiring:
             *   1. Set `column="<id>"`
             *   2. The parent `<md-table>` listens for `mdSortRequest` (auto-bubbled),
             *      updates its `sort-by` / `sort-order`, and PUSHES the resulting
             *      `active` / `order` back into every sort label — no observers.
             * The arrow is hidden at rest and fades in at 60% opacity on hover or keyboard
             * focus, so an unsorted column advertises sorting only when it is pointed at.
             * Once the column is the sort key the arrow stays fully opaque, tinted with the
             * active colour, and rotates 180° to point down for descending. The rotation
             * runs on the spatial spring, the colour/opacity on the effects spring.
             */
            "md-table-sort-label": LocalJSX.IntrinsicElements["md-table-sort-label"] & JSXBase.HTMLAttributes<HTMLMdTableSortLabelElement>;
            /**
             * Material Design 3 — Table Toolbar.
             * The header bar above a `<md-table>` (typically dropped into a
             * `<md-table-container slot="top">`). Shows a title + supporting text and
             * a row of action items. Switches into a "selection" mode when one or more
             * rows are selected (matching the MUI / MD3 data-table pattern).
             * Two modes:
             *   - Default — shows the headline + supporting text + actions
             *   - Selection — shows "%count% selected" + selection-actions (when
             *     `num-selected > 0`)
             * The toolbar can listen to its closest `<md-table>`'s `mdSelectionChange`
             * event automatically (via `auto-bind`) — set `auto-bind` on the toolbar
             * and selection state will track the table without any glue code.
             */
            "md-table-toolbar": LocalJSX.IntrinsicElements["md-table-toolbar"] & JSXBase.HTMLAttributes<HTMLMdTableToolbarElement>;
            "md-tabs": LocalJSX.IntrinsicElements["md-tabs"] & JSXBase.HTMLAttributes<HTMLMdTabsElement>;
            "md-text-field": LocalJSX.IntrinsicElements["md-text-field"] & JSXBase.HTMLAttributes<HTMLMdTextFieldElement>;
            "md-time-picker": LocalJSX.IntrinsicElements["md-time-picker"] & JSXBase.HTMLAttributes<HTMLMdTimePickerElement>;
            /**
             * The toolbar host has no built-in actions — all behavior comes from slotted controls.
             * On the web, the default is `role="toolbar"` implementing the WAI-ARIA toolbar pattern
             * with an ADAPTIVE roving tabindex:
             *  - **Activator buttons** (`md-icon-button`, `md-button`, native `button`/`a[href]`,
             *    `role=button`/`menuitem`/`link`) form a single roving group — one Tab stop, and
             *    ArrowLeft/Right (or Up/Down when floating+vertical), Home, End move focus between them.
             *  - **Everything else** is left as its OWN Tab stop with its native behavior — text-entry /
             *    value controls (input, textarea, contenteditable, md-text-field, md-select, `select`,
             *    range/spinbutton), ALL toggles (`md-switch`/`md-checkbox`/`md-chip`, native
             *    checkbox/radio, role=checkbox/radio), and self-managing composites (md-button-group,
             *    md-menu, role=group/radiogroup/listbox/menu). Arrows are never stolen from them, and
             *    they never become roving dead-ends.
             * A soft-disabled (`aria-disabled`) group member stays arrow-reachable (APG: disabled
             * items remain focusable for discoverability). The default Tab stop prefers an enabled
             * control, falling back to the first member only when every member is soft-disabled;
             * focusing any member makes it the stop.
             * This keeps every control keyboard-reachable regardless of the mix, per APG guidance for
             * toolbars that contain text fields. Use `generic` for a non-semantic container (no role,
             * no roving).
             */
            "md-toolbar": LocalJSX.IntrinsicElements["md-toolbar"] & JSXBase.HTMLAttributes<HTMLMdToolbarElement>;
            "md-tooltip": LocalJSX.IntrinsicElements["md-tooltip"] & JSXBase.HTMLAttributes<HTMLMdTooltipElement>;
            /**
             * `md-transfer-list` — Material Design 3 dual-list selector.
             * Reference parity: https://mui.com/material-ui/react-transfer-list/.
             * Two columns ("source" and "target") with header chips that show the
             * "{n} / {total} selected" count, a search field per side, paired
             * `md-checkbox` rows, and four `md-icon-button` movers in the centre
             * column. Selection inside each side is independent of which side a
             * card lives in; the move buttons only act on items that are both
             * selected AND visible (i.e. matching the search filter).
             * MD3 Expressive: containers are `surface-container-low`, the headers
             * use the `secondary-container` tone for emphasis when the title slot
             * is populated, and movements run on the `motion-emphasized` curve.
             */
            "md-transfer-list": LocalJSX.IntrinsicElements["md-transfer-list"] & JSXBase.HTMLAttributes<HTMLMdTransferListElement>;
        }
    }
}
