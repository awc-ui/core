{
  "name": "@awc-ui/smart-home",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Habitat at Maple House — interactive smart home showcase built with AWC UI",
  "scripts": {
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs",
    "lint": "node --check src/app.js && node --check src/model.js && node --check src/views.js && node --check src/i18n.js && node --check src/preferences.js && node --check src/webmcp.js",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "@awc-ui/core": "workspace:*"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
