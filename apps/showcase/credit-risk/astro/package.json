{
  "name": "@awc-ui/showcase-credit-risk-astro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Credit Risk Console \u2014 Astro build (static, server-rendered shadow DOM, locale-routed)",
  "scripts": {
    "sync-runtime": "node scripts/sync-runtime.mjs",
    "dev": "pnpm sync-runtime && astro dev",
    "build": "pnpm sync-runtime && astro build",
    "preview": "astro preview",
    "lint": "astro check",
    "verify": "node scripts/verify-browser.mjs",
    "serve": "node scripts/serve-dist.mjs"
  },
  "dependencies": {
    "@awc-ui/core": "workspace:*",
    "@awc-ui/showcase-kit": "workspace:*",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.4.0",
    "puppeteer": "^25.11.0"
  }
}
