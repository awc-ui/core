---
title: Installation
description: Install AWC UI Web Components for HTML, React, Angular, Vue, or Svelte, with framework packages and SSR support for Next.js, Nuxt, and SvelteKit.
sidebar:
  order: 1
---

import TechnologyTabs from "../../../components/TechnologyTabs.astro";

## Package Manager

Install the core component library and design tokens:

```bash
npm install @awc-ui/core @awc-ui/tokens
```

Or with other package managers:

```bash
# pnpm
pnpm add @awc-ui/core @awc-ui/tokens

# yarn
yarn add @awc-ui/core @awc-ui/tokens
```

## Technology Wrappers

For the best developer experience, install the wrapper package for your technology:

<TechnologyTabs
  html={`# No wrapper needed — use the core package directly
npm install @awc-ui/core @awc-ui/tokens`}
  react={`npm install @awc-ui/core @awc-ui/tokens @awc-ui/react`}
  angular={`npm install @awc-ui/core @awc-ui/tokens @awc-ui/angular`}
  vue={`npm install @awc-ui/core @awc-ui/tokens @awc-ui/vue`}
  svelte={`npm install @awc-ui/core @awc-ui/tokens @awc-ui/svelte`}
/>

## Required Fonts

Add these to your HTML `<head>` for icons and typography:

```html
<!-- Material Symbols Outlined — the icon font the library is built around.
     The four axis ranges are required: a default request returns a static
     instance and the FILL transition silently stops working. -->
<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap"
/>

<!-- Roboto (default MD3 body font) -->
<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap"
/>
```

:::caution[Subset the icon font for production]
The request above downloads the **entire** variable icon font — close to
4 MB. That is convenient while prototyping (every icon just works) but is by
far the largest transfer an AWC UI app makes. For production, list the icons
you actually use with `icon_names` — Google Fonts then serves a subset font
of a few kB with the same variable axes, so `FILL` transitions keep working:

```html
<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=add,delete,menu,search&display=block"
/>
```

Two rules: the `icon_names` list must be **alphabetically sorted** (the API
rejects unsorted lists), and prefer `display=block` for icon fonts — with
`swap`, users see the raw ligature text ("search") until the font arrives.
Audit your icons with a grep over `icon=` attributes and slotted
`material-symbols-outlined` spans when the list drifts.
:::

Or install from npm:

```bash
npm install material-icons material-symbols
```

```ts
import "material-icons/iconfont/material-icons.css";
import "material-symbols/outlined.css";
```

## Component Registration

There are three ways to register components:

### Option A: Auto-register all (recommended)

```ts
import "@awc-ui/core/define";
```

This registers every component and loads the AWC UI design-token stylesheet.

### Option B: Lazy-load via the loader

```ts
import { defineCustomElements } from "@awc-ui/core/loader";
defineCustomElements(window);
```

Components are registered on-demand when first encountered in the DOM.

### Option C: Per-component tree-shaking

```ts
import "@awc-ui/core/components/md-button";
import "@awc-ui/core/components/md-text-field";
```

Only the imported components are included in your bundle.

## Design Tokens

The tokens CSS is automatically loaded with Option A. For Options B and C, import the tokens separately:

```ts
import "@awc-ui/tokens/tokens.css";
```

This provides all the `--md-sys-*` CSS custom properties for colors, typography, shape, elevation, and motion.

## Preventing Layout Shift

Components register before their code arrives. With Option A or B the runtime
defines every tag at bootstrap and then fetches each component's chunk on
demand — so for a moment a layout-critical element like `md-navigation-rail`
exists in the DOM with none of its own CSS, occupying no space. When the chunk
lands, everything around it moves.

Opt in to size floors that hold the space in advance:

```ts
import "@awc-ui/core/css/tokens.css";
import "@awc-ui/core/css/pre-upgrade.css";
```

The sheet reserves the settled box of the components that carry page
layout — `md-navigation-rail`, `md-app-bar`, `md-toolbar`, `md-navigation-bar`
and `md-fab` — using each component's own public custom property and default, so
a reservation cannot drift from the thing it reserves for. They are minimums, so
a component that settles at its floor does not move at all, and one that settles
larger simply overrides it.

Every rule is gated on `:not(.hydrated)` and stops applying the moment that
element renders.

### `:defined` is the wrong gate

If you write reservations of your own, gate them on `.hydrated` — not
`:not(:defined)`. The lazy runtime calls `customElements.define()` for every tag
in one pass at bootstrap and only then fetches the chunks, so `:defined` flips
while the element is still an empty box: the reservation evaporates before the
styles it was standing in for arrive. `.hydrated` is the per-element flag the
runtime sets when a component has rendered, which is the moment its box becomes
real.

This is a floor, not a measurement: it cannot know how wide a button's label
will make it, or how tall a table will be once its data loads. It removes the
shift that comes from a component having no size at all.

## Next Steps

- [Quick Start](/getting-started/quick-start/) — build your first page with AWC UI components
- [Web Components](/frameworks/web-components/) — use custom elements in plain HTML or any framework
- [React](/frameworks/react/), [Angular](/frameworks/angular/), [Vue](/frameworks/vue/), or [Svelte](/frameworks/svelte/) — framework-specific setup and SSR
- [Components](/components/) — browse all 56 MD3 components, each with HTML, React, Angular, Vue, and Svelte tabs in-page
- [Theming](/theming/overview/) — customize colors, shapes, and typography
