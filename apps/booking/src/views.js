import { properties, quote, nights } from "./model.js";

export const escape = (value = "") => String(value).replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]);
export const money = (value) => new Intl.NumberFormat("en-IE", { style: "currency", currency: "EUR", maximumFractionDigits: Number.isInteger(value) ? 0 : 2 }).format(value);
export const date = (value) => new Intl.DateTimeFormat("en-GB", { day: "numeric", month: "short", year: "numeric", timeZone: "UTC" }).format(new Date(`${value}T12:00:00Z`));
export const icon = (name, className = "") => `<span class="material-symbols-outlined ${className}" aria-hidden="true">${name}</span>`;
const selected = (value) => value ? "selected" : "";
const checked = (value) => value ? "checked" : "";
const image = (src, alt, className = "", eager = false) => `<img class="${className}" src="${escape(src)}" alt="${escape(alt)}" loading="${eager ? "eager" : "lazy"}" decoding="async" />`;
const saveButton = (p, saved) => `<md-tooltip text="${saved ? "Remove saved stay" : "Save this stay"}"><md-icon-button class="save-button" data-action="save" data-id="${p.id}" toggle ${selected(saved)} icon="favorite_border" selected-icon="favorite" variant="tonal" aria-label="${saved ? "Unsave" : "Save"} ${escape(p.name)}"></md-icon-button></md-tooltip>`;

export function shell(profile) {
  return `<md-app-bar class="site-header">
    <div slot="headline" class="header-content"><a class="wordmark" href="#/" aria-label="Roam home">roam<span>✳</span></a><md-navigation-bar id="main-navigation" class="main-navigation" aria-label="Main navigation" manual-activation><md-navigation-tab label="Stays" icon="bed" active-icon="bed" href="#/"></md-navigation-tab><md-navigation-tab id="saved-navigation" label="Saved" icon="favorite_border" active-icon="favorite" href="#/saved"></md-navigation-tab><md-navigation-tab label="My trips" icon="luggage" active-icon="luggage" href="#/trips"></md-navigation-tab></md-navigation-bar></div>
    <div slot="trailing" class="header-actions"><span class="currency-label">EUR</span><md-tooltip text="Make it yours"><md-icon-button data-action="preferences" icon="tune" aria-label="Make it yours"></md-icon-button></md-tooltip><div id="account">${account(profile)}</div></div>
  </md-app-bar>
  <main id="main" tabindex="-1"></main>
  <footer class="site-footer"><div class="footer-inner"><div><a class="wordmark" href="#/">roam<span>✳</span></a><p>A little further from everyday.</p></div><nav aria-label="Footer"><a href="#/search">Explore stays</a><a href="#/saved">Your wishlist</a><a href="#/trips">Manage trips</a><md-button variant="text" data-action="help">About this demo</md-button></nav></div><div class="footer-bottom"><span>Made for the journey. Built with <a href="https://awc-ui.dev/" target="_blank" rel="noopener">awc-ui</a>.</span><span>Fictional stays & sample prices · No real bookings or payments</span></div></footer>`;
}
export function account(profile) {
  if (!profile) return `<md-button variant="text" data-action="login" class="signin">Sign in</md-button><md-button variant="filled" data-action="signup">Join Roam</md-button>`;
  const name = profile.name || profile.firstName || "Traveler";
  return `<md-avatar name="${escape(name)}" size="small"></md-avatar><md-button variant="text" data-action="profile">${escape(name.split(" ")[0])}</md-button>`;
}
export function searchForm(search, compact = false) {
  const today = new Date().toISOString().slice(0, 10);
  return `<section class="search-wrap ${compact ? "compact-search" : ""}" aria-label="Find a place to stay"><md-card variant="elevated" class="search-panel" full-width><form id="search-form" novalidate>
    <md-text-field id="destination" name="destination" label="Where to?" placeholder="City or property in Portugal" variant="outlined" value="${escape(search.destination)}" clearable="internal"><span slot="leading-icon" class="material-symbols-outlined" aria-hidden="true">location_on</span></md-text-field>
    <md-date-picker id="check-in" name="checkIn" label="Check-in" locale="en-GB" variant="docked" supporting-text=" " value="${search.checkIn}" min="${today}" commit-on-select required></md-date-picker>
    <md-date-picker id="check-out" name="checkOut" label="Check-out" locale="en-GB" variant="docked" supporting-text=" " value="${search.checkOut}" min="${search.checkIn}" commit-on-select required></md-date-picker>
    <md-button class="guest-trigger" shape-morph="false" size="md" variant="outlined" data-action="guests" icon="group" trailing-icon="arrow_drop_down" aria-label="Choose guests and rooms" aria-haspopup="dialog"><span><small>Travelers</small><strong id="guest-summary" dir="auto">${search.adults + search.children} guests · ${search.rooms} room${search.rooms > 1 ? "s" : ""}</strong></span></md-button>
    <md-button type="submit" size="md" icon="search" id="search-submit">Search<span slot="loader"><md-progress-indicator variant="circular" indeterminate size="24" label="Searching stays"></md-progress-indicator></span></md-button>
    <p id="search-error" class="form-error" role="alert" hidden></p>
  </form></md-card></section>`;
}

export function home(state) {
  const picks = [properties.find(p => p.id === "algarve-cove"), properties.find(p => p.id === "lisbon-azulejo"), properties.find(p => p.id === "porto-douro"), properties.find(p => p.id === "algarve-villa")].filter(Boolean);
  return `<section class="hero" aria-labelledby="hero-title">${image("./images/coast.jpg", "Turquoise Atlantic water and golden cliffs in Lagos, Portugal", "hero-image", true)}<div class="hero-shade"></div><div class="hero-copy"><p class="eyebrow">A GOOD STAY IS JUST THE BEGINNING</p><h1 id="hero-title">Find your<br><em dir="auto">somewhere.</em></h1><p>City mornings. Coastal hideaways.<br>Places that feel a little more like you.</p><md-button variant="elevated" size="md" data-action="explore" data-destination="">Explore Portugal <span slot="trailing-icon" class="material-symbols-outlined" aria-hidden="true">arrow_forward</span></md-button></div><div class="hero-location"><md-chip appearance="elevated" icon="location_on" label="Lagos, Portugal" data-action="explore" data-destination="Algarve"></md-chip><span>YOUR NEXT CHAPTER, RIGHT HERE</span></div></section>
    <div class="page-width">${searchForm(state.search)}<div class="search-hints"><span>Somewhere in mind?</span>${["Lisbon", "Porto", "Algarve"].map(city => `<md-chip variant="suggestion" label="${city}" data-action="explore" data-destination="${city}"></md-chip>`).join("")}<span class="flexible-note">${icon("event_available")}Flexible plans. Memorable stays.</span></div>
    <section class="home-section" aria-labelledby="destinations-heading"><div class="section-heading"><div><p class="eyebrow">FOLLOW YOUR CURIOSITY</p><h2 id="destinations-heading">Some places just stay with you.</h2><p>Three corners of Portugal. A thousand ways to feel away.</p></div><md-button variant="text" size="sm" href="#/search" trailing-icon="arrow_forward" mirror-icon>Explore all stays</md-button></div><div class="destination-grid">${[
      {name:"Lisbon",image:"lisbon",line:"Golden light & city life",detail:"Boutique hotels & easy mornings"},
      {name:"Porto",image:"porto",line:"A slower kind of city",detail:"Riverside rooms & hidden courtyards"},
      {name:"Algarve",image:"coast",line:"Your out-of-office coast",detail:"Beach escapes & sun-filled villas"},
    ].map(d => `<md-card interactive variant="filled" full-width class="destination-card" data-action="explore" data-destination="${d.name}" aria-label="Explore stays in ${d.name}">${image(`./images/${d.image}.jpg`, `${d.name}, Portugal`)}<div class="destination-overlay"><div><span>${d.line}</span><h3>${d.name}</h3><p>${d.detail}</p></div>${icon("north_east")}</div></md-card>`).join("")}</div></section>
    <section class="home-section" aria-labelledby="picks-heading"><div class="section-heading"><div><p class="eyebrow">A STAY TO LOOK FORWARD TO</p><h2 id="picks-heading">Good places. Great possibilities.</h2><p>A few favorites from our collection of imagined escapes.</p></div><div class="picks-controls" role="group" aria-label="Filter featured stays"><md-chip variant="filter" label="All stays" selected data-feature="all"></md-chip><md-chip variant="filter" label="By the coast" data-feature="coast"></md-chip></div></div><div id="featured-properties" class="property-grid">${picks.map(p => propertyCard(p, state)).join("")}</div></section>
    <md-card class="join-banner" variant="filled" full-width><div class="join-art" aria-hidden="true">${icon("travel_explore")}</div><div><p class="eyebrow">YOUR JOURNEY, ALL TOGETHER</p><h2>A wishlist today.<br>A story tomorrow.</h2><p>Save the places you love and keep every trip in one place.</p></div><md-button variant="filled" size="md" data-action="${state.profile ? "saved" : "signup"}">${state.profile ? "See saved stays" : "Join Roam"}</md-button></md-card>
    <section class="reassurance" aria-label="Booking benefits"><div>${icon("event_available")}<div><h3>Room for a change of plans</h3><p>Filter for stays with free cancellation.</p></div></div><div>${icon("receipt_long")}<div><h3>The full picture, upfront</h3><p>Nightly rates and a clear price breakdown.</p></div></div><div>${icon("favorite_border")}<div><h3>Find it. Love it. Save it.</h3><p>Your next escape starts with a wishlist.</p></div></div></section></div>`;
}

export function propertyCard(p, state, row = false) {
  let total = null;
  try { total = quote(p, state.search); } catch {}
  const rating = Number(p.rating).toFixed(1);
  return `<md-card variant="outlined" full-width class="property-card ${row ? "property-row" : ""}"><div class="property-media">${image(p.image || "./images/room.jpg", `Illustrative photograph for ${p.name}`)}${saveButton(p, state.saved.includes(p.id))}</div><div class="property-body"><div class="property-place">${escape(p.city)} · ${escape(p.type)}</div><h3><a href="#/property/${p.id}">${escape(p.name)}</a></h3><p class="property-area">${icon("location_on")}${escape(p.area)}</p><div class="rating-line"><md-chip label="${rating}" icon="star" appearance="filled" color="primary" data-action="reviews" data-id="${p.id}" aria-label="Sample guest rating ${rating} out of 10; read reviews"></md-chip><strong>${p.rating >= 9.3 ? "Exceptional" : p.rating >= 9 ? "Wonderful" : "Very good"}</strong><span>(${p.reviews})</span></div>${row ? `<p class="row-description">${escape(p.description)}</p><div class="amenity-inline">${p.amenities.slice(0,3).map(a => `<span>${icon(amenityIcon(a))}${escape(a)}</span>`).join("")}</div>` : ""}<div class="property-bottom"><div><p class="cancellation ${p.cancellation ? "" : "muted"}">${icon(p.cancellation ? "check" : "info")}${p.cancellation ? "Free cancellation" : "Non-refundable rate"}</p><div class="price"><strong>${money(p.nightly)}</strong><span> / room / night</span></div><p class="total-hint">${total ? `${money(total.total)} total · ${total.nights} nights · incl. demo tax` : "Adjust guests to see a total"}</p></div><md-button variant="${row ? "tonal" : "text"}" data-action="property" data-id="${p.id}" aria-label="View ${escape(p.name)}">${row ? "View stay" : "View"}</md-button></div></div></md-card>`;
}
export const amenityIcon = (name) => /wifi/i.test(name) ? "wifi" : /pool/i.test(name) ? "pool" : /breakfast/i.test(name) ? "bakery_dining" : /park/i.test(name) ? "local_parking" : /air/i.test(name) ? "ac_unit" : /beach|sea/i.test(name) ? "beach_access" : /kitchen/i.test(name) ? "kitchen" : /spa/i.test(name) ? "spa" : /terrace|garden/i.test(name) ? "yard" : "check_circle";

export function results(state, matches, savedOnly = false) {
  return `<div class="page-width results-page">${savedOnly ? "" : searchForm(state.search, true)}<div class="section-heading result-heading"><div><p class="eyebrow">${savedOnly ? "THE PLACES YOU'VE GOT YOUR EYE ON" : "A LITTLE CLOSER TO YOUR NEXT GETAWAY"}</p><h1>${savedOnly ? "Your saved stays" : state.search.destination ? `Stays in ${escape(state.search.destination)}` : "Somewhere in Portugal"}</h1><p id="results-count" role="status">${matches.length} ${savedOnly ? "saved" : "matching"} ${matches.length === 1 ? "stay" : "stays"}${savedOnly ? " · Make room for a little daydreaming." : ` · ${date(state.search.checkIn)} – ${date(state.search.checkOut)} · ${state.search.adults + state.search.children} guests`}</p></div>${savedOnly ? "" : `<md-select id="sort-order" label="Sort by" value="${state.filters.sort}" class="sort-order"><md-select-option value="recommended">Our picks for you</md-select-option><md-select-option value="price-asc">Price: lowest first</md-select-option><md-select-option value="rating">Highest rated</md-select-option></md-select>`}</div>
    <div class="results-layout ${savedOnly ? "saved-layout" : ""}">${savedOnly ? "" : `<aside class="filters" aria-label="Filter stays"><md-card variant="outlined" full-width><div class="filter-heading"><h2>Find your kind of stay</h2><md-button variant="text" size="xs" data-action="reset-filters">Reset</md-button></div><fieldset><legend>Your budget per night</legend><md-number-field id="max-price" label="Maximum (€)" variant="outlined" min="50" max="500" step="25" value="${state.filters.maxPrice}" supporting-text="Per room, before demo tax"></md-number-field></fieldset><fieldset><legend>The little things that matter</legend>${checkFilter("freeCancellation","Free cancellation",state.filters.freeCancellation)}${checkFilter("breakfast","Breakfast included",state.filters.breakfast)}${checkFilter("topRated","Wonderful: 9+",state.filters.minRating >= 9)}</fieldset><fieldset><legend>Make yourself at home</legend>${[...new Set(properties.map(p => p.type))].map(type => `<label class="check-row"><md-checkbox data-type="${type}" ${checked(state.filters.types.includes(type))}></md-checkbox><span>${type}s</span></label>`).join("")}</fieldset><p class="small-note">This collection contains fictional properties and sample availability.</p></md-card></aside>`}<div id="property-results" class="${savedOnly ? "property-grid" : "result-list"}">${matches.length ? matches.map(p => propertyCard(p,state,!savedOnly)).join("") : empty(savedOnly ? "Your next escape starts here." : "No stays match just yet.",savedOnly ? "Tap the heart on a stay you love. We'll keep it here for you." : "Try another destination, a higher budget, or fewer filters.", savedOnly ? "Explore stays" : "Clear filters",savedOnly ? "browse" : "reset-filters",savedOnly ? "favorite_border" : "travel_explore")}</div></div></div>`;
}
function checkFilter(key,label,value) { return `<label class="check-row"><md-checkbox data-filter="${key}" ${checked(value)}></md-checkbox><span>${label}</span></label>`; }
export function empty(title,text,label,action,glyph="luggage") { return `<div class="empty-state">${icon(glyph)}<h2>${title}</h2><p>${text}</p><md-button variant="tonal" size="md" data-action="${action}">${label}</md-button></div>`; }

export function propertyDetail(p,state) {
  let price; try { price=quote(p,state.search); } catch {}
  const photos=[...new Set([p.image,...(p.images || []),"./images/room.jpg","./images/pool.jpg"])].filter(Boolean).slice(0,3);
  return `<div class="page-width detail-page"><md-button class="back-link" variant="text" size="sm" href="#/search" icon="arrow_back" mirror-icon>Back to stays</md-button><div class="section-heading"><div><p class="eyebrow">${escape(p.type)} · ${escape(p.city)}, PORTUGAL</p><h1>${escape(p.name)}</h1><p>${icon("location_on")}${escape(p.area)} · ${escape(p.city)}</p></div>${saveButton(p,state.saved.includes(p.id))}</div><div class="property-gallery">${photos.map((src,i)=>image(src,`Illustrative ${i === 0 ? "property" : "room and surroundings"} photo for ${p.name}`,"",i===0)).join("")}</div><div class="gallery-caption">Illustrative photography · Fictional property</div><div class="detail-layout"><div><section class="detail-section"><div class="rating-line"><md-chip icon="star" label="${Number(p.rating).toFixed(1)} / 10" appearance="filled" color="primary" data-action="scroll-reviews"></md-chip><strong>Wonderful stays start with the details.</strong><span>${p.reviews} sample reviews</span></div><h2>Settle into somewhere special.</h2><p class="lead-paragraph">${escape(p.description)}</p><div class="amenity-grid">${p.amenities.map(a => `<span>${icon(amenityIcon(a))}${escape(a)}</span>`).join("")}</div></section><section class="detail-section"><h2>Your stay, at a glance</h2><div class="stay-facts"><div>${icon("king_bed")}<strong>${p.bedrooms || 1} bedroom${p.bedrooms > 1 ? "s" : ""}</strong><span>per accommodation</span></div><div>${icon("group")}<strong>Up to ${p.capacity} guests</strong><span>per room or unit</span></div><div>${icon("schedule")}<strong>15:00 check-in</strong><span>11:00 check-out</span></div></div><h3>Good to know</h3><p>${p.cancellation ? "Free cancellation until the day before check-in. Plans can change; your stay can too." : "This sample rate is non-refundable. Once confirmed, the reservation cannot be cancelled in this demo."}</p><p>${p.breakfast ? "Breakfast is included in the room rate." : "Breakfast is not included in this room rate."} The demo accommodation tax is €2 per adult, per night.</p></section><section class="detail-section" id="reviews"><p class="eyebrow">LITTLE DETAILS, LASTING IMPRESSIONS</p><h2>Stories from a stay</h2><div class="review-grid">${[{name:"Emma L.",initials:"EL",text:"A lovely place to slow down. The light in the room and the little details made it feel special."},{name:"Oliver M.",initials:"OM",text:"Exactly the quiet escape we had in mind. An easy base for exploring, and a very comfortable bed."}].map(r => `<md-card variant="filled" full-width><div class="review-person"><md-avatar initials="${r.initials}" size="small"></md-avatar><strong>${r.name}</strong><span>Sample review</span></div><p>“${r.text}”</p></md-card>`).join("")}</div></section></div><aside class="reservation-column"><md-card variant="outlined" full-width class="reservation-card"><p class="eyebrow">MAKE A LITTLE TIME FOR AWAY</p><div class="price"><strong>${money(p.nightly)}</strong><span> / room / night</span></div><div class="reservation-dates"><div><small>CHECK-IN</small><strong>${date(state.search.checkIn)}</strong></div><div><small>CHECK-OUT</small><strong>${date(state.search.checkOut)}</strong></div></div><p>${state.search.adults} adults${state.search.children ? ` · ${state.search.children} children` : ""} · ${state.search.rooms} room${state.search.rooms>1?"s":""}</p><md-button variant="text" data-action="change-dates">Change dates or guests</md-button>${price ? priceBreakdown(price) : `<p class="form-error">This stay accommodates ${p.capacity} guests per room. Adjust your travelers to continue.</p>`}<md-button size="md" full-width data-action="reserve" data-id="${p.id}" ${price ? "" : "disabled"}>Reserve your stay</md-button><p class="small-note center">No payment needed. This is a demo reservation.</p><p class="cancellation">${icon(p.cancellation?"check_circle":"info")}${p.cancellation ? "Free cancellation before check-in" : "Non-refundable sample rate"}</p></md-card></aside></div></div>`;
}
export function priceBreakdown(q) {
  return `<dl class="price-breakdown"><div><dt>${money(q.nightly)} × ${q.nights} nights × ${q.rooms} room${q.rooms>1?"s":""}</dt><dd>${money(q.subtotal)}</dd></div><div><dt>Demo accommodation tax</dt><dd>${money(q.taxes)}</dd></div><div class="total"><dt>Total <small>(EUR)</small></dt><dd>${money(q.total)}</dd></div></dl>`;
}

export function checkout(p,state,step,guest) {
  const q=quote(p,state.search);
  return `<div class="page-width checkout-page"><md-button class="back-link" variant="text" size="sm" href="#/property/${p.id}" icon="arrow_back" mirror-icon>Back to your stay</md-button><div class="checkout-intro"><p class="eyebrow">GOOD THINGS ARE JUST A FEW DETAILS AWAY</p><h1>${step === 0 ? "Make it your getaway." : "One last look, then you're away."}</h1><md-stepper label="Reservation progress" active="${step}" nav="false" readonly><md-step label="Your details"></md-step><md-step label="Review stay"></md-step><md-step label="You're booked"></md-step></md-stepper></div><div class="checkout-layout"><md-card variant="outlined" full-width class="checkout-card">${step === 0 ? `<h2>Who's coming along?</h2><p>Use fictional details to try the booking experience.</p>${!state.profile ? `<md-button variant="tonal" data-action="login" icon="person">Sign in to fill your details</md-button>` : ""}<form id="guest-form" novalidate><div class="form-two"><md-text-field id="first-name" max-length="80" name="firstName" label="First name" variant="outlined" value="${escape(guest.firstName)}" autocomplete="given-name" required reserve-supporting-space></md-text-field><md-text-field id="last-name" max-length="80" name="lastName" label="Last name" variant="outlined" value="${escape(guest.lastName)}" autocomplete="family-name" required reserve-supporting-space></md-text-field></div><md-text-field id="guest-email" max-length="254" name="email" label="Email address" type="email" inputmode="email" variant="outlined" value="${escape(guest.email)}" autocomplete="email" required reserve-supporting-space supporting-text="A sample confirmation appears in My trips. No email is sent."></md-text-field><p id="booking-error" class="form-error" role="alert" hidden></p><md-button type="submit" size="md" full-width>Continue to review</md-button></form>` : `<h2>Everything in its right place.</h2><dl class="review-details"><div><dt>Guest</dt><dd>${escape(guest.firstName)} ${escape(guest.lastName)}</dd></div><div><dt>Email</dt><dd>${escape(guest.email)}</dd></div><div><dt>Dates</dt><dd>${date(state.search.checkIn)} – ${date(state.search.checkOut)}</dd></div><div><dt>Travelers</dt><dd>${state.search.adults} adults · ${state.search.children} children · ${state.search.rooms} room${state.search.rooms > 1 ? "s" : ""}</dd></div></dl><md-button variant="text" data-action="edit-guest" icon="edit">Edit your details</md-button><md-card variant="filled" full-width><h3>${p.cancellation ? "A little flexibility goes a long way." : "Your cancellation policy"}</h3><p>${p.cancellation ? "Cancel this demo reservation at no cost before the check-in date." : "This sample rate is non-refundable and cannot be cancelled after confirmation."}</p></md-card><form id="confirm-form" novalidate><label class="check-row consent"><md-checkbox id="demo-consent" name="demoConsent" required value-missing-label="Please acknowledge the demo reservation."></md-checkbox><span>I understand this is a demo reservation. No room is held and no payment will be taken.</span></label><p id="booking-error" class="form-error" role="alert" hidden></p><md-button type="submit" id="confirm-booking" size="md" full-width>Confirm demo reservation<span slot="loader"><md-progress-indicator variant="circular" indeterminate size="24" label="Confirming reservation"></md-progress-indicator></span></md-button></form>`}</md-card><aside><md-card variant="outlined" full-width class="checkout-summary">${image(p.image,`Illustrative photo for ${p.name}`)}<div><p class="eyebrow">${escape(p.city)} · ${escape(p.type)}</p><h2>${escape(p.name)}</h2><p>${date(state.search.checkIn)} – ${date(state.search.checkOut)}</p><p>${nights(state.search.checkIn,state.search.checkOut)} nights · ${state.search.rooms} room${state.search.rooms>1?"s":""}</p>${priceBreakdown(q)}<p class="small-note">Everything shown here is part of an AWC UI demonstration.</p></div></md-card></aside></div></div>`;
}

export function confirmation(booking) {
  return `<div class="page-width confirmation-page"><div class="confirmation-heading">${icon(booking.status === "cancelled" ? "event_busy" : "check_circle")}<p class="eyebrow">YOUR GETAWAY DETAILS</p><h1>${booking.status === "cancelled" ? "A change of plans." : "You're going somewhere."}</h1><p>${booking.status === "cancelled" ? "This demo reservation has been cancelled." : "Your demo reservation is confirmed. Your getaway details are all here."}</p></div><md-card class="confirmation-card" variant="outlined" full-width>${image(booking.image,`Illustrative photo for ${booking.propertyName}`)}<div><p class="eyebrow">CONFIRMATION ${escape(booking.id)}</p><h2>${escape(booking.propertyName)}</h2><p>${date(booking.search.checkIn)} – ${date(booking.search.checkOut)}</p><p>${booking.search.adults + booking.search.children} guests · ${booking.search.rooms} room${booking.search.rooms > 1 ? "s" : ""}</p><p class="price"><strong>${money(booking.quote.total)}</strong><span>total, including demo tax</span></p><div class="inline-actions"><md-button size="md" data-action="trips">View my trips</md-button><md-button variant="text" data-action="download" data-id="${booking.id}" icon="download">Save confirmation</md-button></div><p class="small-note">No real booking was made and no email or payment was sent.</p></div></md-card></div>`;
}
export function trips(state) {
  return `<div class="page-width trips-page"><div class="section-heading"><div><p class="eyebrow">THE NEXT CHAPTER</p><h1>Your trips, all together.</h1><p>Big adventures. Little escapes. A place for every plan.</p></div><md-button variant="tonal" data-action="browse" icon="add">Plan a new trip</md-button></div>${state.bookings.length ? `<div class="trip-list">${state.bookings.map(b => `<md-card class="trip-card" variant="outlined" full-width>${image(b.image,`Illustrative photo for ${b.propertyName}`)}<div><div class="trip-top"><p class="eyebrow">${escape(b.city)} · ${escape(b.id)}</p><md-chip label="${b.status === "cancelled" ? "Cancelled" : "Confirmed"}" icon="${b.status === "cancelled" ? "event_busy" : "check_circle"}" appearance="filled" color="${b.status === "cancelled" ? "secondary" : "success"}" data-action="trip-info" data-id="${b.id}"></md-chip></div><h2>${escape(b.propertyName)}</h2><p>${date(b.search.checkIn)} – ${date(b.search.checkOut)} · ${b.search.adults+b.search.children} guests</p><div class="trip-bottom"><strong>${money(b.quote.total)} total</strong><div class="inline-actions"><md-button variant="text" data-action="download" data-id="${b.id}" icon="download">Confirmation</md-button>${b.status === "confirmed" && b.freeCancellation && b.search.checkIn > new Date().toISOString().slice(0,10) ? `<md-button variant="outlined" data-action="cancel-booking" data-id="${b.id}">Cancel reservation</md-button>` : ""}</div></div></div></md-card>`).join("")}</div><p class="small-note">Demo reservations are stored in this browser tab. No real rooms are held.</p>` : empty("A little adventure is calling.","Find a stay you love. Your confirmed demo reservations will appear here.","Find your somewhere","browse")}</div>`;
}
