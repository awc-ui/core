interface:
  display_name: "AWC UI Build"
  short_description: "Build interfaces with AWC Core components"
  default_prompt: "Use $awc-ui-build to implement this interface with the installed AWC Core components."
